# GUSTAV dev environment variables

# App environment
GUSTAV_ENV=dev

# Keycloak base URLs
# Public/browser-facing (used in redirects)
KC_PUBLIC_BASE_URL=http://localhost:8080

# Keycloak realm/client
KC_REALM=gustav
KC_CLIENT_ID=gustav-web

# Redirect back to the app after login
REDIRECT_URI=http://localhost:8100/auth/callback

# Keycloak admin for E2E setup (dev only)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# E2E test bases (requests targets)
WEB_BASE=http://localhost:8100
KC_BASE=http://localhost:8080
