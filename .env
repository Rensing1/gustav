## Local development environment variables

# Use the limited role DSN so RLS policies are always enforced at runtime.
# Adjust host/port if your Supabase/Postgres differs.
# NOTE: This file is not committed (ignored by .gitignore).
# Copy these values to your local .env as needed.

DATABASE_URL=postgresql://gustav_limited:gustav-limited@127.0.0.1:54322/postgres
RLS_TEST_DSN=postgresql://gustav_limited:gustav-limited@127.0.0.1:54322/postgres

# Keycloak / App base URLs for E2E and local runs
KC_BASE=http://id.localhost:8100
KC_PUBLIC_BASE_URL=http://id.localhost:8100
KC_REALM=gustav
WEB_BASE=http://app.localhost:8100
REDIRECT_URI=http://app.localhost:8100/auth/callback

# Admin credentials for Keycloak (E2E / local only)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Optional: enable DB-backed sessions only when you provide a service-role DSN.
# By default, sessions use in-memory storage to avoid requiring service-role here.
SESSIONS_BACKEND=db
SESSION_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# Supabase Storage (self-hosted, local dev)
# These values enable the SupabaseStorageAdapter wiring in app startup.
GUSTAV_ENV=dev
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=sb_secret_N7UND0UgjKTVK-Uodkm0Hg_xSvEMPvz
SUPABASE_STORAGE_BUCKET=materials
