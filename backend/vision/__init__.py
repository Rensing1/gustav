"""Vision package

Marks `backend.vision` as a proper Python package so imports like
`from backend.vision.pipeline import stitch_images_vertically` work reliably
in all environments (including Docker images).
"""

