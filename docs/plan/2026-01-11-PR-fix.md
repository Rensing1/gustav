# Plan: PR‑Fix – CSP Reality‑Check + H5P `/ajax` Hardening (Review Follow‑up)

Stand: 2026‑01‑11 · Branch `development` · Head `b2691fa`

Status: ✅ umgesetzt (Fixes committed; Verifikation + Push offen)

## Kontext
Der aktuelle Stand bringt große Änderungen rund um H5P:
- neues Sidecar `h5p-service/` (Node/Express, Lumi H5P Server/Webcomponents)
- neue `/h5p/*` Endpunkte im OpenAPI‑Vertrag (`api/openapi.yml`)
- neue Task‑Kinds (`native|h5p|visual`) inkl. Migrationen/Tests

Ziel dieses Dokuments: die Review‑Analyse (Risiken + Root Causes) festhalten und eine **priorisierte, konkrete** To‑Do‑Liste definieren, um den PR **merge‑ready** zu machen (Security‑first, KISS, Contract‑first, “local = prod”).

## Kurzfazit (≤ 80 Wörter)
Feature‑Plus und gute Testabdeckung (Contract/E2E) für H5P. Merge‑Blocker: (1) Prod‑CSP muss zur UI‑Realität passen (keine Inline‑Scripts; Styles pragmatisch), und (2) `POST /h5p/ajax` muss same‑origin + `no-store` konsistent erzwingen. Zusätzlich empfehlen sich kleine Security‑Quick‑Wins (H5P export filename sanitization, keine Upstream‑Bodies in Logs, non‑root Container), weil Aufwand klein und Risiko hoch.

## Findings (priorisiert)

### MUST FIX (merge blocker)
- **[SEC] CSP vs. Realität (Inline/Dynamic Styles + Inline Script)** — `backend/web/main.py:291@b098d1d`
  - Problem: Prod‑CSP setzt `style-src 'self'` (kein `unsafe-inline`). Gleichzeitig existieren:
    - Inline `<script>` im SSR‑HTML (`backend/web/main.py:4627@b098d1d`) → wird von `script-src 'self'` in Prod blockiert.
    - Dynamische Style‑Mutationen in JS (`backend/web/static/js/gustav.js:846@b098d1d`, `backend/web/static/js/gustav.js:1203@b098d1d`) → riskant unter `style-src 'self'` (Browser blocken Inline‑Styles).
    - Dritt‑JS (HTMX) injiziert per Default ein `<style>`‑Tag (`backend/web/static/js/vendor/htmx.min.js:1@b098d1d`) → CSP‑Violation in Prod.
  - Impact: Prod‑UI kann brechen (Modals/Tooltips/Layout), außerdem CSP‑Violations “spammen” Logs/Console.
  - ✅ Done: CSP vereinheitlicht + Inline‑Script entfernt (`backend/web/main.py`, `backend/web/static/js/task_kind_toggle.js`); Tests: `backend/tests/test_api_cache_headers.py`, `backend/tests/test_ssr_no_inline_scripts_contract.py`

- **[SEC] H5P `/ajax` ist nicht konsistent “same‑origin + no‑store”** — `h5p-service/server.mjs:1261@b098d1d`
  - Problem: `/ajax` ist `POST` und wird von Webcomponents genutzt (Browser). Aktuell:
    - same‑origin wird nur für eine Teilmenge von `action`‑Werten erzwungen (writeActions).
    - Antworten nutzen nicht `sendJson` → `Cache-Control: no-store` fehlt (und `Vary: Origin` ist nicht garantiert).
  - Impact: unnötige CSRF‑/State‑Manipulation‑Surface (z. B. student user data), plus Caching‑Risiko für sensitive Antworten.
  - ✅ Done: same‑origin für alle Actions + `Cache-Control: private, no-store` + `Vary: Origin` (`h5p-service/server.mjs`); Test: `backend/tests/test_h5p_ajax_security_contract.py`

### SHOULD FIX (wichtig, aber ggf. Post‑Merge)
- **[SEC] CSRF‑Diag‑Log kann Referer‑Pfad/Query loggen** — `backend/web/routes/learning.py:802@b098d1d`
  - Risiko: PII/Token‑Leaks in Logs/Dateien (`CSRF_DIAG_LOG`).
  - Fix‑Richtung: nur Origin (scheme+host) loggen, keine Pfade/Queries.
  - ✅ Done: Origin‑Redaction helper + Test (`backend/web/routes/learning.py`, `backend/tests/test_learning_csrf_diag_log_redaction.py`)

- **[SEC] H5P Export: `Content-Disposition` nutzt unsanitized `contentId`** — `h5p-service/server.mjs:1256@b098d1d`
  - Problem: `filename=${contentId}.h5p` wird direkt in einen Header interpoliert.
  - Impact: Header‑Injection/Response‑Splitting (wenn `contentId` jemals nicht strikt numerisch ist).
  - Fix‑Richtung (KISS): `contentId` allowlisten (z. B. `^[0-9]+$`), sonst 400; und Header‑Value sicher quoten.
  - ✅ Done: `sanitizeHeaderFilename()` + quoted filename (`h5p-service/server.mjs`); Test: `backend/tests/test_h5p_service_hardening_contract.py`

- **[SEC] H5P finishedData: Upstream‑Response‑Body wird geloggt** — `h5p-service/server.mjs:1419@b098d1d`
  - Problem: bei Fehlern wird `await r.text()` in Logs geschrieben.
  - Impact: potenziell sensitive Metadaten/IDs in Container‑Logs (DSGVO‑Risiko).
  - Fix‑Richtung: logge nur Status + errorId (oder eine kurze Klassifikation), niemals Bodies.
  - ✅ Done: kein Upstream‑Body‑Read/Log mehr (`h5p-service/server.mjs`); Test: `backend/tests/test_h5p_service_hardening_contract.py`

- **[SEC] H5P Service läuft als root** — `h5p-service/Dockerfile:1@b098d1d`
  - Risiko: größerer Blast Radius bei RCE/Path‑Traversal in Third‑Party (H5P Pakete = “trusted content”, aber trotzdem).
  - Fix‑Richtung: non‑root user + passende Ownership für `/data/h5p`.
  - ✅ Done: `su-exec` + entrypoint drops to `node` (`h5p-service/Dockerfile`, `h5p-service/entrypoint.sh`); Test: `backend/tests/test_h5p_dockerfile_privilege_drop_contract.py`

- **[ROBUSTNESS] Student‑Visibility Check kann false‑deny bei großen Kursen** — `h5p-service/server.mjs:333@b098d1d`
  - Problem: `fetchAllowedH5PContentIdsForCourse(...limit=100...)` kann bei >100 Aufgaben/Abschnitten H5P‑IDs “abschneiden”.
  - Impact: Schüler bekommen 403 “forbidden”, obwohl Content released ist.
  - Fix‑Richtung:
    - **Option B (empfohlen, contract‑first, Datenminimierung):** dedizierter Learning‑Endpoint als **Access‑Check** pro `content_id`.
      - Why: Kein Pagination‑Risiko, kein “Enumeration‑Leak” (Student sieht nicht alle IDs), minimaler Payload, einfache DB‑Query.
      - Vorschlag: `GET /api/learning/courses/{course_id}/h5p/contents/{content_id}/access`
        - `204` = allowed (released H5P task exists for student in this course)
        - `404` = not allowed / not released / not member / course unknown (fail‑closed, leak‑minimizing)
        - `403` = caller not student
        - `400` = invalid_uuid (course_id) / invalid_content_id (format)
      - H5P‑Service kann optional kurz cachen (TTL) pro `(session_id, course_id, content_id)` um Burst‑Loads abzufangen.

- **[ROBUSTNESS] Default Upload‑Limit 512MB** — `h5p-service/server.mjs:44@b098d1d`
  - Risiko: Disk‑DoS (lokal/prod identisch).
  - Fix‑Richtung: `H5P_MAX_UPLOAD_BYTES` in `docker-compose.yml` setzen (MVP‑Grenze).
  - ✅ Done: `H5P_MAX_UPLOAD_BYTES` gesetzt (`docker-compose.yml`); Test: `backend/tests/test_h5p_compose_upload_limit_contract.py`

- **[ROBUSTNESS] Auth‑Caches sind unbounded** — `h5p-service/server.mjs:63@b098d1d`
  - Problem: `authCache` und `h5pContentAccessCache` wachsen mit Sessions/Course‑IDs/Content‑IDs.
  - Impact: Memory‑Leak über Laufzeit (Sidecar lange up).
  - Fix‑Richtung: max‑size + einfache Eviction (LRU) oder periodisches Cleanup (TTL‑Sweep).
  - ✅ Done: max‑entries + TTL‑Sweep + LRU‑touch (`h5p-service/server.mjs`); Test: `backend/tests/test_h5p_service_cache_bounds_contract.py`

### NICE TO HAVE
- **[DOC] Vendor‑Assets Lizenz‑Hinweis** — `h5p-service/vendor/h5p/core/LICENSE.txt:1@b098d1d`
  - Fix‑Richtung: kurze Doku/Notice, welche Third‑Party‑Assets shipped werden (FOSS‑Transparenz).

- **[CONSISTENCY] Cache‑Header H5P Service vs. OpenAPI** — `h5p-service/server.mjs:156@b098d1d`
  - Problem: Runtime nutzte `Cache-Control: no-store`, OpenAPI dokumentiert meist `private, no-store`.
  - Fix‑Richtung: entweder Runtime auf `private, no-store` ziehen (empfohlen) oder Vertrag anpassen.
  - ✅ Done: Runtime vereinheitlicht auf `Cache-Control: private, no-store` (`h5p-service/server.mjs`); Test: `backend/tests/test_h5p_service_cache_bounds_contract.py`

- **[DOC/CONSISTENCY] Vendor‑Drop Hygiene (Maps/Update‑Pfad)** — `h5p-service/vendor/h5p/editor/ckeditor/ckeditor.js.map:1@b098d1d`
  - Problem: sehr große Sourcemaps/Assets erschweren Review/Repo‑Size.
  - Fix‑Richtung: dokumentiere Update‑Prozess + Lizenzquelle; prüfe, ob `.map` in Prod nötig ist.
## Entscheidungen (benötigt/empfohlen)
1) **CSP‑Strategie (empfohlen für Merge‑Fix):**
   - `script-src`: **immer** strikt (`'self'`, kein `unsafe-inline`) → reduziert XSS‑Surface.
   - `style-src`: **realistisch**: `"'self' 'unsafe-inline'"` (mindestens für jetzt), weil:
     - GUSTAV setzt Styles dynamisch in JS (Tooltips/Scroll‑Lock),
     - HTMX injiziert default‑Styles,
     - Inline‑Styles sonst großflächig refactor‑pflichtig wären.
   - Mitigation: `img-src`/`connect-src` bleiben eng (`'self'` + `data:`) → CSS‑Exfil wird stark begrenzt.
2) **H5P `/ajax`:** immer same‑origin erzwingen (Origin oder Referer) und Antworten grundsätzlich `private, no-store` + `Vary: Origin`.
3) **H5P Student‑Visibility:** Wenn Kurse >100 Sections realistisch sind, bevorzuge **Option B** (dedizierter Learning‑Endpoint), weil sie:
   - false‑denies eliminiert (kein `limit=100` Problem),
   - Payload minimal hält (kein Sections/Tasks Dump),
   - Daten minimiert (keine Liste aller erlaubten IDs → weniger Enumeration/Metadaten),
   - die Logik dort bündelt, wo die “released task” Wahrheit liegt (Learning/DB‑Boundary).

## Umsetzung (Contract‑first, TDD, KISS)

### 1) Tests zuerst (RED)
**CSP**
- Passe `backend/tests/test_api_cache_headers.py` so an, dass er:
  - `script-src` explizit prüft (kein `unsafe-inline`, kein `unsafe-eval`)
  - `style-src` (vorerst) `unsafe-inline` erlaubt/erwartet (oder zumindest nicht mehr pauschal “kein unsafe-inline irgendwo” verlangt)
  - weiterhin `img-src 'self' data:` validiert (Syntax‑Regression Guard bleibt)
- Neuer Guard‑Test: keine Inline‑Scripts in SSR‑HTML (mindestens `backend/web/main.py`):
  - z. B. Source‑Level Test, der `<script>` ohne `src=` verbietet

**H5P `/ajax`**
- Neuer Contract‑Test (source‑level) gegen `h5p-service/server.mjs`, der sicherstellt:
  - `app.post("/ajax"...` erzwingt same‑origin (unabhängig von `action`)
  - Response setzt `Cache-Control: private, no-store` (und idealerweise `Vary: Origin`)

**H5P Export + Logging**
- Neuer Contract‑Test (source‑level) gegen `h5p-service/server.mjs`:
  - `Content-Disposition` wird nur mit sanitisiertem `contentId` gesetzt (keine direkte Interpolation)
  - Fehler‑Logs enthalten keine Response‑Bodies (`await r.text()` darf nicht in Warn‑Logs landen)

**H5P Student‑Visibility (optional)**
- Falls wir das false‑deny‑Risiko vor Merge beheben wollen, gibt es zwei Pfade:
  - **Option A (h5p-service Pagination):** source‑level Contract‑Test, der eine Pagination‑Schleife in `fetchAllowedH5PContentIdsForCourse` erzwingt (kein hardcoded `limit=100` ohne Loop).
  - **Option B (dedizierter Learning‑Endpoint, empfohlen):**
    Kontext (wichtig):
    - Der Check wird in `h5p-service/server.mjs` innerhalb von `GET /player/model` nur im **Student‑Fall** genutzt, um `content_id` fail‑closed zu autorisieren.
    - Der H5P‑Service forwardet bereits den `gustav_session` Cookie an den Web‑Container (`GUSTAV_WEB_INTERNAL_BASE`) – der Endpoint muss daher **keine** neuen Auth‑Mechanismen einführen.

    1) **Contract‑first:** `api/openapi.yml` erweitert um
       - `GET /api/learning/courses/{course_id}/h5p/contents/{content_id}/access`
       - `204` No Content bei “allowed”
       - `404` Not Found bei “not allowed” (inkl. not member / course unknown / not released) – fail‑closed, leak‑minimizing
       - `403` Forbidden wenn caller keine student‑Rolle hat
       - `401` unauthenticated
       - `400` bad_request mit `detail=invalid_uuid|invalid_content_id`
       - Security headers: `Cache-Control: private, no-store`
    2) **pytest (RED):** neuer API‑Test gegen FastAPI, der:
       - Given Student enrolled + released H5P task When GET access Then 204
       - Given Student not member When GET access Then 404
       - Given Teacher When GET access Then 403
       - Given invalid UUID When GET access Then 400/detail=invalid_uuid
    3) **Minimal‑Implementierung (GREEN):**
       - `backend/web/routes/learning.py`: neue Route (student‑only, read‑only, kein CSRF‑Guard)
       - `backend/learning/usecases/*`: kleiner Use Case `CheckH5PContentAccessUseCase` (returns bool)
       - `backend/learning/repo_db.py`: `is_h5p_content_released_for_student(...) -> bool`
         - effizient per `exists(...)` Join über `course_memberships` + `module_section_releases.visible` + `unit_tasks(kind='h5p')` + `h5p_content_id = content_id`
         - optional als SQL‑Helper (security invoker, revoke PUBLIC, grant `gustav_limited`)
       - `h5p-service/server.mjs`: ersetzt `/sections?include=tasks`‑Fetch durch Access‑Check Call; optional TTL‑Cache pro `(sid, course_id, content_id)`.
  - ✅ Done: Access‑Check Endpoint + H5P‑Service Umstellung (siehe unten: Umsetzung)

### 2) Minimal‑Fixes (GREEN)
**CSP**
- `backend/web/main.py`: CSP vereinheitlichen (“local = prod”):
  - Entferne env‑Branching oder stelle zumindest sicher, dass `script-src` überall strikt ist.
  - Setze `style-src` auf `"'self' 'unsafe-inline'"` (begründet oben).
- Entferne Inline‑Script aus SSR:
  - `backend/web/main.py`: `kind_toggle_js` in eine Static‑Datei verschieben (z. B. `backend/web/static/js/task_kind_toggle.js`) und per `<script src=... defer>` einbinden.

**H5P `/ajax`**
- `h5p-service/server.mjs`:
  - `requireSameOrigin` für **alle** `/ajax` Requests (POST) anwenden (Origin oder Referer, fail‑closed).
  - `Cache-Control: private, no-store` + `Vary: Origin` setzen, auch wenn `res.send(result)` genutzt wird.
  - Fehlerpfad möglichst über `sendJson` für konsistente Security‑Header/Cache‑Headers.

**H5P Export + Logging**
- `h5p-service/server.mjs`:
  - `GET /contents/:contentId/export`: `contentId` strikt allowlisten und `Content-Disposition` sicher bauen.
  - finishedData → learning submission: entferne Response‑Body aus Logs; logge nur Status/klassifizierte Fehlermeldung.

### 3) Refactor (nur wenn alles grün)
✅ Alle optionalen Refactors aus diesem Plan sind umgesetzt:
- `H5P_MAX_UPLOAD_BYTES` gesetzt (`docker-compose.yml`); Test: `backend/tests/test_h5p_compose_upload_limit_contract.py`
- H5P Service non‑root (`h5p-service/Dockerfile`, `h5p-service/entrypoint.sh`); Test: `backend/tests/test_h5p_dockerfile_privilege_drop_contract.py`
- `CSRF_DIAG_LOG` redacted (`backend/web/routes/learning.py`); Test: `backend/tests/test_learning_csrf_diag_log_redaction.py`
- Auth‑Caches bounded + `Cache-Control: private, no-store` vereinheitlicht (`h5p-service/server.mjs`); Test: `backend/tests/test_h5p_service_cache_bounds_contract.py`
- Student‑Visibility Option B umgesetzt (contract‑first):
  - OpenAPI: `api/openapi.yml`
  - Tests: `backend/tests/test_openapi_learning_h5p_access_contract.py`, `backend/tests/test_learning_h5p_access_check_api.py`
  - Backend: `backend/web/routes/learning.py`, `backend/learning/usecases/h5p_access.py`, `backend/learning/repo_db.py`
  - H5P Service: `h5p-service/server.mjs`

## Tests (BDD‑Skizzen)
- Given prod CSP When loading a page Then `script-src` forbids inline scripts and still works via external JS (path::`backend/tests/test_api_cache_headers.py`)
- Given H5P `/ajax` When request is cross‑site Then 403 `csrf_violation` (path::`backend/tests/test_h5p_ajax_security_contract.py`)
- Given `/h5p/ajax` When response is sent Then `Cache-Control: private, no-store` is present (path::`backend/tests/test_h5p_ajax_security_contract.py`)
- Given H5P export When `content_id` is used for filename Then it is sanitized and quoted (path::`backend/tests/test_h5p_service_hardening_contract.py`)
- Given finishedData forward fails When logging Then no upstream response body is emitted (path::`backend/tests/test_h5p_service_hardening_contract.py`)
- Given student enrolled + released H5P task When GET access-check Then 204 (path::`backend/tests/test_learning_h5p_access_check_api.py`)
- Given student not member When GET access-check Then 404 (path::`backend/tests/test_learning_h5p_access_check_api.py`)
- Given teacher When GET access-check Then 403 (path::`backend/tests/test_learning_h5p_access_check_api.py`)
- Given invalid UUID/content_id When GET access-check Then 400 (path::`backend/tests/test_learning_h5p_access_check_api.py`)

## Verifikation (lokal = prod)
Schnelllauf (nach jedem GREEN‑Schritt):
- `.venv/bin/pytest -q backend/tests/test_api_cache_headers.py`
- `.venv/bin/pytest -q backend/tests/test_h5p_*contract.py`
- `.venv/bin/pytest -q backend/tests/test_openapi_learning_h5p_access_contract.py backend/tests/test_learning_h5p_access_check_api.py`

E2E (nachdem Docker‑Services laufen):
- `docker compose up -d --build caddy web keycloak h5p`
- `.venv/bin/pytest -q -m e2e backend/tests_e2e/test_h5p_service_auth_e2e.py`

Optional (vor Merge):
- `.venv/bin/pytest -q`

## Done‑Definition
- CSP ist konsistent mit tatsächlichem Frontend‑Verhalten (keine Merge‑Blocker‑Violations; Inline‑Scripts entfernt).
- `/h5p/ajax` ist same‑origin‑only und Antworten sind `private, no-store` (defense‑in‑depth).
- H5P Export setzt `Content-Disposition` nur mit sanitisiertem `contentId`.
- H5P finishedData enthält keine Upstream‑Bodies in Logs.
- Tests decken die Regression‑Klassen ab (CSP + `/ajax` + Export/Logging).

## Refactor‑Budget
S (≤ 90 Min) · Blast radius: `api/openapi.yml`, `backend/learning/*`, `backend/web/main.py`, `backend/web/routes/learning.py`, `backend/tests/*`, `h5p-service/server.mjs`, `h5p-service/Dockerfile`, `docker-compose.yml`
