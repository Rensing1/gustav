# Plan: PR‑Fix2 – Security Hardening Follow‑up (Review 2026‑01‑11)
Stand: 2026‑01‑11 · Branch `development` · Base `5526fea0` · Head `756a16d` · Follow‑up: committed on top

## Kontext
Dieser Plan fasst eine priorisierte Code‑Review‑Analyse für den Diff‑Bereich `5526fea0..756a16d` zusammen.

Hintergrund des großen Changesets (high level):
- H5P‑Integration über Sidecar‑Service `h5p-service/` (reverse‑proxied unter `/h5p/*` via `reverse-proxy/Caddyfile` + `docker-compose.yml`).
- Erweiterung des Task‑Modells um `Task.kind` (`native|h5p|visual`) inkl. DB‑Migrations und OpenAPI‑Vertrag.
- Learning‑Pipeline: neue Submission‑Art `kind=h5p` (Score raw/max), plus Visual‑Tasks (upload‑only + VLM/DSPy‑Pipeline).
- Teaching Live: H5P‑Completed‑Flag + H5P Review‑Player (teacher‑only, read‑only).
- Stabilisierung: Verhindere flaky Tests, wenn `pytest` in‑process läuft, während der Docker `learning-worker` aktiv ist (Jobs taggen + optional im Worker skippen).

Projektprinzipien, die hier besonders relevant sind:
- **Security first / DSGVO**: SECURITY‑DEFINER‑DB‑Helper, CSRF/CSP, keine PII in Logs, fail‑closed Verhalten.
- **Local = Prod**: keine “Dev‑Ausnahmen” in Auth/CSRF/Persistenz; Migrationen sind Source of Truth.
- **Contract‑First**: API‑Semantik muss in `api/openapi.yml` klar und konsistent sein.

## Kurzfazit (≤ 80 Wörter)
Die kritischen Punkte aus dem Review sind umgesetzt: DB‑Helper ist gehärtet und per Migration‑Test abgesichert; H5P‑Sidecar ist robuster (Cookie‑Parsing, Async‑Error‑Handling), und der API‑Vertrag ist konsistent (Statuscodes + course‑Scope‑Doku). Zusätzlich sind Worker‑Flakiness‑Guards (`_gustav_source=pytest` + `WORKER_SKIP_PYTEST_JOBS`) getestet und dokumentiert. Gesamtrisiko: niedrig, da alle Änderungen test‑/contract‑first abgesichert sind.

## Umsetzungsstatus (Stand 2026‑01‑11)
- ✅ MUST FIX: DB‑Hardening `get_latest_submission_for_owner` — `supabase/migrations/20260111130000_teaching_latest_submission_owner_helper_hardening.sql` + `backend/tests/migration/test_teaching_latest_submission_owner_helper_hardening.py`
- ✅ Secret‑Guard: Prod/Stage fail‑fast für `H5P_REVIEW_TOKEN_SECRET` — `backend/web/config.py` + `backend/tests/test_config_security.py` + `h5p-service/server.mjs` + `backend/tests/test_h5p_review_token_secret_guard_contract.py`
- ✅ H5P Robustheit: Cookie‑Parsing + Async‑Error‑Handling — `h5p-service/server.mjs` + `backend/tests/test_h5p_cookie_parser_hardening_contract.py` + `backend/tests/test_h5p_async_error_handling_contract.py`
- ✅ Contract‑Konsistenz: H5P Submission Statuscode + `/h5p/player/model` course‑Scope — `backend/web/routes/learning.py` + `api/openapi.yml` + `backend/tests/test_learning_h5p_submission_status_code.py` + `backend/tests/test_openapi_h5p_player_model_contract.py`
- ✅ Worker‑Flakiness: pytest‑Tagging + Skip‑Filter getestet + dokumentiert — `backend/learning/repo_db.py` + `backend/learning/workers/process_learning_submission_jobs.py` + `backend/tests/test_learning_worker_jobs.py` + `.env.example`
- ✅ SSR Presign Injection: testbar entkoppelt — `backend/web/main.py` + `backend/tests/test_learning_ssr_presign_injection.py`
- ✅ Threat Model (H5P scope): Doku + OpenAPI klargestellt — `docs/ARCHITECTURE.md` + `api/openapi.yml`

## Findings (priorisiert)

### MUST FIX (kritisch)
- [SEC] SECURITY DEFINER helper uses unsafe search_path + trusts `p_owner_sub` — `supabase/migrations/20260111124000_fix_latest_submission_owner_member_guard.sql:39@756a16d` — Impact: High (Sicherheit) — Lösung: `search_path=pg_catalog,public`; Owner‑Check an `app.current_sub` binden; `revoke` PUBLIC, `grant` nur für `gustav_limited`.

### SHOULD FIX (wichtig)
- [SEC] Missing prod guard for `H5P_REVIEW_TOKEN_SECRET` (web + h5p-service) — `backend/web/routes/teaching.py:4370@756a16d`, `h5p-service/server.mjs:51@756a16d` — Impact: Med — Lösung: Startup‑Guard: prod bricht bei leer/default ab; Secret Rotation dokumentieren.
- [DOC] Threat Model klären: H5P Content ist bewusst teacher‑global (nicht kurs-/owner‑gebunden) — `h5p-service/server.mjs:1241@756a16d` — Impact: Med (Erwartungsmanagement) — Lösung: In Doku/OpenAPI klarstellen; Security bleibt: student access ist kurs‑gebunden (Access‑Check), teacher routes bleiben teacher/admin only.
- [API] H5P submissions “persisted synchronously”, API returns always 202 — `backend/web/routes/learning.py:943@756a16d` — Impact: Med (Contract) — Lösung: 201 für `kind=h5p` oder OpenAPI‑Beschreibung explizit auf 202‑Semantik anpassen.
- [API] `/h5p/player/model`: `course_id` is required for students (403 if missing) but OpenAPI documents it as optional — `h5p-service/server.mjs:1066@756a16d`, `api/openapi.yml:1655@756a16d` — Impact: Med — Lösung: OpenAPI klarstellen (“required for students”) oder Endpoint‑Semantik vereinheitlichen.
- [TEST] No DB‑hardening test for `get_latest_submission_for_owner` — `supabase/migrations/20260111124000_fix_latest_submission_owner_member_guard.sql:14@756a16d` — Impact: Med — Lösung: Migration‑Test wie bei `get_unit_latest_submissions_for_owner` ergänzen.
- [TEST] No regression test: worker skips pytest‑tagged queue jobs when `WORKER_SKIP_PYTEST_JOBS=true` — `backend/learning/workers/process_learning_submission_jobs.py:225@756a16d` — Impact: Low/Med (Test‑Flakiness) — Lösung: DB‑Test: enqueue job with `_gustav_source=pytest`, set env, assert `_lease_next_job` ignores it.
- [BUG] Malformed Cookie header can crash `decodeURIComponent` — `h5p-service/server.mjs:178@756a16d` — Impact: Med — Lösung: `decodeURIComponent` pro Cookie try/catch; invalide Cookie‑Paare ignorieren.
- [BUG] Express 4 async route errors can become unhandled rejections (DoS) — `h5p-service/server.mjs:1088@756a16d` — Impact: Med — Lösung: async‑Handler‑Wrapper + zentrale Error‑Middleware; alle upstream `fetch`‑Fehler fail‑closed behandeln.
- [BUG] Pytest‑tagged jobs can accumulate in shared dev DB (worker ignores them) — `backend/learning/repo_db.py:861@756a16d` — Impact: Low (Bloat/Noise) — Lösung: Tests löschen Queue‑Rows deterministisch (`delete from public.learning_submission_jobs`) und vermeiden “leaked” jobs.
- [DOC] `WORKER_SKIP_PYTEST_JOBS` fehlt in `.env.example` — `docker-compose.yml:160@756a16d` — Impact: Low (Config‑Discoverability) — Lösung: `.env.example` um Variable + Erklärung ergänzen (Default + Debug‑Hinweis).
- [CONSISTENCY] SSR presign helper imports routes module (`routes.learning`) — `backend/web/main.py:1033@756a16d` — Impact: Low — Lösung: Adapter über Injection/Wiring beziehen (keine Cross‑Module Imports).

### NICE TO HAVE (optional)
- [SEC] Debug pages need CSP_DEBUG_HTML with `'unsafe-inline'` — `h5p-service/server.mjs:103@756a16d` — Impact: Low — Lösung: Debug‑Pages in prod deaktivieren oder inline scripts reduzieren.
- [DOC] Trusted‑content model mainly in plan docs — `docs/plan/2025-12-15-h5p-integration.md:20@756a16d` — Impact: Low — Lösung: Kurzfassung in `docs/ARCHITECTURE.md` und README ergänzen.

## TESTS (relevante Contract/BDD‑Tests)
- Given an enrolled student & released H5P task When GET access-check Then 204 (`backend/tests/test_learning_h5p_access_check_api.py`)
- Given persistence unavailable When POST learning submission Then 503 fail-closed (`backend/tests/test_learning_create_submission_fail_closed.py`)
- Given H5P service source When inspecting `/ajax` Then same-origin + no-store enforced (`backend/tests/test_h5p_ajax_security_contract.py`)
- Given invalid Cookie header When H5P parses cookies Then it must not crash (new: `backend/tests/test_h5p_cookie_parsing_contract.py` or extend `backend/tests_e2e/test_h5p_service_auth_e2e.py`)
- Given `WORKER_SKIP_PYTEST_JOBS=true` and a queued job tagged `_gustav_source=pytest` When worker leases jobs Then it must ignore the pytest job (new: extend `backend/tests/test_learning_worker_jobs.py`)
- Given `PYTEST_CURRENT_TEST` is set When creating a submission that enqueues a job Then payload contains `_gustav_source=pytest` (new: `backend/tests/test_learning_worker_jobs.py`)
- Given OpenAPI spec When validating `/h5p/player/model` Then student `course_id` requirement is documented (`backend/tests/test_openapi_h5p_player_model_contract.py`, `api/openapi.yml`)
- Given teacher requests H5P latest-detail When GET latest Then score + review_token exist (`backend/tests/test_teaching_live_detail_api.py`)
- Given OpenAPI spec When validating contract Then access-check path exists (`backend/tests/test_openapi_learning_h5p_access_contract.py`, `api/openapi.yml`)
- Given SSR pages When rendering Then no inline scripts (`backend/tests/test_ssr_no_inline_scripts_contract.py`)

## Nächste Schritte (Verify)
✅ Alle Punkte dieses Plans sind umgesetzt.

- `supabase migration up`
- `.venv/bin/pytest -q`
- Optional (manuell prüfen): `docker compose up -d --build` + Smoke‑Test H5P Player/Review.

## Done‑Definition
- MUST‑FIX (DB‑Hardening) ist umgesetzt, gemigriert und durch Tests abgesichert.
- Keine PUBLIC‑EXECUTE‑Rechte auf SECURITY‑DEFINER‑Helpern (nur `gustav_limited`).
- `search_path` ist sicher (`pg_catalog, public`) und Owner‑Checks sind session‑gebunden (nicht parameter‑gebunden).
- H5P sidecar ist robust gegen invalid/malicious Cookie Header und upstream failures (kein Prozess‑Crash; fail‑closed Antworten).
- Learning‑Worker Tests sind deterministisch, auch wenn ein lokaler Docker `learning-worker` parallel läuft (`_gustav_source=pytest` + `WORKER_SKIP_PYTEST_JOBS` getestet + dokumentiert).
- Review‑Entscheidung kann auf `⚠️ Teilweise Risiko` oder `✅ Freigabe` wechseln.

## Refactor‑Budget
S (≤ 90 Min) · Blast radius: Supabase migrations + Teaching latest-detail path + H5P service parsing + Learning worker queue

## Freigabeentscheidung (aktualisiert)
✅ Freigabe — Must‑Fixes umgesetzt, Contract + Tests ergänzt; verbleibendes Risiko ist niedrig (hauptsächlich Betriebs-/Konfigurationsrisiken, jetzt fail‑fast abgesichert).
