# Plan: PR-Fixes für `learning-unit-rework` (2026-02-06)

## Ziel
- Die Merge-Blocker aus dem Review zwischen `master` und `learning-unit-rework` beheben.
- Security- und API-Contract-Abweichungen zuerst schließen, danach Test- und Konsistenzlücken.
- Ergebnis: PR ist fachlich freigabefähig ohne bekannte kritische Risiken.

## Kontext
- Vergleichsbasis: `master@b4994ba` vs. `learning-unit-rework@3fc0371`.
- Die funktionale Erweiterung (modulare Lerneinheiten) ist weitgehend vorhanden.
- Security-Baseline (bereits im PR sichtbar):
  - CSP ist streng (`script-src 'self'`), d. h. keine Inline-Skripte. SSR-JSON wird daher sinnvollerweise über nicht-ausführende Container eingebettet (z. B. `<template id="modular-editor-edges-data">…</template>` statt `<script type="application/json">`).
  - Für modulare Units wird „open/done“ bereits im Repo-Layer durchgesetzt (z. B. Submission/History/H5P-Access prüfen `DBLearningRepo._is_modular_section_open_or_done`). Der SQL-Helper aus `20260204110331_learning_submissions_section_id.sql` ist trotzdem noch permissiver → Defense-in-depth-Lücke (siehe MUST FIX #1).

### Umsetzungsstand (Update: 2026-02-06, nach `make verify`)
- **Gesamtlauf erfolgreich:** `make verify` ist grün (u. a. `1133 passed, 18 skipped` im Python-Hauptlauf; nachgelagerte Node-/Supabase-/OpenAI-/E2E-Schritte ebenfalls erfolgreich).
- **MUST-FIX #1 erledigt:** Defense-in-depth für modulare Locked-Tasks implementiert via Migration `supabase/migrations/20260206120000_learning_modular_task_visibility_hardening.sql` inkl. gemeinsamer Unlock-Logik und konsistenter SQL-Helper-Semantik.
- **MUST-FIX #2 erledigt:** Phase-Endpunkte liefern jetzt strikt das öffentliche Contract-Shape (`id, unit_id, title, position`) in `backend/web/routes/teaching.py`.
- **MUST-FIX #3 verifiziert:** 401/403-Semantik ist für die betroffenen Teaching-Endpunkte via Auth-Middleware + Rollenprüfung vertragstreu; abgesichert durch Tests in `backend/tests/test_teaching_unit_phases_api.py`.
- **Zusätzliche Sicherheits-/Robustheitsfixes erledigt:**
  - Reorder-Endpoints crashen nicht mehr bei unhashable Payload-Items (400 statt 500), inkl. neuer Tests.
  - Student-Workspace rendert Fehlertexte ohne `innerHTML`-Interpolation (DOM + `textContent`), inkl. JS-Contract-Test.
- **Flake im Verify-Lauf behoben:** OpenAI-Health-Cache-Key nutzt jetzt Callable-Identität statt `id(...)` (verhindert Cache-Kollisionen durch wiederverwendete Object-IDs), inkl. Regressionstest in `backend/tests/test_openai_health_endpoint.py`.
- **SHOULD-FIX #10 finalisiert:** Die Graph-Renderer-Doppelpflege wurde vollständig entfernt. `backend/web/static/js/student_graph_view.js` ist die einzige Implementierung; `backend/tests/test_student_graph_view_sync_contract.py` erzwingt, dass kein `ui-dummies/`-Artefakt mehr vorhanden ist.

## Scope
### In Scope
- Fixes an Learning-SQL-Helpern und zugehörigen API/Repo-Pfaden.
- Fixes an Teaching-Serializern und Auth-Guard-Verhalten.
- Ergänzende Contract-/Regression-Tests.
- Falls nötig: neue Supabase-Migrationen.

### Out of Scope
- Größere UI-Refactorings.
- Funktionsausbau jenseits der festgestellten Findings.

## Priorisierte Findings

### MUST FIX
1. **[SEC] Locked-Module-Bypass schließen** ✅
   - Umgesetzt: neue Migration `supabase/migrations/20260206120000_learning_modular_task_visibility_hardening.sql`.
   - Ergebnis: SQL-Helper erlauben modulare Tasks nur bei `open/done`; RLS-Insert-Guard bleibt damit fail-closed.
   - Abgesichert durch: `backend/tests/test_learning_modular_units_api_contract.py::test_learning_modular_locked_task_is_hidden_in_sql_helpers`.

2. **[API] Phase-Payload an OpenAPI angleichen** ✅
   - Umgesetzt in: `backend/web/routes/teaching.py` (alle betroffenen Phase-Endpunkte verwenden `_serialize_unit_phase_public(...)`).
   - Ergebnis: `TeachingUnitPhase`-Shape ist contract-konform (ohne `created_at`/`updated_at`).
   - Abgesichert durch: `backend/tests/test_teaching_unit_phases_api.py`.

3. **[API] 401/403-Semantik korrigieren** ✅ (für betroffene Endpunkte verifiziert)
   - Status: Die Endpunkte liefern erwartungsgemäß `401` (unauthenticated) und `403` (authenticated non-teacher).
   - Abgesichert durch: `backend/tests/test_teaching_unit_phases_api.py::test_unit_phases_requires_authentication_returns_401` und `::test_unit_phases_non_teacher_returns_403`.

### SHOULD FIX
1. **[TEST] Contract-Tests für exakte Phase-Response-Felder ergänzen** ✅
2. **[TEST] Unauthenticated-Tests für neue Teaching-Endpoints ergänzen** ✅
3. **[CONSISTENCY] Umgang mit `learning_submissions.section_id` bei Task-Verschiebungen absichern (Trigger oder explizite Restriktion)** ✅
   - Entscheidung: **explizite Restriktion** bleibt der Schutzmechanismus (`section_id` auf `unit_tasks`/`unit_materials` ist DB-seitig immutable).
   - Abgesichert durch: `backend/tests/test_teaching_section_id_immutability_db_contract.py`.
4. **[BUG] Reorder-Endpunkte robust machen (keine 500 bei unhashable JSON-Items)** ✅
   - Betroffen: `backend/web/routes/teaching.py` (`POST /api/teaching/units/{unit_id}/phases/reorder`, `POST /api/teaching/units/{unit_id}/phases/{phase_id}/modules/reorder`)
   - Umgesetzt: gemeinsame `UUID[]`-Validierung vor Duplikatprüfung; keine `TypeError`-500 mehr.
   - Abgesichert durch: neue Tests in `backend/tests/test_teaching_unit_phases_api.py`.

5. **[SEC] DOM-XSS verhindern: Fehlerausgabe im Student-Workspace nicht per `innerHTML` interpolieren** ✅
   - Betroffen: `backend/web/static/js/student_modular_workspace.js` (Error-Overlay im Graph).
   - Umgesetzt: Error-Overlay nutzt DOM APIs + `textContent`.
   - Abgesichert durch: `backend/tests/test_student_modular_workspace_js_contract.py::test_error_overlay_uses_text_content_not_html_interpolation`.

6. **[CONSISTENCY] Section-Counts (`tasks_total`, `materials_count`) auch bei UPDATEs korrekt halten** ✅ (durch Restriktion abgesichert)
   - Betroffen: `supabase/migrations/20260204102248_learning_modular_units_modules.sql`
   - Einordnung: Cross-section-UPDATE ist DB-seitig verboten (`section_id` immutable), daher kann der beschriebene Driftpfad aktuell nicht eintreten.
   - Abgesichert durch: `backend/tests/test_teaching_section_id_immutability_db_contract.py`.

7. **[CONSISTENCY] Migrations-Failfast vor Deploy absichern** ✅
   - Betroffen: `supabase/migrations/20260204110331_learning_submissions_section_id.sql`
   - Umgesetzt: Preflight-Checks + Stop-Kriterien in `docs/runbooks/preflight_checklist.md` ergänzt (`missing_section_id`, `section_mismatch`).
   - Ergebnis: Orphan-/Mismatch-Fälle werden vor Deploy sichtbar und operational fail-fast abgefangen.

8. **[DOC] „stubbed“-Wording in modularen Learning-Docstrings aktualisieren** ✅
   - Umgesetzt in: `backend/web/routes/learning.py` (Docstring für `GET .../modules/graph`).
   - Betroffen: `backend/web/routes/learning.py` (`GET …/modules/graph`)

9. **[CONSISTENCY] Support-Mail im Keycloak-Theme ist ein Placeholder** ✅
   - Betroffen: `keycloak/themes/gustav/email/html/email-verification.ftl`, `keycloak/themes/gustav/email/html/password-reset.ftl`
   - Umgesetzt: zentrale Konfiguration in `keycloak/themes/gustav/email/theme.properties` (`supportEmail=...`) und Templates lesen die Adresse aus `properties.supportEmail`.
   - Abgesichert durch: `backend/tests/test_keycloak_theme_files.py::test_email_templates_reference_support_contact`.

10. **[MAINT] Duplizierter Graph-Renderer (Dummy vs. Backend-Asset)** ✅
   - Betroffen: `backend/web/static/js/student_graph_view.js` und ehemals `ui-dummies/shared/graph-view.js`
   - Entscheidung: Kein Renderer-Duplikat und keine Dummy-App mehr im Repo.
   - Umgesetzt: `ui-dummies/` entfernt; Contract-Test `backend/tests/test_student_graph_view_sync_contract.py` prüft, dass kein `ui-dummies/`-Verzeichnis existiert.

## Umsetzung als TDD (Red-Green-Refactor)

### Arbeitspaket 1: Security-Fix für modulare Locked-Tasks
**Red**
- Test hinzufügen:
  - Given modularer Kurs mit gesperrtem Modul
  - When Submission/Upload-Intent für Task aus gesperrtem Modul
  - Then Request wird fail-closed abgewiesen (`404` im API-Kontext, `false`/RLS-seitig nicht sichtbar)
  - Hinweis: Der API-Fall ist bereits über einen Regression-Test abgedeckt (siehe Review-Update). Für den eigentlichen MUST-FIX geht es um die **DB/RLS-Defense-in-depth**-Ebene.

**Green**
- SQL-Helper für Task-Sichtbarkeit so anpassen, dass bei modularen Units nur `open/done` akzeptiert wird.
- Gleiche Unlock-Semantik in `check_task_visible_to_student` und `get_task_metadata_for_student` erzwingen.

**Refactor**
- Unlock-Bedingung in SQL zentralisieren (z. B. dedizierte Helper-Funktion), um Drift zu vermeiden.

### Arbeitspaket 2: Contract-Fix für Phase-Responses
**Red**
- Tests auf exakte Feldmenge für:
  - `GET /api/teaching/units/{unit_id}/phases`
  - `POST /api/teaching/units/{unit_id}/phases`
  - `PATCH /api/teaching/units/{unit_id}/phases/{phase_id}`
  - `POST /api/teaching/units/{unit_id}/phases/reorder`

**Green**
- In den betroffenen Routen nur noch `TeachingUnitPhase`-konforme Serialisierung (`id, unit_id, title, position`) zurückgeben.

**Refactor**
- Einheitliche Serializer-Nutzung für alle Phase-Endpunkte.

### Arbeitspaket 3: Auth-Semantik 401 vs. 403
**Red**
- Tests ergänzen:
  - unauthenticated => `401`
  - authenticated ohne Teacher-Rolle => `403`

**Green**
- `_require_teacher` entsprechend trennen (keine Session vs. falsche Rolle).

**Refactor**
- Konsistente Guard-Nutzung in allen neuen Teaching-Modular-Endpunkten prüfen.

### Arbeitspaket 4 (empfohlen): Robustness-Fix für Reorder-Payloads
**Red**
- Tests ergänzen (kein 500, sondern 400):
  - Given teacher + modular unit When `phase_ids` enthält nicht-String-Items Then `400 detail=invalid_phase_ids`
  - Given teacher + modular unit When `module_ids` enthält nicht-String-Items Then `400 detail=invalid_module_ids`

**Green**
- Payload-Validierung so anpassen, dass:
  - zuerst „array of strings“ geprüft wird,
  - dann UUID-Check,
  - erst danach Duplikate via `set(...)`.

**Refactor**
- Gemeinsame Validierungs-Helper-Funktion (KISS) für `phase_ids`/`module_ids`, um Fehlercodes konsistent zu halten.

### Arbeitspaket 5 (empfohlen): Frontend-Sicherheits-Fix (Error-Overlay)
**Red**
- String-basierter Contract-Test:
  - Given `student_modular_workspace.js` When Error-Overlay gerendert wird Then kein `${...}` in `innerHTML`-Templates für Error-Text.

**Green**
- Error-Overlay auf DOM-APIs + `textContent` umstellen.

## Konkrete nächste Schritte (ab jetzt)
1. **Review-Findings abgeschlossen:** Es sind keine offenen MUST-/SHOULD-Punkte aus diesem PR-Fix-Dokument mehr offen.
2. **Pflege-Regel für Graph-Renderer einhalten:** Änderungen nur noch in `backend/web/static/js/student_graph_view.js` umsetzen (einziger Pfad).

## Test-Plan (gezielt, dann breiter)
- Gezielt:
  - `.venv/bin/pytest -q backend/tests/test_learning_modular_units_api_contract.py`
  - `.venv/bin/pytest -q backend/tests/test_teaching_unit_phases_api.py`
  - `.venv/bin/pytest -q backend/tests/test_teaching_modular_unit_editor_crud_api_contract.py`
  - `.venv/bin/pytest -q backend/tests/test_student_modular_workspace_js_contract.py`
  - `.venv/bin/pytest -q backend/tests/test_student_graph_view_sync_contract.py`
- Danach breiter:
  - `.venv/bin/pytest -q backend/tests/test_openapi_teaching_modular_unit_editor_contract.py backend/tests/test_openapi_learning_modular_units_contract.py`

## Akzeptanzkriterien (Definition of Done)
- Alle MUST-FIX-Punkte sind umgesetzt.
- Neue/angepasste Tests sind grün.
- OpenAPI-Contract und Runtime-Verhalten sind konsistent (inkl. 401/403-Semantik).
- Keine Regression in modularem Submission-/H5P-Zugriffspfad bekannt.
