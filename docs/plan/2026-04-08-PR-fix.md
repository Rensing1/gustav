# PR Review – SvelteKit Bounded Context Refactor

Datum: 2026-04-08
Zeit: 18:33 CEST
Branch: `feature/sveltekit-bounded-context-refactor`
Base Ref: `master`
Merge-Base: `16aa32329659b630753379306292839dc19d0324`
Head SHA: `a16143948faca8aa7e173e50291a0cc6521a2e8d`

## Review Round – 18:33

### Reviewed Scope Summary

- Browser-BFF-Auth in SvelteKit (`frontend/src/lib/server/*`, `frontend/src/routes/auth/*`)
- neue App-/View-Read-Model-Endpunkte (`backend/web/routes/app.py`)
- neue Concern-Box-Flows inkl. Migrationen und RLS
- task-zentrierte H5P-Authoring-Endpunkte
- Proxy-/Compose-Verkabelung

### Relevant Context Notes

- Branch diff basiert direkt auf lokalem `master`; Review-Grundlage war `16aa323..a161439`.
- Der Diff ist sehr groß; der Review priorisiert deshalb Security-, Session-, RLS- und Persistenzrisiken.
- Vorhandene fokussierte Tests für `session-sync`, Concern Box und H5P-Task-Authoring laufen lokal grün, decken die unten genannten Ausfallpfade aber nicht ab.

### Code References Used For Findings

- `frontend/src/lib/server/backend-auth.ts`
- `frontend/src/lib/server/session.ts`
- `frontend/src/lib/server/api.ts`
- `backend/web/routes/app.py`
- `backend/web/routes/teaching.py`
- `backend/teaching/repo_db.py`
- `backend/web/config.py`
- `docker-compose.yml`
- `backend/tests/test_session_sync_api.py`
- `backend/tests/test_concern_box_views_api.py`
- `backend/tests/test_teaching_h5p_task_authoring_api.py`
- `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- `backend/tests/packaging/test_sveltekit_bff_session_contract.py`

### Kurzfazit

Der Branch bringt die SvelteKit-Umstellung sichtbar voran, hat aber noch zwei klare Freigabeblocker im neuen Auth-Bridge-Pfad: Logout ist nicht vollständig und das OIDC-Flow-Secret bleibt mit bekanntem Placeholder deploybar. Zusätzlich gibt es Robustheitslücken bei BFF-Session-Persistenz und H5P-Authoring-Rollback.

### MUST-FIX

- [SEC] Logout räumt die alte Backend-Session nicht ab — `frontend/src/lib/server/backend-auth.ts:394@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `/auth/logout` löscht nur `gustav_bff_session`; die bestehende `gustav_session` aus `/api/app/session-sync` bleibt aktiv, sodass cookie-basierte Backend-Flows nach dem vermeintlichen Logout weiter authentifiziert sind. — Fix: Lösche beim Frontend-Logout zusätzlich `gustav_session` bzw. rufe vor dem Keycloak-Redirect einen Backend-Logout/Session-Revoke-Pfad auf und sichere das mit einem E2E-Test ab.

- [SEC] Das neue OIDC-Flow-Cookie kann weiter mit einem bekannten Default-Secret signiert werden — `frontend/src/lib/server/backend-auth.ts:39@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Wenn `FRONTEND_SESSION_SECRET` nicht explizit gesetzt ist, wird `CHANGE_ME_DEV` verwendet; `docker-compose.yml` liefert genau diesen Placeholder standardmäßig mit. Damit ist die HMAC-Signatur des Flow-Cookies vorhersagbar. — Fix: Entferne den Runtime-Fallback, ergänze einen Frontend-Startup-Guard analog `backend/web/config.py` und streiche den Compose-Default auf `CHANGE_ME_DEV`.

### SHOULD-FIX

- [BUG] H5P-Save/Import ist nicht atomar — `backend/web/routes/teaching.py:3671@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Das Upstream-H5P-Objekt kann erfolgreich erzeugt/aktualisiert werden, bevor `_get_tasks_service().update_task(...)` scheitert. Dann bleiben verwaiste H5P-Inhalte und ein inkonsistenter Task-Zustand zurück. — Fix: Fange den lokalen Persistenzfehler explizit ab und implementiere einen Kompensationsschritt (Upstream-Delete/Unlink) oder einen idempotenten Zwei-Phasen-Ablauf.

- [BUG] Die BFF-Token-Session ist pro Prozess in einer `Map` gespeichert — `frontend/src/lib/server/session.ts:21@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Ein Frontend-Restart oder mehrere Frontend-Instanzen invalidieren Sitzungen zufällig. Das widerspricht der `lokal = prod`-Leitplanke und macht horizontale Skalierung unzuverlässig. — Fix: Lege die Session in einem geteilten Store oder als serverseitig geschützten, rotierbaren Cookie-Container ab und passe den Contract-Test entsprechend an.

### NICE-TO-HAVE

- [CONSISTENCY] Der Review-Diff enthält kleine Hygiene-Reste — `api/openapi.yml:3078@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Kein Laufzeitfehler, aber unnötiges Rauschen im PR (`git diff --check` meldet Trailing Whitespace und Blank-Line-at-EOF-Treffer). — Fix: Räume die gemeldeten Formatierungsreste vor Merge auf.

### TESTS

- Lokal ausgeführt: `.venv/bin/pytest -q backend/tests/test_session_sync_api.py backend/tests/test_concern_box_views_api.py backend/tests/test_teaching_h5p_task_authoring_api.py`
- Ergebnis: `11 passed in 0.51s`
- Relevante Lücke: Es gibt keinen Test, der für den neuen Frontend-Logout explizit die Löschung von `gustav_session` absichert.
- Relevante Lücke: Es gibt keinen Fehlerpfad-Test für `h5p/save` oder `h5p/import`, der ein erfolgreiches Upstream-Create plus scheiterndes `update_task(...)` simuliert.

### Refactor-Budget

- Ca. `0.5-1 Tag` für die beiden MUST-FIXs inkl. Tests.
- Ca. `0.5-1 Tag` zusätzlich für H5P-Kompensation und belastbare BFF-Session-Persistenz-Entscheidung.

### Freigabe

Nicht freigeben. Erst die beiden MUST-FIXs schließen; danach ist ein kleiner Nachreview für Logout und Secret-Guard ausreichend.

## Review Round – 18:36

### Reviewed Scope Summary

- Bearer-Auth-Verifikation und Middleware-Entscheidungslogik
- Concern-Box-Migrationen (RLS/Funktionen) und DB-Repository-Pfad
- Browser-BFF-Session/Cookie-Brücke inkl. H5P-Kopplung
- Learning-Read-Model für Material-Datei-URLs

### Relevant Context Notes

- Review-Grundlage: `16aa32329659b630753379306292839dc19d0324..a16143948faca8aa7e173e50291a0cc6521a2e8d`
- Fokus auf Security-, API-Contract- und Robustheitsrisiken im neuen BFF-Schnitt
- Keine zusätzlichen Testläufe in dieser Review-Runde ausgeführt

### Code References Used For Findings

- `backend/identity_access/tokens.py`
- `backend/web/main.py`
- `supabase/migrations/20260406093338_concern_box_entries_insert_function.sql`
- `api/openapi.yml`
- `backend/identity_access/admin_client.py`
- `frontend/src/lib/server/session.ts`
- `h5p-service/server.mjs`
- `.env.example`
- `backend/web/routes/learning.py`

### Kurzfazit

Der Branch ist architektonisch ein großer Schritt, hat aber noch drei Freigabeblocker: fehlerhafte Bearer-Fehlerabbildung (500 statt 401), eine impersonierbare `SECURITY DEFINER`-Insert-Funktion für Concern-Box-Einträge und eine Laufzeit-/OpenAPI-Auth-Drift bei Concern-Box-Write-Endpunkten. Zusätzlich gibt es Risiken bei Logging (PII), Session-Konfiguration und Skalierungsrobustheit.

### MUST-FIX

- [SEC] Abgelaufene Bearer-Tokens laufen in einen 500-Pfad statt in 401 — `backend/identity_access/tokens.py:209@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `_validate_temporal_claims` wirft `IDTokenVerificationError`; `main._bearer_auth_context_from_request` fängt aber nur `BearerTokenVerificationError`. Ein ungültiger `Authorization`-Header kann dadurch Requests mit 500 abbrechen. — Fix: Temporal-Fehler in `verify_bearer_token` auf `BearerTokenVerificationError` mappen und einen API-Test ergänzen, der bei abgelaufenem JWT explizit 401 erwartet.

- [SEC] Concern-Box-Insert-Funktion erlaubt `student_sub`-Spoofing unter `SECURITY DEFINER` — `supabase/migrations/20260406093338_concern_box_entries_insert_function.sql:25@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Die Funktion prüft Mitgliedschaft nur gegen den übergebenen Parameter `p_student_sub`, aber nicht gegen `app.current_sub`; so kann ein aufrufender Kontext prinzipiell Einträge im Namen anderer Kursteilnehmer erzeugen. — Fix: Erzwinge `p_student_sub = current_setting('app.current_sub', true)` (oder entferne den Parameter komplett und nutze ausschließlich `app.current_sub`) und decke den Missbrauchspfad per Migrationstest ab.

- [API] Concern-Box-Write-Endpunkte sind laut Vertrag Bearer-only, zur Laufzeit aber weiter Cookie-fähig — `backend/web/main.py:410@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `_requires_bff_bearer_auth` enthält die Pfade `/api/learning/concern-box/*` und `/api/teaching/concern-box/*` nicht; damit akzeptiert die Middleware dort Legacy-Session-Fallback trotz `bearerAuth`-Definition in `api/openapi.yml`. — Fix: Bearer-Pfadmengen um Concern-Box-Write-Pfade erweitern oder den OpenAPI-Contract auf Dual-Auth korrigieren (inkl. Tests, dass gewählter Modus strikt erzwungen wird).

### SHOULD-FIX

- [SEC] Keycloak-Fehlerantwort wird als Rohtext geloggt — `backend/identity_access/admin_client.py:76@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `response.text` kann personenbezogene oder sensible Felder enthalten; Logging dieser Payload kollidiert mit dem „no PII in logs“-Anspruch. — Fix: Logge nur Status + stabilen Fehlercode und entferne/rediere den Response-Body.

- [BUG] Cookie-Name ist in BFF-Session-Helfer hart verdrahtet, aber im Stack als konfigurierbar dokumentiert — `frontend/src/lib/server/session.ts:7@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `h5p-service` liest `FRONTEND_SESSION_COOKIE_NAME` aus ENV, der Frontend-BFF nicht; bei abweichender Konfiguration bricht die H5P-BFF-Auth-Kopplung. — Fix: Cookie-Namen auch im Frontend aus ENV laden (mit identischem Default) oder die Konfigurierbarkeit aus Doku/Compose entfernen.

- [BUG] Token-Session-Store wächst ungebremst pro Prozess — `frontend/src/lib/server/session.ts:21@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `TOKEN_SESSIONS` besitzt weder Größenlimit noch periodische TTL-Bereinigung; bei vielen kurzlebigen Sessions steigt Speicherverbrauch dauerhaft. — Fix: Füge TTL-Sweeper + Größenlimit (LRU) hinzu und überwache Session-Count/Memory.

- [BUG] Material-Datei-Presign erzeugt N+1-Verbindungen und N+1-Queries — `backend/web/routes/learning.py:505@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Für jede Datei wird erneut `psycopg.connect(...)` geöffnet; bei materialreichen Einheiten explodieren Latenz und DB-Last. — Fix: Storage-Keys pro Section/Modul in einem Batch laden und URLs in-memory signieren.

### TESTS

- In dieser Review-Runde wurden keine zusätzlichen Tests ausgeführt.
- Relevante Lücke: Kein Test deckt den Pfad „abgelaufenes Bearer-Token liefert 401 statt 500“ ab.
- Relevante Lücke: Kein Migrationstest erzwingt, dass `create_concern_box_entry` nicht mit fremdem `student_sub` aufrufbar ist.
- Relevante Lücke: Kein Contract-Test stellt sicher, dass Concern-Box-Write-Endpunkte wirklich Bearer-only sind (falls das der gewünschte Modus bleibt).

### Refactor-Budget

- Ca. `0.5 Tag` für Bearer-Fehlerabbildung inkl. Tests.
- Ca. `0.5 Tag` für sichere Concern-Box-Funktion + Migrationstest.
- Ca. `0.5 Tag` für Auth-Contract-Glättung (Middleware/OpenAPI/Tests).
- Ca. `0.5-1 Tag` für Session-/Material-Performance-Härtung.

### Freigabe

Nicht freigeben. Nach Behebung der drei MUST-FIX-Punkte ist ein kurzer Sicherheits-Nachreview ausreichend.

## Review Round – 18:33

Date: 2026-04-08 18:33 CEST

Branch: feature/sveltekit-bounded-context-refactor@a16143948faca8aa7e173e50291a0cc6521a2e8d
Base: master@16aa32329659b630753379306292839dc19d0324
Merge-base: 16aa32329659b630753379306292839dc19d0324

Reviewed scope summary:
- SvelteKit Browser-BFF auth bridge, especially callback/logout/session handling.
- Concern-box create flow plus matching OpenAPI contract.
- Prod-hardening around new frontend auth secrets and session persistence.

Kontextnotizen:
- FastAPI akzeptiert fuer nicht-BFF-Pfade weiterhin Cookie-Sessions; deshalb muss Logout beide Session-Typen sauber invalidieren.
- Der neue Node/BFF-Layer fuehrt eigene sicherheitskritische Cookie- und Session-Logik ein, die aktuell nicht von den bestehenden Backend-Startup-Guards abgesichert wird.

Code references used:
- `frontend/src/lib/server/backend-auth.ts:39-40@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/backend-auth.ts:79-90@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/backend-auth.ts:104-111@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/backend-auth.ts:394-407@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/session.ts:21-21@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/session.ts:94-100@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/session.ts:165-183@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/web/main.py:397-430@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/web/config.py:35-71@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/teaching/repo_db.py:4108-4128@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/web/routes/app.py:857-886@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `api/openapi.yml:5589-5626@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `api/openapi.yml:6082-6143@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `.env.example:51-52@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `docker-compose.yml:123-123@a16143948faca8aa7e173e50291a0cc6521a2e8d`

### Kurzfazit

Die Richtung des Refactors ist stimmig, aber die neue Browser-BFF-Auth ist noch nicht produktionshart. Kritisch sind unvollstaendiger Logout, ein bekannter Default fuer das Flow-Cookie-Secret und ein rein prozesslokaler Token-Store. Dazu kommt ein fachlich falsches Error-Mapping im Concern-Box-Create-Flow. In diesem Zustand ist der Branch nicht freigabefaehig.

### MUST-FIX

- [SEC] Frontend-Logout loescht nur `gustav_bff_session`, nicht `gustav_session` — `frontend/src/lib/server/backend-auth.ts:394@a161439` — Impact: Nach `/auth/logout` bleiben session-authentifizierte Legacy-/API-Pfade weiter nutzbar, obwohl der Contract fuer `/auth/logout` explizit das Loeschen der Application-Session verspricht — Fix: Loesche im SvelteKit-Logout zusaetzlich `gustav_session` host-only oder route den Logout ueber den bestehenden FastAPI-Logout-Pfad und decke den Fall mit einem Regressionstest gegen `/api/me` ab.
- [SEC] OIDC-Flow-Cookies sind mit bekanntem Default-Secret faelschbar, wenn `FRONTEND_SESSION_SECRET` fehlt — `frontend/src/lib/server/backend-auth.ts:39@a161439` — Impact: Der neue Browser-BFF-Flow faellt zur Laufzeit auf `CHANGE_ME_DEV` zurueck; in Prod/Stage ermoeglicht das Login-CSRF bzw. Session-Fixation durch vorhersehbare Cookie-Signaturen — Fix: Erzwinge fuer Prod/Stage einen non-placeholder-Guard analog `APP_CSRF_TOKEN_SECRET` und entferne den stillen Default aus dem Runtime-Pfad.
- [BUG] Frontend-Tokens liegen ausschliesslich im Prozessspeicher — `frontend/src/lib/server/session.ts:21@a161439` — Impact: Jeder Neustart des SvelteKit-Containers oder ein zweiter Replica-Knoten macht `gustav_bff_session` wertlos und wirft aktive Nutzer trotz gueltiger Backend-/Keycloak-Sitzung aus dem BFF — Fix: Persistiere die BFF-Session serverseitig oder vermeide prozesslokale Token-Maps; ergaenze mindestens einen Restart-/Multi-Instance-Regressionstest.

### SHOULD-FIX

- [API] Concern-Box-Create maskiert Format- und DB-Fehler als `403 forbidden` — `backend/teaching/repo_db.py:4111@a161439` — Impact: Ungueltige `course_id`-UUIDs oder echte DB-Probleme landen ueber `created is None` als Mitgliedschaftsfehler, obwohl der Contract fuer ungueltige Requests `400` vorsieht und Serverfehler unterscheidbar bleiben muessen — Fix: Validiere `course_id` als UUID vor dem Repo-Aufruf bzw. mappe Cast-/DB-Fehler getrennt auf `400` und `500` statt pauschal auf `403`.

### NICE-TO-HAVE

- [CONSISTENCY] Die SvelteKit-Logout-Validierung akzeptiert `..`, obwohl der OpenAPI-Contract das explizit verbietet — `frontend/src/lib/server/backend-auth.ts:104@a161439` — Impact: `/auth/logout?redirect=/foo/../bar` verstoesst gegen den dokumentierten Regex und kann auf einen anderen Zielpfad normalisiert werden als vorgesehen — Fix: Nutze denselben Redirect-Validator wie im Contract und teste den Negativfall fuer `..` explizit.

### TESTS

- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_session_sync_api.py backend/tests/test_bff_authorization_session_api.py backend/tests/test_concern_box_views_api.py backend/tests/test_profile_view_api.py` => `15 passed`
- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_openapi_app_session_sync_contract.py backend/tests/test_openapi_profile_contract.py backend/tests/migration/test_concern_box_entries_rls.py` => `3 passed, 1 skipped`
- Fehlend: Kein Regressionstest fuer SvelteKit-Logout gegen verbleibende Backend-Session, kein Prod-Guard-Test fuer `FRONTEND_SESSION_SECRET`, kein Restart-/Multi-Instance-Test fuer die BFF-Session-Persistenz, kein Fehlerpfad-Test fuer ungueltige `course_id` bei Concern-Box-Create.

### Refactor-Budget

- 0.5 Tag fuer Logout- und Secret-Hardening.
- 0.5-1.0 Tag fuer robuste BFF-Session-Persistenz oder eine saubere Alternativarchitektur.
- 0.25 Tag fuer Error-Mapping und zusaetzliche Negativtests im Concern-Box-Flow.

### Freigabe

Nicht freigeben. Drei Must-Fix-Befunde liegen im Auth-/Session-Pfad.

## Reparaturplan – 2026-04-08 18:51 CEST

### WP1 – Logout und Frontend-Secret härten

- Finding: Frontend-Logout räumt `gustav_session` nicht mit auf und `FRONTEND_SESSION_SECRET` fällt weiter auf `CHANGE_ME_DEV` zurück.
- Warum noch offen: `frontend/src/lib/server/backend-auth.ts` löscht aktuell nur `gustav_bff_session`; der Secret-Helper liefert weiter einen stillen Placeholder-Fallback.
- Repo-Regeln: Security first, lokal = prod, keine prod-unsicheren Defaults im Runtime-Pfad.
- Bounded Context: `identity_access`
- Voraussichtliche Dateien: `frontend/src/lib/server/backend-auth.ts`, `frontend/src/lib/server/session.ts`, `frontend/src/routes/auth/logout/+server.ts`, `docker-compose.yml`, `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- Vertrag/Migration zuerst: kein OpenAPI-Änderungsbedarf, keine Migration.
- Test zuerst: Regressionstest für Frontend-Logout gegen verbleibende Backend-Session; Guard-Test für prod-like Frontend-Konfiguration ohne valides Secret.
- Minimale Implementierung: Logout über den bestehenden Backend-Logout-Pfad oder äquivalente Session-Revoke-Logik führen; Secret-Fallback entfernen und prod-like Guard ergänzen.
- Akzeptanzcheck: Nach Frontend-Logout sind `gustav_bff_session` und `gustav_session` wirkungslos; prod-like Frontend-Konfiguration startet nicht mit Placeholder-Secret.

### WP2 – BFF-Session aus Prozessspeicher in persistenten Backend-Store verlagern

- Finding: Die BFF-Token-Session lebt nur in einer prozesslokalen `Map`.
- Warum noch offen: `frontend/src/lib/server/session.ts` nutzt weiterhin `Map<string, FrontendTokenSession>()`.
- Repo-Regeln: lokal = prod, Clean Architecture, keine fragilen Multi-Instance-Sonderpfade.
- Bounded Context: `identity_access`
- Voraussichtliche Dateien: `frontend/src/lib/server/session.ts`, `frontend/src/lib/server/api.ts`, `backend/web/routes/app.py`, ggf. `backend/identity_access/stores_db.py`, `backend/tests/packaging/test_sveltekit_bff_session_contract.py`
- Vertrag/Migration zuerst: kein öffentlicher OpenAPI-Änderungsbedarf; interner Persistenzpfad im Backend ist zulässig. Bestehende Session-Persistenzstruktur wird wiederverwendet, keine neue Migration standardmäßig nötig.
- Test zuerst: Contract-/Unit-Tests für persistenten opaque Cookie + serverseitige Speicherung; Logout-/Read-Flow darf keinen Prozessspeicher mehr voraussetzen.
- Minimale Implementierung: BFF-Token-Session an einen backend-seitigen Persistenzpfad koppeln und Frontend-Helper auf opaque Session-ID + Backend-Lookup umstellen.
- Akzeptanzcheck: Frontend-Code enthält keine Token-`Map` mehr; Refresh/Read/Clear funktionieren weiter mit persistenter serverseitiger Sitzung.

### WP3 – Bearer-Fehlerabbildung und Concern-Box-Write strikt machen

- Finding: Abgelaufene Bearer-Tokens können als 500 enden; Concern-Box-Write ist laut Vertrag Bearer-only, akzeptiert zur Laufzeit aber noch Cookie-Fallback.
- Warum noch offen: `verify_bearer_token()` delegiert temporale Fehler noch als `IDTokenVerificationError`; `_requires_bff_bearer_auth()` enthält die Concern-Box-Write-Pfade nicht.
- Repo-Regeln: API contract first, Security first, fail-closed.
- Bounded Context: `identity_access`, `learning`, `teaching`
- Voraussichtliche Dateien: `backend/identity_access/tokens.py`, `backend/web/main.py`, `backend/tests/test_tokens_bearer_verification.py`, `backend/tests/test_concern_box_views_api.py`
- Vertrag/Migration zuerst: `api/openapi.yml` bleibt unverändert, weil Bearer-only bereits dokumentiert ist.
- Test zuerst: API-Test für 401 bei abgelaufenem Bearer-Token; API-Test, dass Concern-Box-Write Cookie-only ablehnt.
- Minimale Implementierung: temporale JWT-Fehler auf `BearerTokenVerificationError` mappen; Bearer-only-Pfadmengen in der Middleware erweitern.
- Akzeptanzcheck: Bearer-only-Endpunkte liefern bei ungültigem/fehlendem Bearer konsistent 401, auch wenn ein `gustav_session`-Cookie vorhanden ist.

### WP4 – Concern-Box-SECURITY-DEFINER und Fehlermapping härten

- Finding: `create_concern_box_entry(...)` vertraut noch einem übergebenen `student_sub`; Repo-Code mappt Persistenzfehler derzeit zu pauschalem `403`.
- Warum noch offen: Die SQL-Funktion prüft nicht gegen `app.current_sub`, und `backend/teaching/repo_db.py` schluckt sämtliche Ausnahmen in `create_concern_box_entry`.
- Repo-Regeln: Security first, klare Fehlersemantik, RLS-Gedanke robust umsetzen.
- Bounded Context: `teaching` mit Lernenden-Schreibpfad aus `learning`
- Voraussichtliche Dateien: `supabase/migrations/20260406093338_concern_box_entries_insert_function.sql`, `backend/teaching/repo_db.py`, `backend/web/routes/app.py`, `backend/tests/migration/test_concern_box_entries_rls.py`, `backend/tests/test_concern_box_views_api.py`
- Vertrag/Migration zuerst: bestehende Migration wird durch einen nachfolgenden Security-Fix ergänzt; öffentliche API bleibt unverändert.
- Test zuerst: Migrationstest gegen `student_sub`-Spoofing; API-Test für sauberes `400 bad_request` bei ungültiger `course_id`.
- Minimale Implementierung: SQL-Funktion bindet Insert an `current_setting('app.current_sub', true)`; Repo unterscheidet Validierungs-/Cast-Fehler von Mitgliedschaftsfehlern.
- Akzeptanzcheck: Ein fremdes `student_sub` kann keinen Eintrag erzeugen; API liefert bei invaliden Requests `400`, bei fehlender Mitgliedschaft `403`.

### Offene, bewusst nicht gezogene Punkte

- H5P-Save/Import-Rollback bleibt in diesem Durchgang offen, weil der aktuelle H5P-Service keinen vorhandenen Delete-/Unlink-Endpunkt anbietet und dafür eine größere Folgereparatur nötig ist.
- Diff-Hygiene und kleinere Konsistenzreste bleiben `NICE-TO-HAVE`.

## Umsetzungsstand – 2026-04-08 19:03 CEST

### Geschlossene Findings

- `MUST-FIX`: Bearer-Temporalfehler werden jetzt in `BearerTokenVerificationError` gemappt; abgelaufene JWTs laufen damit kontrolliert in `401` statt in einen `500`-Pfad.
- `MUST-FIX`: Concern-Box-Write-Pfade sind zur Laufzeit jetzt Bearer-only wie im Vertrag dokumentiert; Cookie-only reicht dort nicht mehr aus.
- `MUST-FIX`: Concern-Box-Create validiert `course_id` jetzt vor dem Repo-Zugriff und liefert bei ungültiger UUID `400 bad_request`.
- `MUST-FIX`: Die SQL-Funktion für Concern-Box-Inserts bindet den Aufrufer jetzt an `app.current_sub` und schließt `student_sub`-Spoofing im `SECURITY DEFINER`-Pfad.
- `MUST-FIX`: Frontend-BFF-Token-Sessions liegen nicht mehr in einer prozesslokalen `Map`, sondern in einem backend-seitigen, persistenten Store mit opaque Cookie.
- `MUST-FIX`: Frontend-Logout räumt jetzt zusätzlich die bestehende Backend-Session über den vorhandenen `/auth/logout`-Pfad mit auf und propagiert dessen `Set-Cookie`-Invalidierung.
- `MUST-FIX`: `FRONTEND_SESSION_SECRET` fällt nicht mehr still auf `CHANGE_ME_DEV` zurück; ein SvelteKit-Server-Hook erzwingt jetzt einen prod-like Guard.
- `SHOULD-FIX`: Der Frontend-Cookie-Name ist jetzt auch im SvelteKit-Server über ENV konfigurierbar und damit mit dem H5P-Sidecar konsistent.

### Weiterhin offen

- `SHOULD-FIX`: H5P-Save/Import ist weiterhin nicht atomar. Dafür fehlt im aktuellen H5P-Service noch ein sauberer Kompensationspfad.
- `NICE-TO-HAVE`: Kleinere Diff-/Formatierungsreste wurden in diesem Durchgang nicht priorisiert.

### Stale nach Re-Check

- Kein verifizierter `MUST-FIX` wurde als stale verworfen.

### Tests und Verifikation

- Erfolgreich: `supabase status`
- Erfolgreich: `supabase migration new bff_sessions`
- Erfolgreich: `supabase migration up`
- Erfolgreich: `.venv/bin/pytest -q backend/tests/test_tokens_bearer_verification.py backend/tests/test_concern_box_views_api.py backend/tests/migration/test_concern_box_entries_rls.py backend/tests/packaging/test_sveltekit_bff_session_contract.py backend/tests/packaging/test_sveltekit_platform_contract.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- Erfolgreich: `npm run check`
- Nicht vollständig grün: `npm test`
  - ein bestehender, themenfremder Contract-Fail in `src/routes/account-menu-contract.test.ts`
  - zwei bestehende Frontend-Interaktionsfails in `src/routes/teaching/units/[unitId]/nodes/[nodeId]/page-interaction.test.ts`
  - zusätzlich nur Warnungen aus `svelte-check` zu bereits vorhandenem State-Capturing in drei Svelte-Dateien

### Geänderte Subsysteme

- `identity_access`: Bearer-Token-Fehlermapping, neuer BFF-Session-Store (memory + DB)
- `backend/web`: Middleware-/Concern-Box-Härtung und backend-interne BFF-Session-Endpunkte
- `frontend`: BFF-Session-Persistenz, Logout-Brücke, Frontend-Config-Guard
- `supabase/migrations`: regelkonform per CLI erzeugte Migration `20260408170229_bff_sessions.sql`

### Architektur- und Bounded-Context-Entscheidungen

- Die BFF-Token-Session bleibt dem Kontext `identity_access` zugeordnet und wird serverseitig persistent gehalten; der Browser trägt nur die opaque Session-ID.
- Concern-Box-Write bleibt Bearer-only und folgt damit weiter dem bereits dokumentierten Browser-BFF-Vertrag.
- Der zunächst manuell angelegte BFF-Migrationsentwurf wurde verworfen und durch eine per `supabase migration new` erzeugte Migration ersetzt, um den AGENTS-Workflow wieder einzuhalten.

### Release-Empfehlung

- Sicherheitslage gegenüber dem ursprünglichen Review klar verbessert.
- Für Freigabe weiterhin Nachreview auf den offenen H5P-Rollback-Punkt und auf die bereits vorher vorhandenen Frontend-Testfails einplanen.

## Reparaturplan – 2026-04-08 19:10 CEST

### WP1 – H5P-Save/Import mit Kompensation absichern

- Finding: H5P-Save/Import ist weiterhin nicht atomar; nach erfolgreichem Upstream-Create kann ein lokaler Persistenzfehler verwaiste Inhalte hinterlassen.
- Warum noch offen: `backend/web/routes/teaching.py` persistiert nach Upstream-Erfolg direkt mit `update_task(...)`, ohne Kompensationspfad; der H5P-Service bietet dafür noch keinen Delete-Endpunkt an.
- Repo-Regeln: API contract first, Security first, lokal = prod, kein stilles Datenleck über inkonsistente Nebenobjekte.
- Bounded Context: `teaching` mit technischem H5P-Sidecar.
- Voraussichtliche Dateien: `api/openapi.yml`, `h5p-service/server.mjs`, `backend/web/routes/teaching.py`, `backend/tests/test_teaching_h5p_task_authoring_api.py`
- Vertrag/Migration zuerst: öffentlicher H5P-Service-Vertrag wird vor der Implementierung um einen idempotenten Delete-Endpunkt ergänzt; keine DB-Migration nötig.
- Test zuerst: H5P-Delete-Endpunkt und Rollback-Pfad für `h5p/save` und `h5p/import`, wenn `update_task(...)` nach erfolgreichem Upstream-Create fehlschlägt.
- Minimale Implementierung: Delete-Endpunkt im H5P-Service bereitstellen; Teaching-Route fängt Persistenzfehler ab und versucht best-effort ein Upstream-Delete.
- Akzeptanzcheck: Kein erfolgreicher Upstream-Create bleibt ohne Task-Verknüpfung zurück; Fehlerpfad liefert kontrollierte API-Antwort statt still inkonsistentem Zustand.

### WP2 – Keycloak-Fehlerlogging ohne Response-Body

- Finding: `backend/identity_access/admin_client.py` loggt bei fehlgeschlagenem User-Update weiterhin `response.text`.
- Warum noch offen: Das Logging wurde im letzten Durchgang nicht verändert und verletzt weiterhin den PII-armen Logging-Anspruch.
- Repo-Regeln: Security first, keine personenbezogenen Daten in Logs.
- Bounded Context: `identity_access`
- Voraussichtliche Dateien: `backend/identity_access/admin_client.py`, neuer/ergänzter Test in `backend/tests/`
- Vertrag/Migration zuerst: keine.
- Test zuerst: Fehlgeschlagenes `update_user(...)` loggt Status + stabilen Fehlerhinweis, aber keinen Response-Body.
- Minimale Implementierung: Logging auf low-cardinality Felder umstellen; Response-Body nicht mehr protokollieren.
- Akzeptanzcheck: Logs enthalten keinen Keycloak-Body mehr, Diagnose bleibt über Status/Fehlercode möglich.

### WP3 – Material-Datei-Presign auf Batch-Auflösung umstellen

- Finding: Die Material-Datei-Anreicherung öffnet weiter pro Dateimaterial eigene DB-Verbindungen und Queries.
- Warum noch offen: `backend/web/routes/learning.py` ruft `_resolve_student_*_file_url(...)` weiterhin pro Material einzeln auf.
- Repo-Regeln: KISS, verständlicher Code, keine unnötigen N+1-Lastspitzen.
- Bounded Context: `learning`
- Voraussichtliche Dateien: `backend/web/routes/learning.py`, passende Learning-Tests
- Vertrag/Migration zuerst: keine öffentliche API-Änderung.
- Test zuerst: Mehrere Dateimaterialien in einer Antwort werden weiterhin korrekt angereichert; die Auflösung erfolgt über Batch-Helfer statt materialweiser Einzellogik.
- Minimale Implementierung: Batch-Lookup-Helfer für Section- und Modular-Materialien einführen und die bisherigen pro-Material-Resolver aus den Schleifen herausziehen.
- Akzeptanzcheck: Fachliches Verhalten bleibt gleich, aber pro Antwort wird nicht mehr pro Material neu verbunden.

### WP4 – Bereits dokumentierte Frontend-Testfehler bereinigen

- Finding: `account-menu-contract.test.ts` und `page-interaction.test.ts` schlagen weiterhin fehl.
- Warum noch offen: Diese Fails waren schon vor dem letzten Durchgang vorhanden und wurden bewusst nicht mit den Security-Fixes vermischt.
- Repo-Regeln: Design-Vertrag einhalten, bestehende Testbasis wieder grün ziehen.
- Bounded Context: `frontend`
- Voraussichtliche Dateien: `frontend/src/lib/styles/auth-theme.css`, `frontend/src/routes/teaching/units/[unitId]/nodes/[nodeId]/+page.svelte`
- Vertrag/Migration zuerst: keine.
- Test zuerst: die beiden bereits fehlschlagenden Vitest-Dateien reproduzieren.
- Minimale Implementierung: Auth-Theme und Contract wieder synchronisieren; Node-Editor-Effekt so umbauen, dass kein self-triggered Update-Loop entsteht.
- Akzeptanzcheck: beide bisher dokumentierten Frontend-Testdateien laufen grün.

## Umsetzungsstand – 2026-04-08 19:22 CEST

### Geschlossene Findings

- `SHOULD-FIX`: H5P-Save/Import ist jetzt gegen lokale Persistenzfehler abgesichert. Nach erfolgreichem Upstream-Create/Import wird bei `update_task(...)`-Fehlern ein best-effort-Delete gegen den H5P-Service ausgelöst.
- `SHOULD-FIX`: Der H5P-Service besitzt jetzt einen teacher-only Delete-Endpunkt für Inhalte; der öffentliche Vertrag in `api/openapi.yml` wurde dazu ergänzt.
- `SHOULD-FIX`: `backend/identity_access/admin_client.py` loggt bei Keycloak-Update-Fehlern keinen `response.text` mehr.
- `SHOULD-FIX`: Die Material-Datei-Anreicherung in `backend/web/routes/learning.py` nutzt jetzt Batch-Helfer mit einer DB-Verbindung pro Antwortform statt pro Dateimaterial.
- `Verifikation`: Der bisher rote Auth-Theme-Contract-Test läuft wieder grün.
- `Verifikation`: Der bisher rote Teacher-Node-Editor-Interaktionstest läuft wieder grün; der Update-Loop über `handledForm` ist beseitigt.

### Weiterhin offen

- Kein verifizierter Punkt aus diesem Reparaturplan bleibt offen.

### Stale nach Re-Check

- Die frühere Diff-Hygiene ist inzwischen stale; `git diff --check` ist sauber.

### Tests und Verifikation

- Erfolgreich: `.venv/bin/pytest -q backend/tests/test_teaching_h5p_task_authoring_api.py`
- Erfolgreich: `.venv/bin/pytest -q backend/tests/test_identity_access_admin_client.py`
- Erfolgreich: `.venv/bin/pytest -q backend/tests/test_learning_material_file_batching.py`
- Erfolgreich: `.venv/bin/pytest -q backend/tests/test_learning_api_contract.py::test_sections_include_short_lived_file_url_for_released_file_materials backend/tests/test_learning_modular_units_api_contract.py::test_learning_modular_module_content_includes_file_preview_url_for_file_materials`
- Erfolgreich: `npm test -- test/content_delete_contract.test.mjs`
- Erfolgreich: `npm test -- src/routes/account-menu-contract.test.ts`
- Erfolgreich: `npm test -- src/routes/teaching/units/[unitId]/nodes/[nodeId]/page-interaction.test.ts`
- Erfolgreich: `npm run check`
  - Ergebnis: `0 errors`, `3 warnings`
  - Restwarnungen: drei bereits vorhandene Svelte-Warnungen zu `state_referenced_locally`
- Nicht vollständig grün: `make verify`
  - Ergebnis: `63 failed, 1306 passed, 20 skipped`
  - Charakter der Fails: themenfremde Bestandsfehler in Learning-Submission-/Worker-/Live-Summary-Pfaden sowie mehrere bestehende Packaging-/UI-Contract-Fails außerhalb dieses PR-Fix-Scopes

### Geänderte Subsysteme

- `h5p-service`: neuer Delete-Endpunkt für H5P-Inhalte
- `backend/web/routes/teaching.py`: H5P-Rollback-Kompensation
- `identity_access`: PII-armes Keycloak-Fehlerlogging
- `backend/web/routes/learning.py`: Batch-Auflösung für Material-Datei-URLs
- `frontend`: Auth-Theme-Contract und Teacher-Node-Editor-Reaktivität

### Architektur- und Bounded-Context-Entscheidungen

- Die H5P-Kompensation bleibt im `teaching`-Adapter und verschiebt keine Fachlogik in den H5P-Service; der Sidecar stellt nur das notwendige Delete-Primitive bereit.
- Die Material-Datei-Batch-Helfer bleiben im `learning`-Read-Adapter, weil dort die studentische Sicht inklusive Presign-Anreicherung entsteht.
- Das entschärfte Keycloak-Logging protokolliert nur stabile Telemetrie-Felder und keine Antwort-Payloads.

### Release-Empfehlung

- Die im Restplan verifizierten offenen PR-Fix-Punkte sind geschlossen.
- Für eine vollständige Freigabe bleibt trotzdem ein separater Durchgang auf die zahlreichen bestehenden `make verify`-Altfehler nötig; diese wurden durch diesen Reparaturlauf weder eingeführt noch mitbehoben.

## Review Round – 19:32

Datum: 2026-04-08
Zeit: 19:32 CEST
Branch: `feature/sveltekit-bounded-context-refactor`
Base Ref: `master`
Merge-Base: `16aa32329659b630753379306292839dc19d0324`
Head SHA: `ae6c75904c5d8d307fdcb5651381e26f0957c49c`

### Reviewed Scope Summary

- Re-Review der zuletzt gehärteten Auth-, BFF-, Concern-Box-, H5P- und Learning-Dateipfade gegen den aktuellen Head
- Abgleich der früheren Tagesbefunde mit dem realen Branch-Zustand nach `fix: close remaining PR hardening findings`
- schneller Hygiene-Check für den gesamten Branch-Diff

### Relevant Context Notes

- Review-Grundlage war erneut der echte Diff `16aa32329659b630753379306292839dc19d0324..ae6c75904c5d8d307fdcb5651381e26f0957c49c`.
- Die früher dokumentierten Auth-/RLS-/Rollback-Fixes sind im aktuellen Head weitgehend nachvollziehbar geschlossen.
- Im aktuellen Head bleibt noch ein echter Codepflege-Befund im Learning-Adapter sowie ein kleiner Diff-/Dokurest.

### Code References Used For Findings

- `backend/web/routes/learning.py`
- `backend/identity_access/admin_client.py`
- `backend/tests/test_tokens_bearer_verification.py`
- `backend/tests/test_concern_box_views_api.py`
- `backend/tests/test_teaching_h5p_task_authoring_api.py`
- `backend/tests/test_learning_material_file_batching.py`
- `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- `backend/tests/packaging/test_sveltekit_bff_session_contract.py`
- `backend/tests/packaging/test_sveltekit_platform_contract.py`
- `backend/tests/migration/test_concern_box_entries_rls.py`
- `api/openapi.yml`
- `docs/plan/2026-04-08-PR-fix.md`

### Kurzfazit

Die zuvor kritischen Auth-, RLS- und H5P-Härtungspunkte wirken im aktuellen Head geschlossen. Offen bleibt noch ein echter Wartbarkeitsfehler im Learning-Adapter: eine doppelte Helper-Definition verdeckt toten, bereits inkonsistenten Code. Zusätzlich ist der Branch-Diff noch nicht vollständig sauber formatiert. Kein neuer Security-Blocker, aber ein kleiner Cleanup vor Merge ist sinnvoll.

### MUST-FIX

- Keine neuen MUST-FIX-Befunde im aktuellen Head.

### SHOULD-FIX

- [BUG] Doppelte Helper-Definition verdeckt toten, bereits fehlerhaften Code — `backend/web/routes/learning.py:521@ae6c75904c5d8d307fdcb5651381e26f0957c49c` — Impact: `_resolve_student_modular_material_file_url(...)` ist zweimal definiert; die erste Version enthält unerreichbaren Altcode mit inkonsistentem Rückgabetyp und Verweis auf das nicht definierte `valid_material_ids`. Aktuell gewinnt nur die zweite Definition, aber der tote Block macht den Adapter unnötig fragil und irreführend für spätere Refactors. — Fix: Entferne die erste Altdefinition vollständig und behalte genau eine schlanke Implementierung, die auf `_load_modular_material_urls(...)` delegiert.

### NICE-TO-HAVE

- [CONSISTENCY] Review-Hygiene ist noch nicht vollständig sauber — `docs/plan/2026-04-08-PR-fix.md:387@ae6c75904c5d8d307fdcb5651381e26f0957c49c` — Impact: Die Tagesnotiz behauptet bereits, `git diff --check` sei sauber, tatsächlich meldet der aktuelle Branch weiter Trailing-Whitespace-/EOF-Treffer, unter anderem in `api/openapi.yml`. Das ist kein Laufzeitfehler, macht den Review-Verlauf aber unnötig ungenau. — Fix: Aktualisiere die Review-Notiz oder räume die verbleibenden `git diff --check`-Treffer vor Merge weg.

### TESTS

- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_tokens_bearer_verification.py backend/tests/test_concern_box_views_api.py backend/tests/test_teaching_h5p_task_authoring_api.py backend/tests/test_learning_material_file_batching.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py backend/tests/packaging/test_sveltekit_bff_session_contract.py backend/tests/packaging/test_sveltekit_platform_contract.py backend/tests/migration/test_concern_box_entries_rls.py`
- Ergebnis: `25 passed in 1.62s`
- Zusätzlich geprüft: `git diff --check 16aa32329659b630753379306292839dc19d0324..ae6c75904c5d8d307fdcb5651381e26f0957c49c`
- Ergebnis: noch vorhandene Hygiene-Treffer in `api/openapi.yml`, einzelnen Tests/Docs und einigen Frontend-Metadateien

### Refactor-Budget

- Ca. `15-30 Minuten` für den doppelten Learning-Helper plus kurzen Nachtest.
- Ca. `10 Minuten` für `git diff --check`-Bereinigung oder konsistente Review-Doku.

### Freigabe

Fachlich weitgehend freigabefähig; kein neuer Security- oder Contract-Blocker. Den doppelten Learning-Helper würde ich aber noch vor Merge bereinigen, weil er unnötig verwirrenden toten Code im produktiven Adapter stehen lässt.

## Review Round – 19:48

Datum: 2026-04-08
Zeit: 19:48 CEST
Branch: `feature/sveltekit-bounded-context-refactor`
Base Ref: `master`
Merge-Base: `16aa32329659b630753379306292839dc19d0324`
Head SHA: `ae6c75904c5d8d307fdcb5651381e26f0957c49c`

### Reviewed Scope Summary

- gezielte Review-Stichprobe des neuen SvelteKit-BFF-Flows gegen den echten Branch-Diff
- Fokus auf Auth-Bridge, BFF-Session-Persistenz, neue internen Session-Endpunkte, API-Vertrag und Proxy-Verhalten
- zusätzlicher Laufzeit-Check der internen BFF-Session-Route per ASGI-Probe

### Relevant Context Notes

- Review-Grundlage war der Diff `16aa32329659b630753379306292839dc19d0324..ae6c75904c5d8d307fdcb5651381e26f0957c49c`.
- Die Contract- und API-Tests für die neue Auth-Bridge liefen grün, decken aber den entdeckten Integrationsbruch derzeit nicht ab.
- Eine manuelle ASGI-Probe ergab für `PUT /backend-internal/app/bff-session` ohne Auth aktuell `302` mit `Location: /auth/login`.

### Code References Used For Findings

- `backend/web/main.py`
- `backend/web/routes/app.py`
- `frontend/src/lib/server/session.ts`
- `frontend/src/lib/server/backend-auth.ts`
- `frontend/src/lib/server/bff-proxy.ts`
- `backend/tests/packaging/test_sveltekit_bff_session_contract.py`
- `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- `backend/tests/test_session_sync_api.py`
- `backend/tests/test_bff_authorization_session_api.py`
- `api/openapi.yml`

### Kurzfazit

Der Branch bringt die SvelteKit-BFF-Struktur sichtbar voran, hat aber einen harten Integrationsbruch im neuen serverseitigen Token-Session-Hop. Dadurch ist die Anmeldung in der aktuellen Form nicht belastbar. Zusätzlich fehlen genau an dieser Stelle Laufzeittests, und der Browser-BFF-Proxy verliert Sicherheitsheader.

### MUST-FIX

- [API] Interne BFF-Session-Requests landen im allgemeinen Auth-Redirect statt in einer nutzbaren Maschinen-API — `backend/web/main.py:421@ae6c75904c5d8d307fdcb5651381e26f0957c49c` — Impact: `frontend/src/lib/server/session.ts` ruft `/backend-internal/app/bff-session` ohne Bearer/Cookie auf; die FastAPI-Middleware behandelt den Präfix weder als interne Servicefläche noch als API-Fehlerpfad. Die aktuelle Laufzeitreaktion ist `302 -> /auth/login`, damit können Callback, Refresh und nachgelagerte Loader die `gustav_bff_session` nicht stabil anlegen oder lesen. — Fix: Behandle `/backend-internal/*` explizit als interne Maschinenfläche oder authentisiere diese Requests intern, erzeuge für diesen Präfix niemals Browser-Redirects und sichere PUT/GET/PATCH/DELETE per Laufzeittest ab.

### SHOULD-FIX

- [BUG] Der Auth-Callback schlägt nicht fehl, wenn die Token-Session-Persistenz scheitert — `frontend/src/lib/server/backend-auth.ts:393@ae6c75904c5d8d307fdcb5651381e26f0957c49c` — Impact: `createTokenSession()` und danach `syncAppSession()` werden nicht validiert; der Flow-Cookie wird trotzdem gelöscht und der Nutzer weitergeleitet. Das produziert Login-Loops oder Halbsessions statt eines klaren Fehlers. — Fix: Schlage auf `null` oder Exception fail closed fehl, liefere eine strukturierte Fehlerantwort und lösche den Flow-Cookie erst nach erfolgreicher Persistenz/Synchronisierung.
- [TEST] Die vorhandene BFF-Session-Suite prüft nur String-Vorkommen, nicht das Laufzeitverhalten — `backend/tests/packaging/test_sveltekit_bff_session_contract.py:11@ae6c75904c5d8d307fdcb5651381e26f0957c49c` — Impact: Die Suite bleibt grün, obwohl der neue Session-Hop zur Laufzeit nicht erreichbar ist. — Fix: Ergänze einen echten Integrations-/ASGI-Test, der den Session-Store-Hop inklusive Auth-/Redirect-Verhalten ausführt statt nur Quelltext zu prüfen.
- [CONSISTENCY] Der Browser-BFF-Write-Proxy verwirft die Sicherheits-Header des Backends — `frontend/src/lib/server/bff-proxy.ts:39@ae6c75904c5d8d307fdcb5651381e26f0957c49c` — Impact: Die neuen graph/write-Endpunkte verlieren `Cache-Control: private, no-store` und `Vary`, obwohl das Backend diese Antworten bewusst so markiert. Das weicht vom Sicherheitsvertrag der bestehenden Write-APIs ab. — Fix: Reiche die relevanten Response-Header durch oder setze sie im Proxy explizit.

### NICE-TO-HAVE

- Keine weiteren belastbaren Punkte aus der Stichprobe.

### TESTS

- Ausgeführt: `.venv/bin/pytest -q backend/tests/packaging/test_sveltekit_bff_session_contract.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py backend/tests/test_session_sync_api.py backend/tests/test_bff_authorization_session_api.py`
- Ergebnis: `8 passed in 0.65s`
- Zusätzlich geprüft: ASGI-Probe gegen `PUT /backend-internal/app/bff-session`
- Ergebnis: un-authentifiziert aktuell `302` auf `/auth/login`

### Refactor-Budget

- Ca. `0,5-1 Tag` für Middleware-/Auth-Härtung plus 2-3 echte Laufzeittests.

### Freigabe

Nicht freigeben, bevor der interne BFF-Session-Hop stabil und testbar erreichbar ist.

## Reparaturplan – 2026-04-08 20:05 CEST

### WP1 – Internen BFF-Session-Hop wieder als Maschinen-API herstellen

- Finding: `/backend-internal/app/bff-session` landet aktuell in der Browser-Redirect-Logik und ist damit für den SvelteKit-BFF nicht verlässlich nutzbar.
- Warum noch offen: `backend/web/main.py` behandelt den Pfad weder als API-Fehlerpfad noch als interne Servicefläche.
- Repo-Regeln: Security first, fail closed, lokal = prod, keine neue unnötige Sonderarchitektur.
- Bounded Context: `identity_access`
- Voraussichtliche Dateien: `backend/web/main.py`, neuer Laufzeittest unter `backend/tests/`
- Vertrag/Migration zuerst: keine öffentliche API- oder Schema-Änderung.
- Test zuerst: ASGI-Regressionstest für `PUT|GET|PATCH|DELETE /backend-internal/app/bff-session` ohne Browser-Redirect.
- Minimale Implementierung: nur den konkreten internen BFF-Session-Pfad aus der Browser-Redirect-Logik herausnehmen; Semantik der Route unverändert lassen.
- Akzeptanzcheck: `PUT` liefert `200`, `GET` ohne Header `204`, `PATCH` ohne Header `401`, `DELETE` ohne Header `204`, jeweils ohne `Location: /auth/login`.

### WP2 – Auth-Callback fail closed statt Halbsession

- Finding: `handleAuthCallback(...)` prüft den Rückgabewert von `createTokenSession()` und `syncAppSession()` nicht.
- Warum noch offen: Der Callback räumt den Flow-Cookie aktuell auch dann weg, wenn die BFF- oder App-Session nicht vollständig angelegt wurde.
- Repo-Regeln: Security first, KISS, tutorial-taugliche Fehlersemantik.
- Bounded Context: `identity_access`
- Voraussichtliche Dateien: `frontend/src/lib/server/backend-auth.ts`, neuer Vitest unter `frontend/src/lib/server/`
- Vertrag/Migration zuerst: keine öffentliche API- oder Schema-Änderung.
- Test zuerst: Vitest für Persistenzfehler und Session-Sync-Fehler.
- Minimale Implementierung: bei Fehlern `502 {"error":"session_setup_failed"}` liefern, Flow-Cookie nicht vorzeitig löschen und bei fehlgeschlagenem `session-sync` die bereits angelegte BFF-Session zurückrollen.
- Akzeptanzcheck: Kein Redirect und kein stiller Login-Loop mehr, wenn die serverseitige Session-Strecke scheitert.

### WP3 – BFF-Write-Proxy Sicherheitsheader erhalten

- Finding: `frontend/src/lib/server/bff-proxy.ts` reicht `Cache-Control` und `Vary` nicht weiter.
- Warum noch offen: Die Response wird neu aufgebaut und behält bisher nur `content-type`.
- Repo-Regeln: Security first, API-Sicherheitsvertrag der Write-Endpunkte nicht abschwächen.
- Bounded Context: `identity_access` / Plattform-BFF
- Voraussichtliche Dateien: `frontend/src/lib/server/bff-proxy.ts`, neuer Vitest unter `frontend/src/lib/server/`
- Vertrag/Migration zuerst: keine.
- Test zuerst: Vitest, der `content-type`, `cache-control` und `vary` vom Backend-Response erwartet.
- Minimale Implementierung: gezieltes Header-Passthrough für die sicherheitsrelevanten Header.
- Akzeptanzcheck: Browser-BFF-Write-Antworten behalten die Privacy-Header des Backends.

### WP4 – Doppelten Learning-Helper entfernen

- Finding: `_resolve_student_modular_material_file_url(...)` ist doppelt definiert; die erste Definition ist toter Altcode.
- Warum noch offen: Der Branch enthält weiter zwei Funktionsdefinitionen derselben Helferfunktion.
- Repo-Regeln: KISS, FOSS-Lesbarkeit, lehrbarer Adapter-Code.
- Bounded Context: `learning`
- Voraussichtliche Dateien: `backend/web/routes/learning.py`, ergänzender Test unter `backend/tests/`
- Vertrag/Migration zuerst: keine.
- Test zuerst: Source-Contract-Test, dass die Helper-Funktion nur einmal definiert ist.
- Minimale Implementierung: Altdefinition entfernen und nur die schlanke Delegationsvariante behalten.
- Akzeptanzcheck: genau eine Definition im Adapter, bestehende Batch-Tests bleiben grün.

## Umsetzungsstand – 2026-04-08 20:22 CEST

### Geschlossene Findings

- `MUST-FIX`: `/backend-internal/app/bff-session` läuft wieder als interne Maschinen-API und fällt nicht mehr in den Browser-Redirect auf `/auth/login`.
- `SHOULD-FIX`: `handleAuthCallback(...)` schlägt jetzt fail closed mit `502 {"error":"session_setup_failed"}` fehl, wenn BFF-Session-Persistenz oder `session-sync` scheitern.
- `SHOULD-FIX`: Der Callback rollt eine bereits angelegte BFF-Session bei fehlgeschlagenem `session-sync` wieder zurück, statt eine Halbsession stehen zu lassen.
- `SHOULD-FIX`: `frontend/src/lib/server/bff-proxy.ts` reicht `Cache-Control` und `Vary` aus Backend-Write-Antworten wieder an den Browser durch.
- `SHOULD-FIX`: Der doppelte Helper `_resolve_student_modular_material_file_url(...)` wurde auf genau eine Definition reduziert.

### Weiterhin offen

- `NICE-TO-HAVE`: Branchweite `git diff --check`-Hygiene blieb bewusst außerhalb dieses Reparaturlaufs.

### Stale nach Re-Check

- Kein verifizierter Befund aus dem Reparaturplan war stale.

### Tests und Verifikation

- Erfolgreich: `.venv/bin/pytest -q backend/tests/test_bff_session_internal_api.py backend/tests/test_learning_modular_material_helper_contract.py`
- Erfolgreich: `npm --prefix frontend test -- src/lib/server/backend-auth.test.ts src/lib/server/bff-proxy.test.ts`
- Erfolgreich: `.venv/bin/pytest -q backend/tests/test_bff_session_internal_api.py backend/tests/test_learning_modular_material_helper_contract.py backend/tests/test_session_sync_api.py backend/tests/test_bff_authorization_session_api.py backend/tests/test_learning_material_file_batching.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py backend/tests/packaging/test_sveltekit_bff_session_contract.py`
- Erfolgreich: `npm --prefix frontend run check`
  - Ergebnis: `0 errors`, `3 warnings`
  - Restwarnungen: bereits vorhandene Svelte-Warnungen zu `state_referenced_locally` in drei Seitenkomponenten

### Geänderte Subsysteme

- `identity_access`: Auth-Middleware-Allowlist und fail-closed Callback-Verhalten
- `frontend` Browser-BFF: Sicherheitsheader-Passthrough im Write-Proxy
- `learning`: Cleanup des doppelten Material-Helpers
- `docs/plan`: Reparaturplan und Ausführungsprotokoll ergänzt

### Architektur- und Bounded-Context-Entscheidungen

- Der interne BFF-Session-Pfad bleibt absichtlich ein exakt allowlisteter interner Compose-Pfad; es wurde kein neues generisches `/backend-internal/*`-Auth-Modell eingeführt.
- Es gab keine öffentliche API- oder Migrationsänderung; der Fix bleibt vollständig im bestehenden Plattform-/Adapter-Verhalten.

### Release-Empfehlung

- Der konkrete BFF-Integrationsbruch aus der Review-Runde `19:48` ist geschlossen.
- Für Merge bleibt nur noch der bewusst ausgesparte Hygiene-Durchgang als separater Cleanup-Kandidat.
