# PR Review – SvelteKit Bounded Context Refactor

Datum: 2026-04-08
Zeit: 18:33 CEST
Branch: `feature/sveltekit-bounded-context-refactor`
Base Ref: `master`
Merge-Base: `16aa32329659b630753379306292839dc19d0324`
Head SHA: `a16143948faca8aa7e173e50291a0cc6521a2e8d`

## Review Round – 18:33

### Reviewed Scope Summary

- Browser-BFF-Auth in SvelteKit (`frontend/src/lib/server/*`, `frontend/src/routes/auth/*`)
- neue App-/View-Read-Model-Endpunkte (`backend/web/routes/app.py`)
- neue Concern-Box-Flows inkl. Migrationen und RLS
- task-zentrierte H5P-Authoring-Endpunkte
- Proxy-/Compose-Verkabelung

### Relevant Context Notes

- Branch diff basiert direkt auf lokalem `master`; Review-Grundlage war `16aa323..a161439`.
- Der Diff ist sehr groß; der Review priorisiert deshalb Security-, Session-, RLS- und Persistenzrisiken.
- Vorhandene fokussierte Tests für `session-sync`, Concern Box und H5P-Task-Authoring laufen lokal grün, decken die unten genannten Ausfallpfade aber nicht ab.

### Code References Used For Findings

- `frontend/src/lib/server/backend-auth.ts`
- `frontend/src/lib/server/session.ts`
- `frontend/src/lib/server/api.ts`
- `backend/web/routes/app.py`
- `backend/web/routes/teaching.py`
- `backend/teaching/repo_db.py`
- `backend/web/config.py`
- `docker-compose.yml`
- `backend/tests/test_session_sync_api.py`
- `backend/tests/test_concern_box_views_api.py`
- `backend/tests/test_teaching_h5p_task_authoring_api.py`
- `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- `backend/tests/packaging/test_sveltekit_bff_session_contract.py`

### Kurzfazit

Der Branch bringt die SvelteKit-Umstellung sichtbar voran, hat aber noch zwei klare Freigabeblocker im neuen Auth-Bridge-Pfad: Logout ist nicht vollständig und das OIDC-Flow-Secret bleibt mit bekanntem Placeholder deploybar. Zusätzlich gibt es Robustheitslücken bei BFF-Session-Persistenz und H5P-Authoring-Rollback.

### MUST-FIX

- [SEC] Logout räumt die alte Backend-Session nicht ab — `frontend/src/lib/server/backend-auth.ts:394@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `/auth/logout` löscht nur `gustav_bff_session`; die bestehende `gustav_session` aus `/api/app/session-sync` bleibt aktiv, sodass cookie-basierte Backend-Flows nach dem vermeintlichen Logout weiter authentifiziert sind. — Fix: Lösche beim Frontend-Logout zusätzlich `gustav_session` bzw. rufe vor dem Keycloak-Redirect einen Backend-Logout/Session-Revoke-Pfad auf und sichere das mit einem E2E-Test ab.

- [SEC] Das neue OIDC-Flow-Cookie kann weiter mit einem bekannten Default-Secret signiert werden — `frontend/src/lib/server/backend-auth.ts:39@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Wenn `FRONTEND_SESSION_SECRET` nicht explizit gesetzt ist, wird `CHANGE_ME_DEV` verwendet; `docker-compose.yml` liefert genau diesen Placeholder standardmäßig mit. Damit ist die HMAC-Signatur des Flow-Cookies vorhersagbar. — Fix: Entferne den Runtime-Fallback, ergänze einen Frontend-Startup-Guard analog `backend/web/config.py` und streiche den Compose-Default auf `CHANGE_ME_DEV`.

### SHOULD-FIX

- [BUG] H5P-Save/Import ist nicht atomar — `backend/web/routes/teaching.py:3671@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Das Upstream-H5P-Objekt kann erfolgreich erzeugt/aktualisiert werden, bevor `_get_tasks_service().update_task(...)` scheitert. Dann bleiben verwaiste H5P-Inhalte und ein inkonsistenter Task-Zustand zurück. — Fix: Fange den lokalen Persistenzfehler explizit ab und implementiere einen Kompensationsschritt (Upstream-Delete/Unlink) oder einen idempotenten Zwei-Phasen-Ablauf.

- [BUG] Die BFF-Token-Session ist pro Prozess in einer `Map` gespeichert — `frontend/src/lib/server/session.ts:21@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Ein Frontend-Restart oder mehrere Frontend-Instanzen invalidieren Sitzungen zufällig. Das widerspricht der `lokal = prod`-Leitplanke und macht horizontale Skalierung unzuverlässig. — Fix: Lege die Session in einem geteilten Store oder als serverseitig geschützten, rotierbaren Cookie-Container ab und passe den Contract-Test entsprechend an.

### NICE-TO-HAVE

- [CONSISTENCY] Der Review-Diff enthält kleine Hygiene-Reste — `api/openapi.yml:3078@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Kein Laufzeitfehler, aber unnötiges Rauschen im PR (`git diff --check` meldet Trailing Whitespace und Blank-Line-at-EOF-Treffer). — Fix: Räume die gemeldeten Formatierungsreste vor Merge auf.

### TESTS

- Lokal ausgeführt: `.venv/bin/pytest -q backend/tests/test_session_sync_api.py backend/tests/test_concern_box_views_api.py backend/tests/test_teaching_h5p_task_authoring_api.py`
- Ergebnis: `11 passed in 0.51s`
- Relevante Lücke: Es gibt keinen Test, der für den neuen Frontend-Logout explizit die Löschung von `gustav_session` absichert.
- Relevante Lücke: Es gibt keinen Fehlerpfad-Test für `h5p/save` oder `h5p/import`, der ein erfolgreiches Upstream-Create plus scheiterndes `update_task(...)` simuliert.

### Refactor-Budget

- Ca. `0.5-1 Tag` für die beiden MUST-FIXs inkl. Tests.
- Ca. `0.5-1 Tag` zusätzlich für H5P-Kompensation und belastbare BFF-Session-Persistenz-Entscheidung.

### Freigabe

Nicht freigeben. Erst die beiden MUST-FIXs schließen; danach ist ein kleiner Nachreview für Logout und Secret-Guard ausreichend.

## Review Round – 18:36

### Reviewed Scope Summary

- Bearer-Auth-Verifikation und Middleware-Entscheidungslogik
- Concern-Box-Migrationen (RLS/Funktionen) und DB-Repository-Pfad
- Browser-BFF-Session/Cookie-Brücke inkl. H5P-Kopplung
- Learning-Read-Model für Material-Datei-URLs

### Relevant Context Notes

- Review-Grundlage: `16aa32329659b630753379306292839dc19d0324..a16143948faca8aa7e173e50291a0cc6521a2e8d`
- Fokus auf Security-, API-Contract- und Robustheitsrisiken im neuen BFF-Schnitt
- Keine zusätzlichen Testläufe in dieser Review-Runde ausgeführt

### Code References Used For Findings

- `backend/identity_access/tokens.py`
- `backend/web/main.py`
- `supabase/migrations/20260406093338_concern_box_entries_insert_function.sql`
- `api/openapi.yml`
- `backend/identity_access/admin_client.py`
- `frontend/src/lib/server/session.ts`
- `h5p-service/server.mjs`
- `.env.example`
- `backend/web/routes/learning.py`

### Kurzfazit

Der Branch ist architektonisch ein großer Schritt, hat aber noch drei Freigabeblocker: fehlerhafte Bearer-Fehlerabbildung (500 statt 401), eine impersonierbare `SECURITY DEFINER`-Insert-Funktion für Concern-Box-Einträge und eine Laufzeit-/OpenAPI-Auth-Drift bei Concern-Box-Write-Endpunkten. Zusätzlich gibt es Risiken bei Logging (PII), Session-Konfiguration und Skalierungsrobustheit.

### MUST-FIX

- [SEC] Abgelaufene Bearer-Tokens laufen in einen 500-Pfad statt in 401 — `backend/identity_access/tokens.py:209@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `_validate_temporal_claims` wirft `IDTokenVerificationError`; `main._bearer_auth_context_from_request` fängt aber nur `BearerTokenVerificationError`. Ein ungültiger `Authorization`-Header kann dadurch Requests mit 500 abbrechen. — Fix: Temporal-Fehler in `verify_bearer_token` auf `BearerTokenVerificationError` mappen und einen API-Test ergänzen, der bei abgelaufenem JWT explizit 401 erwartet.

- [SEC] Concern-Box-Insert-Funktion erlaubt `student_sub`-Spoofing unter `SECURITY DEFINER` — `supabase/migrations/20260406093338_concern_box_entries_insert_function.sql:25@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Die Funktion prüft Mitgliedschaft nur gegen den übergebenen Parameter `p_student_sub`, aber nicht gegen `app.current_sub`; so kann ein aufrufender Kontext prinzipiell Einträge im Namen anderer Kursteilnehmer erzeugen. — Fix: Erzwinge `p_student_sub = current_setting('app.current_sub', true)` (oder entferne den Parameter komplett und nutze ausschließlich `app.current_sub`) und decke den Missbrauchspfad per Migrationstest ab.

- [API] Concern-Box-Write-Endpunkte sind laut Vertrag Bearer-only, zur Laufzeit aber weiter Cookie-fähig — `backend/web/main.py:410@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `_requires_bff_bearer_auth` enthält die Pfade `/api/learning/concern-box/*` und `/api/teaching/concern-box/*` nicht; damit akzeptiert die Middleware dort Legacy-Session-Fallback trotz `bearerAuth`-Definition in `api/openapi.yml`. — Fix: Bearer-Pfadmengen um Concern-Box-Write-Pfade erweitern oder den OpenAPI-Contract auf Dual-Auth korrigieren (inkl. Tests, dass gewählter Modus strikt erzwungen wird).

### SHOULD-FIX

- [SEC] Keycloak-Fehlerantwort wird als Rohtext geloggt — `backend/identity_access/admin_client.py:76@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `response.text` kann personenbezogene oder sensible Felder enthalten; Logging dieser Payload kollidiert mit dem „no PII in logs“-Anspruch. — Fix: Logge nur Status + stabilen Fehlercode und entferne/rediere den Response-Body.

- [BUG] Cookie-Name ist in BFF-Session-Helfer hart verdrahtet, aber im Stack als konfigurierbar dokumentiert — `frontend/src/lib/server/session.ts:7@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `h5p-service` liest `FRONTEND_SESSION_COOKIE_NAME` aus ENV, der Frontend-BFF nicht; bei abweichender Konfiguration bricht die H5P-BFF-Auth-Kopplung. — Fix: Cookie-Namen auch im Frontend aus ENV laden (mit identischem Default) oder die Konfigurierbarkeit aus Doku/Compose entfernen.

- [BUG] Token-Session-Store wächst ungebremst pro Prozess — `frontend/src/lib/server/session.ts:21@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: `TOKEN_SESSIONS` besitzt weder Größenlimit noch periodische TTL-Bereinigung; bei vielen kurzlebigen Sessions steigt Speicherverbrauch dauerhaft. — Fix: Füge TTL-Sweeper + Größenlimit (LRU) hinzu und überwache Session-Count/Memory.

- [BUG] Material-Datei-Presign erzeugt N+1-Verbindungen und N+1-Queries — `backend/web/routes/learning.py:505@a16143948faca8aa7e173e50291a0cc6521a2e8d` — Impact: Für jede Datei wird erneut `psycopg.connect(...)` geöffnet; bei materialreichen Einheiten explodieren Latenz und DB-Last. — Fix: Storage-Keys pro Section/Modul in einem Batch laden und URLs in-memory signieren.

### TESTS

- In dieser Review-Runde wurden keine zusätzlichen Tests ausgeführt.
- Relevante Lücke: Kein Test deckt den Pfad „abgelaufenes Bearer-Token liefert 401 statt 500“ ab.
- Relevante Lücke: Kein Migrationstest erzwingt, dass `create_concern_box_entry` nicht mit fremdem `student_sub` aufrufbar ist.
- Relevante Lücke: Kein Contract-Test stellt sicher, dass Concern-Box-Write-Endpunkte wirklich Bearer-only sind (falls das der gewünschte Modus bleibt).

### Refactor-Budget

- Ca. `0.5 Tag` für Bearer-Fehlerabbildung inkl. Tests.
- Ca. `0.5 Tag` für sichere Concern-Box-Funktion + Migrationstest.
- Ca. `0.5 Tag` für Auth-Contract-Glättung (Middleware/OpenAPI/Tests).
- Ca. `0.5-1 Tag` für Session-/Material-Performance-Härtung.

### Freigabe

Nicht freigeben. Nach Behebung der drei MUST-FIX-Punkte ist ein kurzer Sicherheits-Nachreview ausreichend.

## Review Round – 18:33

Date: 2026-04-08 18:33 CEST

Branch: feature/sveltekit-bounded-context-refactor@a16143948faca8aa7e173e50291a0cc6521a2e8d
Base: master@16aa32329659b630753379306292839dc19d0324
Merge-base: 16aa32329659b630753379306292839dc19d0324

Reviewed scope summary:
- SvelteKit Browser-BFF auth bridge, especially callback/logout/session handling.
- Concern-box create flow plus matching OpenAPI contract.
- Prod-hardening around new frontend auth secrets and session persistence.

Kontextnotizen:
- FastAPI akzeptiert fuer nicht-BFF-Pfade weiterhin Cookie-Sessions; deshalb muss Logout beide Session-Typen sauber invalidieren.
- Der neue Node/BFF-Layer fuehrt eigene sicherheitskritische Cookie- und Session-Logik ein, die aktuell nicht von den bestehenden Backend-Startup-Guards abgesichert wird.

Code references used:
- `frontend/src/lib/server/backend-auth.ts:39-40@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/backend-auth.ts:79-90@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/backend-auth.ts:104-111@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/backend-auth.ts:394-407@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/session.ts:21-21@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/session.ts:94-100@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `frontend/src/lib/server/session.ts:165-183@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/web/main.py:397-430@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/web/config.py:35-71@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/teaching/repo_db.py:4108-4128@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `backend/web/routes/app.py:857-886@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `api/openapi.yml:5589-5626@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `api/openapi.yml:6082-6143@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `.env.example:51-52@a16143948faca8aa7e173e50291a0cc6521a2e8d`
- `docker-compose.yml:123-123@a16143948faca8aa7e173e50291a0cc6521a2e8d`

### Kurzfazit

Die Richtung des Refactors ist stimmig, aber die neue Browser-BFF-Auth ist noch nicht produktionshart. Kritisch sind unvollstaendiger Logout, ein bekannter Default fuer das Flow-Cookie-Secret und ein rein prozesslokaler Token-Store. Dazu kommt ein fachlich falsches Error-Mapping im Concern-Box-Create-Flow. In diesem Zustand ist der Branch nicht freigabefaehig.

### MUST-FIX

- [SEC] Frontend-Logout loescht nur `gustav_bff_session`, nicht `gustav_session` — `frontend/src/lib/server/backend-auth.ts:394@a161439` — Impact: Nach `/auth/logout` bleiben session-authentifizierte Legacy-/API-Pfade weiter nutzbar, obwohl der Contract fuer `/auth/logout` explizit das Loeschen der Application-Session verspricht — Fix: Loesche im SvelteKit-Logout zusaetzlich `gustav_session` host-only oder route den Logout ueber den bestehenden FastAPI-Logout-Pfad und decke den Fall mit einem Regressionstest gegen `/api/me` ab.
- [SEC] OIDC-Flow-Cookies sind mit bekanntem Default-Secret faelschbar, wenn `FRONTEND_SESSION_SECRET` fehlt — `frontend/src/lib/server/backend-auth.ts:39@a161439` — Impact: Der neue Browser-BFF-Flow faellt zur Laufzeit auf `CHANGE_ME_DEV` zurueck; in Prod/Stage ermoeglicht das Login-CSRF bzw. Session-Fixation durch vorhersehbare Cookie-Signaturen — Fix: Erzwinge fuer Prod/Stage einen non-placeholder-Guard analog `APP_CSRF_TOKEN_SECRET` und entferne den stillen Default aus dem Runtime-Pfad.
- [BUG] Frontend-Tokens liegen ausschliesslich im Prozessspeicher — `frontend/src/lib/server/session.ts:21@a161439` — Impact: Jeder Neustart des SvelteKit-Containers oder ein zweiter Replica-Knoten macht `gustav_bff_session` wertlos und wirft aktive Nutzer trotz gueltiger Backend-/Keycloak-Sitzung aus dem BFF — Fix: Persistiere die BFF-Session serverseitig oder vermeide prozesslokale Token-Maps; ergaenze mindestens einen Restart-/Multi-Instance-Regressionstest.

### SHOULD-FIX

- [API] Concern-Box-Create maskiert Format- und DB-Fehler als `403 forbidden` — `backend/teaching/repo_db.py:4111@a161439` — Impact: Ungueltige `course_id`-UUIDs oder echte DB-Probleme landen ueber `created is None` als Mitgliedschaftsfehler, obwohl der Contract fuer ungueltige Requests `400` vorsieht und Serverfehler unterscheidbar bleiben muessen — Fix: Validiere `course_id` als UUID vor dem Repo-Aufruf bzw. mappe Cast-/DB-Fehler getrennt auf `400` und `500` statt pauschal auf `403`.

### NICE-TO-HAVE

- [CONSISTENCY] Die SvelteKit-Logout-Validierung akzeptiert `..`, obwohl der OpenAPI-Contract das explizit verbietet — `frontend/src/lib/server/backend-auth.ts:104@a161439` — Impact: `/auth/logout?redirect=/foo/../bar` verstoesst gegen den dokumentierten Regex und kann auf einen anderen Zielpfad normalisiert werden als vorgesehen — Fix: Nutze denselben Redirect-Validator wie im Contract und teste den Negativfall fuer `..` explizit.

### TESTS

- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_session_sync_api.py backend/tests/test_bff_authorization_session_api.py backend/tests/test_concern_box_views_api.py backend/tests/test_profile_view_api.py` => `15 passed`
- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_openapi_app_session_sync_contract.py backend/tests/test_openapi_profile_contract.py backend/tests/migration/test_concern_box_entries_rls.py` => `3 passed, 1 skipped`
- Fehlend: Kein Regressionstest fuer SvelteKit-Logout gegen verbleibende Backend-Session, kein Prod-Guard-Test fuer `FRONTEND_SESSION_SECRET`, kein Restart-/Multi-Instance-Test fuer die BFF-Session-Persistenz, kein Fehlerpfad-Test fuer ungueltige `course_id` bei Concern-Box-Create.

### Refactor-Budget

- 0.5 Tag fuer Logout- und Secret-Hardening.
- 0.5-1.0 Tag fuer robuste BFF-Session-Persistenz oder eine saubere Alternativarchitektur.
- 0.25 Tag fuer Error-Mapping und zusaetzliche Negativtests im Concern-Box-Flow.

### Freigabe

Nicht freigeben. Drei Must-Fix-Befunde liegen im Auth-/Session-Pfad.
