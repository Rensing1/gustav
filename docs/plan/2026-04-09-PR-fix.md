# PR Review – 2026-04-09

## Review Round – 07:12

- Datum/Zeit: 2026-04-09 07:12 Europe/Berlin
- Branch: `feature/sveltekit-bounded-context-refactor`
- Base-Ref: `master`
- Merge-Base: `16aa32329659b630753379306292839dc19d0324`
- Head: `0855a21c16822e26604bdd9a24d012ec68884519`
- Reviewter Scope: großer Feature-Branch mit SvelteKit-Produkt-Cutover, BFF-Session-Transport, Auth-Bridge, Concern-Box-Flows, Keycloak-Theme/Realm-Anpassungen, OpenAPI- und Migrationsänderungen.
- Kontextnotizen:
  - Review basiert auf `master..HEAD`.
  - Worktree war sauber (`git status --short` ohne Ausgabe).
  - Fokus der Detailprüfung lag auf Auth/BFF, neuen Write-Endpunkten, Realm-/Policy-Konsistenz und den zuletzt gefixten Contract-Kanten.
- Verwendete Code-Referenzen:
  - `frontend/src/routes/register/+page.server.ts`
  - `frontend/src/lib/server/backend-auth.ts`
  - `backend/web/routes/app.py`
  - `backend/teaching/repo_db.py`
  - `backend/tests/test_concern_box_views_api.py`
  - `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
  - `backend/web/routes/auth.py`
  - `keycloak/realm-gustav.json`
  - `keycloak/themes/gustav/login/register.ftl`
  - `reverse-proxy/Caddyfile`

### Kurzfazit
Die Branch ist technisch deutlich näher an einer freigabefähigen SvelteKit-Umstellung, aber drei Punkte blockieren noch eine saubere PR-Freigabe: der Registrierungs-Flow verliert sein Rücksprungziel, die neuen Concern-Box-Mutationspfade behandeln ungültige UUIDs nicht robust, und die Domain-Whitelist für Selbstregistrierung ist zwischen App und Keycloak widersprüchlich verdrahtet.

### MUST-FIX
- [BUG] Der Registrierungs-Flow verwirft das Rücksprungziel explizit — `frontend/src/lib/server/backend-auth.ts:340@0855a21` — Impact: Nutzer, die aus einem geschützten Deep-Link in die Selbstregistrierung gehen, landen nach dem Callback nicht wieder am gewünschten Ziel. Das UI trägt `redirect` bereits mit, aber die BFF speichert es absichtlich als `null`; zusätzlich baut die Formular-Action aktuell nur `login_hint` weiter. — Fix: Reiche `redirect` aus `frontend/src/routes/register/+page.server.ts` an `/auth/register` durch und speichere in `startRegisterFlow()` denselben `safeRedirectPath(...)` wie im Login-Flow.
- [BUG] Archivieren/Wiederherstellen der Concern-Box validiert `entry_id` nicht vor dem DB-Cast — `backend/web/routes/app.py:1056@0855a21` — Impact: Ein Request wie `/api/teaching/concern-box/entries/not-a-uuid/archive` läuft ungeprüft in `where id = %s::uuid` in `backend/teaching/repo_db.py` und kippt damit voraussichtlich auf `500` statt kontrolliert auf `400` oder `404`. Für eine neue Write-API ist das ein unnötig scharfer Fehlerpfad. — Fix: Prüfe `entry_id` am Router mit derselben UUID-Validierung wie bei `course_id` und ergänze Red-Tests für beide Mutationen mit ungültiger UUID.

### SHOULD-FIX
- [CONSISTENCY] Die Registrierungs-Whitelist ist doppelt, aber nicht aus einer Quelle gesteuert — `keycloak/realm-gustav.json:40@0855a21` — Impact: Frontend und FastAPI lesen `ALLOWED_REGISTRATION_DOMAINS`, Keycloak erzwingt parallel hart `^[^@\\s]+@school\\.example$`. Sobald ENV und Realm auseinanderlaufen, akzeptiert die App die Adresse zuerst, der IdP lehnt sie erst später ab. Das ist für Betrieb und Fehlersuche unnötig verwirrend. — Fix: Leite die Keycloak-User-Profile-Regel aus derselben Konfiguration ab oder streiche die ENV-Konfigurierbarkeit bewusst und dokumentiere die feste Domain im Repo klar.
- [TEST] Die Auth-Bridge-Contracts decken die Redirect-Propagation der Registrierung nicht ab — `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py:61@0855a21` — Impact: Die Contract-Suite prüft derzeit nur Datei-Existenz und Basissymbole; deshalb konnte der verlorene `redirect`-Pfad unbemerkt bleiben. — Fix: Ergänze einen Contract-/Route-Test für `/register?redirect=/teaching/...` und einen Callback-Test, der den finalen Rücksprung nach erfolgreichem Register-Flow festschreibt.

### NICE-TO-HAVE
- Keine weiteren Punkte mit gutem Kosten-Nutzen-Verhältnis im aktuell gelesenen Scope.

### TESTS
- Nicht erneut ausgeführt; Review basiert auf Diff- und Codeanalyse gegen `master..HEAD`.
- Sichtbare Testlücken im Scope: kein Negativtest für ungültige `entry_id` bei Concern-Box-Mutationen, kein Redirect-Propagationstest für den Register-Flow.

### Refactor-Budget
- Ca. `0,5 PT`: zwei kleine Router/Auth-Fixes plus zwei gezielte Regressionstests.

### Freigabe
- Nicht freigeben, bevor die beiden MUST-FIX-Punkte geschlossen sind.

## Reparaturplan – 2026-04-09 07:26

- `Register-Redirect bleibt nicht erhalten`
  - Weiterhin offen: `frontend/src/routes/register/+page.server.ts` baut den Redirect nur mit `login_hint`, und `frontend/src/lib/server/backend-auth.ts` speichert im Register-Flow explizit `null` statt des sicheren Rücksprungziels.
  - Repo-Regeln: Contract-first, TDD, `identity_access` bleibt Eigentümer für OIDC-Flow und Session-Weiterleitung.
  - Betroffene Dateien: `api/openapi.yml`, `frontend/src/routes/register/+page.server.ts`, `frontend/src/lib/server/backend-auth.ts`, `backend/web/routes/auth.py`, zugehörige Tests.
  - Vertrag zuerst: `/auth/register` erhält denselben optionalen sicheren `redirect`-Query-Parameter wie `/auth/login`.
  - Red: neue/erweiterte Tests für Register-Action, BFF-Flow-Cookie, Callback-Redirect und Packaging-Contract.
  - Green: `redirect` validiert weiterreichen und im Register-State speichern, danach Callback auf `flow.redirectPath` zurückführen.
  - Akzeptanz: erfolgreicher Register-Flow landet wieder auf `/teaching/...`; unsichere Redirects bleiben verworfen.

- `Concern-Box-Mutationen validieren entry_id nicht`
  - Weiterhin offen: `backend/web/routes/app.py` ruft das Repo mit beliebigem `entry_id` auf; `backend/teaching/repo_db.py` castet danach direkt auf `uuid`.
  - Repo-Regeln: `teaching` besitzt die Write-API, Fehlerstil soll den bestehenden `invalid_*_id`-Konventionen folgen.
  - Betroffene Dateien: `api/openapi.yml`, `backend/web/routes/app.py`, `backend/tests/test_concern_box_views_api.py`, neuer OpenAPI-Vertragstest.
  - Vertrag zuerst: beide Concern-Box-Mutationspfade dokumentieren `entry_id` als `uuid` und einen `400 invalid_entry_id`-Fehlerfall.
  - Red: Negativtests für `not-a-uuid` bei Archive/Restore sowie ein fokussierter OpenAPI-Contract-Test.
  - Green: frühe UUID-Validierung im Router, Repo bleibt unverändert.
  - Akzeptanz: ungültige IDs liefern `400 bad_request / invalid_entry_id` statt DB-Fehlerpfad.

- `Auth-Bridge-Tests decken Register-Redirect nicht ab`
  - Weiterhin offen: `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py` prüft nur Basissymbole und erkennt verlorene Redirect-Propagation nicht.
  - Repo-Regeln: SvelteKit bleibt primäre Browser-BFF; Packaging-Tests sollen den minimalen Auth-Brückenvertrag absichern, ohne Implementierungsdetails unnötig zu verhärten.
  - Betroffene Dateien: `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`, neue Frontend-Route-Tests.
  - Red: Contract-Test auf `redirect`-Durchreichung der Register-Route schärfen.
  - Green: nur minimale Testschärfung, keine zusätzliche Laufzeitlogik.
  - Akzeptanz: derselbe Verlust des Rücksprungziels fällt künftig in Packaging- oder Route-Tests rot auf.

- `Registrierungs-Whitelist App vs. Keycloak`
  - Verifiziert offen, aber für diesen Lauf bewusst außerhalb des Reparaturscopes.
  - Begründung: würde Realm-/Konfigurationswahrheit anfassen und ist kein billiger Beifang zu den beiden MUST-FIXes.

## Umsetzungsstand – 2026-04-09 07:34

- Geschlossen:
  - Register-Flow propagiert `redirect` jetzt von der SvelteKit-Register-Seite über `/auth/register` bis in den Callback zurück auf das gespeicherte Ziel.
  - FastAPI-Auth-Route `/auth/register` dokumentiert und speichert denselben sicheren `redirect`-Parameter wie die Browser-BFF.
  - Concern-Box-Mutationen validieren `entry_id` jetzt fail-closed vor dem Repo-/DB-Cast und liefern `400 bad_request` mit `invalid_entry_id`.
  - Die Auth-Bridge-Contracts und Route-Tests decken den Register-Redirect-Vertrag jetzt explizit ab.
- Weiterhin offen:
  - Die größere Konsistenzfrage zwischen `ALLOWED_REGISTRATION_DOMAINS` und `keycloak/realm-gustav.json` bleibt bewusst offen und separat zu behandeln.
- Stale:
  - Keine Review-Findings waren stale; beide MUST-FIXes und der günstige Test-Fix waren auf dem aktuellen Branch weiterhin reproduzierbar.
- Geänderte Subsysteme:
  - `identity_access`: SvelteKit-BFF-Auth (`frontend/src/lib/server/backend-auth.ts`), Register-Page-Action und FastAPI-Auth-Route.
  - `teaching`: Concern-Box-Write-Grenze in `backend/web/routes/app.py`.
  - API-Vertrag: `api/openapi.yml` für `/auth/register` und Concern-Box-Mutationen.
- Ausgeführte Tests:
  - `npm --prefix frontend test -- src/lib/server/backend-auth.test.ts src/routes/register/page.server.test.ts`
  - `.venv/bin/pytest -q backend/tests/test_auth_contract.py backend/tests/test_auth_register_redirect.py backend/tests/test_openapi_auth_redirect_pattern.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py backend/tests/test_concern_box_views_api.py backend/tests/test_openapi_concern_box_mutation_contract.py`
  - `npm --prefix frontend run check` → grün mit den bereits bekannten drei `state_referenced_locally`-Warnungen
  - `.venv/bin/pytest -q backend/tests/test_concern_box_views_api.py backend/tests/test_auth_contract.py backend/tests/test_auth_register_redirect.py backend/tests/test_auth_register_domain_whitelist.py backend/tests/test_openapi_auth_redirect_pattern.py backend/tests/test_openapi_home_views_contract.py backend/tests/test_openapi_concern_box_mutation_contract.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
  - `make verify` → grün
- Architektur-/Kontextentscheidung:
  - Der Redirect-Fix bleibt im Kontext `identity_access`; die Concern-Box-UUID-Härtung bleibt an der Adapter-Grenze des Kontexts `teaching`, nicht im Repo-Layer.
- Design-/UI-Checks:
  - Keine visuelle Designänderung; nur Route-/BFF-Verhalten und Vertragsabsicherung.
- Restrisiko:
  - Die Self-Registration-Domain-Policy bleibt weiterhin doppelt modelliert und sollte in einem separaten Lauf auf eine eindeutige Quelle der Wahrheit gebracht werden.
- Release-Empfehlung:
  - Die zwei MUST-FIXes aus der Review sind geschlossen; aus Sicht dieses PR-Fix-Dokuments blockiert jetzt nur noch das bewusst ausgesparte Whitelist-Konsistenzthema keinen Merge mehr.

## Reparaturplan – 2026-04-09 20:31

- `Registrierungs-Whitelist App vs. Keycloak`
  - Weiterhin offen: Auf `feature/sveltekit-bounded-context-refactor` (`a36f8fe52bfa0d8972e6adb14864c94a47afbd2b`) lesen Browser-BFF und FastAPI `ALLOWED_REGISTRATION_DOMAINS`, während `keycloak/realm-gustav.json` die Domain weiter hart auf `@school.example` festschreibt.
  - Repo-Regeln: `identity_access` bleibt Eigentümer der Registrierungs-Policy; local = prod; KISS; Security first; keine doppelte Wahrheit zwischen App und IdP.
  - Bounded Context: `identity_access`.
  - Betroffene Dateien: `keycloak/Dockerfile`, `keycloak/realm-gustav.json`, neue kleine Render-Hilfe unter `keycloak/`, `backend/tests/test_keycloak_realm_config.py`, `docs/references/user_management.md`, ggf. `.env.example`.
  - Vertrag/Migration zuerst: kein OpenAPI- oder DB-Schema-Änderungsbedarf; stattdessen Build-/Config-Vertrag des Realm-Imports schärfen.
  - Red: Test ergänzen, der fordert, dass der Keycloak-Import die Domain-Policy aus derselben Konfiguration ableitet statt einen festen Regex für `school.example` einzubrennen.
  - Green: Realm-Import beim Image-Build aus einer Template-Datei mit `ALLOWED_REGISTRATION_DOMAINS` rendern; App-Verhalten bleibt unverändert, IdP zieht nach.
  - Akzeptanz: Eine Änderung von `ALLOWED_REGISTRATION_DOMAINS` ändert die erlaubte Registrierungs-Domain konsistent in FastAPI, SvelteKit und Keycloak; der Referenzzustand ohne gesetzte Env bleibt defensiv bei `@school.example`.

## Umsetzungsstand – 2026-04-09 20:39

- Geschlossen:
  - Die Registrierungs-Whitelist ist im Repo nicht mehr doppelt modelliert: `keycloak/realm-gustav.json` enthält nur noch einen Platzhalter statt eines hart codierten `school.example`-Regex.
  - Das Keycloak-Image rendert den Realm-Import jetzt aus `ALLOWED_REGISTRATION_DOMAINS`; dieselbe Variable bleibt bereits Quelle der Wahrheit für FastAPI und die SvelteKit-BFF.
  - `docker-compose.yml` reicht `ALLOWED_REGISTRATION_DOMAINS` als Build-Argument an Keycloak weiter, sodass `docker compose up -d --build` den IdP-Import mit derselben Domain-Policy erzeugt.
  - Die Referenzdoku beschreibt den gemeinsamen Konfigurationspfad jetzt explizit.
- Weiterhin offen:
  - Keine weiteren verifizierten Findings im Scope dieses PR-Fix-Laufs.
- Stale:
  - Keine. Der offene SHOULD-FIX aus dem Dokument war auf dem aktuellen Branch weiterhin real.
- Geänderte Subsysteme:
  - `identity_access`-Konfiguration rund um Self-Registration (`keycloak/realm-gustav.json`, `keycloak/render_realm.py`, `keycloak/Dockerfile`, `docker-compose.yml`).
  - Referenzdokumentation in `docs/references/user_management.md`.
  - Infrastruktur- und Konfigurationstests unter `backend/tests/`.
- Ausgeführte Tests:
  - `.venv/bin/pytest -q backend/tests/test_keycloak_realm_config.py backend/tests/test_compose_keycloak_postgres.py`
  - `.venv/bin/pytest -q backend/tests/test_auth_register_domain_whitelist.py backend/tests/test_keycloak_realm_config.py backend/tests/test_compose_keycloak_postgres.py`
  - `make verify` → grün
- Architektur-/Kontextentscheidung:
  - Die Policy bleibt im Bounded Context `identity_access`; statt die App-Guardrail zu entfernen, wird nun der Keycloak-Bootstrap aus derselben Env-Quelle abgeleitet.
- Design-/UI-Checks:
  - Keine visuellen Änderungen.
- Restrisiko:
  - Eine nachträgliche Änderung von `ALLOWED_REGISTRATION_DOMAINS` in einer bereits importierten Keycloak-Installation erfordert weiterhin einen Realm-Reimport oder einen separaten Synchronisationsschritt; die Doku weist jetzt explizit darauf hin.
- Release-Empfehlung:
  - Der verbleibende verifizierte PR-Fix aus diesem Dokument ist für den Standardpfad `docker compose up -d --build` geschlossen; die Branch ist mit dem dokumentierten Bootstrap-Risiko mergebar.

## Review Round – 20:34

- Datum/Zeit: 2026-04-09 20:34 CEST
- Branch: `feature/sveltekit-bounded-context-refactor`
- Base-Ref: `master`
- Merge-Base: `16aa32329659b630753379306292839dc19d0324`
- Head: `a36f8fe52bfa0d8972e6adb14864c94a47afbd2b`
- Reviewter Scope: aktueller Gesamtstand gegen `master` mit Fokus auf den nach dem letzten Review weitergebauten Live-/Dashboard-/Snapshot-/Auth-Scope; insgesamt `469` geänderte Dateien im Branch.
- Kontextnotizen:
  - Review basiert auf `merge-base..HEAD`, nicht auf Commit-Memory.
  - Vertiefte Detailprüfung in `backend/web/routes/app.py`, `backend/web/routes/teaching.py`, `api/openapi.yml`, `backend/tests/test_live_view_api.py`, `frontend/src/lib/server/backend-auth.ts` und `keycloak/realm-gustav.json`.
  - Fokustests `backend/tests/test_live_view_api.py` und `backend/tests/test_openapi_live_dashboard_contract.py` liefen grün, verdecken aber einen der unten genannten Fehler.
- Verwendete Code-Referenzen:
  - `backend/web/routes/app.py`
  - `backend/web/routes/teaching.py`
  - `api/openapi.yml`
  - `backend/tests/test_live_view_api.py`
  - `frontend/src/lib/server/backend-auth.ts`
  - `keycloak/realm-gustav.json`

### Kurzfazit
Der Branch ist im Live-Scope noch nicht freigabefähig. Das Dashboard leitet die „letzte Abgabe“ aus einem Feld ab, das die echte Summary-API nicht liefert, und die Teaching-Live-Summary bricht gleichzeitig ihren eigenen OpenAPI-Vertrag. Zusätzlich bleibt die Registrierungs-Domain weiter doppelt konfiguriert.

### MUST-FIX
- [BUG] Das Live-Dashboard bestimmt die „letzte Abgabe“ aus `created_at`, obwohl die echte Summary-Antwort dieses Feld nie liefert — `backend/web/routes/app.py:1893@a36f8fe` — Impact: Bei mehreren Abgaben pro Lernendem wird standardmäßig die letzte Aufgabe in Task-Reihenfolge statt der neuesten Submission gewählt; `created_at` kann leer bleiben, wodurch Datumsspalte und Detail-Vorauswahl im `/live`-Raum falsch werden. — Fix: Liefere einen echten Zeitstempel aus `get_unit_live_summary()` mit oder ermittle die neueste Submission im Dashboard über eine explizite Latest-Abfrage statt über ein dort nicht garantiertes Feld.
- [API] Die Teaching-Live-Summary sendet `average_score`, obwohl `TeachingUnitTaskCell` dieses Feld im OpenAPI-Vertrag gar nicht kennt und `additionalProperties: false` gesetzt ist — `backend/web/routes/teaching.py:5641@a36f8fe` — Impact: Contract-first ist gebrochen; strikt validierende Clients oder Contract-Tests würden die reale Antwort als ungültig bewerten. Gleichzeitig bleibt die Dashboard-Abhängigkeit von inoffiziellen Summary-Feldern versteckt. — Fix: Dokumentiere `average_score` explizit in `api/openapi.yml:2926` oder entferne das Feld aus der Summary-Response und hole Bewertungsdaten nur im Dashboard-Endpunkt.

### SHOULD-FIX
- [TEST] Die Live-Dashboard-Tests modellieren eine Summary-Antwort, die der echte Endpoint nie liefert — `backend/tests/test_live_view_api.py:243@a36f8fe` — Impact: Die Suite bleibt grün, obwohl der Produktcode von `created_at` abhängt, das `get_unit_live_summary()` real nicht setzt. Genau diese Regression bleibt damit unsichtbar. — Fix: Ersetze das Stub-Shape durch die reale Summary-Form oder ergänze einen Integrationstest, der `/api/live/.../dashboard` gegen die echte Summary-Implementierung prüft.
- [CONSISTENCY] Die Registrierungs-Domain wird weiter doppelt und widersprüchlich konfiguriert — `keycloak/realm-gustav.json:40@a36f8fe` — Impact: Browser-BFF/FastAPI akzeptieren gemäß `ALLOWED_REGISTRATION_DOMAINS`, Keycloak erzwingt parallel hart `@school.example`; sobald ENV und Realm auseinanderlaufen, scheitert die Registrierung erst im IdP und Betrieb/Fehlersuche werden unnötig unklar. — Fix: Ziehe die Keycloak-Regel aus derselben Konfiguration wie `frontend/src/lib/server/backend-auth.ts:328` oder entscheide dich bewusst für eine feste Domain und streiche die ENV-Konfigurierbarkeit.

### NICE-TO-HAVE
- Keine weiteren Punkte mit gutem Kosten-Nutzen-Verhältnis im aktuell vertieft geprüften Scope.

### TESTS
- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_live_view_api.py backend/tests/test_openapi_live_dashboard_contract.py` → `8 passed in 0.97s`.
- Aussagekraft: grün, aber die Live-API-Tests stubben `created_at` in die Summary-Antwort hinein und verifizieren damit nicht den echten Zusammenschnitt von Summary → Dashboard.
- Nicht erneut ausgeführt: breitere Backend-/Frontend-Suites.

### Refactor-Budget
- Ca. `0,5–1 PT`: ein Read-Model-/Contract-Fix im Live-Scope plus zwei gezielte Regressionstests.

### Freigabe
- Nicht freigeben, bevor der Dashboard-Fehler und der OpenAPI-Vertragsbruch im Live-Scope geschlossen sind.

## Review Round – 20:36

- Datum/Zeit: 2026-04-09 20:36 CEST
- Branch: `feature/sveltekit-bounded-context-refactor`
- Base-Ref: `master`
- Merge-Base: `16aa32329659b630753379306292839dc19d0324`
- Head: `a36f8fe52bfa0d8972e6adb14864c94a47afbd2b`
- Reviewter Scope: fokussierte Nachprüfung der noch risikoreichen Bereiche `learning`-Worker-DSN-Auflösung sowie Self-Registration-Domain-Policy zwischen App und Keycloak.
- Kontextnotizen:
  - Review basiert auf `master..HEAD`.
  - Der Worktree war nicht sauber (`backend/tests/test_keycloak_realm_config.py`, `docs/plan/2026-04-09-PR-fix.md`); betroffene Befunde wurden deshalb zusätzlich mit `git show HEAD:...` gegen den Commit-Stand verifiziert.
  - Die neue Realm-Renderer-Testidee ist im aktuellen Worktree sichtbar, gehört aber noch nicht zu `HEAD`; die Laufzeit-Inkonsistenz ist trotzdem bereits im Branch-Stand vorhanden.
- Verwendete Code-Referenzen:
  - `backend/learning/workers/process_learning_submission_jobs.py`
  - `backend/learning/workers/health.py`
  - `backend/tests/test_learning_worker_security.py`
  - `backend/web/routes/auth.py`
  - `backend/tests/test_auth_register_domain_whitelist.py`
  - `keycloak/realm-gustav.json`
  - `keycloak/Dockerfile`

### Kurzfazit
Die Branch ist nah an einer freigabefähigen Form, aber ein echter Runtime-Fehler im Learning-Worker bleibt offen. Zusätzlich ist die Registrierungs-Domain-Policy weiterhin zwischen App und Keycloak doppelt modelliert; das ist nicht sofort überall kaputt, aber betrieblich unnötig fragil und bereits gegen die eigenen Leitplanken "local = prod" und "eine Quelle der Wahrheit".

### MUST-FIX
- [BUG] Der Job-Runner ignoriert die dedizierte Worker-DSN-Variable weiterhin — `backend/learning/workers/process_learning_submission_jobs.py:1129@a36f8fe` — Impact: Der eigentliche Worker-Prozess liest nur `LEARNING_DATABASE_URL/LEARNING_DB_URL`, obwohl der neue Worker-Vertrag parallel `LEARNING_WORKER_DATABASE_URL/LEARNING_WORKER_DB_URL` verwendet (`backend/learning/workers/health.py`). Wer nur die dedizierte Worker-Variable setzt, landet im falschen Fallback-Pfad oder in unnötigen Rollenfehlern. — Fix: Lies zuerst `LEARNING_WORKER_DATABASE_URL`/`LEARNING_WORKER_DB_URL`, synchronisiere Docstring und Fehlermeldung auf denselben Vertrag und sichere die Präzedenz mit einem Red-Test ab.

### SHOULD-FIX
- [CONSISTENCY] Die Self-Registration-Domain ist weiterhin doppelt und widersprüchlich verdrahtet — `keycloak/realm-gustav.json:40@a36f8fe` — Impact: FastAPI akzeptiert Domains dynamisch aus `ALLOWED_REGISTRATION_DOMAINS` (`backend/web/routes/auth.py:349`), der importierte Keycloak-Realm erzwingt aber weiterhin hart `@school.example`; bei jeder abweichenden Schul-Domain bestätigt die App den Start des Flows, während der IdP später wieder blockiert. — Fix: Rendere den Realm aus derselben Konfiguration wie App/BFF oder erkläre Keycloak bewusst zur einzigen Quelle der Wahrheit und ziehe die App-Logik nach.
- [TEST] Die Worker-Sicherheits-Suite deckt den neuen DSN-Vertrag nicht ab — `backend/tests/test_learning_worker_security.py:59@a36f8fe` — Impact: Der Runtime-Bruch beim dedizierten Worker-DSN konnte unbemerkt landen, weil die vorhandenen Tests nur den Fallback über `LEARNING_WORKER_DB_USER/PASSWORD` und die Ablehnung von `LEARNING_DATABASE_URL` prüfen, nicht aber die explizite Worker-DSN. — Fix: Ergänze einen Red-Test für `LEARNING_WORKER_DATABASE_URL`/`LEARNING_WORKER_DB_URL` und räume diese Variablen im Reset-Helper mit ab.

### NICE-TO-HAVE
- Keine weiteren Punkte mit gutem Kosten-Nutzen-Verhältnis im diesmal gelesenen Scope.

### TESTS
- Nicht ausgeführt; Review basiert auf Diff- und Codeanalyse gegen `master..HEAD`.
- Wegen des unsauberen Worktrees wurden betroffene Review-Stellen zusätzlich mit `git show HEAD:...` statt nur aus der Arbeitskopie geprüft.

### Refactor-Budget
- Ca. `0,5 PT`: ein gezielter Worker-Fix, ein kleiner Test-Nachzug und eine klare Produktentscheidung zur Quelle der Registrierungs-Domain.

### Freigabe
- Nicht freigeben, bevor der Worker-MUST-FIX geschlossen ist.

## Reparaturplan – 2026-04-09 21:08

- `Teaching-Live-Summary liefert keinen belastbaren Zeitstempel für die letzte Abgabe`
  - Weiterhin offen: `backend/web/routes/app.py` bestimmt die `latest_submission` im Dashboard über `cell.created_at`, aber `backend/web/routes/teaching.py` verwirft im Summary-Payload das bereits vorhandene `created_at_iso` aus Repo-/Helper-Schicht.
  - Repo-Regeln: Contract-first, KISS, klare Kontextgrenzen; `teaching` bleibt Eigentümer des Summary-Vertrags, der Diagnose-/Dashboard-Read-Model-Adapter konsumiert ihn nur.
  - Bounded Context: `teaching` als Quellvertrag, konsumiert im `diagnostics`-nahen Live-Dashboard.
  - Betroffene Dateien: `api/openapi.yml`, `backend/tests/test_openapi_teaching_live_unit_contract.py`, `backend/tests/test_teaching_live_unit_summary_api.py`, `backend/web/routes/teaching.py`, optional `docs/references/teaching_live.md`.
  - Vertrag zuerst: `TeachingUnitTaskCell` dokumentiert optional `created_at` für die neueste Submission derselben Zelle.
  - Red: OpenAPI-Contract-Test und Summary-API-Test so schärfen, dass `created_at` im echten Summary-Payload verlangt wird.
  - Green: `created_at_iso` aus Aggregat-/Helper-Daten in die Summary-Zellen übernehmen; das Dashboard kann den bestehenden Feldzugriff unverändert korrekt nutzen.
  - Akzeptanz: `/api/teaching/.../submissions/summary` liefert pro belegter Zelle ein `created_at`; `/api/live/.../dashboard` wählt damit die fachlich letzte Abgabe stabil aus.

- `Learning-Worker ignoriert die dedizierte Worker-DSN-Variable`
  - Weiterhin offen: `backend/learning/workers/process_learning_submission_jobs.py` priorisiert noch `LEARNING_DATABASE_URL`/`LEARNING_DB_URL`, obwohl Health-Probe und Referenzdoku bereits `LEARNING_WORKER_DATABASE_URL`/`LEARNING_WORKER_DB_URL` als primären Worker-Vertrag verwenden.
  - Repo-Regeln: Security first, local = prod, keine versteckten Parallelverträge für denselben Runtime-Pfad.
  - Bounded Context: `learning`.
  - Betroffene Dateien: `backend/tests/test_learning_worker_security.py`, `backend/learning/workers/process_learning_submission_jobs.py`.
  - Vertrag/Migration zuerst: kein OpenAPI- oder Schema-Change; maßgeblich ist der Runtime-/Konfigurationsvertrag des Workers.
  - Red: Reset-Helper um Worker-DSN-Variablen erweitern und einen Test ergänzen, der die Präzedenz `LEARNING_WORKER_DATABASE_URL` vor Legacy-Variablen festschreibt.
  - Green: Worker-DSN-Auflösung auf denselben Variablensatz wie `health.py` bringen, Legacy-Variablen nur noch als Fallback behalten und Fehlermeldung/Docstring synchronisieren.
  - Akzeptanz: gesetzte Worker-DSN-Variablen werden tatsächlich verwendet; Fallback über `LEARNING_WORKER_DB_USER/PASSWORD` und Service-Role-Ablehnung bleiben intakt.

- `Stale oder bewusst ausgesparte Findings`
  - Stale nach Re-Check: Der frühere `average_score`-OpenAPI-Befund ist auf aktuellem Branch-Stand bereits geschlossen; `TeachingUnitTaskCell` dokumentiert `average_score` inzwischen explizit.
  - Bewusst nicht angefasst: Die Registrierungs-Domain-Quelle ist im aktuellen Worktree bereits lokal in Bearbeitung (`keycloak/*`, `docker-compose.yml`, Tests, Doku). Diese fremden Änderungen überschreibe ich in diesem Lauf nicht.

## Umsetzungsstand – 2026-04-09 21:12

- Geschlossen:
  - `TeachingUnitTaskCell` dokumentiert jetzt zusätzlich `created_at`, und die Summary-API reicht den echten Zeitstempel der neuesten Abgabe pro Zelle aus der Teaching-Schicht bis zum Payload durch.
  - Das Live-Dashboard kann damit seine bestehende `latest_submission`-Logik auf einen echten, vertraglich abgesicherten Zeitstempel stützen statt auf ein implizit fehlendes Feld.
  - Der Learning-Worker priorisiert jetzt `LEARNING_WORKER_DATABASE_URL`/`LEARNING_WORKER_DB_URL`; Legacy-Variablen bleiben nur Fallback.
  - Die Worker-Sicherheits-Tests decken die neue DSN-Präzedenz jetzt explizit ab.
- Weiterhin offen:
  - In diesem Lauf keine weiteren verifizierten Findings im bearbeiteten Scope.
  - Außerhalb des Scopes bleibt die Registrierungs-Domain-Frage, die im aktuellen Worktree bereits separat lokal bearbeitet wird.
- Stale:
  - Der frühere Review-Befund zum fehlenden `average_score`-OpenAPI-Feld war bereits vor diesem Lauf geschlossen und wurde deshalb nur dokumentiert, nicht erneut implementiert.
- Geänderte Subsysteme:
  - `teaching`: Live-Summary-Vertrag und Summary-Read-Model (`api/openapi.yml`, `backend/web/routes/teaching.py`, `backend/tests/test_teaching_live_unit_summary_api.py`, `backend/tests/test_openapi_teaching_live_unit_contract.py`, `docs/references/teaching_live.md`).
  - `diagnostics`-/Live-Dashboard-Absicherung: bestehende Dashboard-Tests verifizieren weiterhin den Konsumentenpfad.
  - `learning`: Worker-DSN-Auflösung und Sicherheitstests (`backend/learning/workers/process_learning_submission_jobs.py`, `backend/tests/test_learning_worker_security.py`).
- Ausgeführte Tests:
  - `.venv/bin/pytest -q backend/tests/test_openapi_teaching_live_unit_contract.py`
  - `.venv/bin/pytest -q backend/tests/test_teaching_live_unit_summary_api.py -k summary_includes_average_score_for_completed_analysis`
  - `.venv/bin/pytest -q backend/tests/test_live_view_api.py -k dashboard`
  - `.venv/bin/pytest -q backend/tests/test_learning_worker_security.py -k 'worker_dsn'`
  - `.venv/bin/pytest -q backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/test_openapi_live_dashboard_contract.py backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_live_view_api.py backend/tests/test_learning_worker_security.py backend/tests/test_learning_worker_health_endpoint.py` → `36 passed`
- Architektur-/Kontextentscheidung:
  - Der echte Zeitstempel bleibt Eigentum des `teaching`-Kontexts und wird dort vertraglich sichtbar gemacht; das Live-Dashboard konsumiert diesen Vertrag nur noch, statt implizite Datenformen zu erraten.
  - Für den Worker ist `LEARNING_WORKER_DATABASE_URL` nun der kanonische Runtime-Vertrag, konsistent mit Health-Probe und Referenzdoku; alte `LEARNING_DATABASE_URL`-Overrides bleiben nur aus Rückwärtskompatibilität lesbar.
- Design-/UI-Checks:
  - Keine visuellen Änderungen; nur API-, Read-Model- und Worker-Konfiguration.
- Restrisiko:
  - Der lokale Worktree enthält weiterhin fremde Änderungen im Bereich Keycloak/Registrierungs-Domain. Diese wurden bewusst nicht überschrieben und sollten separat fertiggeführt oder bereinigt werden, bevor der gesamte Branch final bewertet wird.
- Release-Empfehlung:
  - Die in diesem Lauf verifizierten offenen MUST-FIXes (Live-Timestamp und Worker-DSN) sind geschlossen; aus diesem Scope bleibt kein zusätzlicher Merge-Blocker zurück.
