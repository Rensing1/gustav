# PR Review – feature/sveltekit-bounded-context-refactor vs master

Datum: 2026-04-10 06:08 (Europe/Berlin)

## Review Round – 06:08

### Git-Kontext

- Branch: `feature/sveltekit-bounded-context-refactor`
- Base ref: `master` @ `16aa32329659b630753379306292839dc19d0324`
- Merge-base: `16aa32329659b630753379306292839dc19d0324`
- Head: `c5d57ef277f10d2eb716e193bbf163bcf08b54ac`
- Review-Basis: `16aa32329659b630753379306292839dc19d0324..c5d57ef277f10d2eb716e193bbf163bcf08b54ac`
- Hinweis: Der Arbeitsbaum war lokal dirty; bewertet wurde nur `merge-base..HEAD`.

### Reviewed Scope

- 469 geaenderte Dateien mit Schwerpunkt auf SvelteKit-Produktumschaltung, Browser-BFF/Auth-Bridge, Live-/Learning-/Teaching-Views, Keycloak-Theme, OpenAPI, Packaging-Tests und Migrationen.
- Vertieft geprueft wurden die Auth- und Session-Pfade `/auth/*`, `/api/app/session-sync`, `/backend-internal/app/bff-session`, Redirect-Validierung, Cookie-/Cache-Header, Compose/.env-Wiring und die zugehoerigen Vertrags-Tests.

### Relevante Kontextnotizen

- Der Browser soll laut Packaging-Contract ueber die neue SvelteKit-Auth-Bridge in den OIDC-Flow einsteigen.
- Caddy leitet `/api/*` und `/internal/*` an FastAPI; `/auth/*` landet standardmaessig im Frontend.
- Damit ist `api/openapi.yml` fuer die oeffentliche Auth-Frontdoor nur dann belastbar, wenn es die SvelteKit-Semantik mitabbildet.

### Verwendete Code-Referenzen

- `frontend/src/lib/server/backend-auth.ts`
- `frontend/src/lib/server/backend-auth.test.ts`
- `frontend/src/routes/auth/callback/+server.ts`
- `api/openapi.yml`
- `backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- `backend/web/routes/redirects.py`
- `.env.example`
- `docker-compose.yml`

### Kurzfazit

Der Branch bewegt GUSTAV klar Richtung SvelteKit/BFF, aber die oeffentliche Auth-Strecke ist noch nicht ganz releasefest. Kritisch sind ein vorhersagbares Flow-Cookie-Geheimnis ausserhalb `prod`, Vertragsdrift im oeffentlichen `/auth/callback` und ein Logout-Fehlerpfad, der die Backend-Session stehen lassen kann.

### MUST-FIX

- [SEC] Vorhersagbare Signatur fuer den Frontend-Flow-Cookie ausserhalb `prod` — `frontend/src/lib/server/backend-auth.ts:116@c5d57ef` — Impact: Die Integritaet von `state`/`nonce` haengt an `FRONTEND_SESSION_SECRET`, aber der Guard greift nur in prod-like Environments. Mit `.env.example` (`CHANGE_ME_DEV`) und dem Compose-Wiring ist die HMAC im Default-Stack vorhersagbar; damit wird der OIDC-Flow fuer Login-CSRF und Flow-Tampering unnötig weich. — Fix: Erzwinge ein nicht-placeholder `FRONTEND_SESSION_SECRET` in jeder echten Laufzeitumgebung und behandle nur explizite Testumgebungen als Ausnahme.
- [API] Öffentlicher Callback-Vertrag bildet die neue SvelteKit-Frontdoor nicht mehr korrekt ab — `api/openapi.yml:5785@c5d57ef` — Impact: `openapi.yml` beschreibt `/auth/callback` weiterhin so, als setze der Browser nur `gustav_session`, obwohl der Branch den Browser laut Packaging-Contract ueber SvelteKit fuehrt und dort zusaetzlich `gustav_bff_session` etabliert. Das bricht Contract-First genau auf einer sicherheitskritischen Route. — Fix: Dokumentiere die reale Browser-Semantik von `/auth/callback` inklusive beider Cookies oder trenne die oeffentliche SvelteKit-Route sauber vom backend-internen Session-Bridge-Vertrag.

### SHOULD-FIX

- [TEST] Die Auth-Bridge-Tests pruefen nur Speicher-Mocks, nicht die reale Route-Antwort — `frontend/src/lib/server/backend-auth.test.ts:40@c5d57ef` — Impact: Die Tests sehen nur `MemoryCookies` und assertieren nie die tatsaechlich emittierten `Set-Cookie`-Header fuer `gustav_bff_session`. Dadurch kann Vertragsdrift an `/auth/callback` unbemerkt bleiben, obwohl die Repo-Contracts gruen sind. — Fix: Ergaenze Route-Level-Tests fuer `frontend/src/routes/auth/login/+server.ts`, `frontend/src/routes/auth/callback/+server.ts` und `frontend/src/routes/auth/logout/+server.ts`, die `Set-Cookie`, `Location` und `Cache-Control` an der echten Response pruefen.
- [BUG] Logout faellt bei Backend-Fehler in einen Halbzustand zurueck — `frontend/src/lib/server/backend-auth.ts:430@c5d57ef` — Impact: `handleLogout()` loescht erst die BFF-Session und bricht dann mit `502` ab, wenn der Backend-Logout kein `Location` liefert. In diesem Fehlerpfad wird der `gustav_session`-Cookie nicht defensiv gecleart; direkte Backend-Pfade koennen damit weiter authentifiziert bleiben. — Fix: Loesche den Backend-Session-Cookie auch im Fehlerpfad oder verschiebe das Zerstoeren der BFF-Session hinter eine validierte Backend-Logout-Antwort.

### NICE-TO-HAVE

- [CONSISTENCY] Redirect-Validierung ist mehrfach dupliziert und nicht deckungsgleich — `frontend/src/lib/server/backend-auth.ts:106@c5d57ef` — Impact: Das Frontend akzeptiert `redirect`-Werte, die der Backend-Validator und der OpenAPI-Regex bewusst ablehnen (z. B. Query/Fragment-Faelle). Das erzeugt schwer nachvollziehbare Unterschiede zwischen Login-, Register- und Logout-Flows. — Fix: Fuehre einen gemeinsamen Redirect-Validator mit gemeinsamem Testvektor fuer Frontend, Backend und `api/openapi.yml` ein.

### TESTS

- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_openapi_app_session_sync_contract.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- Ergebnis: `3 passed in 1.16s`
- Nicht ausgefuehrt: Frontend-Route-Integrationstests und Vollsuite

### Refactor-Budget

- Klein bis mittel: etwa 0.5 bis 1.5 Tage
- Hauptarbeit: Secret-Guard/Wiring, OpenAPI-Nachzug fuer die Frontdoor, Logout-Failure-Hardening, ein kleiner Satz echter Route-Tests

### Freigabe

- Keine Freigabe vor Behebung der MUST-FIX-Punkte.
- Danach reicht eine kurze Nachreview auf Auth-Route-Vertrag, Cookie-Header und Logout-Failure-Path.

## Review Round – 06:11

- Datum/Zeit: 2026-04-10 06:11 CEST
- Branch: `feature/sveltekit-bounded-context-refactor`
- Base-Ref: `master`
- Merge-Base: `16aa32329659b630753379306292839dc19d0324`
- Head: `c5d57ef277f10d2eb716e193bbf163bcf08b54ac`
- Reviewter Scope: fokussierte Nachpruefung des Commit-Stands `master..HEAD` in den Bereichen Learning-Worker-DSN-Vertrag, Live-Dashboard-Read-Model, persistente Browser-BFF-Sessions und Self-Registration-Domain-Policy.
- Kontextnotizen:
  - Der Arbeitsbaum war lokal dirty; die Befunde wurden deshalb gegen `HEAD` verifiziert (`git show c5d57ef:...`), nicht gegen uncommitted Dateien.
  - Die vorhandenen zielgerichteten Tests fuer Worker-DSN, Keycloak-Realm, Live-Dashboard und interne BFF-Session-API sind im aktuellen Branch gruen; mehrere Probleme sind also Abdeckungs- oder Vertragsluecken, kein allgemeiner Red-Build.
- Verwendete Code-Referenzen:
  - `backend/learning/workers/process_learning_submission_jobs.py`
  - `backend/learning/workers/health.py`
  - `backend/tests/test_learning_worker_security.py`
  - `backend/web/routes/app.py`
  - `backend/tests/test_live_view_api.py`
  - `backend/tests/test_openapi_live_dashboard_contract.py`
  - `backend/identity_access/bff_sessions_db.py`
  - `supabase/migrations/20260408170229_bff_sessions.sql`
  - `keycloak/realm-gustav.json`
  - `frontend/src/lib/server/backend-auth.ts`
  - `backend/web/routes/auth.py`
  - `backend/tests/test_keycloak_realm_config.py`

### Kurzfazit
Der Branch ist nah an einer freigabefaehigen Form, aber ein echter Laufzeitbruch im Learning-Worker bleibt offen. Zusaetzlich schneidet das Live-Dashboard grosse Kurse still ab, die neuen Browser-BFF-Token-Sessions werden nie bereinigt, und die Registrierungs-Domain ist weiterhin doppelt und widerspruechlich modelliert.

### MUST-FIX

- [BUG] Der Job-Runner ignoriert den dokumentierten Worker-DSN-Vertrag im Commit-Stand weiterhin — `backend/learning/workers/process_learning_submission_jobs.py:1129@c5d57ef` — Impact: `LearningWorkerHealthService` und Doku setzen auf `LEARNING_WORKER_DATABASE_URL`/`LEARNING_WORKER_DB_URL`, der eigentliche Runner liest in `HEAD` aber nur `LEARNING_DATABASE_URL`/`LEARNING_DB_URL`. Solange Compose beide Variablen spiegelt, bleibt der Fehler latent; sobald nur der dokumentierte Worker-DSN gesetzt ist, laufen Health-Probe und Job-Prozess auf unterschiedlichen Vertraegen. — Fix: Lies im Runner zuerst `LEARNING_WORKER_DATABASE_URL`/`LEARNING_WORKER_DB_URL`, halte Legacy-Namen nur als Fallback und ziehe Docstring, Fehlermeldung und Tests auf dieselbe Reihenfolge nach.

### SHOULD-FIX

- [SEC] Abgelaufene Browser-BFF-Token-Sessions bleiben unbegrenzt in Postgres liegen — `backend/identity_access/bff_sessions_db.py:96@c5d57ef` — Impact: Die neue Tabelle `public.bff_sessions` speichert Access-, Refresh- und ID-Tokens serverseitig, aber `get()` filtert nur `expires_at > now()` und weder Store noch Migration loeschen Altlasten. Dadurch wachsen Tabelle und Token-Restbestand unbegrenzt weiter, obwohl die Sessions fachlich schon tot sind. — Fix: Fuehre eine Bereinigung abgelaufener Datensaetze bei Read/Write oder ueber einen expliziten Cleanup-Job/eine SQL-Funktion ein und sichere das Verhalten mit einem Regressionstest ab.
- [API] Das Live-Dashboard schneidet Kursdaten still auf 100 Lernende ab — `backend/web/routes/app.py:1856@c5d57ef` — Impact: `/api/live/views/courses/{course_id}/units/{unit_id}/dashboard` exponiert laut OpenAPI keine Pagination, ruft die Summary intern aber hart mit `limit=100, offset=0` auf. Fuer groessere Kurse werden `rows`, `completion_rate_percent`, `average_score` und `selected_student_panel` damit fachlich unvollstaendig. — Fix: Aggregiere fuer das Dashboard ueber den vollen Kursbestand oder fuehre explizite Pagination samt Metadaten im Vertrag und in der UI ein.
- [TEST] Die Worker-Sicherheits-Suite pinnt den expliziten Worker-DSN-Pfad nicht fest — `backend/tests/test_learning_worker_security.py:29@c5d57ef` — Impact: Die aktuellen Tests laufen gruen (`3 passed`), obwohl der dokumentierte `LEARNING_WORKER_DATABASE_URL`-Pfad in `HEAD` defekt ist, weil nur Service-DSN-Ablehnung und Credential-Fallback geprueft werden. Genau dieser Vertragsbruch konnte so unbemerkt landen. — Fix: Erweitere den Reset-Helper um `LEARNING_WORKER_DATABASE_URL`/`LEARNING_WORKER_DB_URL` und schreibe einen Red-Test, der deren Praezedenz vor Legacy-Variablen festschreibt.
- [CONSISTENCY] Die Self-Registration-Domain ist weiterhin doppelt und widerspruechlich verdrahtet — `keycloak/realm-gustav.json:40@c5d57ef` — Impact: Browser-BFF und FastAPI lesen `ALLOWED_REGISTRATION_DOMAINS`, Keycloak importiert parallel weiter einen harten Regex auf `@school.example`. Abweichende Schul-Domains passieren damit zuerst die App-Vorpruefung und scheitern erst spaeter im IdP, was Betrieb, Fehlersuche und `local = prod` unnötig fragil macht. — Fix: Rendere die Keycloak-User-Profile-Regel aus derselben Konfiguration wie `frontend/src/lib/server/backend-auth.ts` und `backend/web/routes/auth.py` oder entscheide dich bewusst fuer genau eine Quelle der Wahrheit.

### NICE-TO-HAVE

- Keine weiteren Punkte mit gutem Kosten-Nutzen-Verhaeltnis im diesmal vertieft geprueften Scope.

### TESTS

- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_learning_worker_security.py -k 'worker_dsn'` → `3 passed, 4 deselected in 0.52s`
- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_keycloak_realm_config.py` → `9 passed in 0.59s`
- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_openapi_live_dashboard_contract.py` → `1 passed in 0.86s`
- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_live_view_api.py -k 'dashboard'` → `2 passed, 5 deselected in 0.52s`
- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_bff_session_internal_api.py` → `2 passed in 0.51s`
- Aussagekraft: Der relevante Ausschnitt ist gruen, deckt die oben genannten Vertrags- und Lifecycle-Luecken aber nicht ab.

### Refactor-Budget

- Ca. `0,75–1,5 PT`: ein gezielter Worker-Vertragsfix, ein Cleanup-Pfad fuer BFF-Sessions, ein kleiner Dashboard-Vertragsentscheid und 2-3 fokussierte Regressionstests.

### Freigabe

- Nicht freigeben, solange der Worker-MUST-FIX offen ist.
- Die SHOULD-FIX-Punkte zum Live-Dashboard und zu den BFF-Session-Altlasten sollten vor einem Release-Branch geschlossen werden; die Domain-Quelle-der-Wahrheit spaetestens vor produktiver Self-Registration.

## Review Round – 06:12

### Reviewed Scope Summary

- fokussierte Nachpruefung der Browser-BFF-Wrapper und des Deployment-Routings rund um `/backend-internal`, Teacher-Workspace-Refresh und Material-Download-Redirects
- gezielt geprueft wurden nur die Pfade, an denen Frontend-Routen bestehende Backend-Sicherheitsheader oder interne Sichtbarkeitsgrenzen neu abbilden

### Relevant Context Notes

- Das Backend setzt in den geprueften Read- und Download-Pfaden konsequent `Cache-Control: private, no-store`.
- Die neue SvelteKit-Schicht fuehrt mehrere eigene `+server.ts`-Wrapper ein; dort ist entscheidend, ob Privacy- und Routing-Grenzen unveraendert erhalten bleiben.
- Zusaetzlich verifiziert: `Response.redirect(...)` und SvelteKit-`json(...)` setzen standardmaessig keinen `Cache-Control`-Header.

### Code References Used For Findings

- `backend/web/main.py`
- `backend/web/routes/app.py`
- `backend/web/routes/teaching.py`
- `reverse-proxy/Caddyfile`
- `frontend/src/routes/teaching/units/[unitId]/graph/workspace/+server.ts`
- `frontend/src/routes/teaching/units/[unitId]/sections/[sectionId]/materials/[materialId]/file/+server.ts`
- `frontend/src/routes/teaching/units/[unitId]/+page.svelte`
- `backend/tests/packaging/test_sveltekit_teaching_unit_workspace_contract.py`

### Kurzfazit

Im BFF-/Routing-Layer bleiben noch zwei echte Security-Blocker und eine Cache-Regression offen: der interne Token-Session-Store ist nur per Routing abgeschirmt, Material-Download-Redirects verlieren `no-store`, und der Teacher-Workspace-Refresh macht ein zuvor privates Read-Model cachebar.

### MUST-FIX

- [SEC] Der interne BFF-Token-Store ist effektiv nur per Routing geschuetzt — `backend/web/main.py:343@c5d57ef` — Impact: `_is_public_path()` nimmt `/backend-internal/app/bff-session` komplett aus der Auth raus; dieselbe CRUD-Route liefert in `backend/web/routes/app.py:764-846` rohe Access-, Refresh- und ID-Tokens zurueck, und `reverse-proxy/Caddyfile:35-38` exponiert auf `https://localhost` den kompletten Backend-Host ohne Pfadfilter. Damit haengt der Schutz des Token-Stores nicht an eigener Authentisierung, sondern nur an fehlerfreiem Deployment-Routing. — Fix: Nimm den Pfad aus der Public-Allowlist heraus und sichere ihn separat per Shared Secret/mTLS oder ueber einen strikt internen Listener; der `localhost`-Passthrough darf dieses Namespace nicht von aussen erreichbar machen.
- [SEC] Der Material-Download-Wrapper verliert den Privacy-Header im Erfolgsfall — `frontend/src/routes/teaching/units/[unitId]/sections/[sectionId]/materials/[materialId]/file/+server.ts:35@c5d57ef` — Impact: Das Backend liefert fuer den Presign-Pfad explizit `Cache-Control: private, no-store` (`backend/web/routes/teaching.py:4416`), aber der Frontend-Wrapper baut daraus einen nackten `302` via `Response.redirect(...)`. Dadurch koennen signierte Storage-URLs im Browser- oder Proxy-Cache landen und der bisherige No-Store-Vertrag fuer teacher-scoped Materialdateien wird still gebrochen. — Fix: Erzeuge die Redirect-Response manuell und uebernimm mindestens `Cache-Control: private, no-store` aus dem Backend-Vertrag auf dem 302-Erfolgsweg.

### SHOULD-FIX

- [BUG] Der Teacher-Workspace-Refresh macht ein privates Read-Model cachebar — `frontend/src/routes/teaching/units/[unitId]/graph/workspace/+server.ts:32@c5d57ef` — Impact: Die Backend-API fuer den Workspace antwortet mit `_private_headers()` (`backend/web/routes/app.py:1218-1234`), der Frontend-Wrapper serialisiert aber mit nacktem `json(workspace)` neu. Der Browser ruft diesen Pfad in `frontend/src/routes/teaching/units/[unitId]/+page.svelte:381-390` aktiv per `fetch()` nach; ohne `private, no-store` werden sowohl sensible Teacher-Daten cachebar als auch Stale-Reads nach Mutationen wahrscheinlicher. — Fix: Reiche `cache-control` und sinnvollerweise auch `vary` aus der Backend-Antwort durch, statt einen headerlosen JSON-Response zu emittieren.
- [TEST] Frontend-BFF-Wrapper sind nicht auf Security-Header regressionsfest abgedeckt — `backend/tests/packaging/test_sveltekit_teaching_unit_workspace_contract.py:11@c5d57ef` — Impact: Die Packaging-Tests pruefen nur Existenz und String-Anker der Workspace-Route, aber nicht deren echte HTTP-Semantik. Genau deshalb koennen der cachebare Workspace-Refresh und der headerlose Material-Redirect gruen durchlaufen, obwohl das Backend fuer dieselben Daten explizit `private, no-store` absichert. — Fix: Ergaenze SvelteKit-Route-Tests fuer `/teaching/units/[unitId]/graph/workspace` und den Material-Download-Wrapper, die den Erfolgsfall explizit auf `Cache-Control: private, no-store` festnageln.

### NICE-TO-HAVE

- Keine weiteren Punkte mit gutem Kosten-Nutzen-Verhaeltnis im vertieft geprueften Scope.

### TESTS

- Ausgefuehrt: `.venv/bin/pytest -q backend/tests/test_auth_hardening.py backend/tests/test_bff_session_internal_api.py backend/tests/test_session_sync_api.py backend/tests/test_openapi_app_session_sync_contract.py backend/tests/packaging/test_sveltekit_auth_bridge_contract.py`
- Ergebnis: `32 passed in 1.69s`
- Manuelle Laufzeitprobe: `node -e "const r=Response.redirect(...)"` bestaetigt fuer den Erfolgsfall keinen `Cache-Control`-Header.
- Manuelle Laufzeitprobe: `node -e "import('@sveltejs/kit').then(({json}) => ...)"` bestaetigt, dass nacktes `json(...)` ebenfalls keinen `Cache-Control`-Header setzt.
- Nicht ausgefuehrt: Vollsuite und Frontend-Route-Tests.

### Refactor-Budget

- Ca. `0,5-1 PT`: internen BFF-Pfad hart absichern, zwei Frontend-Wrapper auf Privacy-Header nachziehen und zwei kleine Route-Regressionstests ergaenzen.

### Freigabe

- Nicht freigeben, bevor die beiden MUST-FIX-Punkte geschlossen sind.
