# PR Review: lokaler `master` gegen `origin/master`

Datum/Zeit: 2026-04-12 16:41 CEST

Branch: `master`
Base-Ref: `origin/master`
Merge-Base-SHA: `a188cf4f33fe5e145b44581ce62d8d8219087208`
Head-SHA: `22dddc5be3dd8ccdcea450c52d1f8c0d07780334`

Reviewter Scope:
- 5 lokale Commits auf `master` seit dem Merge-Base gegen `origin/master`
- 78 geänderte Dateien in Auth/BFF-Session-Bridge, DSPy-Feedback-Pipeline, Upload-Flows, stabile File-Routen und Teaching-Node-Editor
- Nicht Teil der Review: uncommittete Änderung in `README.md`

Kontextnotizen:
- Gezielte Tests für BFF-Session-API/-Store, DSPy-Adapter sowie die geänderten Frontend-Dateien liefen grün.
- Beim Frontend-Testlauf meldet Svelte weiterhin eine Warnung zur State-Erfassung in `+page.svelte`.

Verwendete Code-Referenzen:
- `frontend/src/routes/teaching/units/[unitId]/nodes/[nodeId]/+page.svelte`
- `frontend/src/routes/teaching/units/[unitId]/nodes/[nodeId]/+page.server.ts`
- `frontend/src/lib/server/backend-auth.ts`
- `frontend/src/hooks.server.ts`
- `backend/web/main.py`

## Review Round – 16:41

### Kurzfazit
Die Änderungen gehen in die richtige Richtung und die gezielten Tests sind grün. Drei Punkte bleiben aber offen: ein echter Navigationsfehler im Node-Editor, eine neue JS-Pflicht für Datei-Materialien und eine inkonsistente Startup-Validierung für das neue interne BFF-Secret. In der aktuellen Form würde ich den Stand noch nicht freigeben.

### MUST-FIX
- [BUG] `editorState` kapselt nur den Initialwert von `data.editor` und reagiert nicht auf spätere Prop-Updates — `frontend/src/routes/teaching/units/[unitId]/nodes/[nodeId]/+page.svelte:70@22dddc5be3dd8ccdcea450c52d1f8c0d07780334` — Impact: Bei Route-Reuse oder Navigation zwischen Knoten kann der Editor veraltete Node-/Material-/Task-Daten anzeigen oder weiterbearbeiten; der Svelte-Warnhinweis im Testlauf bestätigt genau dieses Muster. — Fix: Leite den lokalen Zustand sauber aus `data.editor` ab oder resynchronisiere ihn explizit bei Prop-Wechseln und ergänze einen Navigationstest.

### SHOULD-FIX
- [BUG] Datei-Materialien lassen sich serverseitig nicht mehr ohne vorbereitete `intent_id`/`sha256` anlegen; der Action-Handler bricht dann hart mit „Datei-Uploads benötigen aktiviertes JavaScript.“ ab — `frontend/src/routes/teaching/units/[unitId]/nodes/[nodeId]/+page.server.ts:427@22dddc5be3dd8ccdcea450c52d1f8c0d07780334` — Impact: Die Funktion regressiert von formulierter Server-Aktion zu JS-pflichtigem Flow; bei deaktiviertem JS, CSP-/Browserproblemen oder fehlgeschlagenem Enhance ist der zentrale Lehrer-Upload komplett blockiert. — Fix: Behalte einen serverseitigen Fallback für Datei-Uploads oder dokumentiere und feature-gate die neue harte JS-Abhängigkeit explizit.
- [CONSISTENCY] Das Frontend verweigert in prod-like Umgebungen den Start ohne `BFF_INTERNAL_SHARED_SECRET`, das Backend nicht — `frontend/src/lib/server/backend-auth.ts:132@22dddc5be3dd8ccdcea450c52d1f8c0d07780334`, `frontend/src/hooks.server.ts:5@22dddc5be3dd8ccdcea450c52d1f8c0d07780334`, `backend/web/main.py:342@22dddc5be3dd8ccdcea450c52d1f8c0d07780334` — Impact: Ein Backend-Misconfigurationsfall wird erst zur Laufzeit sichtbar und äußert sich dann als Login-/Callback-Ausfall statt als sauberer Startup-Fehler; das erschwert Betrieb und Diagnose unnötig. — Fix: Ergänze dieselbe verpflichtende Prod-Validierung im Backend-Startup oder ziehe die Prüfung in eine gemeinsame Konfigurations-Guardrail.

### NICE-TO-HAVE
- Keine weiteren belastbaren Punkte im überprüften Scope.

### TESTS
- Ausgeführt: `pytest -q backend/tests/test_bff_session_internal_api.py backend/tests/test_bff_session_store.py backend/tests/test_db_bff_session_store.py`
- Ausgeführt: `pytest -q backend/tests/learning_adapters/test_feedback_program_dspy_structured.py backend/tests/learning_adapters/test_visual_feedback_program_dspy_structured.py backend/tests/learning_adapters/test_local_feedback_dspy.py`
- Ausgeführt: `cd frontend && npm test -- --run src/lib/server/backend-auth.test.ts src/lib/server/session.test.ts src/routes/teaching/units/[unitId]/nodes/[nodeId]/page.server.test.ts src/routes/teaching/units/[unitId]/nodes/[nodeId]/page-interaction.test.ts`
- Testlücke: Es gibt keinen Test, der eine echte Prop-/Navigationsaktualisierung des Node-Editors gegen den lokal gehaltenen `editorState` absichert.

### Refactor-Budget
- Mittel. Die Korrekturen sind lokal begrenzt, betreffen aber einen kritischen Lehrer-Workflow und die Auth-Startkonfiguration.

### Freigabe
- Nicht freigegeben, bis der `editorState`-Synchronisationsfehler behoben ist.

## Reparaturplan – 2026-04-12 16:49 CEST

### Arbeitspaket 1: Node-Editor bei Prop-Wechsel sauber resynchronisieren
- Finding: `editorState` hält lokal einen Clone von `data.editor`, die aktuelle Ableitung erzeugt aber weiterhin die Svelte-Warnung zur nur initial erfassten `data`-Referenz.
- Warum offen: Die vorhandenen Tests decken Formular-Erfolge ab, aber keinen echten Re-Render mit neuem `data.editor` bei gleicher Route.
- Regeln/Constraints: `AGENTS.md` verlangt TDD und lehrbaren Code; `docs/ARCHITECTURE.md` fordert klare Trennung zwischen Svelte-Seitenzustand und Backend-Read-Model; `docs/DESIGN.md` bleibt unverändert, weil nur Zustandslogik angepasst wird.
- Bounded Context: `teaching`
- Dateien: `frontend/src/routes/teaching/units/[unitId]/nodes/[nodeId]/page-interaction.test.ts`, `frontend/src/routes/teaching/units/[unitId]/nodes/[nodeId]/+page.svelte`
- Vertrag/Migration zuerst: kein `api/openapi.yml`, keine Migration
- Test-first: Vitest ergänzt einen Navigations-/Re-Render-Fall mit geändertem `data.editor`
- Minimale Implementierung: Resync-Effekt so umbauen, dass eingehende Editor-Daten vollständig übernommen werden und lokale UI-Hilfszustände sauber zurückgesetzt werden
- Akzeptanzcheck: neuer Test grün, bestehende Node-Editor-Tests grün, Svelte-Warnung verschwindet

### Arbeitspaket 2: Backend-Startup-Guard für internes BFF-Secret ergänzen
- Finding: Frontend verweigert den Start in prod-like Umgebungen ohne `BFF_INTERNAL_SHARED_SECRET`, Backend-Startup noch nicht.
- Warum offen: `backend/web/config.py` enthält mehrere prod-like Guards, aber keinen für dieses Secret.
- Regeln/Constraints: `AGENTS.md` und `docs/ARCHITECTURE.md` priorisieren Security-first und fail-fast Konfiguration; `docs/references/auth_sessions_and_cookies.md` beschreibt `BFF_INTERNAL_SHARED_SECRET` bereits als Pflicht in prod-artigen Umgebungen.
- Bounded Context: `identity_access`
- Dateien: `backend/tests/test_config_security.py`, `backend/web/config.py`
- Vertrag/Migration zuerst: kein `api/openapi.yml`, keine Migration
- Test-first: pytest ergänzt einen prod-like Fehlerfall für fehlendes oder Platzhalter-Secret
- Minimale Implementierung: bestehende Startup-Guard um dieselbe Secret-Prüfung erweitern
- Akzeptanzcheck: neuer pytest grün, bestehende Security-Tests weiter grün

### Revalidierter Review-Punkt: Datei-Materialien ohne JavaScript
- Status: stale / bewusst entschieden, kein offener Fix in diesem Arbeitspaket
- Grund: `docs/references/teaching.md` und `docs/plan/2026-04-12-teaching-client-upload-realignment.md` definieren Datei-Materialien ausdrücklich als Browser-Upload mit klarer Ablehnung ohne JavaScript; vorhandene Server-Tests bestätigen dieses Verhalten.

## Umsetzungsstand – 2026-04-12 16:58 CEST

- Geschlossen:
  - `MUST-FIX` Node-Editor-Resync: `editorState` liest eingehende Load-Daten nun wieder live und verwendet lokale Overrides nur noch für erfolgreiche Formularantworten.
  - `SHOULD-FIX` Backend-BFF-Guard: `backend/web/config.py` bricht in prod-like Umgebungen jetzt auch ohne gültiges `BFF_INTERNAL_SHARED_SECRET` fail-fast ab.
- Weiter offen:
  - Keine verifizierten offenen MUST-FIXes aus diesem Dokument.
- Stale nach Re-Check:
  - Datei-Materialien ohne JavaScript. Der Punkt bleibt bewusst unverändert, weil Teaching-Datei-Uploads laut Referenz und bestehender Planung explizit JS-only sind.
- Tests ausgeführt:
  - `cd frontend && npm test -- --run src/routes/teaching/units/[unitId]/nodes/[nodeId]/page-interaction.test.ts src/routes/teaching/units/[unitId]/nodes/[nodeId]/page.server.test.ts src/lib/server/backend-auth.test.ts`
  - `.venv/bin/pytest -q backend/tests/test_config_security.py -q`
- Geänderte Dateien/Subsysteme:
  - Node-Editor in Svelte (`teaching`)
  - Security-Startup-Guard und zugehörige Tests (`identity_access`)
- Architektur-/Context-Entscheidungen:
  - Kein API-Vertrag geändert, keine Migration nötig.
  - Frontend-Editorzustand folgt jetzt dem Read-Model aus dem Load und hält nur noch gezielte lokale Erfolgs-Overrides.
  - Das interne BFF-Secret wird nun konsistent in Frontend und Backend als Pflicht für prod-like Start behandelt.
- Design-Check:
  - Kein Eingriff in den visuellen Designvertrag, nur Zustands- und Konfigurationslogik.
- Restrisiko:
  - `make verify` steht noch als breiter Repo-Check aus; fachlich betroffene Zieltests sind bereits grün.
- Freigabeempfehlung:
  - Aus Sicht der verifizierten Findings jetzt freigabefähig, sofern der breite Repo-Check keine neuen Regressionen zeigt.
