# PR Review Log 2026-04-13

## Review Round – 15:13

### Review Title
Review der uncommitted Änderungen auf `master`

### Git Context
- Branch: `master`
- Base ref: `master`
- Merge-base SHA: `e64e14530fd83c75cd876c7a8efdf687faaa7902`
- Head SHA: `e64e14530fd83c75cd876c7a8efdf687faaa7902`
- Review scope: uncommitted Änderungen im Worktree gegen `HEAD`

### Reviewed Scope Summary
- `api/openapi.yml`
- `backend/web/routes/learning.py`
- `backend/web/main.py`
- `backend/web/material_file_access.py`
- `backend/tests/test_learning_api_contract.py`
- `backend/tests/test_learning_material_file_batching.py`
- `backend/tests/test_learning_modular_units_api_contract.py`
- `backend/tests/test_openapi_learning_material_file_route_contract.py`
- `supabase/migrations/20260413143000_learning_material_file_visibility_helper.sql`
- `docs/plan/2026-04-13-learning-material-content-visibility-unification.md`

### Relevant Context Notes
- Die Änderung führt eine kanonische Material-Dateiroute ohne `section_id` ein und behält die bisherige Route als Legacy-Alias.
- Die Sichtbarkeitsprüfung wird in einen gemeinsamen Python-/SQL-Helper verschoben, der nun von Learning API und SSR benutzt wird.
- Die gezielten Happy-Path-Tests laufen lokal, decken aber die neuen Fehlerpfade nicht vollständig ab.

### Code References Used For Findings
- `backend/web/routes/learning.py:2625-2730`
- `backend/web/material_file_access.py:90-133`
- `backend/web/main.py:1567-1640`
- `api/openapi.yml:5786-5920`
- `supabase/migrations/20260413143000_learning_material_file_visibility_helper.sql:14-91`
- `backend/tests/test_learning_api_contract.py:1048-1165`
- `backend/tests/test_learning_modular_units_api_contract.py:395-486`
- `backend/tests/migration/test_modular_unlock_helper_no_edge_n_plus_one.py:1-65`
- `supabase/migrations/20260206203634_learning_modular_unlock_single_source.sql:15-191`

### Kurzfazit
Die Vereinheitlichung ist konzeptionell gut und die Happy-Path-Tests sind grün. Aktuell kippt die neue Lookup-Kette Betriebsfehler aber in `404` um, der Legacy-Alias öffnet einen kleinen Auth-Reihenfolge-Bruch, und der Batch-Helper versteckt für modulare Inhalte wieder eine SQL-seitige N+1-Kostenstruktur.

### MUST-FIX
- [API] Lookup-/DB-Fehler werden als `404` statt `503` maskiert — `backend/web/routes/learning.py:2648@e64e145+worktree`, `backend/web/routes/learning.py:2711@e64e145+worktree`, `backend/web/material_file_access.py:99@e64e145+worktree` — Impact: Fehlende DSN, Verbindungsfehler oder eine nicht migrierte SQL-Funktion laufen im neuen Helper auf `metadata is None` und enden als `404`, obwohl der Vertrag weiter `503` dokumentiert. Das verschleiert echte Betriebsstörungen und erschwert Monitoring wie Incident-Diagnose. — Fix: Trenne „nicht sichtbar/nicht vorhanden“ explizit von Infrastrukturfehlern; gib bei DSN-/Connect-/Query-Fehlern `503 service_unavailable` zurück und ergänze einen Contract-Test dafür.

### SHOULD-FIX
- [SEC] Der Legacy-Alias validiert Pfad-UUIDs vor der Authentifizierung — `backend/web/routes/learning.py:2700@e64e145+worktree` — Impact: Anonyme Aufrufer bekommen bei ungültigen IDs `400` statt `401` und können damit den geschützten Endpunkt vom Auth-Gate unterscheiden; das weicht außerdem vom kanonischen Handler direkt darüber ab. — Fix: Ziehe `_require_student(request)` vor die UUID-Prüfung und sichere das Verhalten mit einem Unauthorized-Regressionstest ab.
- [BUG] Der neue Batch-Helper verlagert die Sichtbarkeit in ein SQL-internes N+1 für modulare Materialien — `backend/web/material_file_access.py:108@e64e145+worktree`, `supabase/migrations/20260413143000_learning_material_file_visibility_helper.sql:80@e64e145+worktree` — Impact: Für jedes angefragte Material wird erneut `get_material_file_metadata_for_student(...)` und darin bei modularen Units `modular_section_is_open_or_done_for_student(...)` ausgeführt; diese Funktion berechnet die Modulzustände wiederum über den kompletten Unit-Graphen. Die Python-Seite wirkt gebatched, die eigentliche Unlock-Arbeit skaliert aber wieder pro Material. — Fix: Mache die neue Material-Metadaten-Auflösung set-basiert, sodass Modulzustände pro Unit nur einmal berechnet und dann auf alle angefragten Materialien projiziert werden.

### NICE-TO-HAVE
- Keine weiteren Findings im geprüften Scope.

### TESTS
- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_openapi_learning_material_file_route_contract.py`
- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_learning_material_file_batching.py`
- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_learning_api_contract.py -k material_file`
- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_learning_modular_units_api_contract.py -k material_file`
- Lücke: Es fehlt ein gezielter Regressionstest für Lookup-/DB-Fehler => `503`.
- Lücke: Es fehlt ein gezielter Test für anonyme Aufrufe des Legacy-Alias mit ungültigen UUIDs.

### Refactor-Budget
- Mittel: etwa `0.5-1 Tag` für saubere Fehlerklassifikation (`404` vs. `503`), Reihenfolgefix im Alias, set-basierten SQL-Helper und die zwei fehlenden Regressionstests.

### Freigabe
Nicht freigeben vor dem API-Fix für die `404`/`503`-Regression. Die beiden SHOULD-FIX-Punkte sollten im gleichen Zug mitgezogen werden, weil sie dieselbe neue Route- und Helper-Oberfläche betreffen.

## Reparaturplan – 2026-04-13 15:21

### Arbeitspaket 1: `404`/`503`-Regression der Material-Dateirouten
- Finding: Lookup-/DB-Fehler werden aktuell in `404` maskiert.
- Noch offen, weil `load_student_material_file_metadata(...)` im Fehlerfall keine Ursache transportiert und beide Routen daraus pauschal `404` machen.
- Relevante Regeln: API Contract-First aus `AGENTS.md`; Learning-Fehlersemantik und fail-closed-Regeln aus `docs/ARCHITECTURE.md` und `docs/references/learning.md`.
- Bounded Context: `learning`
- Wahrscheinliche Dateien: `api/openapi.yml`, `backend/web/material_file_access.py`, `backend/web/routes/learning.py`, `backend/tests/test_learning_api_contract.py`
- Vorabänderungen: OpenAPI `503` präzisieren, dann Test.
- Test-first: Contract-Test für kanonische Route mit fehlender DSN/Lookup-Ausfall => `503 service_unavailable` mit `detail=authorization_unavailable`.
- Minimale Implementierung: explizite Lookup-Exception im Helper einführen und in beiden Material-Dateirouten sauber auf `503` mappen.
- Acceptance: Sichtbarkeits-`404` bleibt unverändert, Infrastrukturfehler werden `503`.

### Arbeitspaket 2: Legacy-Alias Auth vor UUID-Validierung
- Finding: Der Legacy-Alias verrät vor dem Auth-Gate per `400`, dass der Endpunkt existiert.
- Noch offen, weil `get_material_file_legacy_alias(...)` UUIDs vor `_require_student(request)` prüft.
- Relevante Regeln: Security-first aus `AGENTS.md`; konsistente Auth-Middleware-Semantik aus `docs/ARCHITECTURE.md`.
- Bounded Context: `learning`
- Wahrscheinliche Dateien: `backend/web/routes/learning.py`, `backend/tests/test_learning_api_contract.py`
- Vorabänderungen: keine neuen API-Felder; bestehender Vertrag bleibt.
- Test-first: Regressionstest für unauthenticated Legacy-Alias mit ungültiger UUID => `401`.
- Minimale Implementierung: `_require_student(request)` vor die UUID-Prüfung ziehen und die übrige Fehlersemantik unverändert lassen.
- Acceptance: unauthenticated immer `401`, authenticated + invalid UUID weiter `400`.

### Arbeitspaket 3: Set-basierte Material-Visibility für modulare Batches
- Finding: Der neue Batch-Helper ist Python-seitig gebündelt, rechnet SQL-seitig modulare Unlock-Logik aber weiter pro Material.
- Noch offen, weil `load_student_material_file_metadata_batch(...)` derzeit pro Material lateral die Single-Material-Funktion aufruft und diese wiederum `modular_section_is_open_or_done_for_student(...)` nutzt.
- Relevante Regeln: KISS und Performance-Refactor aus `AGENTS.md`; Clean-Architecture-/Context-Map-Regel aus `docs/bounded_contexts.md`; Learning ist Downstream und besitzt die studentische Sichtbarkeitsentscheidung.
- Bounded Context: `learning`
- Wahrscheinliche Dateien: neue Folge-Migration unter `supabase/migrations/`, `backend/web/material_file_access.py`, `backend/tests/test_learning_material_file_batching.py`, neuer Migrationstest unter `backend/tests/migration/`
- Vorabänderungen: neue Folge-Migration statt Umschreiben der bereits lokal angewendeten Funktion.
- Test-first: DB-Guardrail gegen `modular_section_is_open_or_done_for_student(...)` im Batch-Pfad plus bestehende Batch-API-Tests beibehalten.
- Minimale Implementierung: `get_material_file_metadata_batch_for_student(...)` einführen, die Modulzustände je Unit set-basiert über `get_modular_unit_module_states_for_student(...)` auflöst; Single-Funktion wird dünner Wrapper.
- Acceptance: genau ein SQL-Batch-Call aus Python, kein modularer N+1-Pfad mehr im DB-Helper.

## Umsetzungsstand – 2026-04-13 15:33

### Geschlossene Findings
- `[API]` Lookup-/DB-Fehler der Material-Dateirouten werden nicht mehr als `404` maskiert.
- `[BUG]` Der Batch-Pfad für sichtbare Material-Dateien nutzt jetzt eine set-basierte DB-Funktion statt pro Material den Single-Helper erneut anzustoßen.

### Stale nach Re-Check
- `[SEC]` Der Legacy-Alias validiert UUIDs vor Auth — fachlich als Risiko dokumentiert, aber auf der laufenden App bereits durch die Auth-Middleware abgefangen. Ein unauthenticated Request auf die Alias-Route liefert schon jetzt `401`, auch bei ungültiger `section_id`. Deshalb kein Laufzeitcode geändert.

### Geänderte Dateien / Subsysteme
- API-Vertrag: `api/openapi.yml`
- Learning Web-Adapter: `backend/web/routes/learning.py`
- gemeinsamer Visibility-Helper: `backend/web/material_file_access.py`
- Folge-Migration: `supabase/migrations/20260413153000_learning_material_file_visibility_batch_helper.sql`
- Tests: `backend/tests/test_openapi_learning_material_file_route_contract.py`, `backend/tests/test_learning_api_contract.py`, `backend/tests/test_learning_material_file_batching.py`, `backend/tests/migration/test_learning_material_visibility_batch_helper_contract.py`

### Architektur- und Context-Entscheidungen
- Die studentische Sichtbarkeitsentscheidung bleibt im Bounded Context `learning`.
- SSR bleibt bei Lookup-Ausfall fail-closed und rendert keine neue Dateivorschau, statt Infrastrukturfehler im UI aufzublasen.
- Die bereits lokal angewendete Single-Helper-Migration wurde nicht rückwirkend verändert; stattdessen wurde eine Folge-Migration ergänzt, damit `lokal = prod` konsistent bleibt.

### Tests und Verifikation
- OpenAPI-/Targeted-Tests:
  - `.venv/bin/pytest -q backend/tests/test_openapi_learning_material_file_route_contract.py`
  - `.venv/bin/pytest -q backend/tests/test_learning_api_contract.py -k material_file`
  - `.venv/bin/pytest -q backend/tests/test_learning_modular_units_api_contract.py -k material_file`
  - `.venv/bin/pytest -q backend/tests/test_learning_material_file_batching.py`
  - `.venv/bin/pytest -q backend/tests/migration/test_learning_material_visibility_batch_helper_contract.py`
  - `.venv/bin/pytest -q backend/tests/migration/test_modular_unlock_helper_no_edge_n_plus_one.py`
- Migration lokal angewendet:
  - `supabase migration up --local --yes`
- Breitentest:
  - `make verify`

### Ergebnis
- `make verify` ist grün.
- Lokale Migrationen und relevante Learning-/Migration-Regressionen sind grün.
- Für den überprüften Scope ist die ursprüngliche Freigabe-Blockade aufgehoben.

### Restrisiko
- Die Stream-Routen unterscheiden jetzt explizit zwischen Lookup-Ausfall (`503`) und nicht sichtbarem Material (`404`). Weitere studentische Payload-Listen bleiben absichtlich fail-closed und zeigen bei Lookup-Ausfall einfach keine `file_url`; das ist bewusst nicht erweitert worden.

## Reparatur-Notiz – 2026-04-13 16:02

### Anlass
- Re-Check der Review-Finding-Lage für die kanonische Material-Dateiroute und den stillgelegten SSR-Modul-Fragmentpfad.
- Der zunächst vermutete Laufzeitfehler liegt nicht auf einem aktiven Student-Request-Pfad, sondern in unerreichbarem Altcode hinter einem frühen `410 Gone`.

### Technische Einordnung
- Die Route `/learning/courses/{course_id}/units/{unit_id}/modules/{module_id}/fragment` beendet die Anfrage sofort mit `410`.
- Der darunter verbliebene HTML-/Preview-Code wird deshalb nie ausgeführt, enthält aber noch veraltete Section-Mapping- und Fallback-Logik aus der Zeit vor der kanonischen Material-Dateiroute.
- Diese tote Logik verwendet weiterhin `_fetch_student_material_section_map_for_unit(...)` sowie einen veralteten Aufruf von `_resolve_student_material_file_url(..., section_id=...)`.

### Umsetzungsentscheidung
- Scope: `Fix + Cleanup`, ohne Reaktivierung des Legacy-Fragmentpfads.
- TDD-Ziel: Cleanup per Source-/Contract-Test absichern, dann den unerreichbaren Altblock in `backend/web/main.py` entfernen.
- Öffentliche APIs, OpenAPI-Vertrag, Migrationen und aktive Learning-Routen bleiben unverändert.

## Reparaturplan – 2026-04-13 17:45

### Arbeitspaket 1: Snapshot-Cursor für das kanonische `/live`-Polling
- Finding: Das SvelteKit-`/live` seeded den Delta-Poll mit `now()` statt mit einem snapshot-konsistenten Cursor aus dem Summary-Read.
- Noch offen, weil `GET /api/teaching/courses/{course_id}/units/{unit_id}/submissions/summary` aktuell keinen Cursor liefert und `frontend/src/routes/live/+page.server.ts` daher auf `new Date().toISOString()` ausweicht.
- Relevante Regeln: API Contract-First und TDD aus `AGENTS.md`; Polling-/Cursor-Semantik aus `docs/ARCHITECTURE.md` und `docs/references/teaching_live.md`.
- Bounded Context: `teaching` fuer den Read-Model-Contract, SvelteKit-BFF fuer die Browser-Orchestrierung.
- Wahrscheinliche Dateien: `api/openapi.yml`, `backend/web/routes/teaching.py`, `backend/tests/test_openapi_teaching_live_unit_contract.py`, `backend/tests/test_teaching_live_unit_summary_api.py`, `frontend/src/routes/live/+page.server.ts`, `frontend/src/routes/live/page-state.ts`, zugehoerige Vitest-Dateien.
- Vorabänderungen: OpenAPI und Referenzdoku zuerst erweitern.
- Test-first: Contract-Test fuer `summary.cursor` plus Frontend-Regressionsfall fuer initialen Poll aus dem Summary-Cursor.
- Minimale Implementierung: Summary erzeugt und liefert einen Snapshot-Cursor; SvelteKit nutzt exakt diesen Wert fuer SSR und Polling.
- Acceptance: erste Delta-Anfrage startet mit `summary.cursor`, nicht mit Browser-Zeit.

### Arbeitspaket 2: Polling-Cursor nur nach erfolgreichem Refresh fortschreiben
- Finding: `frontend/src/routes/live/page-state.ts` zieht den Cursor vor, bevor Summary- und Detail-Refresh sicher durchgelaufen sind.
- Noch offen, weil `poll()` `state.cursor = result.cursor` vor `fetchSummary()` setzt und Fehler im Polling-Loop spaeter still schluckt.
- Relevante Regeln: KISS und robuste Lehreransicht aus `AGENTS.md`; Live-Ansicht muss bei Fehlern fail-safe und nachvollziehbar bleiben.
- Bounded Context: SvelteKit-BFF / Live-Workspace-Controller.
- Wahrscheinliche Dateien: `frontend/src/routes/live/page-state.ts`, `frontend/src/routes/live/page-interaction.test.ts`, optional `frontend/src/routes/live/+page.svelte`.
- Vorabänderungen: keine neue API, nur Frontend-Regressions-Tests.
- Test-first: Wenn Delta `200` liefert, aber Summary- oder Detail-Reload fehlschlaegt, bleibt der alte Cursor erhalten und die offene Zelle geht nicht verloren.
- Minimale Implementierung: Cursor erst committen, wenn Summary und ggf. Detail erfolgreich nachgeladen wurden.
- Acceptance: bei Polling-Fehlern wird derselbe Delta-Bereich erneut abgefragt.

### Arbeitspaket 3: Live-Namensauflösung ohne blockierenden N+1-Hot-Path
- Finding: `resolve_live_student_names_by_sub(...)` fuehrt auf dem Request-Pfad sequentielle synchrone Admin-Requests pro Lernendem aus.
- Noch offen, weil die Logik direkt in `backend/web/routes/teaching.py` lebt, die Event-Loop blockiert und keinen kleinen Wiederverwendungs-Cache nutzt.
- Relevante Regeln: Clean Architecture und Bounded Context-Trennung aus `AGENTS.md`, `docs/ARCHITECTURE.md` und `docs/bounded_contexts.md`.
- Bounded Context: `identity_access` besitzt die Directory-/Keycloak-Aufloesung; `teaching` konsumiert nur Labels.
- Wahrscheinliche Dateien: `backend/identity_access/directory.py`, `backend/web/routes/teaching.py`, `backend/web/routes/app.py`, `backend/tests/test_identity_access_directory_login_labels.py`, `backend/tests/test_live_view_api.py`, relevante Summary-API-Tests.
- Vorabänderungen: kein neuer externer Endpoint, keine Migration.
- Test-first: Personennamen haben Vorrang vor Localpart; fehlende Personendaten fallen auf Localpart zurueck; wiederholte Resolver-Aufrufe sparen Keycloak-GETs ueber einen kleinen TTL-Cache.
- Minimale Implementierung: Resolver in `identity_access` erweitern, kleinen In-Memory-TTL-Cache je `sub` einfuehren und die Web-Routen via `asyncio.to_thread(...)` anbinden.
- Acceptance: Live-Summary und Detail-Sheet zeigen weiterhin Personenname > Localpart > `Unbekannt`, ohne bei jedem Poll dieselben Netzwerkaufrufe zu wiederholen.

### Arbeitspaket 4: Direkt verwandte SHOULD-FIX-Themen
- Finding: Die neuen sichtbaren `/live/courses/...`-Svelte-Seiten enthalten noch ASCII-Notbehelfe wie `Waehle` und `oeffnen`; ausserdem fehlen die neuen Cursor-Regressionsfaelle in der Test-Suite.
- Noch offen, weil die neuen Seiten und Tests parallel zur Live-Workspace-Umstellung eingefuehrt wurden.
- Relevante Regeln: sichtbare deutsche Texte mit Umlauten aus `AGENTS.md`; keine neue visuelle Sprache neben dem bestehenden Designvertrag.
- Bounded Context: Frontend/SvelteKit.
- Wahrscheinliche Dateien: `frontend/src/routes/live/courses/[courseId]/+page.svelte`, `frontend/src/routes/live/courses/[courseId]/units/[unitId]/+page.svelte`, `frontend/src/routes/live/page-interaction.test.ts`.
- Vorabänderungen: keine API-/Schema-Aenderung.
- Test-first: die neuen Polling-Regressionsfaelle ergaenzen; UI-Texte werden per Code-Review konsistent angepasst.
- Minimale Implementierung: sichtbare Texte orthografisch korrigieren und Tests mitlaufen lassen.
- Acceptance: keine ASCII-Ersatzschreibweise mehr in sichtbaren deutschen UI-Texten des neuen Live-Pfads.

## Umsetzungsstand – 2026-04-13 17:49

### Geschlossene Findings
- `[BUG]` Das kanonische `/live` seeded den Delta-Poll nicht mehr mit Browser-Zeit, sondern mit `summary.cursor` aus dem Summary-Read-Model.
- `[BUG]` Der Live-Workspace committet einen neuen Polling-Cursor erst nach erfolgreichem Summary- und optionalem Detail-Refresh.
- `[BUG]` Die Live-Namensauflösung liegt wieder im Bounded Context `identity_access`, priorisiert echte Personennamen vor Localparts und verwendet einen kleinen TTL-Cache fuer wiederholte Polls.
- `[TEST]` Die fehlenden Regressionstests fuer Cursor-Commit und Live-Namensauflösung sind ergänzt.
- `[CONSISTENCY]` Sichtbare deutsche Texte in den neuen `/live/courses/...`-Svelte-Seiten verwenden wieder korrekte Umlaute.

### Geänderte Dateien / Subsysteme
- API-/Referenzvertrag: `api/openapi.yml`, `docs/references/teaching_live.md`
- Identity-Adapter: `backend/identity_access/directory.py`
- Teaching-/Live-Web-Adapter: `backend/web/routes/teaching.py`, `backend/web/routes/app.py`
- Frontend-Live-Workspace: `frontend/src/routes/live/+page.server.ts`, `frontend/src/routes/live/page-state.ts`
- Frontend-Tests und neue BFF-Route: `frontend/src/routes/live/page-interaction.test.ts`, `frontend/src/routes/live/courses/[courseId]/units/[unitId]/summary/+server.ts`, `frontend/src/routes/live/courses/[courseId]/units/[unitId]/summary/server.test.ts`
- Backend-Tests: `backend/tests/test_openapi_teaching_live_unit_contract.py`, `backend/tests/test_teaching_live_unit_summary_api.py`, `backend/tests/test_identity_access_directory_login_labels.py`
- UI-Konsistenz: `frontend/src/routes/live/courses/[courseId]/+page.svelte`

### Architektur- und Bounded-Context-Entscheidungen
- Die Keycloak-/Directory-Auflösung verbleibt im Kontext `identity_access`; `teaching` konsumiert nur noch einen Resolver statt selbst HTTP-Requests zu orchestrieren.
- Der Summary-Cursor ist Teil des offiziellen Teaching-Live-Read-Models und damit contract-first dokumentiert.
- Das Frontend benutzt fuer den Fortschreibungs-Cursor den erfolgreichen Summary-Snapshot statt eines optimistischen Vorab-Commits.

### Tests und Verifikation
- Backend:
  - `.venv/bin/pytest -q backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_teaching_live_unit_summary_names_humanized.py backend/tests/test_teaching_live_unit_summary_legacy_email_fallback.py backend/tests/test_live_view_api.py`
  - `.venv/bin/pytest -q backend/tests/packaging/test_sveltekit_live_dashboard_contract.py`
- Frontend:
  - `npm --prefix frontend test -- --run src/routes/live/page-interaction.test.ts src/routes/live/courses/[courseId]/units/[unitId]/summary/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/detail-sheet/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/submissions/delta/server.test.ts`
- Typecheck:
  - `npm --prefix frontend run check`
  - Ergebnis: Die von diesem PR-Fix eingeführten Fehler sind bereinigt; der Lauf bleibt aber wegen bereits vorhandener, fachfremder Fehler in `submission-artifacts` und `teaching/units/[unitId]/nodes/[nodeId]` rot.

### Verbleibende offene Punkte / stale
- Kein offener MUST-FIX mehr im heute bearbeiteten Live-Dashboard-Scope.
- Repo-weiter Frontend-Typecheck bleibt ausserhalb dieses PR-Fix ungrün; die Restfehler lagen schon in anderen Bereichen und wurden bewusst nicht in diesen Scope hineingezogen.

### Freigabe-Einschätzung
- Fuer den geaenderten Live-Dashboard-Scope ist die urspruengliche Freigabe-Blockade aufgehoben.
- Vor einer globalen Repo-Freigabe bleibt der bestehende, fachfremde Svelte-Check-Befund separat zu bereinigen.

## Reparaturplan – 2026-04-13 18:08

### Arbeitspaket 1: Security-Header fuer die browserseitigen `/live`-BFF-Routen
- Finding: Die neuen Read-BFFs fuer `summary`, `detail-sheet`, `delta` und `dashboard` serialisieren Backend-Antworten neu und verlieren dabei `Cache-Control: private, no-store` sowie `Vary: Origin`.
- Noch offen, weil die Routen aktuell `json(...)` oder eine reduzierte `new Response(...)`-Variante verwenden statt die vorhandene Header-Weitergabe aus dem allgemeinen BFF-Muster zu nutzen.
- Relevante Regeln: Security-first aus `AGENTS.md`; API-/Caching-Vertrag aus `docs/ARCHITECTURE.md` und `docs/references/teaching_live.md`.
- Bounded Context: SvelteKit-Browser-BFF vor `teaching`.
- Wahrscheinliche Dateien: `frontend/src/lib/server/bff-proxy.ts`, `frontend/src/lib/server/bff-proxy.test.ts`, `frontend/src/routes/live/courses/[courseId]/units/[unitId]/{summary,detail-sheet,submissions/delta,dashboard}/+server.ts`, zugehoerige Vitest-Dateien.
- Vorabänderungen: kein OpenAPI-/DB-Change; nur Browser-BFF.
- Test-first: Read-BFF-Proxy und Live-Route-Tests pruefen explizit auf `cache-control` und `vary`, inklusive `204`.
- Minimale Implementierung: zentrales Read-Proxy-Hilfsmuster einfuehren und alle vier Live-BFFs darauf umstellen.
- Acceptance: Die browserseitigen `/live`-Responses behalten dieselben Security-Header wie das FastAPI-Backend.

### Arbeitspaket 2: Kanonische `/live`-Auswahl statt Loader-Abbruch bei stale `task_id`
- Finding: Ein veralteter oder fachfremder `task_id` in der `/live`-URL fuehrt aktuell zu einem Detail-Fetch gegen eine ungueltige Auswahl und kann die gesamte Seite kippen.
- Noch offen, weil `frontend/src/routes/live/+page.server.ts` das Detail derzeit mit den rohen Query-Parametern laedt statt zuerst die Auswahl gegen das Summary-Read-Model zu kanonisieren.
- Relevante Regeln: KISS, robuste Lehreransicht und TDD aus `AGENTS.md`; `/live` bleibt ein stabiler Workspace gemaess `docs/DESIGN.md`.
- Bounded Context: SvelteKit-Live-Workspace.
- Wahrscheinliche Dateien: `frontend/src/routes/live/+page.server.ts`, `frontend/src/routes/live/+page.server.test.ts`, `frontend/src/routes/live/page-state.ts`.
- Vorabänderungen: kein API-/Schema-Change; die bestehende Summary bleibt Quelle der Wahrheit.
- Test-first: Loader-Test fuer stale `task_id` => `302` auf die kanonische URL; zusaetzlich ungültiger `student_sub` => `302` ohne Schueler-/Task-Auswahl.
- Minimale Implementierung: Pure Function fuer Auswahl-Normalisierung exportieren und serverseitig vor dem Detail-Fetch anwenden.
- Acceptance: Veraltete Bookmarks werden auf die kanonische `/live`-URL umgebogen statt mit Fehler zu enden.

### Arbeitspaket 3: Contract-treue Summary-Typen und lokale Link-Ableitung
- Finding: Das Frontend typisiert die Teaching-Live-Summary aktuell gegen einen Matrix-Typ mit `href`-Feldern, die der Backend-Vertrag gar nicht liefert.
- Noch offen, weil `LiveSummaryPayload` derzeit `rows` aus `LiveUnitMatrixView` erbt und Tests kuenstliche `href`s in die Summary-Fixtures schreiben.
- Relevante Regeln: Contract-first aus `AGENTS.md`; `teaching` besitzt den Read-Model-Vertrag, das Frontend passt sich an.
- Bounded Context: Frontend/SvelteKit.
- Wahrscheinliche Dateien: `frontend/src/lib/types/home.ts`, `frontend/src/routes/live/page-state.ts`, `frontend/src/routes/live/page-interaction.test.ts`.
- Vorabänderungen: kein OpenAPI-Change; nur Frontend-Typgrenzen und Tests.
- Test-first: View-Model-Tests mit contract-treuer Summary ohne `href` verifizieren lokal generierte Tabellen- und Task-Links.
- Minimale Implementierung: dedizierte Summary-Typen einfuehren und alle `href`s im Dashboard-View-Model aus der aktuellen Auswahl ableiten.
- Acceptance: Das Live-Frontend bleibt lauffaehig und contract-treu, auch wenn Summary-Zeilen selbst keine Links enthalten.

## Umsetzungsstand – 2026-04-13 18:15

### Geschlossene Findings
- `[SEC]` Die browserseitigen `/live`-Read-BFFs fuer `summary`, `detail-sheet`, `delta` und `dashboard` propagieren jetzt wieder `Cache-Control: private, no-store` und `Vary: Origin`.
- `[BUG]` Stale oder fachfremde `/live`-Query-Parameter (`student_sub`, `task_id`) werden serverseitig vor dem Detail-Fetch auf eine kanonische URL normalisiert.
- `[CONSISTENCY]` Das Frontend verwendet fuer die Teaching-Live-Summary jetzt einen eigenen, contract-treuen Typ statt eines Matrix-Typs mit nicht vorhandenen `href`-Feldern.
- `[TEST]` Die fehlenden Regressionstests fuer Header-Propagation, kanonische `/live`-Weiterleitungen und lokale Link-Ableitung sind ergänzt.

### Geänderte Dateien / Subsysteme
- Browser-BFF: `frontend/src/lib/server/bff-proxy.ts`, `frontend/src/lib/server/bff-proxy.test.ts`
- Live-BFF-Routen: `frontend/src/routes/live/courses/[courseId]/units/[unitId]/{summary,detail-sheet,submissions/delta,dashboard}/+server.ts`
- Live-Workspace: `frontend/src/routes/live/+page.server.ts`, `frontend/src/routes/live/page-state.ts`, `frontend/src/lib/types/home.ts`
- Frontend-Regressionstests: `frontend/src/routes/live/page.server.test.ts`, `frontend/src/routes/live/page-interaction.test.ts`, die vier Live-BFF-Route-Tests

### Architektur- und Bounded-Context-Entscheidungen
- Die Security-Header-Regel bleibt zentral im SvelteKit-BFF, statt in jeder Live-Route erneut kopiert zu werden.
- Die Auswahl-Normalisierung fuer `/live` liegt in einer gemeinsamen Pure Function im Workspace-State; Loader und Browser-Controller verwenden damit dieselbe Regelquelle.
- `teaching` bleibt Besitzer des Summary-Vertrags; das Frontend leitet Links lokal ab, statt nicht dokumentierte `href`-Felder vorauszusetzen.

### Tests und Verifikation
- Frontend gezielt:
  - `npm --prefix frontend test -- --run src/lib/server/bff-proxy.test.ts src/routes/live/page.server.test.ts src/routes/live/page-interaction.test.ts src/routes/live/courses/[courseId]/units/[unitId]/summary/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/detail-sheet/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/submissions/delta/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/dashboard/server.test.ts`
- Packaging-Contract:
  - `.venv/bin/pytest -q backend/tests/packaging/test_sveltekit_live_dashboard_contract.py`
- Typecheck:
  - `npm --prefix frontend run check`
  - Ergebnis: Der neue `/live`-Scope erzeugt keine zusaetzlichen Fehler mehr; der Lauf bleibt aber wegen bereits vorhandener, fachfremder Altfehler in `submission-artifacts` und `teaching/units/[unitId]/nodes/[nodeId]` rot.

### Verbleibende offene Punkte / stale
- Kein offener MUST-FIX mehr im hier bearbeiteten `/live`-Scope.
- Der globale Frontend-Typecheck ist weiterhin nicht vollstaendig gruen, aber die verbleibenden Fehler liegen ausserhalb dieses PR-Fix-Bereichs.

### Freigabe-Einschätzung
- Fuer den bearbeiteten `/live`-Scope ist die akute Freigabe-Blockade aufgehoben.
- Vor einer repo-weiten Freigabe muessen die bereits bekannten, fachfremden `svelte-check`-Fehler separat bereinigt werden.

## Review Round – 18:18

### Review-Titel
Uncommitted-Review auf `master` fuer den aktuellen `/live`-Umbau

### Datum / Zeit
2026-04-13 18:18 Europe/Berlin

### Git-Kontext
- Branch: `master`
- Base-Ref: `master`
- Merge-Base: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Head-SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Review-Modus: Working-Tree-Review der uncommitted Änderungen gegen `HEAD`

### Reviewed Scope Summary
- Live-Read-Model-Umstellung in FastAPI und SvelteKit
- neuer Summary-Cursor im Teaching-Contract
- BFF-Read-Proxies fuer `summary`, `detail-sheet`, `dashboard`, `delta`
- Live-Workspace-State, Polling und URL-Kanonisierung
- neue Directory-Namensauflösung fuer `/live`

### Relevant Context Notes
- `HEAD`, `master` und `merge-base` sind identisch; bewertet wurde der lokale Working Tree.
- Gezielte Scope-Tests liefen gruen:
  - `.venv/bin/pytest -q backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/test_live_view_api.py backend/tests/test_teaching_live_unit_summary_api.py backend/tests/packaging/test_sveltekit_live_dashboard_contract.py`
  - `npm --prefix frontend test -- --run src/lib/server/bff-proxy.test.ts src/routes/live/page.server.test.ts src/routes/live/page-interaction.test.ts src/routes/live/courses/[courseId]/units/[unitId]/summary/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/detail-sheet/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/submissions/delta/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/dashboard/server.test.ts`

### Code References Used For Findings
- `backend/identity_access/directory.py`
- `backend/web/routes/teaching.py`
- `backend/web/routes/app.py`
- `frontend/src/routes/live/+page.server.ts`
- `frontend/src/routes/live/+page.svelte`
- `frontend/src/routes/live/page-state.ts`
- `frontend/src/lib/server/bff-proxy.ts`
- `backend/tests/test_identity_access_directory_login_labels.py`
- `backend/tests/test_live_view_api.py`
- `backend/tests/test_openapi_teaching_live_unit_contract.py`
- `backend/tests/test_teaching_live_unit_summary_api.py`
- `backend/tests/packaging/test_sveltekit_live_dashboard_contract.py`
- `frontend/src/lib/server/bff-proxy.test.ts`
- `frontend/src/routes/live/page.server.test.ts`
- `frontend/src/routes/live/page-interaction.test.ts`

### Kurzfazit
Der `/live`-Umbau ist fuer den geprueften Scope insgesamt sauber und die gezielten Regressionstests sind gruen. Offene Risiken sitzen fast ausschliesslich im neuen Directory-Cache: Der Hot-Path spart noch nicht den teuren Token-Fetch ein und der globale Dict-Cache waechst ohne Aufraeumlogik dauerhaft weiter.

### MUST-FIX
- Keine.

### SHOULD-FIX
- [BUG] Verschiebe den Keycloak-Token-Fetch hinter den Cache-Hit-Check — `backend/identity_access/directory.py:469@c942cb14888f7d9559fbaea533d06e38db03aabd` — Impact: Auch bei komplett warmem `_LIVE_STUDENT_NAME_CACHE` holt jeder `/live`-Poll weiterhin synchron ein Admin-Token; der neue Resolver reduziert damit die Last auf dem eigentlichen Hot-Path deutlich weniger als behauptet. — Fix: Ermittle erst `unresolved`; hole `kc.token()` und baue Request-Header nur, wenn nach dem Cache-Check wirklich noch Directory-Lookups noetig sind.
- [BUG] Begrenze oder bereinige den neuen globalen Live-Name-Cache aktiv — `backend/identity_access/directory.py:26@c942cb14888f7d9559fbaea533d06e38db03aabd` — Impact: `_LIVE_STUDENT_NAME_CACHE` behaelt abgelaufene Eintraege dauerhaft im Prozess; in einem langlebigen Deployment mit vielen Kursen/Schuljahren fuehrt das zu stillem, unnoetigem Speicherwachstum fuer personenbezogene Labels. — Fix: Entferne abgelaufene Keys vor dem Lesen/Schreiben oder ersetze das Dict durch einen kleinen bounded TTL-Cache mit Max-Size.

### NICE-TO-HAVE
- Keine weiteren Findings im geprueften Scope.

### TESTS
- Gezielte Backend- und Frontend-Tests fuer den neuen Live-Scope sind gruen.
- Nicht erneut geprueft: globaler `frontend`-Typecheck und repo-weite Verifikation ausserhalb dieses Scopes.

### Refactor-Budget
0.5-1.0 h. Beide Punkte liegen im selben neuen Resolver und sollten mit 2 kleinen Regressionstests abgesichert werden: `cache hit avoids token fetch` und `expired entries are pruned / cache size stays bounded`.

### Freigabe
Bedingt freigabefaehig fuer den aktuellen Scope, aber nicht ganz sauber. Vor dem naechsten Merge wuerde ich die beiden Cache-Punkte noch bereinigen, weil sie genau den neuen Hot-Path des Live-Pollings betreffen.

## Reparaturplan – 2026-04-13 18:30

### Arbeitspaket 1: Warm-Cache ohne unnötigen Keycloak-Token-Fetch
- Finding: `resolve_live_student_names_by_sub(...)` holt den Admin-Token aktuell schon vor dem Cache-Check und spart damit im Warm-Cache-Fall den teuersten Teil des Hot-Paths nicht.
- Noch offen, weil `backend/identity_access/directory.py` `kc.token()` unmittelbar nach `_KC()` aufruft, bevor feststeht, ob ueberhaupt noch ungelöste `sub`s existieren.
- Relevante Regeln: KISS, TDD und saubere Bounded-Context-Verantwortung aus `AGENTS.md`, `docs/ARCHITECTURE.md` und `docs/bounded_contexts.md`.
- Bounded Context: `identity_access` ist Owner des Directory-Adapters; `teaching` bleibt reiner Konsument.
- Wahrscheinliche Dateien: `backend/identity_access/directory.py`, `backend/tests/test_identity_access_directory_login_labels.py`
- Vorabänderungen: kein API-/OpenAPI-Change, keine Migration, keine Frontend-Arbeit.
- Test-first: neuer Resolver-Test verifiziert, dass ein zweiter Aufruf mit warmem Cache weder `requests.get(...)` noch `_KC.token()` erneut ausloest.
- Minimale Implementierung: Cache-Pruefung vor `_KC()`/`kc.token()` ziehen und Token nur bei echten Directory-Lookups anfordern.
- Acceptance: Vollstaendig gecachte `/live`-Namensauflösung verlaesst den Prozess nicht mehr fuer Token- oder User-Lookups.

### Arbeitspaket 2: Live-Name-Cache aktiv aufraeumen und begrenzen
- Finding: `_LIVE_STUDENT_NAME_CACHE` behaelt abgelaufene Eintraege dauerhaft und hat aktuell keine feste Obergrenze.
- Noch offen, weil das neue Dict nur TTL-Zeitpunkte speichert, aber weder expired Keys sweeped noch die Eintragszahl begrenzt.
- Relevante Regeln: Security-first und DSGVO-Bewusstsein aus `AGENTS.md`; personenbezogene Labels sollen nicht laenger als noetig im Prozess gehalten werden.
- Bounded Context: weiter `identity_access`.
- Wahrscheinliche Dateien: `backend/identity_access/directory.py`, `backend/tests/test_identity_access_directory_login_labels.py`
- Vorabänderungen: kein Contract-/Schema-Change.
- Test-first: neuer Test verifiziert, dass expired Keys vor Wiederverwendung entfernt werden und der Cache nach mehreren Inserts auf einer festen Obergrenze bleibt.
- Minimale Implementierung: kleine private Cache-Helper fuer prune/get/set einfuehren; expired Keys entfernen und bei Ueberlauf den aeltesten Eintrag verwerfen.
- Acceptance: Cache bleibt klein, kurzlebig und deterministisch, ohne die bestehende Label-Prioritaet zu veraendern.

## Umsetzungsstand – 2026-04-13 18:32

### Geschlossene Findings
- `[BUG]` `resolve_live_student_names_by_sub(...)` fordert im Warm-Cache-Fall keinen neuen Keycloak-Admin-Token mehr an.
- `[BUG]` `_LIVE_STUDENT_NAME_CACHE` sweeped expired Eintraege aktiv und ist jetzt auf eine kleine feste Obergrenze begrenzt.
- `[TEST]` Zwei gezielte Regressionstests sichern Warm-Cache ohne Token-Fetch sowie Pruning plus Max-Size-Cap ab.

### Verbleibende offene Punkte / stale
- Kein offenes Finding mehr aus der Review-Runde `18:18`.
- Keine stale Findings in diesem Mini-Scope.

### Geänderte Dateien / Subsysteme
- `backend/identity_access/directory.py`
- `backend/tests/test_identity_access_directory_login_labels.py`
- `docs/plan/2026-04-13-PR-fix.md`

### Architektur- und Bounded-Context-Entscheidungen
- Der Fix bleibt vollstaendig im Bounded Context `identity_access`; `teaching` und die Live-Web-Adapter mussten nicht angepasst werden.
- Die Resolver-Signatur bleibt unveraendert; es gibt keinen API- oder Schema-Drift.
- Fuer den kleinen In-Memory-Cache wird bewusst ein simples, bereits im Repo etabliertes Muster genutzt: TTL-Sweep plus deterministische Oldest-Entry-Eviction.

### Tests und Verifikation
- `.venv/bin/pytest -q backend/tests/test_identity_access_directory_login_labels.py`
- `.venv/bin/pytest -q backend/tests/test_live_view_api.py backend/tests/test_teaching_live_unit_summary_api.py`
- Ergebnis: gruen, `8 + 21` Tests erfolgreich.

### Restrisiko
- Der Resolver bleibt ein synchroner Directory-Adapter und macht fuer ungecachte `sub`s weiterhin sequentielle Admin-Requests. Das ist fuer diesen kleinen PR-Fix akzeptabel, weil die beiden dokumentierten Review-Findings gezielt geschlossen wurden, aber kein weitergehender Batch-/Async-Umbau Teil dieses Scopes war.

### Freigabe-Einschätzung
- Der dokumentierte `/live`-Cache-Nachlauf ist fuer den geprueften Scope geschlossen.
- Fuer diese beiden Findings ist die Review-Empfehlung jetzt auf freigabefaehig angehoben.

## Reparaturplan – 2026-04-13 18:46

### Arbeitspaket 1: Thread-sicherer Live-Name-Cache im `identity_access`-Adapter
- Finding: Der neue `_LIVE_STUDENT_NAME_CACHE` wird aus `asyncio.to_thread(...)`-Pfaden parallel gelesen und geschrieben; die aktuelle Dict-Iteration ist dabei nicht threadsicher.
- Noch offen, weil `backend/identity_access/directory.py` den Cache ohne gemeinsamen Lock prune/read/write bearbeitet und sich der Fehler lokal als `RuntimeError: dictionary changed size during iteration` reproduzieren laesst.
- Relevante Regeln: Security-first, KISS und saubere Bounded-Context-Grenzen aus `AGENTS.md`, `docs/ARCHITECTURE.md` und `docs/bounded_contexts.md`.
- Bounded Context: `identity_access` bleibt alleiniger Owner der Directory-/Label-Aufloesung; `teaching` konsumiert nur das Ergebnis.
- Wahrscheinliche Dateien: `backend/identity_access/directory.py`, `backend/tests/test_identity_access_directory_login_labels.py`
- Vorabänderungen: kein API-/OpenAPI-Change, keine Migration, keine Frontend-Aenderung.
- Test-first: neuer Regressionstest mit parallelem Prune/Write-Zugriff darf keinen RuntimeError mehr erzeugen.
- Minimale Implementierung: kleiner module-lokaler Lock fuer alle Cache-Helfer (`prune/get/set`) ohne Aenderung der Resolver-Signatur.
- Acceptance: Der Live-Resolver bleibt auch unter parallelen Polling-Threads stabil.

### Arbeitspaket 2: Kein doppelter Keycloak-Lookup im 404-/Fehler-Fallback
- Finding: Wenn `GET /users/{sub}` fuer `/live` kein brauchbares Ergebnis liefert, laeuft der Resolver aktuell ueber `resolve_student_login_labels_by_sub(...)` denselben Endpoint fuer dieselbe `sub` noch einmal an.
- Noch offen, weil `resolve_live_student_names_by_sub(...)` den Fallback derzeit an den allgemeinen Login-Label-Resolver delegiert, obwohl im Fehlerfall nur noch `localpart` oder `Unbekannt` benoetigt wird.
- Relevante Regeln: KISS, Performance-Sorgfalt und datensparsame Verarbeitung aus `AGENTS.md`.
- Bounded Context: weiter `identity_access`.
- Wahrscheinliche Dateien: `backend/identity_access/directory.py`, `backend/tests/test_identity_access_directory_login_labels.py`
- Vorabänderungen: kein Contract-/Schema-Change.
- Test-first: neuer Test verifiziert bei `404`, dass pro `sub` genau ein Keycloak-GET stattfindet und der Rueckgabewert direkt auf `localpart`/`Unbekannt` faellt.
- Minimale Implementierung: Fehlerfaelle unmittelbar auf lokalen Fallback abbilden, statt den allgemeinen Resolver erneut zu rufen.
- Acceptance: Ein fehlender oder problematischer Keycloak-User loest im `/live`-Pfad keinen Doppel-Request mehr aus.

### Arbeitspaket 3: Robuster `/live`-Workspace-Fallback bei fehlgeschlagenem Detail-Refresh
- Finding: `openStudent(...)` und `openTask(...)` verhindern die normale Link-Navigation sofort; wenn der lokale Detail-Refresh fehlschlaegt, bleibt die UI auf der alten Auswahl stehen.
- Noch offen, weil `frontend/src/routes/live/+page.svelte` die In-Place-Auswahl ohne Fehler-Fallback ausfuehrt, obwohl bereits kanonische `href`s vorhanden sind.
- Relevante Regeln: robuste Lehreransicht aus `AGENTS.md`, Designvertrag fuer `/live` aus `docs/DESIGN.md`, Polling-/Workspace-Semantik aus `docs/references/teaching_live.md`.
- Bounded Context: SvelteKit-Browser-BFF / Live-Workspace, kein neuer Fachlogik-Leak in `teaching`.
- Wahrscheinliche Dateien: `frontend/src/routes/live/page-state.ts`, `frontend/src/routes/live/page-interaction.test.ts`, `frontend/src/routes/live/+page.svelte`
- Vorabänderungen: kein API-/OpenAPI-Change, keine Migration.
- Test-first: neue Interaktions-Tests verifizieren, dass fehlgeschlagene Student-/Task-Auswahl auf den vorhandenen kanonischen `href` via `goto(...)` zurueckfaellt.
- Minimale Implementierung: kleiner Helper fuer „lokal versuchen, sonst navigieren“ und dessen Nutzung in `openStudent(...)` / `openTask(...)`.
- Acceptance: Der `/live`-Workspace bleibt benutzbar, auch wenn ein einzelner Detail-Reload fehlschlaegt.

## Umsetzungsstand – 2026-04-13 21:16

### Geschlossene Findings
- `[BUG]` Der globale `_LIVE_STUDENT_NAME_CACHE` ist jetzt ueber einen gemeinsamen re-entrant Lock gegen parallele `to_thread(...)`-Zugriffe abgesichert.
- `[BUG]` `resolve_live_student_names_by_sub(...)` faellt bei `404` oder leerem Keycloak-Ergebnis direkt auf `localpart` bzw. `Unbekannt` zurueck, ohne denselben `/users/{sub}`-Pfad ein zweites Mal anzufragen.
- `[BUG]` Der `/live`-Workspace faellt bei fehlgeschlagenem lokalen Student-/Task-Refresh auf die vorhandene kanonische Navigation zurueck, statt auf der alten Auswahl haengen zu bleiben.
- `[TEST]` Neue Regressionstests sichern den Cache-Race, den 404-Einfachlookup und den Browser-Fallback fuer fehlgeschlagene Live-Auswahlen ab.

### Verbleibende offene Punkte / stale
- Kein offenes Finding mehr aus dem Reparaturplan `18:46`.
- Keine stale Findings in diesem Nachlauf.

### Geänderte Dateien / Subsysteme
- `backend/identity_access/directory.py`
- `backend/tests/test_identity_access_directory_login_labels.py`
- `frontend/src/routes/live/page-state.ts`
- `frontend/src/routes/live/+page.svelte`
- `frontend/src/routes/live/page-interaction.test.ts`
- `docs/plan/2026-04-13-PR-fix.md`

### Architektur- und Bounded-Context-Entscheidungen
- Die Cache-Haertung und der 404-Fallback bleiben vollstaendig im Bounded Context `identity_access`; es war keine neue Verantwortung in `teaching` noetig.
- Der Browser-Fallback bleibt auf den SvelteKit-Live-Workspace begrenzt und nutzt bestehende kanonische `href`s, statt neue API- oder Read-Model-Regeln einzufuehren.
- Es gibt keinen API- oder Schema-Drift; `api/openapi.yml`, Migrationen und Teaching-Vertraege bleiben unveraendert.

### Design-Contract-Check
- Der `/live`-Fix aendert keine visuelle Struktur und keine CSS-Konventionen.
- Die Anpassung respektiert `docs/DESIGN.md`, weil nur die Interaktionsrobustheit des bestehenden Workspace verbessert wurde.

### Tests und Verifikation
- `.venv/bin/pytest -q backend/tests/test_identity_access_directory_login_labels.py`
- `.venv/bin/pytest -q backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_live_view_api.py`
- `npm --prefix frontend test -- --run src/routes/live/page-interaction.test.ts`
- `npm --prefix frontend test -- --run src/routes/live/page-interaction.test.ts src/routes/live/page.server.test.ts`
- Ergebnis: gruen, `10 + 19` Backend-Tests sowie `9 + 11` Frontend-Tests erfolgreich.

### Restrisiko
- Der Directory-Resolver bleibt fuer ungecachte `sub`s weiterhin ein synchroner, sequentieller Adapter. Das ist fuer diesen PR-Fix akzeptabel, weil der konkrete Stabilitaets- und Doppel-Request-Befund jetzt geschlossen ist.
- Es wurden bewusst keine zusaetzlichen repo-weiten Checks ausserhalb des Live-/Directory-Scopes nachgezogen.

### Freigabe-Einschätzung
- Fuer die in diesem PR-Fix dokumentierten Findings ist die Branch-Bereinigung jetzt abgeschlossen.
- Der bearbeitete `/live`- und Directory-Scope ist auf Basis der gezielten Regressionstests freigabefaehig.

## Review Round – 21:21

### Review-Titel
Uncommitted-Review auf `master` fuer den aktuellen `/live`-Umbau

### Datum / Zeit
2026-04-13 21:21 Europe/Berlin

### Git-Kontext
- Branch: `master`
- Base-Ref: `master`
- Merge-Base: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Head-SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Review-Modus: Working-Tree-Review der uncommitted Aenderungen gegen `HEAD`

### Reviewed Scope Summary
- Live-Read-Model-Umstellung in FastAPI und SvelteKit
- Summary-/Detail-/Delta-BFFs fuer den kanonischen `/live`-Workspace
- Polling-Cursor, URL-Kanonisierung und neues Frontend-Workspace-State
- Directory-basierte Namensauflösung fuer `/live`
- API-/Referenzvertrag und sichtbare Live-UI-Texte

### Relevant Context Notes
- `HEAD`, `master` und `merge-base` sind identisch; bewertet wurde der lokale Working Tree inklusive untracked `/live`-Dateien.
- `docs/plan/*.md` wurden nur als Kontext gelesen; primärer Bewertungsgegenstand waren Produktcode, Vertragsdateien, Tests und nutzernahe Doku.
- Fuer diese Review-Runde wurden keine Checks erneut ausgefuehrt; die Bewertung basiert auf Diff, Source-Inspection und den vorhandenen Tests im Scope.

### Code References Used For Findings
- `frontend/src/lib/server/api.ts`
- `frontend/src/lib/server/bff-proxy.ts`
- `frontend/src/routes/live/+page.server.ts`
- `frontend/src/routes/live/+page.svelte`
- `frontend/src/routes/live/page-state.ts`
- `frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/+server.ts`
- `frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/server.test.ts`
- `backend/tests/packaging/test_sveltekit_live_dashboard_contract.py`

### Kurzfazit
Der neue `/live`-Workspace ist insgesamt gut strukturiert und deutlich sauberer als der fruehere Dashboard-Pfad. Zwei Punkte wuerde ich vor dem naechsten Merge noch bereinigen: Der Read-BFF setzt die interne Same-Origin-Markierung nicht konsistent fort, und parallel bleibt ein ungenutzter `/dashboard`-BFF als zweite Browser-Oberflaeche im Repo liegen.

### MUST-FIX
- Keine.

### SHOULD-FIX
- [SEC] Reiche fuer Read-BFFs ebenfalls die interne Same-Origin-Markierung durch — `frontend/src/lib/server/bff-proxy.ts:78@c942cb14888f7d9559fbaea533d06e38db03aabd` — Impact: `proxyBackendWrite(...)` setzt bewusst `includeSameOrigin: true`, `proxyBackendRead(...)` aber nicht. Damit sehen die neuen `/live`-Read-Routen kein konsistentes internes `Origin`/`Referer`, obwohl genau dieser BFF laut Kommentar die Browser-Grenze und Same-Origin-Durchleitung absichert. Das ist heute noch kein sichtbarer Functional-Fail, aber ein unnötiger Sicherheits- und Governance-Drift zwischen Read- und Write-Pfaden. — Fix: Uebergib in `proxyBackendRead(...)` dieselbe `includeSameOrigin: true`-Option wie im Write-Proxy und sichere das mit einem kleinen Vitest fuer `backendRequest(...)`-Argumente ab.
- [CONSISTENCY] Entferne den ungenutzten Browser-BFF fuer `/live/.../dashboard` oder nimm ihn bewusst in die kanonische Architektur auf — `frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/+server.ts:1@c942cb14888f7d9559fbaea533d06e38db03aabd` — Impact: Der Packaging-Contract erklaert den kanonischen `/live`-Pfad inzwischen ausdruecklich als Summary-/Detail-/Delta-Modell ohne Loader-Nutzung von `/dashboard` (`backend/tests/packaging/test_sveltekit_live_dashboard_contract.py:28@c942cb14888f7d9559fbaea533d06e38db03aabd`). Gleichzeitig fuehren Route und Test (`frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/server.test.ts:12@c942cb14888f7d9559fbaea533d06e38db03aabd`) weiterhin eine zweite Browser-Read-Flaeche mit. Das verdoppelt Wartungsaufwand und laesst offen, welcher Pfad fuer Browser wirklich noch supported ist. — Fix: Entferne die ungenutzte Dashboard-BFF samt Test oder dokumentiere klar, warum sie weiterhin Teil des oeffentlichen Browser-Surfaces bleibt.

### NICE-TO-HAVE
- Keine weiteren Findings im geprueften Scope.

### TESTS
- Vorhandene Scope-Tests decken Cursor, Header-Weitergabe, URL-Kanonisierung und Workspace-Interaktionen bereits gut ab.
- Nicht erneut ausgefuehrt in dieser Review-Runde: `pytest`, `vitest`, globaler Typecheck und repo-weite Verifikation.
- Fuer den ersten SHOULD-FIX fehlt noch ein Test, der nicht nur Response-Header, sondern auch das `includeSameOrigin`-Verhalten des Read-BFF gegen `backendRequest(...)` absichert.

### Refactor-Budget
0.5-1.0 h. Beide Punkte sind klein und lokal: ein kompakter Proxy-Nachzug plus das Entfernen oder explizite Absichern eines uebrig gebliebenen BFF-Pfads.

### Freigabe
Bedingt freigabefaehig. Der neue `/live`-Pfad wirkt tragfaehig, aber ich wuerde die beiden SHOULD-FIX-Punkte noch vor dem naechsten Merge schliessen, damit Sicherheitskonsistenz und Architektur-Surface wieder sauber zusammenpassen.

## Reparaturplan – 2026-04-14 10:17

### Arbeitspaket 1: Read-BFF reicht Same-Origin-Markierung konsistent weiter
- Finding: `proxyBackendRead(...)` setzt im Gegensatz zu `proxyBackendWrite(...)` kein `includeSameOrigin: true`.
- Noch offen, weil `frontend/src/lib/server/bff-proxy.ts` Read-Requests nur mit `{ method: "GET" }` an `backendRequest(...)` weiterreicht.
- Relevante Regeln: Security-first aus `AGENTS.md`; SvelteKit bleibt laut `docs/ARCHITECTURE.md` der kanonische Browser-BFF; die Live-Referenz fordert konsistente Browser-Grenzen fuer Summary/Detail/Delta.
- Bounded Context: Frontend / Browser-BFF, keine Fachlogik in `teaching`.
- Wahrscheinliche Dateien: `frontend/src/lib/server/bff-proxy.ts`, `frontend/src/lib/server/bff-proxy.test.ts`
- Vorabänderungen: kein `api/openapi.yml`-Change, keine Migration, keine Backend-API-Aenderung.
- Test-first: Vitest erweitert die bestehende Read-BFF-Suite um eine Erwartung auf `backendRequest(..., { method: "GET", includeSameOrigin: true })`.
- Minimale Implementierung: `proxyBackendRead(...)` setzt dieselbe Option wie der Write-Proxy.
- Acceptance: Read- und Write-BFFs verhalten sich fuer die interne Same-Origin-Durchleitung identisch.

### Arbeitspaket 2: Ungenutzten `/live/.../dashboard`-Browser-BFF entfernen
- Finding: Die browserseitige SvelteKit-Route `/live/.../dashboard` existiert noch, obwohl der Packaging-Contract den kanonischen `/live`-Pfad bereits auf Summary/Detail/Delta festlegt.
- Noch offen, weil unter `frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/` weiterhin Route plus Test liegen, ohne dass der aktuelle Workspace sie nutzt.
- Relevante Regeln: KISS und lehrbare Architektur aus `AGENTS.md`; `docs/ARCHITECTURE.md` priorisiert SvelteKit als klare Browser-Oberflaeche; `docs/DESIGN.md` bleibt unveraendert, weil keine visuelle Struktur angepasst wird.
- Bounded Context: Frontend / Live-Workspace.
- Wahrscheinliche Dateien: `frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/+server.ts`, `frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/server.test.ts`
- Vorabänderungen: kein Contract-/Schema-Change; FastAPI-Backend-Endpunkt bleibt unberuehrt.
- Test-first: bestehender Packaging-Contract bleibt die Spezifikation; nach dem Entfernen duerfen keine Browser-Referenzen auf den SvelteKit-`/dashboard`-Pfad uebrig bleiben.
- Minimale Implementierung: ungenutzte SvelteKit-Route und zugehoerigen Test loeschen.
- Acceptance: Der kanonische Browser-Surface fuer `/live` besteht nur noch aus Summary/Detail/Delta.

## Umsetzungsstand – 2026-04-14 10:18

### Geschlossene Findings
- `[SEC]` `proxyBackendRead(...)` reicht jetzt wie der Write-BFF `includeSameOrigin: true` an `backendRequest(...)` weiter.
- `[CONSISTENCY]` Die ungenutzte SvelteKit-Route `/live/.../dashboard` samt zugehoerigem Vitest ist entfernt; der Browser-Surface fuer den kanonischen `/live`-Workspace besteht nur noch aus Summary/Detail/Delta.
- `[TEST]` Der Read-BFF ist jetzt explizit auf die Same-Origin-Weitergabe getestet, nicht nur auf Response-Header.

### Verbleibende offene Punkte / stale
- Die vom User referenzierte Stelle `docs/plan/2026-04-13-PR-fix.md:366` war auf dem aktuellen Branch bereits geschlossen/stale: Warm-Cache-Token-Fetch, Cache-Pruning und Max-Size-Cap sind im `identity_access`-Adapter schon umgesetzt und durch bestehende Tests abgesichert.
- Kein offenes Finding mehr aus der Review-Runde `21:21` in diesem Mini-Scope.

### Geänderte Dateien / Subsysteme
- `frontend/src/lib/server/bff-proxy.ts`
- `frontend/src/lib/server/bff-proxy.test.ts`
- SvelteKit-Live-BFF `frontend/src/routes/live/courses/[courseId]/units/[unitId]/dashboard/` entfernt
- `docs/plan/2026-04-13-PR-fix.md`

### Architektur- und Bounded-Context-Entscheidungen
- Der Fix bleibt vollstaendig im Frontend-/Browser-BFF-Scope; es gibt keinen Drift in `teaching`, `identity_access` oder im FastAPI-API-Vertrag.
- Der kanonische `/live`-Workspace folgt jetzt wieder eindeutiger der in `docs/references/teaching_live.md` beschriebenen Summary-/Detail-/Delta-Struktur.
- Die interne Same-Origin-Markierung wird fuer Read- und Write-BFFs konsistent ueber denselben `backendRequest(...)`-Pfad durchgereicht.

### Design-Contract-Check
- Kein visueller Eingriff in den `/live`-Workspace.
- `docs/DESIGN.md` bleibt eingehalten, weil nur Browser-BFF-Verhalten und ein ungenutzter Route-Surface bereinigt wurden.

### Tests und Verifikation
- `npm --prefix frontend test -- --run src/lib/server/bff-proxy.test.ts`
- `.venv/bin/pytest -q backend/tests/packaging/test_sveltekit_live_dashboard_contract.py`
- zusaetzlicher Suchcheck: keine verbleibende Browser-Referenz auf den entfernten SvelteKit-`/dashboard`-BFF im aktuellen `/live`-Scope gefunden
- Ergebnis: gruen, `4` Vitest-Tests und `1` Packaging-Test erfolgreich.

### Restrisiko
- Der FastAPI-Endpunkt `/api/live/views/.../dashboard` bleibt weiterhin im Backend bestehen. Das ist fuer diesen PR-Fix bewusst akzeptiert, weil nur der ungenutzte browserseitige SvelteKit-Surface reduziert wurde.
- Es wurden keine breiteren Frontend- oder repo-weiten Checks ausserhalb des direkt betroffenen Scopes nachgezogen.

### Freigabe-Einschätzung
- Die beiden offenen SHOULD-FIX-Punkte aus der Review-Runde `21:21` sind fuer den bearbeiteten Scope geschlossen.
- Fuer diesen Mini-Scope ist die Freigabe-Einschaetzung auf freigabefaehig angehoben.
