# PR Review - Uncommitted Changes on master

Date: 2026-04-14 10:42:54 CEST

## Review Round - 10:42

- Branch: `master`
- Base ref: `master`
- Merge-base SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Head SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Reviewed scope:
  - uncommitted changes on `master`
  - live dashboard summary/detail/delta refactor in backend and SvelteKit
  - live learner-name resolution and H5P workspace refresh coupling
- Relevant context notes:
  - `HEAD`, `master` and merge-base are identical; the review covers the working tree diff only.
  - Focused review surfaces: API contract, live polling semantics, performance in hot paths, and consistency with teaching live docs.
- Code references used for findings:
  - `backend/web/routes/teaching.py:5638`
  - `backend/web/routes/teaching.py:5687`
  - `backend/identity_access/directory.py:507`
  - `frontend/src/routes/live/page-state.ts:399`
  - `frontend/src/routes/live/page-interaction.test.ts:196`
  - `backend/tests/test_teaching_live_unit_summary_api.py:199`
  - `docs/references/teaching_live.md:9`

## Kurzfazit

Die Richtung ist sinnvoll, aber der neue Cursor-Pfad für `/live` ist API-seitig noch nicht sauber geschlossen. Dazu kommt eine neue serielle Keycloak-Namensauflösung im Hot-Path der Live-Summary. Die Scope-Tests sind grün, decken genau den riskanten Cursor-Fall aber noch nicht ab.

## MUST-FIX

- [API] Cursor-Vertrag ist zwischen Summary, Delta und Client-Refresh inkonsistent — `backend/web/routes/teaching.py:5638@c942cb14888f` — Impact: Die Summary erzeugt `cursor` aus der App-Uhr vor dem eigentlichen Datenaufbau, und `frontend/src/routes/live/page-state.ts:415@c942cb14888f` bevorzugt diesen Wert später sogar gegenüber dem Delta-Cursor. Das widerspricht der dokumentierten Polling-Semantik in `docs/references/teaching_live.md:60-63` und öffnet unter Host/DB-Clock-Skew ein Verlustfenster für Änderungen. — Fix: `cursor` aus derselben DB-Sicht wie die Summary ableiten oder nach dem Summary-Reload beim Delta-Cursor bleiben; Doku, OpenAPI und Runtime auf genau eine Semantik festziehen.

## SHOULD-FIX

- [TEST] Die neuen Tests prüfen nur den Gleichheitsfall `summary.cursor === delta.changed_at` — `frontend/src/routes/live/page-interaction.test.ts:196@c942cb14888f` — Impact: Die Suite bleibt grün, obwohl der problematische Divergenzfall ungetestet bleibt; `backend/tests/test_teaching_live_unit_summary_api.py:199@c942cb14888f` validiert aktuell nur, dass irgendein Cursor vorhanden ist. — Fix: Einen Backend- und einen Controller-Test ergänzen, in denen Summary-Cursor, Delta-Cursor und geänderte Zellen bewusst auseinanderlaufen.
- [BUG] Die neue Namensauflösung hängt den Live-Hot-Path an serielle Keycloak-Admin-Requests — `backend/identity_access/directory.py:534@c942cb14888f` — Impact: Jeder Cold-Cache-Refresh macht pro unaufgelöstem Lernenden einen blockierenden `/users/{sub}`-Call; `backend/web/routes/teaching.py:5687@c942cb14888f` ruft das direkt im Summary-Pollingpfad auf. Bei Klassenstärke und trägem Keycloak wird die Live-Ansicht damit unnötig langsam oder timeout-anfällig. — Fix: Namen pro Kurs/Request gebündelt auflösen oder mindestens parallelisieren und als Roster-Mapping cachen.

## NICE-TO-HAVE

- Keine zusätzlichen Punkte mit höherem Hebel als die drei Punkte oben.

## TESTS

- `.venv/bin/pytest -q backend/tests/test_live_view_api.py backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/packaging/test_sveltekit_live_dashboard_contract.py` -> `35 passed`
- `npm test -- --run src/routes/live/page.server.test.ts src/routes/live/page-interaction.test.ts src/lib/server/bff-proxy.test.ts src/lib/components/H5PTaskPlayer.test.ts src/lib/learning-unit/workspace.test.ts src/routes/learning/courses/[courseId]/units/[unitId]/page.server.test.ts src/lib/components/learning-unit/LearningUnitContentWorkspace.test.ts` -> `37 passed`
- `npm test -- --run src/routes/live/courses/[courseId]/units/[unitId]/summary/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/detail-sheet/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/submissions/delta/server.test.ts` -> `4 passed`
- `npm run check` ist nicht grün, aber wegen bereits bestehender, nicht scope-spezifischer Typecheck-Fehler in anderen Teaching-Dateien.

## Refactor-Budget

- `0.5-1 Tag` für Cursor-Semantik sauber schließen, Regressionstests ergänzen und die Namensauflösung aus dem Hot-Path ziehen.

## Freigabe

- Nicht freigeben, bevor der Cursor-Vertrag geschlossen ist.

## Reparaturplan – 2026-04-14 10:52 CEST

### Arbeitspaket 1: Cursor-Vertrag zwischen Summary, Delta und Client schließen

- Finding: `MUST-FIX [API] Cursor-Vertrag ist zwischen Summary, Delta und Client-Refresh inkonsistent`
- Weiter offen, weil `backend/web/routes/teaching.py` den Summary-Cursor noch aus der App-Uhr erzeugt und `frontend/src/routes/live/page-state.ts` diesen Wert beim Polling-Refresh weiter bevorzugt.
- Relevante Regeln: Contract-First (`api/openapi.yml` zuerst), TDD, KISS, Polling-Semantik aus `docs/references/teaching_live.md`, Designvertrag fuer `/live` ohne neue UI-Struktur.
- Bounded Context Owner: `teaching`
- Voraussichtliche Dateien: `api/openapi.yml`, `docs/references/teaching_live.md`, `backend/web/routes/teaching.py`, `backend/tests/test_teaching_live_unit_summary_api.py`, `frontend/src/routes/live/page-state.ts`, `frontend/src/routes/live/page-interaction.test.ts`
- Vertrag/Migration/Config zuerst: OpenAPI- und Doku-Text zuerst; keine Migration erwartet.
- Test-first: Backend-Test fuer divergierenden Summary-/Delta-Cursor und Frontend-Controller-Test, in dem `delta.changed_at` bewusst neuer als `summary.cursor` ist.
- Minimale Implementierung: Summary-Cursor aus derselben DB-Zeitbasis wie die Summary ableiten und im Client nach Delta-Treffern nur den Delta-Fortschritt als naechsten Cursor behalten.
- Akzeptanzcheck: Folge-Poll mit dem gespeicherten Cursor verliert keine Aenderung; Dokumentation, OpenAPI und Runtime beschreiben dieselbe Semantik.

### Arbeitspaket 2: Testluecke fuer den Divergenzfall schliessen

- Finding: `SHOULD-FIX [TEST] Tests pruefen nur den Gleichheitsfall`
- Weiter offen, weil die bestehenden Tests nur `summary.cursor === delta.changed_at` modellieren und den eigentlichen Risk-Fall nicht erzwingen.
- Relevante Regeln: TDD, Contract-First, Lernbarkeit durch klare Regressionstests.
- Bounded Context Owner: `teaching`
- Voraussichtliche Dateien: `backend/tests/test_teaching_live_unit_summary_api.py`, `frontend/src/routes/live/page-interaction.test.ts`
- Vertrag/Migration/Config zuerst: keine weiteren Aenderungen.
- Test-first: Neue Regressionstests scheitern zuerst auf der aktuellen Cursor-Prioritaet.
- Minimale Implementierung: Nur die Cursor-Ableitung und Cursor-Fortschreibung anpassen, bis die neuen Tests gruen sind.
- Akzeptanzcheck: Die vorhandenen Scopes bleiben gruen und die neuen Tests decken den Divergenzfall explizit ab.

### Arbeitspaket 3: Live-Namensaufloesung aus dem seriellen Hot-Path holen

- Finding: `SHOULD-FIX [BUG] serielle Keycloak-Admin-Requests im Live-Hot-Path`
- Weiter offen, weil `resolve_live_student_names_by_sub(...)` uncached Lernende sequentiell ueber `/users/{sub}` aufloest.
- Relevante Regeln: KISS, Security-first (ein Token, keine Zusatzdaten), Clean Architecture mit Zustaendigkeit im Kontext `identity_access`.
- Bounded Context Owner: `identity_access`
- Voraussichtliche Dateien: `backend/identity_access/directory.py`, `backend/tests/test_identity_access_directory_login_labels.py`
- Vertrag/Migration/Config zuerst: keine API- oder Schema-Aenderung; keine Migration.
- Test-first: Resolver-Test fuer mehrere uncached Subs mit genau einem Token-Abruf und gebuendelten parallelen Lookups.
- Minimale Implementierung: Direkte `/users/{sub}`-Lookups beibehalten, aber uncached Subs bounded-parallel mit gemeinsamer Token-Nutzung aufloesen; Cache- und Fallback-Semantik unveraendert lassen.
- Akzeptanzcheck: Warm-Cache-Faelle erzeugen keine Zusatzrequests, Cold-Cache-Faelle bleiben semantisch gleich und vermeiden serielle Latenz.

## Umsetzungsstand – 2026-04-14 10:55 CEST

- Geschlossen:
  - `MUST-FIX [API] Cursor-Vertrag ist zwischen Summary, Delta und Client-Refresh inkonsistent`
  - `SHOULD-FIX [TEST] Tests pruefen nur den Gleichheitsfall`
  - `SHOULD-FIX [BUG] serielle Keycloak-Admin-Requests im Live-Hot-Path`
- Offen:
  - Kein offener MUST-FIX aus diesem Dokument.
  - Keine direkt zusammenhaengenden SHOULD-FIX-Punkte mehr aus dem verifizierten Scope.
- Stale:
  - Keine Findings waren nach Re-Check stale.
- Geaenderte Dateien/Subsyteme:
  - `docs/plan/2026-04-14-PR-fix.md`
  - `api/openapi.yml`
  - `docs/references/teaching_live.md`
  - `backend/web/routes/teaching.py`
  - `backend/identity_access/directory.py`
  - `backend/tests/test_teaching_live_unit_summary_api.py`
  - `backend/tests/test_identity_access_directory_login_labels.py`
  - `frontend/src/routes/live/page-state.ts`
  - `frontend/src/routes/live/page-interaction.test.ts`
- Architektur- und Bounded-Context-Entscheidungen:
  - Cursor-Semantik bleibt im Kontext `teaching`; der Summary-Seed wird jetzt aus der DB-Zeitbasis abgeleitet statt aus der Host-Uhr.
  - Die direkte Lernenden-Namensaufloesung bleibt im Kontext `identity_access`; nur die Ausfuehrung wurde bounded-parallelisiert, ohne die Fallback-Semantik zu aendern.
  - Der SvelteKit-Controller konsumiert den Delta-Cursor jetzt als autoritativen Polling-Fortschritt und benutzt den Summary-Reload nur fuer das UI-Read-Model.
- Design-Contract-Check:
  - Keine visuelle oder strukturelle `/live`-Layoutaenderung; der Fix beschraenkt sich auf Polling-Controller-Logik und respektiert den bestehenden Workspace-/Detailpanel-Vertrag aus `docs/DESIGN.md`.
- Tests ausgefuehrt:
  - `.venv/bin/pytest -q backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_teaching_live_unit_delta_api.py backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/packaging/test_sveltekit_live_dashboard_contract.py` -> `33 passed`
  - `npm test -- --run src/routes/live/page-interaction.test.ts` -> `10 passed`
- Residual Risk:
  - Der Summary-Cursor nutzt jetzt die DB-Zeitbasis ueber einen separaten DB-Read. Das schliesst den verifizierten Host/DB-Skew-Fehler, ohne den Summary-Aufbau tiefer zu refactoren.
  - Breitere Performance-Optimierungen fuer rosterweite Namens-Mappings oder weitere `/live`-Hot-Paths sind bewusst nicht Teil dieses gezielten PR-Fix.
- Aktualisierte Freigabeempfehlung:
  - Die in diesem Dokument blockierende Cursor-Inkonsistenz ist geschlossen; aus Sicht dieses PR-Fix-Dokuments besteht kein verbleibender Freigabeblocker mehr.

## Review Round - 11:06

- Branch: `master`
- Base ref: `master`
- Merge-base SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Head SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Reviewed scope:
  - uncommitted changes on `master`
  - live dashboard summary/detail/delta refactor in backend and SvelteKit
  - live learner-name resolution and learner workspace graph refresh wiring
- Relevant context notes:
  - `HEAD`, `master` and merge-base are identical; the review covers the working tree diff only.
  - The previous review round in this file is now partly stale because the cursor logic and live name lookup have changed again.
  - This round is based on the current working tree plus targeted backend/frontend test execution.
- Code references used for findings:
  - `backend/tests/test_identity_access_directory_login_labels.py:291`
  - `backend/identity_access/directory.py:559`
  - `api/openapi.yml:11397`
  - `docs/references/teaching_live.md:10`
  - `backend/web/routes/teaching.py:2034`
  - `backend/web/routes/teaching.py:5666`

## Kurzfazit

Die Live-Änderungen sind nah an freigabefähig, aber der Scope ist aktuell nicht vollständig grün: Ein Python-Regressionstest ist rot, und der dokumentierte Summary-Cursor-Vertrag ist strenger als die tatsächliche Runtime-Semantik. Die zielgerichteten Frontend-Tests sind dagegen grün.

## MUST-FIX

- [TEST] Der aktuelle Scope ist im Backend-Testlauf rot — `backend/tests/test_identity_access_directory_login_labels.py:291@c942cb14888f` — Impact: Die neue Parallelisierung in `resolve_live_student_names_by_sub(...)` verarbeitet Futures per `as_completed(...)` in `backend/identity_access/directory.py:559@c942cb14888f`, aber der Test erwartet noch eine serielle URL-Reihenfolge. Damit ist der verifizierte Scope derzeit nicht release-ready. — Fix: Entkopple den Test von der Request-Reihenfolge oder stelle im Code eine explizit deterministische Reihenfolge her.

## SHOULD-FIX

- [API] OpenAPI und Referenzdoku versprechen mehr als die Runtime tatsächlich garantiert — `api/openapi.yml:11397@c942cb14888f` — Impact: `api/openapi.yml` und `docs/references/teaching_live.md:10@c942cb14888f` sprechen von einem Cursor aus demselben Summary-Snapshot. Die Runtime erzeugt ihn aber in `_summary_snapshot_cursor(...)` via separater DB-Verbindung und bereits vor dem Summary-Aufbau in `backend/web/routes/teaching.py:5666@c942cb14888f`. Das ist robust gegen Host/DB-Skew, aber kein echter Same-Snapshot-Vertrag. — Fix: Entweder den Cursor wirklich in derselben DB-Sicht erzeugen oder den Vertrag in OpenAPI/Doku auf die tatsächlich garantierte Semantik abschwächen.

## NICE-TO-HAVE

- Keine zusätzlichen Punkte mit höherem Hebel als die beiden Punkte oben.

## TESTS

- `.venv/bin/pytest -q backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_live_view_api.py backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/packaging/test_sveltekit_live_dashboard_contract.py` -> `36 passed, 1 failed`
- `cd frontend && npm test -- --run src/routes/live/page.server.test.ts src/routes/live/page-interaction.test.ts src/lib/server/bff-proxy.test.ts src/lib/components/H5PTaskPlayer.test.ts src/routes/learning/courses/[courseId]/units/[unitId]/page.server.test.ts src/lib/components/learning-unit/LearningUnitContentWorkspace.test.ts src/lib/learning-unit/workspace.test.ts` -> `38 passed`
- `cd frontend && npm run check` -> nicht grün wegen bereits bestehender, fachfremder TypeScript-/Svelte-Fehler in Teaching-Editor- und Submission-Artifact-Dateien

## Refactor-Budget

- `0.25-0.5 Tag` für Testvertrag bereinigen und Cursor-Vertrag zwischen Doku/OpenAPI und Runtime wieder exakt zusammenziehen.

## Freigabe

- Nicht freigeben, bevor der rote Python-Test behoben ist.

## Reparaturplan – 2026-04-14 11:11 CEST

### Arbeitspaket 1: Deterministische Cache-Semantik trotz paralleler Namensauflösung

- Finding: `MUST-FIX [TEST] Der aktuelle Scope ist im Backend-Testlauf rot`
- Weiter offen, weil `resolve_live_student_names_by_sub(...)` die Keycloak-Lookups zwar sinnvoll parallelisiert, Cache und Ergebnis-Map aber in Future-Completion-Reihenfolge statt in Eingabereihenfolge befuellt. Dadurch wird die LRU-artige Cache-Begrenzung nondeterministisch.
- Relevante Regeln: TDD, KISS, Security-first ohne Zusatzdaten, bounded context `identity_access`.
- Bounded Context Owner: `identity_access`
- Voraussichtliche Dateien: `backend/tests/test_identity_access_directory_login_labels.py`, `backend/identity_access/directory.py`
- Vertrag/Migration/Config zuerst: keine API- oder Schema-Aenderung; keine Migration.
- Test-first: Der bestehende pytest-Regressionsfall bleibt rot und dient als Akzeptanzanker fuer deterministische Cache-Eviction bei weiter parallelen Requests.
- Minimale Implementierung: Futures weiter parallel ausfuehren, Ergebnisse aber gesammelt und anschliessend in `unresolved`-Reihenfolge in Cache und Rueckgabe uebernehmen.
- Akzeptanzcheck: Test ist gruen; Token wird weiter genau einmal geholt; die Requests bleiben parallelisierbar, aber Cache-Inhalt und Rueckgabereihenfolge sind stabil.

### Arbeitspaket 2: Cursor-Vertrag auf die tatsaechlich garantierte Runtime-Semantik zurueckziehen

- Finding: `SHOULD-FIX [API] OpenAPI und Referenzdoku versprechen mehr als die Runtime tatsächlich garantiert`
- Weiter offen, weil OpenAPI und `docs/references/teaching_live.md` von demselben Summary-Snapshot sprechen, waehrend `_summary_snapshot_cursor(...)` den Seed ueber einen separaten DB-Read liefert.
- Relevante Regeln: Contract-First, TDD, Clean Architecture im Kontext `teaching`, keine unnötige Repo-Aufweitung fuer einen bereits geschlossenen Skew-Bug.
- Bounded Context Owner: `teaching`
- Voraussichtliche Dateien: `api/openapi.yml`, `docs/references/teaching_live.md`, `backend/tests/test_openapi_teaching_live_unit_contract.py`
- Vertrag/Migration/Config zuerst: OpenAPI zuerst, danach Contract-Test und Referenzdoku; keine Migration.
- Test-first: Contract-Test auf die neue Beschreibung erweitern, damit der abgeschwaechte Cursor-Vertrag explizit abgesichert ist.
- Minimale Implementierung: Beschreibung auf „DB-based initial poll cursor“ / „host-DB-skew robust seed“ anpassen; Runtime unveraendert lassen.
- Akzeptanzcheck: OpenAPI-Test ist gruen, Referenzdoku spricht dieselbe Semantik, und die bestehende Cursor-Regression gegen Host/DB-Skew bleibt gruen.

## Umsetzungsstand – 2026-04-14 11:14 CEST

- Geschlossen:
  - `MUST-FIX [TEST] Der aktuelle Scope ist im Backend-Testlauf rot`
  - `SHOULD-FIX [API] OpenAPI und Referenzdoku versprechen mehr als die Runtime tatsächlich garantiert`
- Offen:
  - Kein offener MUST-FIX aus diesem Review-Durchlauf.
  - Keine direkt zusammenhaengenden SHOULD-FIX-Punkte mehr im verifizierten Scope.
- Stale:
  - Die Formulierung des MUST-FIX aus 11:06 war nach Re-Check zu eng: Nicht nur die Request-Reihenfolge, sondern die gesamte Cache-Belegung war nondeterministisch. Der Test wurde daher auf die fachliche Parallelisierungs-Semantik statt auf serielle Request-Reihenfolge fokussiert.

## Review Round - 11:24

- Branch: `master`
- Base ref: `master`
- Merge-base SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Head SHA: `c942cb14888f7d9559fbaea533d06e38db03aabd`
- Reviewed scope:
  - uncommitted changes on `master`
  - live summary/detail/delta refactor across FastAPI and SvelteKit
  - live learner-name resolution and learning workspace graph refresh wiring
- Relevant context notes:
  - `HEAD`, `master` and merge-base are identical; this round reviews the current working tree only.
  - Targeted backend and frontend scope tests are green.
  - `npm run check` is still red, but only because of pre-existing type issues in unrelated teaching editor and submission-artifact files.
- Code references used for findings:
  - `backend/web/routes/teaching.py:2034`
  - `backend/web/routes/teaching.py:5666`
  - `frontend/src/routes/live/page-state.ts:399`
  - `frontend/src/routes/live/page-state.ts:420`
  - `frontend/src/routes/live/page-state.ts:274`
  - `api/openapi.yml:11397`
  - `docs/references/teaching_live.md:10`

## Kurzfazit

Der neue `/live`-Pfad ist insgesamt tragfähig und die gezielten Scopes sind grün. Zwei Risiken bleiben aber offen: Der dokumentierte DB-basierte Cursor kann zur Host-Uhr zurückfallen, und der Poll-Refresh hält Detaildaten nicht garantiert synchron, wenn sich die kanonische Task-Auswahl ohne aktive Zelländerung verschiebt.

## MUST-FIX

- [BUG] Poll-Refresh kann eine neue `selected_task_id` mit altem Detailinhalt kombinieren — `frontend/src/routes/live/page-state.ts:420@c942cb14888f` — Impact: `poll()` lädt Detail nur, wenn genau die bisher offene Zelle in `result.cells` auftaucht. Wenn `normalizeLiveSelection(...)` nach dem Summary-Reload auf eine andere Task kippt, bleibt `state.detail` unverändert und `buildDashboardViewModel(...)` bindet dieses alte Detail trotzdem als `selected_task_detail` an die neue Auswahl (`frontend/src/routes/live/page-state.ts:274@c942cb14888f`). Dadurch kann die rechte Detailansicht in seltenen, aber realen Re-Kanonisierungsfällen fachlich falsche Submission-Daten zeigen. — Fix: Detail neu laden oder auf `null` setzen, sobald sich die normalisierte Auswahl nach `fetchSummary()` ändert, auch wenn `activeCellChanged` false ist; ergänze einen Vitest für diesen Re-Kanonisierungsfall.

## SHOULD-FIX

- [API] Der Cursor-Vertrag verspricht einen DB-basierten Seed, die Runtime fällt aber still auf die Host-Uhr zurück — `backend/web/routes/teaching.py:2034@c942cb14888f` — Impact: OpenAPI und Referenzdoku beschreiben `cursor` als DB-clock-basierten, skew-robusten Seed (`api/openapi.yml:11397@c942cb14888f`, `docs/references/teaching_live.md:10@c942cb14888f`). `_summary_snapshot_cursor(...)` schluckt jedoch jede Exception und gibt dann `datetime.now(...)` zurück. Bei DSN-/Connect-/Query-Problemen lebt damit genau die Clock-Skew-Gefahr wieder auf, die der neue Vertrag eigentlich ausschließen soll, und der Drift bleibt im Betrieb unsichtbar. — Fix: Entweder den Fallback als expliziten Fehler behandeln oder den abgeschwächten Fallback-Vertrag dokumentieren und testen; stilles Zurückfallen auf die App-Uhr sollte nicht unkommentiert Teil des API-Verhaltens sein.

## NICE-TO-HAVE

- Keine weiteren Punkte mit höherem Hebel im geprüften Scope.

## TESTS

- `.venv/bin/pytest -q backend/tests/test_live_view_api.py backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/packaging/test_sveltekit_live_dashboard_contract.py` -> `37 passed`
- `cd frontend && npm test -- --run src/routes/live/page.server.test.ts src/routes/live/page-interaction.test.ts src/lib/server/bff-proxy.test.ts src/routes/live/courses/[courseId]/units/[unitId]/summary/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/detail-sheet/server.test.ts src/routes/live/courses/[courseId]/units/[unitId]/submissions/delta/server.test.ts src/lib/components/H5PTaskPlayer.test.ts src/lib/components/learning-unit/LearningUnitContentWorkspace.test.ts src/lib/learning-unit/workspace.test.ts src/routes/learning/courses/[courseId]/units/[unitId]/page.server.test.ts` -> `42 passed`
- `cd frontend && npm run check` -> weiter rot wegen bereits bestehender fachfremder Fehler in `src/lib/utils/submission-artifacts.test.ts` und `src/routes/teaching/units/[unitId]/nodes/[nodeId]/+page.*`

## Refactor-Budget

- `0.5 Tag` fuer den Polling-Rekanonisierungsfall plus das Schliessen des Cursor-Fallback-Vertrags.

## Freigabe

- Noch nicht freigeben, bevor der Polling-/Detail-Sync sauber geschlossen ist. Der Cursor-Fallback ist danach der naechste sinnvolle Nachzug im gleichen Themenblock.
- Geaenderte Dateien/Subsyteme:
  - `docs/plan/2026-04-14-PR-fix.md`
  - `backend/tests/test_identity_access_directory_login_labels.py`
  - `backend/identity_access/directory.py`
  - `api/openapi.yml`
  - `backend/tests/test_openapi_teaching_live_unit_contract.py`
  - `docs/references/teaching_live.md`
- Architektur- und Bounded-Context-Entscheidungen:
  - `identity_access` behaelt die bounded-parallele Keycloak-Namensauflösung fuer `/live`, commitet Cache-Eintraege aber wieder deterministisch in Roster-Reihenfolge.
  - `teaching` behaelt den bestehenden DB-clock-basierten Summary-Cursor; nur der oeffentliche Vertrag wurde auf die tatsaechlich garantierte Semantik zurueckgezogen.
- Design-Contract-Check:
  - Keine UI-Struktur oder visuelle Svelte-Komponente angepasst; der Scope betrifft nur Backend-Resolver, API-Vertrag und Referenzdoku.
- Tests ausgefuehrt:
  - `.venv/bin/pytest -q backend/tests/test_identity_access_directory_login_labels.py::test_resolve_live_student_names_by_sub_prunes_expired_entries_and_caps_cache_size backend/tests/test_openapi_teaching_live_unit_contract.py::test_openapi_has_unit_live_summary_path_and_cache_header` -> `2 passed`
  - `.venv/bin/pytest -q backend/tests/test_identity_access_directory_login_labels.py backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_openapi_teaching_live_unit_contract.py backend/tests/packaging/test_sveltekit_live_dashboard_contract.py` -> `28 passed`
- Residual Risk:
  - Der Summary-Cursor bleibt ein separater DB-Read und damit bewusst kein echter Same-Snapshot-Cursor. Diese Entscheidung ist dokumentiert und getestet, aber ein spaeterer Repo-Refactor koennte ihn weiter an den Summary-Aufbau heranziehen.
  - Der Resolver skaliert fuer den aktuellen Scope robuster; weitergehende rosterweite Batch-Endpunkte im Keycloak-Adapter sind nicht Teil dieses PR-Fix.
- Aktualisierte Freigabeempfehlung:
  - Der in diesem Dokument blockierende Backend-Test ist geschlossen, und der Cursor-Vertrag ist wieder konsistent mit der Runtime. Aus Sicht dieses PR-Fix-Dokuments besteht kein verbleibender Freigabeblocker mehr.

## Reparaturplan – 2026-04-14 11:31 CEST

### Arbeitspaket 1: Poll-Rekanonisierung haelt Auswahl und Detail synchron

- Finding: `MUST-FIX [BUG] Poll-Refresh kann eine neue selected_task_id mit altem Detailinhalt kombinieren`
- Weiter offen, weil `frontend/src/routes/live/page-state.ts` bei `poll()` die kanonische Auswahl nach dem Summary-Reload aktualisiert, das Detail aber nur neu laedt, wenn exakt die zuvor offene Zelle im Delta auftaucht.
- Relevante Regeln: TDD, KISS, Designvertrag fuer `/live` ohne neue UI-Struktur, Lernbarkeit durch klaren Controller-Zustand.
- Bounded Context Owner: `teaching`
- Voraussichtliche Dateien: `frontend/src/routes/live/page-state.ts`, `frontend/src/routes/live/page-interaction.test.ts`
- Vertrag/Migration/Config zuerst: keine API- oder Schema-Aenderung; keine Migration.
- Test-first: Vitest fuer den Re-Kanonisierungsfall, in dem `fetchSummary()` auf eine andere kanonische Aufgabe kippt und das Detail sofort fuer diese neue Auswahl geladen werden muss; Fehlerfall behaelt den alten State.
- Minimale Implementierung: `poll()` erkennt jede Aenderung von `studentSub` oder `taskId` nach `normalizeLiveSelection(...)` und laedt dann das Detail fuer die neue Auswahl nach, auch ohne `activeCellChanged`.
- Akzeptanzcheck: Rechte Detailansicht und Task-Auswahl bleiben nach Polling konsistent; bei Fehlern bleibt der bisherige State unveraendert.

### Arbeitspaket 2: Summary-Cursor fail-closed statt stiller Host-Clock-Fallback

- Finding: `SHOULD-FIX [API] Der Cursor-Vertrag verspricht einen DB-basierten Seed, die Runtime faellt aber still auf die Host-Uhr zurueck`
- Weiter offen, weil `_summary_snapshot_cursor(...)` in `backend/web/routes/teaching.py` nach jedem Fehler auf `datetime.now(...)` zurueckfaellt, waehrend OpenAPI und Referenzdoku einen DB-basierten Seed beschreiben.
- Relevante Regeln: Contract-First, Security-first, lokal = prod, ehrliche Fehlersemantik statt stiller Degradierung.
- Bounded Context Owner: `teaching`
- Voraussichtliche Dateien: `api/openapi.yml`, `docs/references/teaching_live.md`, `backend/web/routes/teaching.py`, `backend/tests/test_teaching_live_unit_summary_api.py`, `backend/tests/test_openapi_teaching_live_unit_contract.py`
- Vertrag/Migration/Config zuerst: OpenAPI und Referenzdoku zuerst; keine Migration.
- Test-first: pytest fuer `503 service_unavailable` mit `detail=summary_cursor_unavailable` bei Cursor-Seed-Fehler sowie Contract-Test fuer den dokumentierten `503`.
- Minimale Implementierung: `_summary_snapshot_cursor(...)` liefert keinen Host-Fallback mehr; der Summary-Endpunkt antwortet bei fehlendem Seed mit `503` und privaten Cache-Headern.
- Akzeptanzcheck: Runtime, OpenAPI und Doku beschreiben dieselbe Cursor-Semantik; bei Seed-Fehlern gibt es keinen stillen Host-Clock-Cursor mehr.

## Umsetzungsstand – 2026-04-14 11:40 CEST

- Geschlossen:
  - `MUST-FIX [BUG] Poll-Refresh kann eine neue selected_task_id mit altem Detailinhalt kombinieren`
  - `SHOULD-FIX [API] Der Cursor-Vertrag verspricht einen DB-basierten Seed, die Runtime faellt aber still auf die Host-Uhr zurueck`
- Offen:
  - Kein offener MUST-FIX aus der Review-Runde 11:24.
  - Keine direkt zusammenhaengenden SHOULD-FIX-Punkte mehr im verifizierten Scope.
- Stale:
  - Keine Findings waren nach der Umsetzung stale.
- Geaenderte Dateien/Subsyteme:
  - `docs/plan/2026-04-14-PR-fix.md`
  - `api/openapi.yml`
  - `docs/references/teaching_live.md`
  - `backend/web/routes/teaching.py`
  - `backend/tests/test_teaching_live_unit_summary_api.py`
  - `backend/tests/test_openapi_teaching_live_unit_contract.py`
  - `frontend/src/routes/live/page-state.ts`
  - `frontend/src/routes/live/page-interaction.test.ts`
- Architektur- und Bounded-Context-Entscheidungen:
  - Der DB-basierte Summary-Cursor bleibt im Kontext `teaching`; bei fehlendem Seed failt der Endpoint jetzt explizit mit `503`, statt in eine unklare Host-Uhr-Semantik zu degradierten.
  - Die `/live`-Controller-Logik bleibt im SvelteKit-Read-Model; sie laedt Detaildaten jetzt auch dann nach, wenn ein Summary-Reload die kanonische Auswahl aendert.
- Design-Contract-Check:
  - Keine Aenderung an Layout, Shell oder Komponentenstruktur von `/live`; der Fix beschraenkt sich auf Zustandskonsistenz und API-Fehlersemantik und respektiert den bestehenden Detailpanel-Vertrag.
- Tests ausgefuehrt:
  - `.venv/bin/pytest -q backend/tests/test_teaching_live_unit_summary_api.py backend/tests/test_openapi_teaching_live_unit_contract.py` -> `17 passed`
  - `cd frontend && npm test -- --run src/routes/live/page-interaction.test.ts src/routes/live/page.server.test.ts` -> `14 passed`
- Residual Risk:
  - Der Summary-Cursor bleibt ein separater DB-Read und kein echter Same-Snapshot-Cursor. Das ist nun fail-closed abgesichert und dokumentiert, aber kein Voll-Refactor der Summary-Erzeugung.
  - Es wurden bewusst nur die gezielten Live-Scopes erneut verifiziert; fachfremde bestehende Frontend-Typecheck-Probleme bleiben ausserhalb dieses PR-Fix.
- Aktualisierte Freigabeempfehlung:
  - Aus Sicht dieses PR-Fix-Dokuments besteht kein verbleibender Freigabeblocker mehr.
