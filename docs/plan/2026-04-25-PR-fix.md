# PR Review Fix Log

## Review Round - 13:45 CEST

### Git-Kontext

- Branch: `master`
- Base-Ref: `origin/master`
- Base-SHA: `69ed2c89fe1b48fcf528df9e0eda812462efa0c1`
- Merge-Base-SHA: `69ed2c89fe1b48fcf528df9e0eda812462efa0c1`
- Head-SHA: `53be1037a29d393234a28a89539ac1b2325dca66`

### Scope

Review von `origin/master..master`: zwei lokale Commits für Reload-Kontinuität, Browser-BFF-Session-Recovery, Silent-OIDC-Continuation und modularen Lernraum-Reload. Geprüft wurden insbesondere Auth/OIDC, Cookies, Redirect-Sanitizing, OpenAPI-Vertrag, Loader-Guards, Session-Refresh-Race-Handling und die neuen/angepassten Tests.

### Relevante Code-Referenzen

- `api/openapi.yml:5993`
- `api/openapi.yml:6041`
- `frontend/src/lib/server/backend-auth.ts:356`
- `frontend/src/lib/server/backend-auth.ts:406`
- `frontend/src/lib/server/api.ts:16`
- `frontend/src/lib/server/guards.ts:38`
- `frontend/src/routes/+layout.server.ts:7`
- `frontend/src/routes/protected-page-bootstrap-contract.test.ts:31`
- `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.server.ts:54`
- `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/page.server.test.ts:247`

### Kurzfazit

Der Fix ist fachlich stimmig und sicherheitsbewusst: Bearer-only Backend bleibt erhalten, `gustav_session` dient nur als Recovery-Signal, OIDC nutzt `state`, `nonce`, PKCE und `prompt=none`. Keine Blocker gefunden. Zwei Follow-ups erhöhen Vertragsklarheit und Testrobustheit.

### MUST-FIX

Keine.

### SHOULD-FIX

- [API] Callback-Vertrag erlaubt formal `state` ohne `code` und ohne `error` — `api/openapi.yml:6055@53be103` — Impact: Client- und Contract-Tests können eine Anfrageform als gültig interpretieren, die die Implementierung korrekt mit `invalid_code_or_state` ablehnt. — Fix: Dokumentiere den Callback mit `oneOf`/Beschreibung: `state` plus entweder `code` oder `error`; ergänze einen Contract-Test für diese Invariante.
- [TEST] Protected-Page-Contract testet Quelltextmarker statt Verhalten — `frontend/src/routes/protected-page-bootstrap-contract.test.ts:31@53be103` — Impact: Der Test kann bei semantisch falschem Guarding grün bleiben, solange passende Strings im Loader stehen. — Fix: Ergänze mindestens einen Verhaltenstest für `requireParentSessionBootstrap`, der `bootstrap=null` und `appSessionActive=true` konkret als Redirect nach `/auth/continue?...` validiert.

### NICE-TO-HAVE

- [CONSISTENCY] Action-Pfad im modularen Lernraum nutzt weiter den direkten Space-Guard — `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.server.ts:63@53be103` — Impact: Bei abgelaufener BFF-Session während eines offenen Tabs behandelt eine POST-Action Recovery anders als Page-Loads; das ist nicht der gemeldete Hard-Reload-Bug, aber uneinheitlich. — Fix: Entscheide bewusst, ob Actions bei aktiver App-Session eine klare Reauth-Meldung oder denselben Continuation-Einstieg bekommen sollen; teste den gewählten Pfad.

### TESTS

Nicht erneut ausgeführt; Review basiert auf lokalem Diff und der bereits gemeldeten Verifikation. Aus dem Verlauf: `make verify` war grün, Frontend-Vitest war grün, `npm --prefix frontend run check` hatte vorbestehende nicht verwandte `svelte-check`-Fehler.

### Refactor-Budget

Klein: ca. 1-2 Stunden für OpenAPI-Präzisierung und einen echten Guard-Verhaltenstest. Der Action-Follow-up ist optional und sollte nur umgesetzt werden, wenn wir POST-Recovery ausdrücklich konsistent machen wollen.

### Freigabe

Freigabe mit SHOULD-FIX-Follow-up. Für Merge/Push ist kein Blocker sichtbar; die beiden SHOULD-FIX-Punkte sollten zeitnah nachgezogen werden, damit Vertrag und Tests die Sicherheitsarchitektur präziser absichern.

## Reparaturplan - 2026-04-25 13:49

### Work Package 1: Callback-Vertrag präzisieren

- Finding: `[API] Callback-Vertrag erlaubt formal state ohne code und ohne error`.
- Status nach Re-Check: offen; `/auth/callback` markiert `code` und `error` optional, ohne die notwendige Alternative explizit maschinenlesbar zu machen.
- Regeln/Kontext: API Contract-First, Security first; Bounded Context `identity_access`.
- Dateien: `api/openapi.yml`, `backend/tests/test_openapi_auth_contract.py`.
- Contract zuerst: OpenAPI dokumentiert `state` plus genau einen von `code` oder `error` als Callback-Query-Invariante.
- Test-first: Contract-Test prüft die Invariante am OpenAPI-Dokument.
- Minimaler Fix: Extension/Beschreibung im OpenAPI-Operation-Objekt ergänzen; keine Laufzeitänderung nötig, da Code bereits fail-closed ist.
- Acceptance Check: Contract-Test ist grün.

### Work Package 2: Guard-Verhalten statt Quelltextmarker testen

- Finding: `[TEST] Protected-Page-Contract testet Quelltextmarker statt Verhalten`.
- Status nach Re-Check: offen; die vorhandenen Tests prüfen Marker, aber keinen konkreten Redirect.
- Regeln/Kontext: TDD, KISS, Security first; Bounded Context `identity_access`.
- Dateien: `frontend/src/lib/server/guards.test.ts`, optional bestehender Contract-Test unverändert als Strukturwächter.
- Test-first: Verhaltenstest für `requireParentSessionBootstrap`: `bootstrap=null` und `appSessionActive=true` führt zu `/auth/continue?...`; ohne App-Session bleibt der Login-Fallback.
- Minimaler Fix: keiner erwartet, falls bestehender Parent-Guard korrekt ist.
- Acceptance Check: neuer Vitest-Test ist grün.

### Work Package 3: Direkten Guard-Pfad mit Silent Continuation vereinheitlichen

- Finding: `[CONSISTENCY] Action-Pfad im modularen Lernraum nutzt weiter den direkten Space-Guard`.
- Status nach Re-Check: offen; die Lernraum-Action lädt Page-Daten über `requireSpaceBootstrap`, der bisher nur Login-Fallback kennt.
- Regeln/Kontext: Auth-Entscheidung bleibt im `identity_access`-Rand; `learning` ruft nur den Guard. Keine UI- oder DB-Änderung.
- Dateien: `frontend/src/lib/server/guards.ts`, `frontend/src/lib/server/guards.test.ts`.
- Test-first: Verhaltenstest für `requireSessionBootstrap`: bei fehlendem Bootstrap und aktiver `gustav_session` erfolgt Redirect nach `/auth/continue?...`; ohne aktive App-Session bleibt `/?redirect=...`.
- Minimaler Fix: `requireSessionBootstrap` nutzt `readAppSessionActive(fetch,cookies)` bevor es zum Login redirectet; `requireSpaceBootstrap` und die Lernraum-Action profitieren automatisch.
- Acceptance Check: neue Guard-Tests, relevante Frontend-Tests und `make verify` sind grün.

## Umsetzungsstand - 2026-04-25 13:55

### Geschlossene Findings

- `[API] Callback-Vertrag erlaubt formal state ohne code und ohne error`: geschlossen. `/auth/callback` dokumentiert jetzt die Query-Invariante `state` plus genau einen von `code` oder `error` über `x-gustav-query-contract`; der Contract-Test prüft diese Struktur.
- `[TEST] Protected-Page-Contract testet Quelltextmarker statt Verhalten`: geschlossen. `frontend/src/lib/server/guards.test.ts` prüft das konkrete Redirect-Verhalten des Parent-Guards für Silent Continuation und Login-Fallback.
- `[CONSISTENCY] Action-Pfad im modularen Lernraum nutzt weiter den direkten Space-Guard`: geschlossen. Der direkte Guard `requireSessionBootstrap` nutzt jetzt bei fehlendem Bootstrap dieselbe App-Session-Recovery-Entscheidung wie der Parent-Guard. `requireSpaceBootstrap` und damit die Lernraum-Action profitieren ohne Lernraum-spezifische Auth-Logik.

### Tests

- `backend/tests/test_openapi_auth_contract.py`: 3 passed.
- `frontend/src/lib/server/guards.test.ts`: 4 passed.
- `frontend/src/routes/protected-page-bootstrap-contract.test.ts` und `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/page.server.test.ts`: 8 passed.
- `backend/tests/packaging/test_sveltekit_entry_routing_contract.py` und `backend/tests/test_openapi_auth_contract.py`: 5 passed.
- `make verify`: grün.
  - Backend pytest: 1458 passed, 22 skipped, 1 warning.
  - H5P node tests: 15 passed.
  - Supabase integration: 5 passed.
  - OpenAI-compatible smoke: 2 passed.
  - E2E: 13 passed.

### Architektur- und Kontextentscheidungen

- Auth- und Recovery-Entscheidungen bleiben im Bounded Context `identity_access`.
- Der Lernraum (`learning`) erhält keine eigene Session-Recovery-Regel; er verwendet weiterhin den gemeinsamen Guard.
- Keine Migrationen, keine UI-Designänderungen, keine neuen ENV- oder Config-Anforderungen.

### Restrisiko

- Der OpenAPI-Vertrag nutzt eine projektinterne Extension für die Query-Invariante, weil OpenAPI-Parameterlisten Alternativen zwischen Query-Parametern nur eingeschränkt ausdrücken. Der zusätzliche Contract-Test verhindert Drift.
- `make verify` meldete nur bekannte Build-/Dependency-Warnungen; keine neue Blockade.

### Release-Empfehlung

Freigabe ohne offene PR-Fix-Findings. Die Änderungen sind klein, sicherheitsnah getestet und durch `make verify` bestätigt.
