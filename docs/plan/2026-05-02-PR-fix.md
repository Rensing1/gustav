# PR-Fix Review: master vs origin/master

Datum: 2026-05-02 14:47 CEST

## Review Round - 14:47

Branch: `master`
Base ref: `origin/master`
Merge-base: `4922df18a13be37ec1a6c306cac9173a45ff64c5`
Head: `e23a52ed7d9c2de0da227d93eea38bcbb78bf97d`

Reviewed scope:

- AI token usage accounting: OpenAPI, migration, DSPy usage capture, worker persistence, teacher read-model.
- Graph editor mutation stabilization and Playwright harness.
- Relevant tests, docs and migration contracts.

Context notes:

- Local `master` is 5 commits ahead of `origin/master`.
- The review compared `origin/master..master`.
- `git diff --check origin/master..master` reported trailing whitespace in `docs/plan/2026-05-01-filius-fls-upload-pipeline.md`.

Code references used:

- `backend/learning/adapters/dspy/usage.py:103`
- `backend/learning/adapters/dspy/feedback_program.py:260`
- `backend/learning/adapters/dspy/visual_feedback_program.py:249`
- `api/openapi.yml:7273`
- `backend/web/routes/app.py:1777`
- `docs/plan/2026-04-30-ai-token-usage-accounting.md:118`
- `docs/plan/2026-05-01-filius-fls-upload-pipeline.md:3`

### Kurzfazit

Die Richtung passt, aber das AI-Usage-Accounting verfehlt zwei zentrale Plan-Zusagen: unbekannte Provider-Usage wird nicht gezählt, und bereits erfasste Usage kann bei nachgelagerter Feedback-Validierung verloren gehen. Zusätzlich ist der neue API-Fehlervertrag unvollständig. Vor Merge bitte diese Punkte plus Whitespace-Hygiene beheben.

### MUST-FIX

- [BUG] Provideraufrufe ohne Usage werden verworfen statt als `usage_known=false` gezählt — `backend/learning/adapters/dspy/usage.py:103@e23a52e` — Impact: `unknown_events` bleibt trotz echter Modellaufrufe 0 und die Kostenschätzung unterschlägt Provider ohne Usage-Payload — Fix: Erzeuge für sicher ausgeführte DSPy-Operationen ohne Counter ein Unknown-Event mit `unknown_reason='missing_provider_usage'`.
- [BUG] Usage geht verloren, wenn `_validate_feedback_md` nach erfolgreichem Modellaufruf fehlschlägt — `backend/learning/adapters/dspy/feedback_program.py:260@e23a52e` und `backend/learning/adapters/dspy/visual_feedback_program.py:249@e23a52e` — Impact: Invalid-Feedback-Failures verbrauchen Tokens, werden aber nicht persistiert — Fix: Hänge die bis dahin gesammelten `usage_events` an den Fehler, bevor er über `local_feedback` zum Worker läuft.
- [API] Neuer AI-Usage-Endpunkt dokumentiert/erzwingt den Fehlervertrag nicht vollständig — `api/openapi.yml:7273@e23a52e`, `backend/web/routes/app.py:1777@e23a52e` — Impact: 404 ist nicht im OpenAPI-Vertrag, und Guard-Responses können ohne `Cache-Control: private, no-store` zurückgehen — Fix: 404 dokumentieren, Guard-Responses mit privaten Headers normalisieren und Contract/API-Tests ergänzen.

### SHOULD-FIX

- [TEST] Der geplante Security- und Fehlerpfad-Testumfang fehlt noch größtenteils — `docs/plan/2026-04-30-ai-token-usage-accounting.md:118@e23a52e` — Impact: RLS-Fremdzugriff, Unknown-Usage, Event-Idempotenz und Fehlerpfad-Persistenz können regressieren — Fix: Die im Plan genannten Tests für Unknown-Usage, RLS-Negativfälle, Helper-Ableitung und Fehlerpfad-Usage ergänzen.

### NICE-TO-HAVE

- [CONSISTENCY] `git diff --check origin/master..master` schlägt wegen Trailing Whitespace fehl — `docs/plan/2026-05-01-filius-fls-upload-pipeline.md:3@e23a52e` — Impact: unnötiger Hygiene-Fail vor Review/Merge — Fix: Trailing Whitespace in der Datei entfernen.

### TESTS

Nicht ausgeführt während der Review; Plan Mode war auf nicht-mutierende Analyse beschränkt. Für die Reparatur gezielt ausführen: `backend/tests/learning_adapters/test_dspy_usage_capture.py`, `backend/tests/test_learning_worker_jobs.py`, `backend/tests/migration/test_ai_usage_events_security.py`, `backend/tests/test_teaching_course_context_view_api.py`, danach `git diff --check origin/master..master` und `make verify`.

### Refactor-Budget

Klein bis mittel: Die Fixes bleiben lokal in Usage-Capture, Feedback-Adapter-Fehlertransport, API-Fehlervertrag und Tests. Keine große Architekturänderung nötig.

### Freigabe

Keine Freigabe vor MUST-FIX. Nach SHOULD-FIX ist die Änderung reviewbar; NICE-TO-HAVE kann zusammen mit der Hygiene-Reparatur erledigt werden.
