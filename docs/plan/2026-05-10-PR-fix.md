# PR-Review: feature/filius-fls-upload-pipeline vs. master

Datum: 2026-05-10 11:39 CEST

Branch: `feature/filius-fls-upload-pipeline`
Base: `master` (`cf9ea1ac42646b2178b32be4921c9c0d4323f5ea`)
Merge-Base: `cf9ea1ac42646b2178b32be4921c9c0d4323f5ea`
Head: `f168544bef22b94731a28f2d71612439e4f0f7b3`

Scope: Statisches Review des aktuellen Branches gegen lokalen `master`. Schwerpunkt: Filius-`.fls`-Upload, Validierung, Evidence-Extraktion, API-/Migrationsvertrag, Learning-Worker und UI-Anbindung.

## Kurzfazit

Die Filius-Pipeline ist sauber contract-first angelegt und hat gute Validator-/Adapter-Tests. Offen bleiben ein echter Worker-Integrationstest für `.fls` und eine Storage-Migration-Abdeckung für den weiterhin unterstützten Legacy-Bucket. Beides ist vor dem Merge sinnvoll, aber aus dieser Review heraus nicht blockierend.

## MUST-FIX

Keine.

## SHOULD-FIX

- [TEST] Es gibt keinen echten Worker-Durchlauf für akzeptierte Filius-`.fls`-Submissions — `backend/tests/test_learning_worker_jobs.py:505@f168544` — Impact: API- und Adaptertests beweisen nicht, dass ein DB-Job mit realer Submission, Storage-Key, `local_vision`, Feedback-Adapter und Persistenz bis `completed` funktioniert; ein Upload könnte 202 liefern und später im Worker hängen bleiben. — Fix: Einen Worker-Integrationstest analog zum Calliope-Test ergänzen, aber mit `task_kind='filius'`, lokaler `.fls`-Datei, erwarteter `filius.evidence.v1`-Evidence, `completed`-Status, Feedback und gelöschtem Job.

- [CONSISTENCY] Die Filius-Storage-Migration aktualisiert nur den Bucket `submissions`, obwohl der Code weiterhin `LEARNING_SUBMISSIONS_BUCKET=learning-submissions` unterstützt — `supabase/migrations/20260507120000_storage_submissions_bucket_allow_filius_fls.sql:37@f168544` — Impact: Deployments mit dem Legacy-Bucket können `.fls`-Uploads serverseitig erlauben, aber Supabase Storage lehnt den PUT wegen fehlender MIME-Allowlist ab. — Fix: Migration und Contract-Test so erweitern, dass alle unterstützten Submission-Buckets, mindestens `submissions` und `learning-submissions`, additiv `application/x.filius.fls` erhalten.

## NICE-TO-HAVE

- [CONSISTENCY] Der Teaching-DB-Repo-Serializer kennt `filius` nicht als Marker-Config — `backend/teaching/repo_db.py:142@f168544` — Impact: Die API-Route normalisiert das aktuell nachträglich, aber direkte Repo-/Service-Verbraucher bekommen für Filius-Tasks keine symmetrische Struktur zu `scratch` und `calliope`. — Fix: In `_task_row_to_dict` `filius = {}` für `kind == 'filius'` setzen und einen kleinen Repo-/Service-Test ergänzen.

## TESTS

Nicht ausgeführt. Review basiert auf Diff- und Code-Inspektion.

Empfohlene Verifikation nach Fixes:

- `.venv/bin/pytest -q backend/tests/test_filius_evidence_v1.py backend/tests/learning_adapters/test_local_vision_filius_fls.py backend/tests/test_learning_filius_fls_submission_api.py backend/tests/test_learning_worker_jobs.py -k "filius or worker_calliope_hex_missing_source_uses_fallback_evidence_and_completes"`
- `.venv/bin/pytest -q backend/tests/test_storage_buckets_provisioning.py backend/tests/test_filius_migrations_contract.py`
- `make verify`

## Refactor-Budget

Ca. 0,5 Arbeitstage: ein Worker-Integrationstest und die Bucket-Migration/Tests. Der Repo-Serializer ist klein und kann mitlaufen.

## Freigabe

Aus dieser Review heraus keine blockierenden Findings. Worker- und Migrationstest sollten vor dem Merge nachgezogen oder bewusst als Restarbeit dokumentiert werden.

## Reparaturplan – 2026-05-10 11:50

### Paket 1: Filius-Worker-Durchlauf

- Finding: fehlender echter Worker-Durchlauf für akzeptierte Filius-`.fls`-Submissions.
- Warum noch offen: `test_learning_worker_jobs.py` enthält einen Calliope-Pfad mit realem `local_vision`, aber keinen analogen `task_kind='filius'`-Fall.
- Regeln/Constraints: Red-Green-Refactor, `learning` verarbeitet Abgaben; `teaching` bleibt Quelle für Task-Struktur. Keine UI-Änderung, keine Migration.
- Bounded Context: `learning`, konsumiert `teaching`-Task-Metadaten.
- Voraussichtliche Dateien: `backend/tests/test_learning_worker_jobs.py`.
- Contract/Migration zuerst: nicht nötig; bestehender API-/DB-Vertrag erlaubt Filius bereits.
- Test-first: Worker-Test mit realer `.fls`-Fixture, lokalem Storage-Root, `local_vision.build()`, `FeedbackAdapter`, Status `completed`, `filius.evidence.v1`, Feedback und gelöschtem Job.
- Minimale Implementierung: voraussichtlich keine Produktivcode-Änderung, falls Worker bereits korrekt verdrahtet ist.
- Acceptance Check: gezielter pytest für den neuen Worker-Test.

### Paket 2: Storage-Allowlist für Legacy-Bucket

- Finding: Filius-Storage-Migration aktualisiert nur `submissions`, obwohl `LEARNING_SUBMISSIONS_BUCKET=learning-submissions` weiterhin unterstützt wird.
- Warum noch offen: Migration `20260507120000_storage_submissions_bucket_allow_filius_fls.sql` hat `where id = 'submissions'`.
- Regeln/Constraints: Migrationen sind Quelle der Wahrheit; lokal = prod; Bucket-Update muss additiv und idempotent bleiben.
- Bounded Context: `learning` Storage-Infrastruktur.
- Voraussichtliche Dateien: `supabase/migrations/20260507120000_storage_submissions_bucket_allow_filius_fls.sql`, `backend/tests/test_storage_buckets_provisioning.py`.
- Contract/Migration zuerst: Der Migrationsvertrag wird per Test präzisiert; keine OpenAPI-Änderung.
- Test-first: Contract-Test erwartet `where id in (...)` bzw. beide unterstützten Bucket-IDs `submissions` und `learning-submissions`.
- Minimale Implementierung: Migration auf beide Bucket-IDs erweitern, ohne vorhandene Allowlist-Einträge zu ersetzen.
- Acceptance Check: gezielter pytest für Storage-Provisioning-Contract.

### Paket 3: Teaching-Repo-Shape für Filius

- Finding: `_task_row_to_dict` kennt kein `filius`-Markerfeld.
- Warum noch offen: Der DB-Serializer setzt Marker für `visual`, `scratch`, `calliope`, aber nicht für `filius`.
- Regeln/Constraints: Clean Architecture; Repo liefert plain dicts an Use Cases/Web-Adapter, Shape soll OpenAPI-konform und für Lernende verständlich bleiben.
- Bounded Context: `teaching`.
- Voraussichtliche Dateien: `backend/teaching/repo_db.py`, `backend/tests/test_teaching_repo_task_row_serializer.py` oder ein bestehender Teaching-Test.
- Contract/Migration zuerst: keine Schema- oder OpenAPI-Änderung nötig.
- Test-first: kleiner Unit-Test gegen `_task_row_to_dict` erwartet `filius == {}` und andere Spezialkonfigurationen `None`.
- Minimale Implementierung: `filius = None`, Branch `kind == "filius"`, Rückgabefeld ergänzen.
- Acceptance Check: gezielter pytest für den neuen Serializer-Test plus betroffene Filius/Storage/Worker-Tests.

## Umsetzungsstand – 2026-05-10 11:54

### Geschlossen

- [TEST] Worker-Durchlauf für akzeptierte Filius-`.fls`-Submissions ergänzt. Der neue Test legt eine echte `.fls`-Fixture in den lokalen Storage-Root, setzt Task und Submission auf `filius`/`application/x.filius.fls`, nutzt `local_vision.build()` und prüft `completed`, `filius.evidence.v1`, Feedback-Persistenz und gelöschten Queue-Job.
- [CONSISTENCY] Filius-Storage-Migration erweitert. Die Allowlist-Migration aktualisiert jetzt additiv beide unterstützten Learning-Submission-Buckets: `submissions` und `learning-submissions`.
- [CONSISTENCY] Teaching-DB-Repo-Serializer erweitert. `_task_row_to_dict` liefert für `kind == "filius"` nun `filius: {}` und lässt andere Marker-Konfigurationen `None`.

### Tests

- `.venv/bin/pytest -q backend/tests/test_storage_buckets_provisioning.py::test_filius_storage_migration_updates_all_supported_submission_buckets backend/tests/test_teaching_repo_task_row_serializer.py::test_task_row_to_dict_exposes_filius_marker_config` — erst rot mit 2 Fehlern, danach grün mit 2 passed.
- `.venv/bin/pytest -q backend/tests/test_learning_worker_jobs.py -k "filius_fls or worker_calliope_hex_missing_source_uses_fallback_evidence_and_completes"` — 2 passed, 17 deselected.
- `.venv/bin/pytest -q backend/tests/test_filius_evidence_v1.py backend/tests/learning_adapters/test_local_vision_filius_fls.py backend/tests/test_learning_filius_fls_submission_api.py backend/tests/test_learning_worker_jobs.py -k "filius or worker_calliope_hex_missing_source_uses_fallback_evidence_and_completes"` — 25 passed, 17 deselected.
- `.venv/bin/pytest -q backend/tests/test_storage_buckets_provisioning.py backend/tests/test_filius_migrations_contract.py backend/tests/test_teaching_repo_task_row_serializer.py` — 8 passed, 1 skipped.
- `git diff --check` — bestanden.

### Geänderte Dateien / Subsysteme

- `learning`: Worker-Regressionstest für Filius-FLS-Verarbeitung.
- `learning` Storage-Infrastruktur: Supabase-Migration für beide unterstützten Submission-Bucket-Namen.
- `teaching`: DB-Repo-Serializer-Shape für Filius-Task-Marker.
- `docs/plan`: Reparaturplan und Umsetzungsstand an dieses PR-Fix-Dokument angehängt.

### Architektur- und Kontextentscheidungen

- Filius-Task-Struktur bleibt im `teaching`-Kontext; Abgabenverarbeitung und Evidence-Erzeugung bleiben im `learning`-Kontext.
- Keine UI- oder Design-Contract-Änderung.
- Keine neue Migration angelegt, weil die PR-Branch die Filius-Allowlist-Migration noch nicht gemergt hat; die bestehende Migration wurde contract-konform korrigiert.

### Restliches Risiko

- `supabase migration up` wurde nicht erfolgreich abgeschlossen, weil die lokale Migration-History fremde Versionen `20260507118000` und `20260507119000` enthält, die nicht im lokalen Migrationsverzeichnis liegen. Die reparierte SQL-Migration ist per Contract-Test abgedeckt; vor einer lokalen End-to-End-Migration sollte die Supabase-Migration-History separat bereinigt werden.

### Aktualisierte Freigabeempfehlung

Alle dokumentierten Findings aus diesem PR-Fix-Dokument sind geschlossen. Aus dieser Reparaturrunde bleibt nur das lokale Migration-History-Thema als Umgebungsrisiko, nicht als Branch-Code-Befund.
