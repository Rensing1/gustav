# PR-Fix Review: Teaching Authoring CLI

Datum: 2026-05-11 20:14 CEST

Branch: `codex/h5p-drag-question-ajax-libraries`  
Base ref: `master` (`b2e71b1b57cf3435657ae0046a1a889e1c8f6538`)  
Merge-base: `b2e71b1b57cf3435657ae0046a1a889e1c8f6538`  
Head: `9e07bca9b1c7b76621f80f221df41d351af8d713`

## Review Round - 20:14

### Reviewed Scope

- Branch-Diff `b2e71b1..9e07bca` mit Fokus auf den letzten Commit `9e07bca feat: add teaching authoring CLI`.
- Geprüft wurden CLI-Code, CLI-Token-Store, Auth-Middleware, Profil-Token-Endpunkte, OpenAPI-Vertrag, Migration, Profil-UI, Dokumentation und die zugehörigen Tests.
- Der Branch-Diff gegen `master` enthält zusätzlich H5P-Änderungen aus `eb57e4e`; die Detailfindings beziehen sich auf den CLI-Commit, die Branch-Scope-Abweichung ist als Konsistenzrisiko notiert.

### Kontextnotizen

- Positiv: Der Scope ist breit abgedeckt: CLI-Befehle für Lerneinheiten, Abschnitte, Phasen, Module, Modul-Kanten, Markdown-Materialien und Aufgaben sind vorhanden.
- Positiv: Die Sicherheitsidee ist gut: opake Tokens, Hash-only-Speicherung, getrennte Scopes `read`, `write`, `delete`, Widerruf, Ablaufzeit und lokale Config-Datei mit `0600`.
- Positiv: Es gibt relevante Unit-/Contract-Tests für CLI-Routing, Token-Semantik, OpenAPI und Middleware-Scope-Prüfungen.
- Kritisch: Runtime und Vertrag weichen bei der Reichweite von CLI-Tokens auseinander. Das ist vor PR-Freigabe sicherheitsrelevant.

### Code-Referenzen

- `backend/web/main.py:421@9e07bca` - CLI-Bearer wird in der allgemeinen Bearer-Auth akzeptiert.
- `backend/web/routes/app.py:1087@9e07bca` - Profil-Endpunkt erstellt neue CLI-Tokens.
- `backend/web/main.py:300@9e07bca` - DB-CLI-Token-Store fällt bei Fehlern auf In-Memory zurück.
- `backend/web/routes/teaching.py:3005@9e07bca` - Unit-Delete führt vor dem DB-Delete einen Storage-Metadaten-Cleanup aus.
- `backend/tests/test_auth_middleware.py:137@9e07bca` - Middleware-Tests für CLI-Bearer beginnen hier, aber ohne Profil-Token-Management-Negativfall.
- `backend/tests/test_profile_view_api.py:219@9e07bca` - Profil-Token-Lifecycle-Test nutzt JWT-Bearer, aber keinen CLI-Bearer-Negativtest.
- `backend/tests/test_cli_tokens.py:118@9e07bca` - DB-Store-Test prüft nur unsafe table names.
- `backend/tools/gustav_cli/cli.py:30@9e07bca` und `backend/tools/gustav_cli/cli.py:33@9e07bca` - CLI parst Erfolg und Fehler strikt als JSON.
- `.env.example:19@9e07bca` - Session-Konfigurationsblock ohne `CLI_TOKENS_BACKEND`.
- `h5p-service/lib/ajax_body.mjs:1@eb57e4e` - zusätzliche H5P-Änderung im Branch-Diff gegen `master`.

## Kurzfazit

Die CLI ist in Scope, Tests und Dokumentation erstaunlich weit. Kritisch ist aber die Auth-Grenze: CLI-Tokens werden runtime breiter akzeptiert als vertraglich geplant. Dadurch können CLI-Tokens aktuell Token-Management-Endpunkte erreichen. Das muss vor PR-Freigabe behoben werden.

## MUST-FIX

- [SEC] CLI-Tokens können Token-Management-Endpunkte erreichen — `backend/web/main.py:421@9e07bca`, `backend/web/routes/app.py:1087@9e07bca` — Impact: Ein CLI-Token mit `write` kann `POST /api/app/profile/cli-tokens` nutzen und neue Tokens mit stärkeren Scopes erzeugen, obwohl Plan und Doku das ausschließen. — Fix: Akzeptiere CLI-Bearer nur für explizit erlaubte Teaching-Authoring-Pfade oder lehne Profil-/Token-Endpunkte bei `request.state.cli_token_id` hart ab.
- [SEC] DB-Token-Store fällt in Nicht-Test-Umgebungen still auf Memory zurück — `backend/web/main.py:300@9e07bca` — Impact: In Prod/Compose können Tokens pro Prozess volatil werden; Restart/Multi-Worker bricht Nutzbarkeit und verschleiert Konfigurationsfehler. — Fix: Schlage bei `CLI_TOKENS_BACKEND=db` fail-fast fehl statt auf Memory zurückzufallen; erlaube Memory nur explizit per Konfiguration.

## SHOULD-FIX

- [TEST] Runtime-Test fehlt für "CLI-Token darf keine CLI-Tokens verwalten" — `backend/tests/test_auth_middleware.py:137@9e07bca`, `backend/tests/test_profile_view_api.py:219@9e07bca` — Impact: OpenAPI testet den Vertrag, aber nicht die tatsächliche Middleware-Route. — Fix: Ergänze einen Regressionstest: gültiges CLI-Token gegen `/api/app/profile/cli-tokens` muss `401/403` liefern.
- [TEST] DB-backed CLI-Token-Store ist kaum gegen echte Migration getestet — `backend/tests/test_cli_tokens.py:118@9e07bca` — Impact: Prod nutzt DB, Tests decken aber vor allem In-Memory ab. — Fix: Ergänze einen optionalen DB-Test: Migration vorhanden, Hash-only, list/revoke/verify, `anon/authenticated` ohne Rechte.
- [BUG] CLI crasht bei nicht-JSON API-Fehlern — `backend/tools/gustav_cli/cli.py:30@9e07bca`, `backend/tools/gustav_cli/cli.py:33@9e07bca` — Impact: Proxy-/HTML-/Plaintext-Fehler erzeugen Tracebacks statt verständlicher CLI-Fehler. — Fix: Fange `JSONDecodeError` ab und gib eine gekürzte, tokenfreie Fehlermeldung aus.
- [BUG] `units delete` scheitert im lokalen Compose-Smoke-Test mit `503 storage_adapter_unavailable` — `backend/web/routes/teaching.py:3005@9e07bca`, `docker-compose.yml:98@9e07bca` — Impact: Die CLI kann Lerneinheiten, Abschnitte, Markdown-Materialien und Aufgaben anlegen sowie Abschnittsinhalte löschen, aber eine leere Test-Lerneinheit nicht per CLI entfernen, wenn der Unit-Delete im Storage-Metadaten-Schritt eine container-ungeeignete DSN nutzt. Im Webservice-Log stand: Verbindung zu `127.0.0.1:54322` aus dem Container fehlgeschlagen. — Fix: Stelle sicher, dass der Storage-Metadaten-Cleanup denselben containerfähigen Teaching-/Service-DSN wie der Repository-Zugriff nutzt, z. B. `supabase_db_gustav-alpha2:5432` im Compose-Netz; ergänze einen lokalen/DB-gestützten Regressionstest für `DELETE /api/teaching/units/{unit_id}` ohne Storage-Objekte.

## NICE-TO-HAVE

- [CONSISTENCY] Branch-Diff gegen `master` enthält zusätzlich H5P-Änderungen aus `eb57e4e` — `h5p-service/lib/ajax_body.mjs:1@eb57e4e` — Impact: Ein CLI-PR wäre schwerer reviewbar und enthält thematisch fremde Änderungen. — Fix: Picke den CLI-Commit auf einen sauberen Feature-Branch oder führe H5P separat als PR.
- [CONSISTENCY] `.env.example` dokumentiert `CLI_TOKENS_BACKEND` nicht — `.env.example:19@9e07bca` — Impact: Der Betriebsmodus ist nicht sichtbar, obwohl `main.py` ihn auswertet. — Fix: Ergänze einen kurzen Env-Hinweis bei Sessions/CLI-Tokens.

## Runtime Smoke Test - 20:24

Ausgeführt mit isolierter CLI-Konfiguration unter `/tmp/gustav-cli-e2e`, weil die bestehende CLI-Konfiguration `http://app.localhost` nutzt und Caddy auf `https://app.localhost` weiterleitet.

Workflow:

- `units create` erstellte `CLI-Test 2026-05-11 Dummy-Inhalte`.
- `sections create` erstellte einen linearen Testabschnitt.
- `materials create` erstellte Markdown-Dummy-Material.
- `tasks create` erstellte eine native Dummy-Aufgabe mit zwei `criteria` und `teacher_context_md`.
- `materials list --json` und `tasks list --json` bestätigten die gespeicherten Felder.
- `tasks delete`, `materials delete` und `sections delete` liefen erfolgreich.
- `units delete` scheiterte mit `503 {"error":"service_unavailable","detail":"storage_adapter_unavailable"}`.

Root-Cause-Hinweis aus dem Webservice-Log:

```text
unit delete storage metadata unavailable unit_id=ad554fd9-f5ea-44fe-9072-8747579443a0
psycopg.OperationalError: connection to server at "127.0.0.1", port 54322 failed: Connection refused
```

Die verbliebene leere Test-Lerneinheit wurde anschließend eng begrenzt per lokaler Supabase-DB mit `id`- und `title`-Bedingung gelöscht. Abschlussprüfung: CLI-Listing enthielt den Testtitel nicht mehr; DB-Zählung für Test-UUID/Testtitel ergab `0`.

## TESTS

- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_gustav_cli.py backend/tests/test_cli_tokens.py backend/tests/test_auth_middleware.py backend/tests/test_openapi_profile_contract.py backend/tests/test_profile_view_api.py backend/tests/test_teaching_module_content_target_api.py backend/tests/test_openapi_teaching_tasks_contract.py`
- Ergebnis: `56 passed in 3.38s`
- Nicht ausgeführt: komplette Testsuite und E2E.

## Refactor-Budget

Mittel: Die Hauptkorrektur liegt sauber in der Auth-Middleware/Allowlist plus 2-3 Tests. Der DB-Fail-fast ist klein. Ein optionaler DB-Integrationstest braucht etwas mehr Sorgfalt wegen lokaler Supabase-Abhängigkeit.

## Freigabe

Keine Freigabe vor den beiden MUST-FIX-Punkten. Danach ist das Feature aus meiner Sicht PR-fähig, mit verbleibendem Testausbau als SHOULD-FIX.

## Reparaturplan – 2026-05-11 20:21

### 1. CLI-Token-Reichweite begrenzen

- Finding: `[SEC] CLI-Tokens können Token-Management-Endpunkte erreichen`.
- Warum offen: `backend/web/main.py` akzeptiert `gustav_cli_...` aktuell in der allgemeinen Bearer-Auth, bevor der Zielpfad eingegrenzt wird.
- Regeln/Kontext: Security first, API Contract-First, `identity_access` verantwortet AuthN/AuthZ; Teaching-Endpunkte behalten ihre fachlichen Authoring-Guards.
- Dateien: `api/openapi.yml`, `backend/web/main.py`, `backend/tests/test_auth_middleware.py`, `backend/tests/test_openapi_profile_contract.py`.
- Contract-first: Profil-Token-Endpunkte dokumentieren explizit, dass CLI-Bearer dort nicht akzeptiert wird.
- Test-first: Regressionstests für CLI-Bearer auf `/api/app/profile/cli-tokens` und `/api/teaching/views/teacher-home`; positiver Gegencheck für `/api/teaching/units`.
- Minimaler Fix: CLI-Bearer nur für `/api/teaching/units` und `/api/teaching/units/...` zulassen.
- Akzeptanz: CLI-Authoring funktioniert weiter; CLI-Tokens können keine weiteren Tokens erstellen/widerrufen.

### 2. DB-Token-Store fail-fast machen

- Finding: `[SEC] DB-Token-Store fällt in Nicht-Test-Umgebungen still auf Memory zurück`.
- Warum offen: `backend/web/main.py` fängt `DBCLITokenStore`-Initialisierungsfehler ab und nutzt `InMemoryCLITokenStore`.
- Regeln/Kontext: Lokal = Prod; Konfigurationsfehler dürfen nicht als flüchtiger Runtime-Modus versteckt werden. Owner: `identity_access` plus Platform/App-Konfiguration.
- Dateien: `backend/web/main.py`, `backend/tests/test_cli_tokens.py` oder ein kleiner Auth-Wiring-Test, `.env.example`, `docs/references/gustav_cli.md`.
- Contract/Migration: Keine OpenAPI- oder Schemaänderung.
- Test-first: Store-Wiring-Test, der im DB-Modus einen Initialisierungsfehler erwartet statt Memory-Fallback.
- Minimaler Fix: Helper für CLI-Token-Store-Auswahl; DB-Modus propagiert Fehler, Memory-Modus bleibt explizit möglich.
- Akzeptanz: Prod-nahe Konfiguration startet nicht mit unpersistierten CLI-Tokens.

### 3. CLI-Fehlerparser robust machen

- Finding: `[BUG] CLI crasht bei nicht-JSON API-Fehlern`.
- Warum offen: `_http_json` ruft `json.loads` für Erfolg und `HTTPError` ohne Fallback auf.
- Regeln/Kontext: KISS, FOSS/lernbarer Code, keine Token-Leaks in Fehlermeldungen.
- Dateien: `backend/tools/gustav_cli/cli.py`, `backend/tests/test_gustav_cli.py`.
- Contract/Migration: Keine.
- Test-first: Nicht-JSON `HTTPError` und nicht-JSON Erfolgsantwort liefern strukturierte, tokenfreie Fehler statt Traceback.
- Minimaler Fix: kleine JSON-Fallback-Funktion mit kurzer Textvorschau.
- Akzeptanz: CLI zeigt verständliche `API-Fehler (...)`-Meldung und leakt keine Tokens.

### 4. Unit-Delete-DSN im Compose-Pfad absichern

- Finding: `[BUG] units delete scheitert im lokalen Compose-Smoke-Test mit storage_adapter_unavailable`.
- Warum offen: Der Storage-Metadaten-Cleanup nutzt einen DSN aus dem Repo bzw. Service-Fallback. `docker-compose.yml` stellt zwar inzwischen einen containerfähigen Default bereit, aber ein vorhandener `.env`-Wert wie `SERVICE_ROLE_DSN=...@127.0.0.1:54322/...` überschreibt diesen Default und landet unverändert im Webcontainer.
- Regeln/Kontext: Lokal = Prod, Compose-Service-Namen statt `127.0.0.1` im Container. Owner: `teaching` und Platform/App-Konfiguration.
- Dateien: `docker-compose.yml`, `docs/references/config_matrix.md`, bestehende Teaching-Delete-Tests.
- Contract/Migration: Keine.
- Test-first: Config-Test, der nicht nur die YAML-Vorlage, sondern die effektiv aufgelöste Webcontainer-Konfiguration gegen `127.0.0.1:54322` absichert; alternativ ein Compose-CLI-Smoke-Test für `units create` + `units delete`.
- Minimaler Fix: `SERVICE_ROLE_DSN` im Webservice setzen und lokale `.env`/Dokumentation so ausrichten, dass containerinterne Service-DSNs nicht mit hostinternen Test-DSNs verwechselt werden.
- Akzeptanz: Unit-Delete kann im Container denselben DB-Service erreichen wie die Teaching-Repository-DSN.

## Test-Coverage-Befund – 2026-05-11 20:52

Auslöser: Nach der Codeüberarbeitung wurde die CLI erneut end-to-end gegen den laufenden Compose-Stack getestet. `units create`, `sections create`, Markdown-Materialien, Aufgaben mit `criteria`/`teacher_context_md`, sowie das Löschen von Aufgabe, Material und Abschnitt funktionierten. `units delete` scheiterte weiterhin mit `503 storage_adapter_unavailable`.

Ergebnis der Testanalyse:

- Es gibt vorhandene Unit-Delete-Regressionstests in `backend/tests/test_teaching_units_modules_api.py`, u. a. `test_teacher_can_crud_units_and_ownership_is_enforced`, `test_deleting_unit_removes_material_and_submission_storage_objects` und `test_deleting_unit_aborts_when_storage_delete_fails`.
- Mit DB-Zugriff laufen diese Tests grün: `.venv/bin/pytest -q -rs backend/tests/test_teaching_units_modules_api.py -k 'teacher_can_crud_units or deleting_unit_removes_material_and_submission_storage_objects or deleting_unit_aborts_when_storage_delete_fails'` ergab `3 passed, 17 deselected`.
- Diese Tests laufen über `ASGITransport` im Pytest-Hostprozess. Für diesen Prozess ist `127.0.0.1:54322` die host-exponierte Supabase-DB und damit gültig.
- Der fehlerhafte Runtime-Pfad läuft dagegen im Docker-Webcontainer. Dort zeigt `127.0.0.1:54322` auf den Webcontainer selbst, nicht auf `supabase_db_gustav-alpha2`.
- `backend/tests/test_compose_keycloak_postgres.py::test_web_service_sets_container_service_role_dsn_for_storage_cleanup` prüft nur die `docker-compose.yml`-Vorlage. Der Test sieht den guten Default `SERVICE_ROLE_DSN=${SERVICE_ROLE_DSN:-postgresql://...@supabase_db_gustav-alpha2:5432/...}`, aber nicht die effektiv durch `.env` aufgelöste Container-Umgebung.
- Maskierte Runtime-Prüfung im Webcontainer zeigte: `SERVICE_ROLE_DSN` war effektiv `postgresql://postgres:***@127.0.0.1:54322/postgres`.
- Die lokale `.env` enthielt explizit `SERVICE_ROLE_DSN=postgresql://postgres:postgres@127.0.0.1:54322/postgres`; dieser Wert überschreibt den Compose-Default.

Coverage-Lücke:

- Die bestehende API-Testabdeckung prüft den fachlichen Delete-Pfad und den Storage-Cleanup, aber nicht die Container-Netzwerkperspektive.
- Der bestehende Compose-Test prüft die statische Vorlage, aber nicht `docker compose config` bzw. die laufende Webcontainer-Umgebung nach `.env`-Substitution.
- Es fehlt ein Regressionstest, der entweder `docker compose config` gegen hostinterne DSNs im Webservice blockiert oder als E2E-Smoke-Test echte CLI-Aufrufe gegen den laufenden Compose-Stack ausführt und `units delete` einschließt.

## Umsetzung – 2026-05-11 20:39

Status: umgesetzt.

- CLI-Bearer werden in `backend/web/main.py` nur noch für `/api/teaching/units` und darunter akzeptiert. Profil-/Tokenverwaltung und Teaching-Views behandeln CLI-Tokens damit als nicht authentifiziert.
- `CLI_TOKENS_BACKEND=db` nutzt keinen stillen Memory-Fallback mehr. Der neue Store-Factory-Helper propagiert Initialisierungsfehler außerhalb des pytest-Defaults.
- Die CLI verarbeitet nicht-JSON-Antworten robust: erfolgreiche Plaintext-Antworten werden strukturiert zurückgegeben, nicht-JSON-HTTP-Fehler liefern `http_error`, Statusdetail und kurze Body-Vorschau statt Traceback.
- `docker-compose.yml` setzt für den Webservice `SERVICE_ROLE_DSN` mit `supabase_db_gustav-alpha2:5432`, damit Teaching-Storage-Metadaten-Cleanups im Container nicht auf `127.0.0.1:54322` zeigen.
- `api/openapi.yml`, `.env.example`, `docs/references/config_matrix.md` und `docs/references/gustav_cli.md` dokumentieren die geänderten Sicherheits- und Konfigurationsregeln.

Verifikation:

- Rotlauf vor Implementierung: 7 erwartete Failures für CLI-Token-Reichweite, Store-Fail-fast, nicht-JSON-CLI-Parsing, OpenAPI-Sicherheitsnotiz und Compose-DSN.
- Grünlauf nach Implementierung: `.venv/bin/pytest -q backend/tests/test_auth_middleware.py backend/tests/test_cli_tokens.py backend/tests/test_gustav_cli.py backend/tests/test_teaching_units_modules_api.py::test_deleting_unit_removes_material_and_submission_storage_objects backend/tests/test_teaching_units_modules_api.py::test_deleting_unit_aborts_when_storage_delete_fails backend/tests/test_openapi_profile_contract.py backend/tests/test_openapi_teaching_contract.py backend/tests/test_compose_keycloak_postgres.py::test_web_service_sets_container_service_role_dsn_for_storage_cleanup`
- Ergebnis: `68 passed, 2 skipped in 7.97s`.

# PR-Review: lokaler master gegen origin/master

Datum: 2026-05-11 20:37 CEST

Branch: `master` (`77c1a97e78a56fca3120539e0f2b1e048e6257e2`)
Base ref: `origin/master` (`cfd9914878679a45b63f68b7bb9f950abe6a9435`)
Merge-base: `eb57e4e2d709827b7a5493bef51798397e3e87d5`
Head: `77c1a97e78a56fca3120539e0f2b1e048e6257e2`

## Review Round - 20:37

### Reviewed Scope

- Geprüft wurde der lokale Scope `eb57e4e..master` gegenüber dem aktuellen `origin/master`.
- `git fetch --prune origin` wurde ausgeführt; danach war `master` 2 Commits voraus und 2 Commits hinter `origin/master`.
- Fokus: Teaching-Authoring-CLI, CLI-Tokens, Auth-Middleware, OpenAPI-Vertrag, Profil-Token-Endpunkte, Migration, Profil-UI und Tests.

### Kontextnotizen

- Produktentscheidung von Felix: Das CLI-Tool soll erweiterbar sein. Daher ist eine breitere CLI-Reichweite nicht per se falsch.
- Review-Maßstab bleibt Contract-first: Jede bewusst freigegebene CLI-Reichweite muss explizit in OpenAPI, Scope-Regeln und Regressionstests stehen.
- Der Arbeitsbaum war beim Review sauber.

### Code-Referenzen

- `backend/web/main.py:447@77c1a97` - CLI-Bearer-Allowlist akzeptiert das komplette Präfix `/api/teaching/units/...`.
- `api/openapi.yml:8552@77c1a97` - OpenAPI dokumentiert CLI-Auth nur für ausgewählte Teaching-Authoring-Endpunkte.
- `api/openapi.yml:10522@77c1a97` - H5P-Reset bleibt im Vertrag cookie-only, ist runtime aber unter dem CLI-Präfix erreichbar.
- `backend/tools/gustav_cli/cli.py:217@77c1a97` - CLI-Konfiguration speichert die Base-URL ohne HTTPS-Prüfung.
- `backend/web/routes/app.py:1088@77c1a97` - CLI-Token-Create nutzt Pydantic-Request-Validierung vor der Handler-400-Logik.
- `backend/tests/test_openapi_profile_contract.py:29@77c1a97` - OpenAPI-Tests decken Profil-Token-Endpunkte ab, aber nicht alle runtime per Präfix erreichbaren Teaching-Endpunkte.

## Kurzfazit

Der lokale `master` enthält ein gutes CLI-Fundament mit Token-Scopes, Hash-only-Speicherung und breiter Testbasis. Vor Freigabe muss der divergierte Branch mit `origin/master` zusammengeführt werden. Außerdem muss die gewollte CLI-Erweiterbarkeit contract-first modelliert werden: offen ist nicht das Problem, undokumentiert offen ist das Problem.

## MUST-FIX

- [CONSISTENCY] Lokaler `master` ist gleichzeitig 2 Commits vor und 2 Commits hinter `origin/master` — `git log origin/master...master@77c1a97` — Impact: Ein Push/Merge aus diesem Stand kann Remote-Änderungen wie `cfd9914 docs(tickets): add auth session continuity blocker` übergehen oder unnötige Konflikte erzeugen. — Fix: Integriere `origin/master` sauber in den lokalen Stand, erhalte die Remote-Ticket-Dokumentation und reviewe danach den finalen Diff erneut.
- [API] CLI-Reichweite ist runtime breiter als im OpenAPI-Vertrag beschrieben — `backend/web/main.py:447@77c1a97`, `api/openapi.yml:10522@77c1a97` — Impact: CLI-Tokens erreichen über das Präfix auch H5P- und Datei-Endpunkte, die im Vertrag nicht mit `cliTokenAuth` und `x-required-cli-scopes` dokumentiert sind; Security-Review, Client-Generierung und Tests sehen damit eine andere API als die Produktion. — Fix: Dokumentiere und teste alle absichtlich CLI-fähigen Präfix-Endpunkte oder ersetze die Präfixregel durch eine explizite, erweiterbare Route-/Scope-Tabelle.

## SHOULD-FIX

- [SEC] CLI akzeptiert beliebige nicht-HTTPS-Base-URLs für Bearer-Tokens — `backend/tools/gustav_cli/cli.py:217@77c1a97`, `backend/tools/gustav_cli/config.py:27@77c1a97` — Impact: Ein falsch konfiguriertes `http://`-Ziel überträgt CLI-Tokens im Klartext. — Fix: Erzwinge `https://` für `auth configure` oder verlange eine explizite, eng benannte Ausnahme für lokale Testziele.
- [API] Ungültige CLI-Token-Create-Payloads können FastAPI-422 statt dokumentiertem 400 liefern — `backend/web/routes/app.py:1088@77c1a97`, `api/openapi.yml:6819@77c1a97` — Impact: Direkte API-Clients bekommen ein anderes Fehlerformat als der Vertrag beschreibt; die bestehende 400-Handlerlogik greift erst nach erfolgreicher Pydantic-Validierung. — Fix: Validiere den Body wie bei Teaching-Endpunkten handlernah oder ergänze gezielte 422-zu-400-Abbildung plus Regressionstests.
- [TEST] Tests sichern die absichtlich breite CLI-Reichweite nicht vollständig ab — `backend/tests/test_openapi_profile_contract.py:29@77c1a97`, `backend/tests/test_auth_middleware.py:139@77c1a97` — Impact: Künftige CLI-Erweiterungen können wieder Vertrag und Runtime auseinanderlaufen lassen. — Fix: Ergänze eine parametrisierte Contract-/Runtime-Matrix für alle per CLI erreichbaren Teaching-Pfade mit erwarteten Scopes.

## NICE-TO-HAVE

- [CONSISTENCY] Die CLI-Scope-Logik steckt aktuell in HTTP-Methoden-Heuristik — `backend/web/main.py:452@77c1a97` — Impact: Sonderfälle wie Export, Upload-Intent oder Reset lassen sich schwer erklären, wenn sie später bewusst CLI-fähig werden. — Fix: Verwende eine kleine zentrale Capability-Tabelle mit Pfadpattern, Methode, Scope und Dokumentationshinweis.

## TESTS

- Für diese Review-Runde nicht erneut ausgeführt; es war ein statischer Review nach `git fetch`.
- Vorhandene lokale Review-Dokumentation nennt zuletzt: `68 passed, 2 skipped in 7.97s`.

## Refactor-Budget

Mittel: Branch-Sync ist Git-Arbeit. Die sauberste technische Korrektur ist eine kleine CLI-Capability-Tabelle plus Contract-/Runtime-Tests; das bleibt überschaubar und unterstützt spätere CLI-Erweiterungen besser als eine harte Ad-hoc-Allowlist.

## Freigabe

Keine Freigabe aus diesem divergierten Stand. Nach Sync mit `origin/master` und contract-first Dokumentation der gewünschten CLI-Reichweite ist das Feature grundsätzlich reviewfähig.

## Reparaturplan – 2026-05-11 20:44

### 1. Divergierten `master` sauber integrieren

- Finding: `[CONSISTENCY] Lokaler master ist 2 Commits vor und 2 Commits hinter origin/master`.
- Warum offen: `git rev-list --left-right --count origin/master...master` zeigt weiterhin `2 2`; `origin/master` enthält zusätzlich das Ticket `auth-session-continuity-classroom-regression-2026-05-11.md`.
- Regeln/Kontext: Public-Repo-Git-Workflow; keine History-Rewrites auf `master`. Owner: Repo-/Release-Disziplin.
- Dateien: voraussichtlich nur Merge-Metadaten plus Remote-Ticketdatei; bestehende lokale PR-Fix-Datei bleibt append-only.
- Contract/Migration: Keine.
- Test-first: Nicht anwendbar; es ist ein nicht-destruktiver Git-Sync.
- Minimaler Schritt: `origin/master` in den lokalen `master` mergen und Konflikte nur additiv lösen.
- Akzeptanz: `master` enthält die Remote-Ticketdokumentation und ist nicht mehr hinter `origin/master`.

### 2. CLI-Authoring-Capability-Tabelle einführen

- Finding: `[API] CLI-Reichweite ist runtime breiter als OpenAPI`.
- Warum offen: `_cli_bearer_allowed_for_path()` erlaubt das komplette Präfix `/api/teaching/units/...`, während mehrere darunterliegende H5P-/Datei-Endpunkte im Vertrag cookie-only sind.
- Regeln/Kontext: API Contract-First, Security first, Teaching bleibt Owner der Authoring-Oberfläche; `identity_access` erzwingt die Auth-Grenze.
- Dateien: `backend/web/main.py`, `backend/tests/test_auth_middleware.py`, `backend/tests/test_openapi_profile_contract.py`, `docs/references/gustav_cli.md`.
- Contract-first: Keine neuen CLI-Endpunkte; stattdessen wird getestet, dass Runtime-Capabilities exakt den vorhandenen OpenAPI-`cliTokenAuth`-Einträgen entsprechen.
- Test-first: Parametrisierte OpenAPI-/Runtime-Matrix; zusätzlich Negativtests für H5P-Reset, Datei-Upload-Intent und Unit-Detail mit gültigem CLI-Bearer.
- Minimaler Fix: Zentrale Tabelle mit Methode, Pfadtemplate und Scope; Middleware matcht Methode + Template statt Präfix + HTTP-Heuristik.
- Akzeptanz: Dokumentierte CLI-Endpunkte funktionieren weiter; cookie-only-Endpunkte unter demselben Präfix liefern mit CLI-Bearer `401`.

### 3. HTTPS für CLI-Base-URL erzwingen

- Finding: `[SEC] CLI akzeptiert nicht-HTTPS-Base-URLs`.
- Warum offen: `auth configure` speichert `args.base_url` ohne Prüfung.
- Regeln/Kontext: Security first; lokale Umgebung entspricht Prod und nutzt `https://app.localhost`.
- Dateien: `backend/tools/gustav_cli/cli.py`, `backend/tests/test_gustav_cli.py`, `docs/references/gustav_cli.md`.
- Contract/Migration: Keine API- oder Schemaänderung.
- Test-first: `auth configure --base-url http://...` muss fehlschlagen und keine Config schreiben; `https://...` bleibt grün.
- Minimaler Fix: URL-Schema vor Token-Speicherung prüfen und mit deutscher Fehlermeldung abbrechen.
- Akzeptanz: CLI-Tokens werden nicht versehentlich über Klartext-HTTP konfiguriert.

### 4. CLI-Token-Create-Payloads auf 400 statt 422 bringen

- Finding: `[API] Ungültige CLI-Token-Create-Payloads können FastAPI-422 statt dokumentiertem 400 liefern`.
- Warum offen: `CLITokenCreatePayload` nutzt Pydantic-Constraints, bevor Handler-Logik das Fehlerformat kontrollieren kann.
- Regeln/Kontext: API Contract-First; App-/Profil-Endpunkte gehören zu `identity_access`.
- Dateien: `backend/web/routes/app.py`, `backend/tests/test_profile_view_api.py`, `api/openapi.yml` nur falls die vorhandenen Fehlercodes präzisiert werden müssen.
- Contract-first: Vertrag bleibt bei `400` für ungültige Token-Requests.
- Test-first: Ungültige Payloads für leeres Label, leere Scopes, falschen Scope und `ttl_days` außerhalb `1..90` erwarten `400` mit Error-Schema.
- Minimaler Fix: Lose Pydantic-Felder akzeptieren und im Handler normalisieren/validieren.
- Akzeptanz: Direkte Clients sehen vertragskonforme `400`-Antworten statt FastAPI-`422`.

## Umsetzungsstand – 2026-05-11 20:56

Status: umgesetzt.

Geschlossene Findings:

- `[CONSISTENCY]` Lokaler `master` wurde per Merge mit `origin/master` synchronisiert. Ergebnis: `git rev-list --left-right --count origin/master...master` zeigt `0 3`; die Remote-Ticketdatei ist enthalten.
- `[API]` CLI-Reichweite ist jetzt explizit modelliert: `backend/web/main.py` nutzt eine `CLI_AUTHORING_CAPABILITIES`-Tabelle mit Methode, OpenAPI-Pfadtemplate und Scope statt einer Präfix-Allowlist.
- `[TEST]` OpenAPI und Runtime werden durch eine Contract-Matrix verglichen. Cookie-only Teaching-Pfade unter `/api/teaching/units/...` werden mit CLI-Bearer vor dem Handler als `401` abgelehnt.
- `[SEC]` `gustav auth configure` akzeptiert nur noch `https://`-Base-URLs und schreibt bei ungültiger URL keine Config.
- `[API]` Ungültige CLI-Token-Create-Payloads liefern jetzt vertragskonform `400` mit GUSTAV-Error-Schema statt FastAPI-`422`.

Zusätzlicher Safety-Fund:

- Das gemergte Remote-Ticket enthielt eine echte Produktionsdomain. Der Public-Repo-Safety-Test fand dies im ersten `make verify`-Lauf. Die Domain wurde zu `gustav.example` anonymisiert; der isolierte Safety-Test lief danach grün.

Verifikation:

- Rotlauf vor Implementierung: `.venv/bin/pytest -q backend/tests/test_auth_middleware.py backend/tests/test_openapi_profile_contract.py backend/tests/test_gustav_cli.py backend/tests/test_profile_view_api.py` ergab `10 failed, 53 passed`.
- Grünlauf nach Implementierung: dieselbe Testgruppe ergab `63 passed in 3.24s`.
- Safety-Regressionslauf nach Ticket-Anonymisierung: `.venv/bin/pytest -q backend/tests/test_public_repo_safety_contract.py::test_public_repo_contains_no_production_domain` ergab `1 passed in 0.56s`.
- Abschlussprüfung: `make verify` ergab `1615 passed, 33 skipped` für pytest, `18 passed` für H5P Node-Tests, `5 passed` für Supabase-Integration, `2 passed` für OpenAI-kompatiblen Endpoint-Smoke und `13 passed` für E2E.
- `git diff --check` ist sauber.

Architektur- und Kontextentscheidungen:

- `identity_access` bleibt verantwortlich für Tokenprüfung und Scope-Grenzen.
- `teaching` bleibt fachlicher Owner der Authoring-Endpunkte; die CLI bekommt keine implizite Freigabe für H5P-Authoring oder Datei-Uploads.
- Neue CLI-Erweiterungen müssen künftig OpenAPI-`cliTokenAuth`, Scope und Runtime-Capability gemeinsam ergänzen.

Verbleibendes Risiko:

- Kein offener MUST-FIX aus dieser Runde.
- Rest-Risiko: Die Capability-Tabelle ist bewusst dupliziert zum OpenAPI-Vertrag, wird aber durch den neuen Contract-Test gegen Drift abgesichert.

Freigabeempfehlung:

- Reviewfähig nach Commit/Push. Vor Merge bleibt die übliche PR-Prüfung gegen den finalen Remote-Stand sinnvoll.

## Nachtrag Units-Delete-DSN – 2026-05-11 21:00

Auslöser: Der CLI-Smoke-Test zeigte weiterhin `503 storage_adapter_unavailable` bei
`units delete`. Der laufende Webcontainer hatte effektiv
`SERVICE_ROLE_DSN=postgresql://postgres:***@127.0.0.1:54322/postgres`.

Root Cause:

- Der vorherige Fix setzte in `docker-compose.yml` nur einen Compose-Default:
  `SERVICE_ROLE_DSN=${SERVICE_ROLE_DSN:-...@supabase_db_gustav-alpha2:5432/...}`.
- Die lokale `.env` definiert `SERVICE_ROLE_DSN` für Host-Prozesse mit
  `127.0.0.1:54322`. Docker Compose verwendet diesen Wert vor dem Default.
- Der vorhandene Regressionstest prüfte nur die YAML-Vorlage, nicht die
  Überschreibbarkeit durch `.env`.

Umsetzung:

- `backend/tests/test_compose_keycloak_postgres.py` prüft jetzt zusätzlich, dass
  der Webcontainer-`SERVICE_ROLE_DSN` nicht mehr durch `${SERVICE_ROLE_DSN...}`
  aus der Host-`.env` ersetzt werden kann.
- `docker-compose.yml` setzt den Webcontainer-`SERVICE_ROLE_DSN` fest auf den
  containerinternen Host `supabase_db_gustav-alpha2:5432`.
- `docs/references/config_matrix.md` dokumentiert den Unterschied zwischen
  Host-DSNs für pytest und Container-DSNs für Compose.

Verifikation:

- Rotlauf: `.venv/bin/pytest -q backend/tests/test_compose_keycloak_postgres.py::test_web_service_sets_container_service_role_dsn_for_storage_cleanup` fiel mit der erwarteten `${SERVICE_ROLE_DSN...}`-Assertion.
- Grünlauf: derselbe Test ergab `1 passed in 0.48s`.
- `docker compose config` zeigt für den `web`-Service
  `SERVICE_ROLE_DSN=...@supabase_db_gustav-alpha2:5432/postgres`.
- Laufender Webcontainer nach Recreate: `SERVICE_ROLE_DSN=...@supabase_db_gustav-alpha2:5432/postgres`.
- CLI-Smoke: neue Test-Unit `60e7e7be-52a5-42ba-95b7-6dc5454ef9c8` wurde per
  CLI erstellt und anschließend mit `units delete ... --yes` erfolgreich gelöscht.
