# PR Review: master vs origin/master

## Review Round - 16:43 CEST

Datum: 2026-05-14 16:43 CEST

Branch: `master`
Base ref: `origin/master`
Base SHA: `f85a7906b6cac9da3928c2ada29dcf368409777f`
Merge-base SHA: `f85a7906b6cac9da3928c2ada29dcf368409777f`
Head SHA: `756750b1684987977be121aac0839dfe28d2fed0`

Reviewed Scope:

- Auth-Session-Continuity in SvelteKit BFF, geschützten Loadern/Actions und Keycloak-Templates.
- Learning Upload Content-Signature Validation in API, Storage-Policy, Worker-Adaptern, Frontend-Meldung, OpenAPI und Docs.
- Visual-Feedback PNG-Normalisierung und Provider-Rate-Limit-Klassifikation.
- Relevante Tests, Referenzdokumentation und Tickets.

Context Notes:

- Lokaler `master` ist gegenüber `origin/master` um fünf Commits voraus.
- Review-Basis war der Diff `origin/master..master`.
- Kein `git fetch` ausgeführt; `origin/master` ist der lokale Remote-Tracking-Stand.
- `git diff --check origin/master..master` ist ohne Ausgabe erfolgreich.

Code References:

- `frontend/src/lib/server/api.ts:108`
- `frontend/src/lib/server/api.test.ts:121`
- `backend/storage/learning_policy.py:44`
- `backend/learning/adapters/local_vision.py:305`
- `backend/learning/adapters/local_vision.py:407`
- `backend/web/routes/learning.py:26`
- `backend/web/routes/learning.py:1514`
- `backend/web/routes/learning.py:1577`
- `frontend/src/routes/protected-page-bootstrap-contract.test.ts:55`

## Kurzfazit

Die Richtung ist sinnvoll: Uploads werden früher validiert, Auth-Recovery wird robuster, und die Visual-Pipeline wird providerfreundlicher. Vor Freigabe müssen aber zwei Punkte raus: ein DSGVO-relevantes Redirect-Logging und ein Worker-Bruch im Dev-Upload-Stub-Fallback. Zusätzlich sollte die Task-Kind-Validierung aus der Web-Route herausgezogen werden.

## MUST-FIX

- [SEC] Auth-Continuity loggt den vollständigen Redirect-Pfad inklusive Query — `frontend/src/lib/server/api.ts:108@756750b` — Impact: geschützte Pfade können `studentSub`, Kurs-/Unit-IDs oder Suchparameter enthalten; der eigene Plan verbietet Request-URLs mit Query in Auth-Logs. — Fix: Logge nur `reason` und einen niedrig-kardinalen Recovery-Typ; entferne `redirect` aus `console.info` und passe `frontend/src/lib/server/api.test.ts:121@756750b` entsprechend an.
- [BUG] Worker liest den neuen Upload-Stub-Default-Root nicht — `backend/learning/adapters/local_vision.py:305@756750b` — Impact: Die API akzeptiert bei `ENABLE_DEV_UPLOAD_STUB=true` Uploads aus `.tmp/dev_uploads`, weil `resolve_local_verify_root_from_env()` greift; der Worker verwendet aber weiter nur `STORAGE_VERIFY_ROOT` und kann diese Jobs nicht verarbeiten. — Fix: Verwende `resolve_local_verify_root_from_env()` in allen lokalen Worker-Lesepfaden für Bild/PDF/SB3/HEX/FLS und ergänze einen Worker-Test ohne gesetztes `STORAGE_VERIFY_ROOT`.

## SHOULD-FIX

- [CONSISTENCY] Web-Route importiert eine private Repo-Funktion und lädt Task-Kind mehrfach — `backend/web/routes/learning.py:26@756750b` — Impact: Die Route hängt an einer privaten DB-Repo-Implementierung und ruft `get_task_kind_for_student` für dieselbe Submission bis zu viermal auf; das verletzt die Clean-Architecture-Grenze und erzeugt unnötige DB-/Policy-Arbeit. — Fix: Verschiebe `_validate_task_submission_kind` in ein gemeinsames Domain-/Policy-Modul und ermittle `task_kind_for_submission` einmal, bevor Spezialvalidatoren laufen.
- [TEST] Auth-Redirect-Contract deckt nur drei geschützte Seiten ab — `frontend/src/routes/protected-page-bootstrap-contract.test.ts:55@756750b` — Impact: Viele Loader/Actions wurden um `authRedirectPath` erweitert, aber der Regressionstest schützt nur einen kleinen Teaching-Ausschnitt; Learning/Profile/Live/Diagnostics können wieder aus dem Recovery-Pfad fallen. — Fix: Prüfe alle geschützten Server-Load-Dateien mit Backend-Aufrufen oder ergänze repräsentative Tests je Space.

## NICE-TO-HAVE

- [CONSISTENCY] Die neue Upload-Validierung wiederholt lokale Root-Auflösung an mehreren Stellen — `backend/storage/learning_policy.py:44@756750b` — Impact: API und Worker können erneut auseinanderlaufen, wenn ein weiterer Upload-Pfad hinzukommt. — Fix: Kapsle Root-Auflösung und lokale Bytes-Ladung in einem gemeinsamen Storage-Helper statt in Route und Adapter getrennt.

## TESTS

- Ausgeführt: `git diff --check origin/master..master`
- Nicht ausgeführt: Backend-/Frontend-Test-Suites. Der Review ist ein statischer Diff-Review; die MUST-FIX-Punkte sollten vor `make verify` behoben und gezielt getestet werden.

## Refactor-Budget

0,5 bis 1 Tag: Redirect-Log entfernen und Tests anpassen ist klein. Worker-Root-Konsolidierung plus Regressionstest ist überschaubar, sollte aber alle lokalen Read-Pfade in `local_vision.py` sauber mitnehmen.

## Freigabe

Keine Freigabe vor den beiden MUST-FIX-Punkten. Danach reicht ein fokussierter Testlauf für `frontend/src/lib/server/api.test.ts`, die neue Worker-Root-Regression und die bestehenden Upload-Signature-Tests; anschließend `make verify`.

## Reparaturplan - 2026-05-14 17:07

### Arbeitspaket 1: Auth-Continuity-Log sanitizen

- Finding: `[SEC] Auth-Continuity loggt den vollständigen Redirect-Pfad inklusive Query`.
- Warum offen: `frontend/src/lib/server/api.ts` loggt weiterhin `redirect: authRedirectPath`.
- Regeln/Kontext: Security first, DSGVO, `identity_access`; Auth-Logs dürfen keine Request-URL mit Query enthalten.
- Dateien: `frontend/src/lib/server/api.test.ts`, `frontend/src/lib/server/api.ts`.
- Contract/Migration/Docs: Keine OpenAPI-, DB- oder Config-Änderung.
- Test-first: Erwartung in `api.test.ts` ändern, sodass `console.info("auth.continuity", ...)` keinen `redirect`-Key mehr enthält.
- Minimaler Fix: Redirect-Feld aus dem Log-Payload entfernen; Browser-Redirect bleibt unverändert.
- Acceptance: fokussierter Vitest-Lauf für `src/lib/server/api.test.ts`.

### Arbeitspaket 2: Worker liest denselben lokalen Upload-Root wie API

- Finding: `[BUG] Worker liest den neuen Upload-Stub-Default-Root nicht`.
- Warum offen: API nutzt `resolve_local_verify_root_from_env()`, Worker-Pfade in `local_vision.py` lesen nur `STORAGE_VERIFY_ROOT`.
- Regeln/Kontext: `learning`, local=prod, keine divergierenden lokalen Sonderpfade; Storage-Validierung muss in API und Worker dieselbe Root-Auflösung verwenden.
- Dateien: `backend/tests/learning_adapters/test_local_vision_dev_upload_root.py`, `backend/learning/adapters/local_vision.py`.
- Contract/Migration/Docs: Keine OpenAPI-, DB- oder Config-Änderung.
- Test-first: Neuer Adapter-Test ohne `STORAGE_VERIFY_ROOT`, mit `ENABLE_DEV_UPLOAD_STUB=true`, der ein valides PNG unter `.tmp/dev_uploads/...` schreibt und beweist, dass `extract()` lokale Bytes verarbeitet.
- Minimaler Fix: `resolve_local_verify_root_from_env()` in `local_vision.py` verwenden und alle lokalen Image/PDF/SB3/HEX/FLS-Lesepfade darüber routen.
- Acceptance: neuer Test plus relevante bestehende `local_vision`-Tests.

### Arbeitspaket 3: Task-Kind-Guard aus DB-Repo-Privatfunktion herausziehen

- Finding: `[CONSISTENCY] Web-Route importiert eine private Repo-Funktion und lädt Task-Kind mehrfach`.
- Warum offen: `backend/web/routes/learning.py` importiert `_validate_task_submission_kind` aus `repo_db.py` und ruft `get_task_kind_for_student` in den Spezialvalidatoren erneut auf.
- Regeln/Kontext: Clean Architecture, `learning`; Fachpolicy gehört in den Learning-Kontext, nicht als private DB-Repo-Funktion in den Web-Adapter.
- Dateien: `backend/learning/submission_kind_policy.py`, `backend/learning/repo_db.py`, `backend/web/routes/learning.py`, `backend/tests/test_learning_submission_kind_guard.py`.
- Contract/Migration/Docs: Keine OpenAPI-, DB- oder Config-Änderung; Fehlercodes bleiben gleich.
- Test-first: Submission-Kind-Guard-Test auf das neue Policy-Modul umstellen.
- Minimaler Fix: Policy-Funktion extrahieren, Repo und Route darauf umstellen, Task-Kind in der Route einmal laden und wiederverwenden.
- Acceptance: Guard-Test und fokussierte Upload-API-Tests bleiben grün.

### Arbeitspaket 4: Auth-Redirect-Contract-Test auf alle geschützten Backend-Aufrufe ausweiten

- Finding: `[TEST] Auth-Redirect-Contract deckt nur drei geschützte Seiten ab`.
- Warum offen: Der Test iteriert nur drei Teaching-Seiten, obwohl viele geschützte Loader/Actions `backendRequest()` oder `requireBackendJson()` verwenden.
- Regeln/Kontext: `identity_access` über alle SvelteKit-Spaces; recoverbare 401 dürfen nicht wieder zu generischen Fehlern werden.
- Dateien: `frontend/src/routes/protected-page-bootstrap-contract.test.ts` und nur bei Testfund ggf. betroffene `+page.server.ts`.
- Contract/Migration/Docs: Keine OpenAPI-, DB- oder Config-Änderung.
- Test-first: Contract-Test so erweitern, dass alle `protectedPages` mit geschützten Backend-Aufrufen geprüft werden.
- Minimaler Fix: Fehlende `authRedirectPath`-Weitergaben in geschützten Loadern/Actions ergänzen, falls der neue Test sie findet.
- Acceptance: fokussierter Vitest-Lauf für `protected-page-bootstrap-contract.test.ts`.

## Umsetzung - 2026-05-14 17:19

### Ergebnis

- `[SEC]` behoben: `auth.continuity` loggt nur noch den niedrig-kardinalen Grund `app_session_active_without_bearer`; der konkrete Redirect-Pfad bleibt nur im Redirect selbst.
- `[BUG]` behoben: `local_vision.py` nutzt jetzt `resolve_local_verify_root_from_env()` für Image-, PDF-, SB3-, HEX- und FLS-Lesepfade. Damit lesen API und Worker bei `ENABLE_DEV_UPLOAD_STUB=true` denselben `.tmp/dev_uploads`-Fallback.
- `[CONSISTENCY]` behoben: Die Task-Kind-Submission-Regel liegt in `backend/learning/submission_kind_policy.py`; Repo und Web-Route nutzen diese Policy. Die Upload-Route lädt `task_kind_for_submission` einmal und verwendet den Wert in den Spezialvalidatoren wieder.
- `[TEST]` behoben: `protected-page-bootstrap-contract.test.ts` prüft jetzt alle `protectedPages` mit `requireBackendJson`-/`backendRequest`-Aufrufen.

### TDD-Nachweis

- Red: `frontend/src/lib/server/api.test.ts` erwartete keinen `redirect`-Key und schlug gegen den alten Log-Payload fehl.
- Red: `backend/tests/learning_adapters/test_local_vision_dev_upload_root.py` schlug mit `VisionTransientError("image_unavailable")` fehl, solange der Worker nur `STORAGE_VERIFY_ROOT` las.
- Red: `backend/tests/test_learning_submission_kind_guard.py` schlug beim neuen Policy-Import fehl, bevor das Policy-Modul existierte.
- Red: `backend/tests/test_learning_upload_content_signature_validation.py::test_specialized_file_validation_reuses_loaded_task_kind` zeigte `2 == 1` Task-Kind-Reads vor dem Routen-Refactor.
- Green: Alle oben genannten Tests sind nach den minimalen Fixes grün.

### Verifikation

- `.venv/bin/pytest -q backend/tests/learning_adapters/test_local_vision_dev_upload_root.py backend/tests/learning_adapters/test_local_vision.py backend/tests/learning_adapters/test_local_vision_pdf_cached_paths.py backend/tests/learning_adapters/test_local_vision_remote_fetch.py backend/tests/test_learning_submission_kind_guard.py backend/tests/test_learning_upload_content_signature_validation.py` → `50 passed`.
- `npm test -- --run src/lib/server/api.test.ts src/routes/protected-page-bootstrap-contract.test.ts` → `10 passed`.
- `git diff --check` → ohne Ausgabe.
- `make verify` → grün: `1653 passed, 33 skipped` Backend, `18 passed` H5P Node, `5 passed` Supabase-Integration, `2 passed` OpenAI-kompatibler Smoke, `13 passed` E2E.
