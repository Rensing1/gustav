# PR Review: master vs origin/master

## Review Round - 12:31

Datum/Zeit: 2026-05-15 12:31:14 CEST

Branch: `master`
Base ref: `origin/master`
Lokaler Head: `b08ed2fff44519f91058b1b9020b50a73ea52ff3`
Origin Head: `9d64d944b9e58c0d865f0ae9273f21a866c663db`
Merge-Base: `f85a7906b6cac9da3928c2ada29dcf368409777f`

Geprüfter Scope:

- Lokaler `master` gegen Merge-Base: 89 Dateien, 3253 Einfügungen, 460 Löschungen.
- `origin/master` gegen Merge-Base: 1 Datei, 49 Einfügungen.
- Der lokale Branch ist 8 Commits vor und 1 Commit hinter `origin/master`; die Review behandelt die Divergenz als eigenes Integrationsrisiko.

Kontextnotizen:

- Schwerpunkt der lokalen Änderungen: Lern-Upload-Signaturprüfung, Session-Continuity, Feedback-Fehlerabbildung, Keycloak-Theme-Fehlerzustände und zugehörige Tests/Dokumentation.
- Sicherheitscheck im Scope: Auth-Logging, PII in Logs, Session-Weiterleitung, Upload-Validierung, Cache-/No-Store-Verhalten, CSRF-nahe Uploadpfade.
- Es wurden keine Tests ausgeführt; die Review basiert auf Git-Diff- und Quelltextinspektion.

Code-Referenzen:

- `backend/web/main.py:646@b08ed2f`
- `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md:166@9d64d94`
- `backend/learning/adapters/local_feedback.py:72@b08ed2f`
- `backend/learning/adapters/local_feedback.py:73@b08ed2f`
- `api/openapi.yml:6610@b08ed2f`
- `backend/web/routes/learning.py:1720@b08ed2f`
- `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte:542@b08ed2f`
- `backend/tests/test_learning_upload_content_signature_validation.py:90@b08ed2f`

### Kurzfazit

Lokaler `master` ist gegen `origin/master` noch nicht review-ready: Der Branch ist divergiert, und die neue Auth-Telemetrie loggt rohe API-Pfade. Upload-Signaturen und Session-Continuity sind grundsätzlich nachvollziehbar, aber Provider-429-Fehler werden für große PNGs zu früh als permanent eingestuft. Vor Freigabe müssen Divergenz, DSGVO-Logging und 429-Klassifikation bereinigt werden.

### MUST-FIX

- [SEC] Auth-Middleware loggt rohe API-Pfade bei 401 — `backend/web/main.py:646@b08ed2f` — Impact: Dynamische Pfade können `student_sub`, Kurs-, Task- oder Submission-IDs enthalten und damit DSGVO-relevante Identifikatoren in Logs schreiben. — Fix: Logge nur Route-Templates oder niedrig-kardinale Pfadklassen und ergänze einen Regressionstest mit einem dynamischen, emailartigen `student_sub`-Pfad.
- [CONSISTENCY] Lokaler `master` ist nicht auf `origin/master` integriert — `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md:166@9d64d94` — Impact: Der Remote-Commit fordert noch Provider-Bildnormalisierung und längere 429-Retry-Strategie; der lokale Branch ersetzt das durch "nicht normalisieren" und permanente große-PNG-Fehler. Ohne bewusste Konfliktauflösung geht Produktentscheidung verloren. — Fix: Integriere `origin/master`, löse das Ticket fachlich eindeutig auf und reviewe danach den resultierenden Stand.
- [BUG] Große PNGs werden nach dem ersten Provider-429 permanent fehlgeschlagen — `backend/learning/adapters/local_feedback.py:72@b08ed2f` — Impact: Ein temporärer Provider-Engpass bei einem gültigen großen PNG wird sofort zu `feedback_failed`, obwohl Recovery noch plausibel sein kann. — Fix: Stufe erst dann permanent ein, wenn ein provider-spezifischer stabiler Fehlercode oder wiederholte deterministische Evidenz vorliegt; sonst bleibt der Fehler transient.

### SHOULD-FIX

- [CONSISTENCY] Rate-Limit-Diagnostik erfüllt die eigene Ticket-Anforderung nicht vollständig — `backend/learning/adapters/local_feedback.py:73@b08ed2f` — Impact: Logs enthalten Status, Retry-After und Request-ID, aber nicht Provider-Fehlertyp/-Code, Basis-Host, Stage/Modality oder nicht-geheime Provider-Labels; Operations kann Mistral `code=1300` kaum von normaler Quote unterscheiden. — Fix: Ergänze nur sichere Exception-Felder und keine Prompts, Objekt-Keys, Hashes oder API-Secrets.
- [API] OpenAPI dokumentiert frontend-interne Auth-Gründe als Backend-401-Codes — `api/openapi.yml:6610@b08ed2f` — Impact: Contract-Leser können erwarten, dass `/api/app/session-bootstrap` `token_refresh_failed` oder `app_session_active_bff_bearer_unavailable` als Backend-Antwort liefert, obwohl diese Gründe nur im Frontend entstehen. — Fix: Verschiebe sie in Observability-Doku oder liefere einen echten, getesteten Reason-Code im Backend-Response/Header.
- [BUG] Dev-PDF-Hook ignoriert den neuen Default-Upload-Stub-Root — `backend/web/routes/learning.py:1720@b08ed2f` — Impact: Uploads über `ENABLE_DEV_UPLOAD_STUB=true` ohne `STORAGE_VERIFY_ROOT` validieren aus `.tmp/dev_uploads`, triggern aber kein lokales PDF-Rendering. — Fix: Nutze `resolve_local_verify_root_from_env()` auch im PDF-Hook und ergänze einen Test mit unset `STORAGE_VERIFY_ROOT`.
- [BUG] Workspace-URL behält stale `history`-Query beim Modul-/View-Wechsel — `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte:542@b08ed2f` — Impact: Refresh oder Auth-Continuation kann eine alte Aufgabenhistorie nachladen/fokussieren, obwohl der Nutzer in ein anderes Modul gewechselt ist. — Fix: Lösche `history` beim normalen Workspace-Wechsel und setze es nur beim expliziten Öffnen einer Historie.

### NICE-TO-HAVE

- [TEST] Content-Signature-API-Test mockt Storage-Bytes und Repo vollständig — `backend/tests/test_learning_upload_content_signature_validation.py:90@b08ed2f` — Impact: Der neue Fail-Closed-Pfad ist gut unit-getrieben, aber der produktionsnahe Presign-/Storage-Adapter-Pfad bleibt nur indirekt abgesichert. — Fix: Ergänze einen kleinen Integrationstest mit echter lokaler Storage-Struktur oder bestehender Supabase-Storage-Fixture.

### TESTS

Nicht ausgeführt. Für diese Review wurden Diff, Quelltext, OpenAPI-Ausschnitte und betroffene Tests gelesen.

### Refactor-Budget

Ca. 1-2 Stunden für Auth-Log-Sanitizing plus Tests, 2-4 Stunden für 429-Entscheidung/Dokumentationskonflikt, je ca. 30 Minuten für OpenAPI-Klarstellung, Dev-PDF-Root und stale `history`-Query.

### Freigabe

Keine Freigabe. Erst `origin/master` integrieren und die drei MUST-FIX-Punkte beheben.

## Reparaturplan – 2026-05-15 12:42

### Paket 1 – Auth-Logging ohne rohe Pfade

- Finding: `[SEC] Auth-Middleware loggt rohe API-Pfade bei 401`.
- Warum offen: `backend/web/main.py` schreibt weiterhin `path=%s` mit dem vollständigen Request-Pfad.
- Regeln/Kontext: `identity_access`; Security first, DSGVO-Minimierung, keine PII in Logs.
- Voraussichtliche Dateien: `backend/web/main.py`, `backend/tests/test_session_bootstrap_api.py`.
- Vertrag/Migrationen: Keine OpenAPI- oder DB-Änderung nötig.
- Test-first: Erweitere den Auth-Test um einen dynamischen API-Pfad mit PII-ähnlichem `student_sub`; der rohe Pfad darf nicht im Log erscheinen.
- Minimaler Fix: Ersetze `path` im Auth-Failure-Log durch eine niedrig-kardinale Pfadklasse.
- Acceptance: Auth-Test grün, Log enthält Reason und Path-Klasse, aber keine dynamischen Identifikatoren.

### Paket 2 – Session-Bootstrap-Vertrag bereinigen

- Finding: `[API] OpenAPI dokumentiert frontend-interne Auth-Gründe als Backend-401-Codes`.
- Warum offen: `api/openapi.yml` listet `token_refresh_failed` und `app_session_active_bff_bearer_unavailable` noch unter Backend-401.
- Regeln/Kontext: `identity_access`; API Contract-First, Auth-Referenz als Observability-Doku.
- Voraussichtliche Dateien: `api/openapi.yml`, `backend/tests/test_openapi_session_bootstrap_contract.py`, `docs/references/auth_sessions_and_cookies.md`.
- Vertrag/Migrationen: OpenAPI zuerst korrigieren; keine Migration.
- Test-first: Contract-Test erwartet nur echte Backend-Gründe; Referenz dokumentiert Frontend-Observability separat.
- Minimaler Fix: Entferne frontend-interne Gründe aus dem OpenAPI-Extension-Block und ergänze sie in der Auth-Referenz.
- Acceptance: Contract-Test grün und Auth-Referenz erklärt die BFF-seitigen Gründe.

### Paket 3 – Provider-429-Entscheidung konsistent dokumentieren und absichern

- Finding: `[CONSISTENCY] Divergenz zu origin/master` und Review-Annahme `[BUG] große PNGs permanent zu früh`.
- Warum offen: Remote-Dokumentation fordert Bildnormalisierung; aktuelle Produktentscheidung ist Logging plus konkrete Lernendenmeldung ohne automatische Bildumschreibung.
- Regeln/Kontext: `learning`; KISS, keine nicht validierte Provider-Umschreibung, PII-freie Telemetrie.
- Voraussichtliche Dateien: `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md`, `backend/learning/adapters/local_feedback.py`, zugehörige Adaptertests.
- Vertrag/Migrationen: Keine OpenAPI- oder DB-Änderung.
- Test-first: Erweitere Adaptertests um sichere Provider-Fehlertyp/-Code-Diagnostik; bestehende Tests für große PNGs als `image_too_complex_for_provider` bleiben bewusst gültig.
- Minimaler Fix: Logge sichere Felder `provider_error_type` und `provider_error_code`; markiere die Review-Annahme als durch Produktentscheidung überholt.
- Acceptance: Tests zeigen kleine PNG-429 transient, große PNG-429 mit konkretem internen Grund, Logs ohne Payload/Storage-Key/Hash.

### Paket 4 – PDF-Hook nutzt denselben lokalen Upload-Root

- Finding: `[BUG] Dev-PDF-Hook ignoriert Default-Upload-Stub-Root`.
- Warum offen: Der Hook liest nur `STORAGE_VERIFY_ROOT`, während Upload-Validierung `resolve_local_verify_root_from_env()` nutzt.
- Regeln/Kontext: `learning`; lokal = prod-kompatible ENV-Mechanik, keine zweite Root-Semantik.
- Voraussichtliche Dateien: `backend/web/routes/learning.py`, `backend/tests/test_learning_pdf_processing_hook.py`.
- Vertrag/Migrationen: Keine OpenAPI- oder DB-Änderung.
- Test-first: Ergänze einen Test mit `ENABLE_DEV_UPLOAD_STUB=true`, unset `STORAGE_VERIFY_ROOT` und Datei unter `.tmp/dev_uploads`.
- Minimaler Fix: Nutze `resolve_local_verify_root_from_env()` im PDF-Hook.
- Acceptance: PDF-Hook läuft mit explizitem Root und Stub-Default-Root, bleibt in prod-artiger Umgebung deaktiviert.

### Paket 5 – Stale History-Query beim Workspace-Wechsel löschen

- Finding: `[BUG] Workspace-URL behält stale history-Query`.
- Warum offen: `syncModularWorkspaceUrl()` löscht `history` nur, wenn keine History existiert; bestehender Contract-Test erwartet dieses Verhalten noch.
- Regeln/Kontext: Svelte-Learning-UI; `docs/DESIGN.md` bleibt unberührt, da nur URL-State geändert wird.
- Voraussichtliche Dateien: `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte`, `page-contract.test.ts`.
- Vertrag/Migrationen: Keine OpenAPI- oder DB-Änderung.
- Test-first: Contract-Test erwartet `next.searchParams.delete("history")` im normalen Workspace-Sync.
- Minimaler Fix: History-Query bei Workspace-Sync immer entfernen; explizite Historienlinks setzen sie weiterhin serverseitig.
- Acceptance: Frontend-Contract-Test grün.

### Verifikation

- Backend: `.venv/bin/pytest -q backend/tests/test_session_bootstrap_api.py backend/tests/test_openapi_session_bootstrap_contract.py backend/tests/learning_adapters/test_local_feedback_visual_pipeline.py backend/tests/learning_adapters/test_local_feedback_dspy.py backend/tests/test_learning_pdf_processing_hook.py`
- Frontend: `cd frontend && npm test -- --run src/routes/learning/courses/[courseId]/units/[unitId]/page-contract.test.ts`
- Optional breiter Abschluss: `make verify`, wenn die fokussierten Checks sauber sind.

## Umsetzungsstand – 2026-05-15 12:51

### Erledigt

- Auth-401-Logging schreibt keine rohen Request-Pfade mehr, sondern nur noch `path_class` mit niedriger Kardinalität.
- Der öffentliche `/api/app/session-bootstrap`-401-Vertrag enthält nur noch Backend-Gründe: `missing_bearer`, `invalid_bearer`, `bff_session_missing`.
- Die Auth-Referenz trennt Backend-Response-Codes von BFF-internen Observability-Gründen.
- Provider-429-Diagnostik loggt zusätzlich sichere Felder `provider_error_type` und `provider_error_code`; automatische Bildnormalisierung bleibt nach Produktentscheidung bewusst ein späterer, provider-validierter Folgepunkt.
- Der Dev-PDF-Hook verwendet denselben lokalen Upload-Root wie Upload-Validierung und Upload-Stub.
- Der Learning-Workspace-Sync löscht stale `history`-Query-Parameter bei normalen View-/Modulwechseln.

### Tests

- Rot vor Fix: fokussierte Backend-Tests mit 5 erwarteten Fehlern; Frontend-Vertragstest mit erwarteter `history`-Assertion rot.
- Grün nach Fix: `.venv/bin/pytest -q backend/tests/test_session_bootstrap_api.py backend/tests/test_openapi_session_bootstrap_contract.py backend/tests/learning_adapters/test_local_feedback_visual_pipeline.py backend/tests/learning_adapters/test_local_feedback_dspy.py backend/tests/test_learning_pdf_processing_hook.py` → `37 passed, 2 warnings`.
- Grün nach Fix: `cd frontend && npm test -- --run 'src/routes/learning/courses/[courseId]/units/[unitId]/page-contract.test.ts'` → `13 passed`.
- Grün breit: `make verify` → vollständiger Lauf erfolgreich, unter anderem `1657 passed, 33 skipped`, H5P-Node-Tests `18 passed`, Supabase-Integration `5 passed`, OpenAI-kompatibler Smoke-Test `2 passed`, E2E `13 passed`.

### Hinweise

- Der erste Root-Aufruf `npm test ...` war kein gültiger Repo-Befehl, weil das Frontend-Script unter `frontend/` liegt.
- Der Frontend-Vertragstest brauchte in der Sandbox eine eskalierte Wiederholung, weil `localhost` dort mit `EAI_AGAIN` nicht auflösbar war; der Test selbst ist grün.
- Die lokale Branch-Ancestry bleibt unverändert `ahead 8, behind 1` gegenüber `origin/master`; diese Umsetzung bereinigt die fachlichen Findings, führt aber keinen Merge/Commit durch.

## Review Round – 13:00

Datum/Zeit: 2026-05-15 13:00:34 CEST

Branch: `master`
Base ref: lokaler `master`
Base SHA: `7cd3709b8f45fa1d736654ae8f1e93129637aa10`
Merge-base SHA: `7cd3709b8f45fa1d736654ae8f1e93129637aa10`
Head SHA: `7cd3709b8f45fa1d736654ae8f1e93129637aa10`
Origin SHA: `9d64d944b9e58c0d865f0ae9273f21a866c663db`
Origin merge-base SHA: `f85a7906b6cac9da3928c2ada29dcf368409777f`

### Reviewed Scope Summary

- Formaler Skill-Diff `merge-base(HEAD, master)..HEAD`: leer, weil `HEAD == master`.
- Ergänzend geprüft: Divergenz `HEAD...origin/master`; lokaler Branch ist `ahead 9, behind 1`.
- Follow-up-Commit `7cd3709` geprüft: Auth-Logging, Session-Bootstrap-Vertrag, Provider-429-Diagnostik, PDF-Hook, Learning-Workspace-URL-State und zugehörige Tests.
- Branch-weiter Diff gegenüber `origin/master`-Merge-Base: 91 Dateien, 3499 Insertions, 467 Deletions.

### Kontextnotizen

- Arbeitsbaum war vor dem Anhängen dieses Reviews sauber.
- `git diff --check 7cd3709^..7cd3709` ist sauber.
- `git diff --check f85a7906..HEAD` schlägt wegen trailing whitespace in `docs/plan/2026-05-14-PR-fix.md:7-10` fehl.
- Die breite Verifikation `make verify` war unmittelbar vor dem Commit `7cd3709` für denselben Dateistand grün; in dieser Review-Runde wurde sie nicht erneut gestartet.

### Code References Used

- `backend/web/main.py:635@7cd3709`: Auth-Fehler werden über `path_class` statt rohem Pfad geloggt.
- `api/openapi.yml:6611@7cd3709`: Öffentlicher Session-Bootstrap-401-Vertrag enthält nur Backend-Gründe.
- `backend/learning/adapters/local_feedback.py:75@7cd3709`: Provider-429-Diagnostik loggt sichere Fehlerfelder.
- `backend/web/routes/learning.py:1718@7cd3709`: PDF-Hook nutzt `resolve_local_verify_root_from_env()`.
- `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte:542@7cd3709`: Workspace-Sync löscht stale `history`.
- `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md:166@9d64d94`: Remote-only Commit fordert automatische Provider-Bildnormalisierung.
- `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md:126@7cd3709`: Lokaler Head dokumentiert bewusst die Gegenentscheidung für den ersten Fix.
- `docs/plan/2026-05-14-PR-fix.md:7@f6af19f8`: Branch-weiter Whitespace-Fehler.

### Kurzfazit

Der Follow-up-Commit `7cd3709` behebt die vorherigen Code-Findings sauber; ich sehe keine neue Auth-, API- oder Learning-Regression im Fix selbst. Nicht freigabefähig ist der Stand trotzdem noch, weil `master` weiterhin gegenüber `origin/master` divergiert und ein branch-weiter Whitespace-Check in einem älteren Plan-Dokument fehlschlägt.

### MUST-FIX

- [CONSISTENCY] Branch bleibt gegenüber `origin/master` divergent — `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md:166@9d64d94` — Impact: `origin/master` enthält eine parallele Implementierungsanleitung zur Bildnormalisierung, während `HEAD` in derselben Ticket-Datei bewusst eine andere Produktentscheidung dokumentiert; ohne Merge/Rebase ist die PR-Basis fachlich und historisch nicht eindeutig. — Fix: Integriere `origin/master` bewusst, löse die Ticket-Fassung zugunsten der aktuellen Produktentscheidung oder einer neuen Entscheidung auf, und führe danach `make verify` erneut aus.

### SHOULD-FIX

- [CONSISTENCY] Branch-weiter Whitespace-Check scheitert in einem Review-Dokument — `docs/plan/2026-05-14-PR-fix.md:7@f6af19f8` — Impact: `git diff --check f85a7906..HEAD` meldet trailing whitespace in Zeilen 7-10; das ist kein Produktfehler, aber stört PR-Hygiene und automatisierbare Safety-Checks. — Fix: Entferne die trailing spaces in `docs/plan/2026-05-14-PR-fix.md:7-10`.

### NICE-TO-HAVE

Keine neuen Punkte.

### TESTS

- Nicht neu ausgeführt: `make verify`.
- Ausgeführt: `git diff --check 7cd3709^..7cd3709` → grün.
- Ausgeführt: `git diff --check f85a7906..HEAD` → rot wegen trailing whitespace in `docs/plan/2026-05-14-PR-fix.md:7-10`.
- Referenz: Unmittelbar vor `7cd3709` war `make verify` für denselben Dateistand grün.

### Refactor-Budget

0,5-1 Stunde für bewusste Branch-Integration, Ticket-Konfliktauflösung und Whitespace-Bereinigung. Kein zusätzlicher Code-Refactor ist aus diesem Review heraus nötig.

### Freigabe

Keine Freigabe, bis `origin/master` integriert und der Whitespace-Fehler bereinigt ist. Der Code-Fix-Commit selbst ist nach dieser Prüfung ohne neuen Code-Blocker.

## Reparaturplan – 2026-05-15 13:08

### Paket 1 – Review-Historie vor der Reparatur sichern

- Finding: vorhandene Review-Ergänzung war nach der Review-Runde uncommitted.
- Warum offen: Der Arbeitsbaum enthielt nur `docs/plan/2026-05-15-PR-fix.md`; die eigentliche Reparatur soll darauf aufbauen.
- Regeln/Kontext: Public-Repo-Hygiene, nachvollziehbarer Verlauf, keine stillen Überschreibungen von Review-Runden.
- Bounded Context: Dokumentation/Projektsteuerung.
- Dateien wahrscheinlich geändert: `docs/plan/2026-05-15-PR-fix.md`.
- Vertrag/Migrationen: Keine OpenAPI-, Migrations- oder Config-Änderung.
- Test-first: Git-Status prüfen und sicherstellen, dass nur die Review-Ergänzung im Commit landet.
- Minimaler Fix: Review-Ergänzung als separates Commit `docs: record PR follow-up review` sichern.
- Acceptance: Arbeitsbaum ist danach sauber; der Branch ist nur noch wegen `origin/master` divergent.

### Paket 2 – `origin/master` fachlich integrieren

- Finding: `[CONSISTENCY] Branch bleibt gegenüber origin/master divergent`.
- Warum offen: `origin/master` enthält den Remote-Commit `9d64d94` mit zusätzlicher Provider-Rate-Limit-Evidenz und einer abweichenden Empfehlung zur automatischen Bildnormalisierung.
- Regeln/Kontext: `learning`; KISS, Security first, keine nicht validierte Änderung an Lernenden-Uploads, lokale Umgebung bleibt nicht als Sonderpfad anzunehmen.
- Bounded Context: `learning`.
- Dateien wahrscheinlich geändert: `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md`.
- Vertrag/Migrationen: Keine OpenAPI-, DB- oder Config-Änderung.
- Test-first: Vorher `git log --oneline --left-right --cherry-pick HEAD...origin/master` als roten Divergenz-Check festhalten.
- Minimaler Fix: `origin/master` mit `--no-commit` integrieren, Remote-Evidenz behalten und die Implementierungsanleitung eindeutig auf die getroffene Produktentscheidung setzen: keine automatische Bildumschreibung im ersten Fix, PII-freie Diagnostik, interne Klassifikation `image_too_complex_for_provider`, öffentliche Submission bleibt `feedback_failed`.
- Acceptance: Kein Remote-only Commit bleibt offen; die Ticket-Datei enthält keine widersprüchliche Anweisung zur automatischen Normalisierung als aktuellen First-Fix.

### Paket 3 – Branch-weiten Whitespace-Check reparieren

- Finding: `[CONSISTENCY] Branch-weiter Whitespace-Check scheitert in einem Review-Dokument`.
- Warum offen: `docs/plan/2026-05-14-PR-fix.md:7-10` enthalten Markdown-Zeilumbrüche mit trailing spaces.
- Regeln/Kontext: Public-Repo-Hygiene und automatisierbare Safety-Checks.
- Bounded Context: Dokumentation/Projektsteuerung.
- Dateien wahrscheinlich geändert: `docs/plan/2026-05-14-PR-fix.md`.
- Vertrag/Migrationen: Keine OpenAPI-, DB- oder Config-Änderung.
- Test-first: `git diff --check f85a7906..HEAD` ist vor dem Fix rot.
- Minimaler Fix: Die vier trailing spaces entfernen, ohne den Inhalt umzuschreiben.
- Acceptance: `git diff --check f85a7906..HEAD` ist grün.

### Verifikation

- Divergenz: `git log --oneline --left-right --cherry-pick HEAD...origin/master`.
- Whitespace: `git diff --check f85a7906..HEAD`.
- Breite Freigabeprüfung: `make verify`, weil die Review-Freigabe explizit daran gekoppelt ist.

## Umsetzungsstand – 2026-05-15 13:14

### Geschlossen

- Review-Historie gesichert: Die vorher uncommitted Review-Runde wurde separat als `a71109a docs: record PR follow-up review` committed.
- `[CONSISTENCY] Branch bleibt gegenüber origin/master divergent`: `origin/master` wurde per `git merge --no-commit origin/master` integriert. Die zusätzliche Remote-Evidenz zu Mistral/OpenAI-kompatiblen Bild-429s bleibt in `docs/tickets/learning-visual-feedback-provider-rate-limit-2026-05-12.md` erhalten.
- Die Ticket-Dokumentation ist fachlich bereinigt: Der aktuelle First-Fix bleibt keine automatische Bildumschreibung, sondern PII-freie Diagnostik, interne Klassifikation `image_too_complex_for_provider` für screenshotartige PNG-429s und öffentlicher Code `feedback_failed`.
- `[CONSISTENCY] Branch-weiter Whitespace-Check scheitert`: Die trailing spaces in `docs/plan/2026-05-14-PR-fix.md:7-10` wurden entfernt.

### Stale oder offen

- Keine Findings wurden als stale markiert.
- Keine offenen MUST-FIX- oder SHOULD-FIX-Punkte aus dieser Review-Runde bleiben übrig.

### Tests

- Rot vor Fix: `git log --oneline --left-right --cherry-pick HEAD...origin/master` zeigte den Remote-only Commit `9d64d94`.
- Rot vor Fix: `git diff --check f85a7906b6cac9da3928c2ada29dcf368409777f..HEAD` meldete trailing whitespace in `docs/plan/2026-05-14-PR-fix.md:7-10`.
- Grün nach Fix: `git diff --check f85a7906b6cac9da3928c2ada29dcf368409777f` ohne Ausgabe.
- Grün breit: `make verify` erfolgreich, unter anderem `1657 passed, 33 skipped`, H5P-Node-Tests `18 pass`, Supabase-Integration `5 passed`, OpenAI-kompatibler Smoke-Test `2 passed`, E2E `13 passed`.

### Architektur und Kontext

- Bounded Context: `learning` für die Provider-Rate-Limit-Ticketentscheidung; Dokumentation/Projektsteuerung für PR-Fix- und Whitespace-Dateien.
- Keine API-Vertragsänderung, keine Migration und keine Config-Änderung.
- Keine Frontend- oder Design-Änderung; `docs/DESIGN.md` war damit nicht im Änderungsscope.

### Restrisiko und Empfehlung

- Restrisiko: Automatische Bildnormalisierung bleibt bewusst ein provider-verifizierter Folgepunkt, nicht Teil dieses First-Fix.
- Release-Empfehlung: Nach finalem Commit freigabefähig, sofern `HEAD...origin/master` keinen Remote-only Commit mehr zeigt.
