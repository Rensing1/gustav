# PR-Fix Review: CLI-Datei- und H5P-Authoring

## Review Round - 16:39 CEST

Datum: 2026-05-26 16:39 CEST

Branch: `feature/cli-upload-h5p`

Base ref: `master`

Base SHA: `bbf990deb84b17d995c1c7fd67d84726fadf9a9e`

Merge-Base SHA: `bbf990deb84b17d995c1c7fd67d84726fadf9a9e`

Head SHA: `bbf990deb84b17d995c1c7fd67d84726fadf9a9e`

Reviewed scope:

- Der committed Branch-Diff `merge-base..HEAD` ist leer.
- Auf Wunsch wurde die aktuelle uncommitted Arbeitskopie gegen `master` reviewt.
- Geprüfte Flächen: `api/openapi.yml`, CLI (`backend/tools/gustav_cli/cli.py`), Auth-Middleware (`backend/web/main.py`), Teaching-Routen (`backend/web/routes/teaching.py`), H5P-Sidecar (`h5p-service/server.mjs`), Docker/ENV-Dokumentation und geänderte Tests.

Kontextnotizen:

- Zeilenreferenzen mit `@bbf990d+WT` beziehen sich auf die aktuelle Arbeitskopie auf Basis von `bbf990d`; diese Stellen sind noch nicht in einem Commit enthalten.
- Ziel des Features: Datei-Materialien, H5P-Paket-Import/-Export/-Reset und Spezialaufgaben über CLI-Token nutzbar machen, ohne Browser-Cookies oder zusätzliche read-scoped Lookups für Modul-Mutationen.

Verwendete Code-Referenzen:

- `backend/web/routes/teaching.py:1617`, `backend/web/routes/teaching.py:4106`, `backend/web/routes/teaching.py:4325`, `backend/web/routes/teaching.py:4477`, `backend/web/routes/teaching.py:5015`
- `backend/web/main.py:460`
- `backend/web/config.py:21`
- `backend/tools/gustav_cli/cli.py:82`, `backend/tools/gustav_cli/cli.py:616`, `backend/tools/gustav_cli/cli.py:888`
- `h5p-service/server.mjs:53`, `h5p-service/server.mjs:287`, `h5p-service/server.mjs:458`, `h5p-service/server.mjs:609`
- `api/openapi.yml:10423`, `api/openapi.yml:10580`, `api/openapi.yml:11518`

### Kurzfazit

Die CLI-Erweiterung ist insgesamt sauber contract-first angelegt, und die gezielten Tests sind grün. Vor einer PR-Freigabe muss aber der H5P-Import im FastAPI-Web-Hop eine eigene Upload-Größenbegrenzung bekommen; sonst schützt nur der Sidecar, während der Webprozess die Datei bereits vollständig in den Speicher lädt.

### MUST-FIX

- [SEC] H5P-Import lädt Uploads ohne Web-seitiges Größenlimit vollständig in den Speicher — `backend/web/routes/teaching.py:4354@bbf990d+WT` — Impact: Ein gültiger Lehrer-/CLI-Token kann sehr große `.h5p`-Uploads an den Webprozess senden; `H5P_MAX_UPLOAD_BYTES` im Sidecar greift erst nach dem vollständigen `await file.read()` im Webprozess und schützt damit nicht vor Speicher-Druck im vorgeschalteten Dienst. — Fix: Begrenze den FastAPI-H5P-Import vor dem Forwarding auf denselben zentralen Grenzwert (`H5P_MAX_UPLOAD_BYTES`) oder streame/chunke die Datei mit harter Byte-Zählung; gib bei Überschreitung einen definierten 413/400-Fehler zurück und ergänze einen pytest-Regressionsfall für übergroße Uploads.

### SHOULD-FIX

- [SEC] Web-Startup-Guard prüft das neue `H5P_INTERNAL_SHARED_SECRET` nicht in prod-like Umgebungen — `backend/web/config.py:60@bbf990d+WT` — Impact: Die H5P-CLI-Endpunkte sind im OpenAPI-Vertrag als nutzbar dokumentiert, aber der Webdienst kann ohne passenden internen Secret starten und fällt dann erst zur Laufzeit beim Web->H5P-Hop aus; die H5P-Seite fail-fastet bereits, der Webdienst nicht. — Fix: Ergänze `ensure_secure_config_on_startup()` um einen prod/stage-Check für `H5P_INTERNAL_SHARED_SECRET` inklusive Platzhalter-Erkennung und Test.
- [API] Neue moduladressierte Authoring-Endpunkte können `503 modular_repo_unavailable` liefern, dokumentieren das aber nicht durchgängig im OpenAPI-Vertrag — `backend/web/routes/teaching.py:4133@bbf990d+WT` — Impact: Generierte Clients und API-Governance sehen für mehrere neue Modul-Task/-Material-Endpunkte keinen möglichen Service-Unavailable-Fall, obwohl die Runtime ihn explizit zurückgibt. — Fix: Ergänze die 503-Antwort in `api/openapi.yml` für die neuen moduladressierten Task-/Material-Mutationsendpunkte oder entferne den Runtime-Zweig, falls er produktiv unmöglich ist; sichere das mit dem bestehenden OpenAPI-vs-Runtime-Contract-Test ab.
- [TEST] H5P-internes Secret-Auth-Verhalten ist nur per Quelltext-Guard abgesichert — `backend/tests/test_h5p_service_hardening_contract.py:84@bbf990d+WT` — Impact: Der wichtigste neue Cookie-freie H5P-Pfad wird nicht ausführbar dagegen getestet, dass falsche/fehlende Secrets scheitern, richtige Secrets CSRF nur intern umgehen und `requireTeacher` weiterhin greift. — Fix: Ergänze einen Node-/Integrationstest oder einen schmalen ausführbaren Service-Test für `authenticateInternalTeacher`, `requireSameOrigin` und `requireTeacher`.

### NICE-TO-HAVE

- [BUG] Multipart-Header der CLI übernehmen den Dateinamen ohne Header-Encoding/Escaping — `backend/tools/gustav_cli/cli.py:97@bbf990d+WT` — Impact: Lokale Dateinamen mit Anführungszeichen oder Steuerzeichen können einen ungültigen Multipart-Body erzeugen; das ist selten, aber unnötig fragil. — Fix: Verwende eine kleine, getestete Header-Value-Sanitization für `filename` oder baue Multipart über eine robuste Standardbibliotheks-/Helper-Funktion.
- [CONSISTENCY] Zwei Modul-Resolver duplizieren fast dieselbe Autorisierungs- und Section-Auflösung — `backend/web/routes/teaching.py:4106@bbf990d+WT` — Impact: Künftige Änderungen an Autorisierung, Fehlercodes oder Repo-Fallbacks können zwischen Material/Task-Mutationen und H5P-Mutationen auseinanderlaufen. — Fix: Führe die Resolver zu einem Helper mit optionaler `task_id`-Validierung zusammen, ohne das Verhalten der bestehenden Endpunkte zu ändern.

### TESTS

- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_auth_middleware.py backend/tests/test_gustav_cli.py backend/tests/test_h5p_service_hardening_contract.py backend/tests/test_openapi_profile_contract.py`
- Ergebnis: `78 passed in 4.77s`
- Nicht ausgeführt: `make verify`, Docker-/E2E-Checks.

### Refactor-Budget

Ca. 0,5 bis 1 Tag: H5P-Web-Uploadlimit plus Test ist der größte Block; Startup-Guard und OpenAPI-503-Dokumentation sind klein. Der Resolver-Refactor ist optional und sollte erst nach den Sicherheits- und Contract-Fixes erfolgen.

### Freigabe

Keine Freigabe vor dem MUST-FIX. Nach Uploadlimit, Secret-Guard und Contract/Test-Nachschärfung reicht eine erneute gezielte Review-Runde plus `make verify`; bei Änderungen an Compose/H5P zusätzlich Docker-Validierung.

## Reparaturplan – 2026-05-26 16:51 CEST

- MUST-FIX `[SEC]` H5P-Web-Uploadlimit: weiterhin offen, weil `backend/web/routes/teaching.py` den `UploadFile` vollständig liest, bevor der H5P-Sidecar sein Limit anwenden kann. Owner: `teaching` Webadapter. Contract-first: `api/openapi.yml` dokumentiert `413 payload_too_large` für section- und moduladressierte H5P-Imports. Test-first: pytest-Regressionsfall für einen zu großen `.h5p`-Upload, der nicht an den Sidecar weitergereicht wird. Minimaler Fix: chunkweise lesen, `H5P_MAX_UPLOAD_BYTES` mit 100 MiB Default verwenden, bei Überschreitung `413` mit `h5p_upload_too_large` liefern.
- SHOULD-FIX `[SEC]` Web-Startup-Guard: weiterhin offen, weil nur der H5P-Sidecar auf `H5P_INTERNAL_SHARED_SECRET` fail-fastet. Owner: `identity_access`/Web-Konfiguration. Test-first: prod-like Config-Test für fehlendes und Platzhalter-Secret. Minimaler Fix: `ensure_secure_config_on_startup()` prüft das Secret analog zu BFF/H5P-Review/CSRF-Secrets.
- SHOULD-FIX `[API]` Modul-Authoring-503: weiterhin offen, weil Runtime `modular_repo_unavailable` liefern kann, der OpenAPI-Vertrag aber mehrere moduladressierte Mutationen nicht vollständig dokumentiert. Owner: `teaching` API-Vertrag. Contract-first: 503-Responses in `api/openapi.yml`; Test-first: OpenAPI-Contract-Test für die betroffenen Modul-Endpunkte.
- SHOULD-FIX `[TEST]` H5P-internes Secret-Auth-Verhalten: weiterhin offen, weil die wichtigste Absicherung bisher als Quelltext-Guard statt als ausführbarer Node-Test vorliegt. Owner: H5P-Sidecar. Test-first: `node:test` für gültige/ungültige interne Secrets, gesetzten internen Teacher-Kontext und CSRF-Bypass-Prädikat. Minimaler Fix: interne Auth-Helfer aus `server.mjs` in ein kleines Modul unter `h5p-service/lib/` extrahieren und in `server.mjs` wiederverwenden.
- NICE-TO-HAVE `[BUG]` CLI-Multipart-Filename: weiterhin offen, weil Dateinamen direkt in `Content-Disposition` gelangen. Owner: CLI. Test-first: Multipart-Test mit `"`/CR/LF/Backslash im Dateinamen. Minimaler Fix: kleine Header-Token-Sanitization für Feldname und Dateiname.
- NICE-TO-HAVE `[CONSISTENCY]` Modul-Resolver: weiterhin offen, weil zwei Resolver dieselbe Autorisierungs- und Section-Auflösung duplizieren. Owner: `teaching` Webadapter. Test-first: bestehende und neue Regressionen für Modul-H5P/Material/Task-Pfade grün halten. Minimaler Fix: ein Helper mit optionaler `task_id`-Validierung, unveränderten Fehlercodes und unveränderten Payloads.

Akzeptanz: gezielte pytest-Suite (`test_auth_middleware.py`, `test_gustav_cli.py`, `test_h5p_service_hardening_contract.py`, `test_openapi_profile_contract.py`, `test_config_security.py`), `npm test --prefix h5p-service` und anschließend `make verify`.

## Umsetzungsstand – 2026-05-26 17:12 CEST

Geschlossen:

- MUST-FIX `[SEC]` H5P-Web-Uploadlimit: FastAPI liest H5P-Uploads jetzt chunkweise mit hartem `H5P_MAX_UPLOAD_BYTES`-Limit und liefert bei Überschreitung `413 payload_too_large` / `h5p_upload_too_large`, bevor der H5P-Sidecar aufgerufen wird.
- SHOULD-FIX `[SEC]` Web-Startup-Guard: `ensure_secure_config_on_startup()` prüft `H5P_INTERNAL_SHARED_SECRET` in prod-/stage-ähnlichen Umgebungen und lehnt leere sowie `CHANGE_ME*`-Platzhalter ab.
- SHOULD-FIX `[API]` Modul-Authoring-503: `api/openapi.yml` dokumentiert `503 modular_repo_unavailable` für die moduladressierten Task-/Material-/H5P-Mutationen; die 204-DELETE-Responses dokumentieren zusätzlich den verpflichtenden `Cache-Control`-Header.
- SHOULD-FIX `[TEST]` H5P-internes Secret-Auth-Verhalten: Die interne H5P-Teacher-Auth liegt in `h5p-service/lib/internal_auth.mjs` und wird per `node:test` ausführbar gegen falsche/gegebene Secrets, sanitisierten Kontext und Teacher-Rollenprüfung getestet.
- NICE-TO-HAVE `[BUG]` CLI-Multipart-Filename: `_http_multipart()` sanitisiert `Content-Disposition`-Parameter für Feldname und Dateiname gegen CR/LF, Anführungszeichen und Backslashes.
- NICE-TO-HAVE `[CONSISTENCY]` Modul-Resolver: Die doppelte Modul-Section-Auflösung wurde zu einem gemeinsamen Helper mit optionaler `task_id`-Validierung zusammengeführt.

Stale Findings: keine.

Verifikation:

- Neue rote Tests wurden vor der Implementierung ausgeführt und schlugen gezielt fehl: Config-Guard, CLI-Multipart, H5P-Web-Uploadlimit und H5P-Node-Auth-Helper.
- Gezielte Suite: `.venv/bin/pytest -q backend/tests/test_auth_middleware.py backend/tests/test_gustav_cli.py backend/tests/test_h5p_service_hardening_contract.py backend/tests/test_openapi_profile_contract.py backend/tests/test_config_security.py` → `103 passed`.
- H5P Node: `npm test --prefix h5p-service` → `21 passed`.
- Vollcheck: `make verify` → grün, inklusive DB-Preflight, `1696 passed, 33 skipped` im Haupt-pytest, H5P-Node-Tests, Supabase-/OpenAI-Smoke-Checks und `13 passed` E2E.

Geänderte Subsysteme: `teaching` Webadapter/API-Vertrag, Web-Konfigurationsguard, GUSTAV-CLI, H5P-Sidecar-Auth-Helper und zugehörige Contract-/Regressionstests. Keine Migration nötig.

Restrisiko: gering. Der Webprozess puffert weiterhin erlaubte H5P-Pakete als Bytes für den vorhandenen H5P-Forwarding-Call; übergroße Pakete werden aber vor dem Forwarding abgelehnt und maximal bis `Limit + 1` gelesen. Eine echte Streaming-Weiterleitung an den Sidecar bleibt außerhalb dieses PR-Fix-Scopes.

## Review Round – 17:18

Datum/Zeit: 2026-05-26 17:18 CEST  
Branch: `feature/cli-upload-h5p`  
Base ref: `master` @ `bbf990deb84b17d995c1c7fd67d84726fadf9a9e`  
Merge-Base: `bbf990deb84b17d995c1c7fd67d84726fadf9a9e`  
HEAD: `bbf990deb84b17d995c1c7fd67d84726fadf9a9e`  
Review-Modus: aktuelle uncommitted Arbeitskopie gegen `master`, da Branch/HEAD/Merge-Base identisch sind.

Scope:

- Tracked Worktree-Diff: `.env.example`, `api/openapi.yml`, CLI, FastAPI-Teaching-Routen, Web-Config/Auth-Capability-Tabelle, H5P-Sidecar, Tests und Referenzdokumentation.
- Untracked Worktree-Dateien: `docs/plan/2026-05-26-PR-fix.md`, `docs/plan/2026-05-26-gustav-cli-upload-h5p.md`, `h5p-service/lib/internal_auth.mjs`, `h5p-service/test/internal_auth.test.mjs`.
- Fokus: Regressionen der vorherigen PR-Findings, API-Vertrag vs. Runtime, CLI-Token-Scopes, Cookie-/CSRF-Abgrenzung, interne Web→H5P-Authentifizierung, Upload-Limits und Testabdeckung.

Kontextnotizen:

- `HEAD`, `master` und Merge-Base zeigen auf denselben Commit; alle Feature-/Fix-Änderungen sind aktuell uncommitted.
- Die vorherige Fix-Runde hat die bekannten Findings geschlossen; diese Review-Runde prüft den jetzt vorliegenden Worktree-Zustand erneut.
- Keine Migration erforderlich; die Änderungen betreffen API-/Service-/CLI-Verhalten und ENV-Konfiguration.

Verwendete Code-Referenzen:

- `backend/web/routes/teaching.py:1629@bbf990d+WT`, `backend/web/routes/teaching.py:1660@bbf990d+WT`, `backend/web/routes/teaching.py:4149@bbf990d+WT`, `backend/web/routes/teaching.py:4372@bbf990d+WT`, `backend/web/routes/teaching.py:5020@bbf990d+WT`
- `backend/web/main.py:517@bbf990d+WT`, `backend/web/config.py:68@bbf990d+WT`
- `backend/tools/gustav_cli/cli.py:82@bbf990d+WT`, `backend/tools/gustav_cli/cli.py:632@bbf990d+WT`, `backend/tools/gustav_cli/cli.py:904@bbf990d+WT`
- `h5p-service/server.mjs:65@bbf990d+WT`, `h5p-service/server.mjs:414@bbf990d+WT`, `h5p-service/server.mjs:565@bbf990d+WT`, `h5p-service/lib/internal_auth.mjs:15@bbf990d+WT`
- `api/openapi.yml:10423@bbf990d+WT`, `api/openapi.yml:10581@bbf990d+WT`, `api/openapi.yml:10694@bbf990d+WT`, `api/openapi.yml:11533@bbf990d+WT`
- `backend/tests/test_auth_middleware.py:337@bbf990d+WT`, `backend/tests/test_auth_middleware.py:381@bbf990d+WT`, `backend/tests/test_gustav_cli.py:128@bbf990d+WT`, `h5p-service/test/internal_auth.test.mjs:21@bbf990d+WT`

### Kurzfazit

Die erneute Review der uncommitted Arbeitskopie findet keine offenen PR-Blocker. Die vorherigen Security-, API-, Test-, Bug- und Konsistenz-Findings sind im aktuellen Worktree nachvollziehbar umgesetzt und gezielt getestet.

### MUST-FIX

Keine.

### SHOULD-FIX

Keine.

### NICE-TO-HAVE

Keine.

### TESTS

- Ausgeführt: `git diff --check master` → keine Ausgabe, also keine Whitespace-Fehler.
- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_auth_middleware.py backend/tests/test_gustav_cli.py backend/tests/test_h5p_service_hardening_contract.py backend/tests/test_openapi_profile_contract.py backend/tests/test_config_security.py` → `103 passed in 6.84s`.
- Ausgeführt: `npm test --prefix h5p-service` → `7 passed`.
- Nicht erneut ausgeführt in dieser Review-Runde: `make verify`; der Vollcheck ist im Umsetzungsstand von 17:12 CEST als grün dokumentiert.

### Refactor-Budget

0 Tage für PR-Blocker. Optional nach Merge/Review: Wenn die H5P-Pakete künftig deutlich größer werden sollen, kann eine echte Streaming-Weiterleitung Web→H5P den erlaubten 100-MiB-Puffer ersetzen; das ist kein Freigabehindernis für diese Änderung.

### Freigabe

Freigabe aus Review-Sicht. Vor Commit/PR bleibt wie üblich `make verify` der formale Merge-Gate, besonders weil die Arbeitskopie uncommitted ist.

## Review Round – 18:19 CEST

Datum/Zeit: 2026-05-26 18:19 CEST

Branch: `keycloak-error-template-runtime-hardening-fix`

Base ref: `master` @ `32809941141df55574150aef1a1dd56d065e5ec1`

Merge-Base: `32809941141df55574150aef1a1dd56d065e5ec1`

HEAD: `32809941141df55574150aef1a1dd56d065e5ec1`

Review-Modus: aktuelle uncommitted Arbeitskopie gegen `master`, da `HEAD`, `master` und Merge-Base identisch sind. Hinweis: Die Arbeitskopie liegt auf dem Branch `keycloak-error-template-runtime-hardening-fix`, nicht direkt auf `master`.

Scope:

- Tracked Worktree-Diff: `backend/tests/test_keycloak_theme_files.py`, `keycloak/themes/gustav/login/_gustav_error_components.ftl`, `error.ftl`, `footer.ftl`, `info.ftl`, `login-page-expired.ftl`, `logout-confirm.ftl`.
- Untracked Worktree-Dateien im Scope: `backend/tests_e2e/test_keycloak_error_pages_e2e.py`, `docs/plan/2026-05-26-keycloak-error-template-runtime-hardening.md`.
- Nicht geändert: `api/openapi.yml`, Supabase/PostgreSQL-Migrationen.

Kontextnotizen:

- Zeilenreferenzen mit `@3280994+WT` beziehen sich auf die uncommitted Arbeitskopie auf Basis von `3280994`.
- Laufzeitprüfung gegen den lokalen Keycloak zeigte: Die beiden neuen Error-Flows rendern gethemtes HTML ohne HTTP 500. Die ausgegebenen Recovery-Links zeigen aber auf `login-actions/authenticate` bzw. `login-actions/registration` und reproduzieren den Cookie-Fehler erneut.
- Dieselben Laufzeitantworten setzen `KC_STATE_CHECKER` mit `HttpOnly`, aber ohne sichtbare `Secure`- und `SameSite`-Attribute. Die konkreten Cookie-Werte wurden bewusst nicht dokumentiert.

Verwendete Code-Referenzen:

- `backend/tests/test_keycloak_theme_files.py:486@3280994+WT`, `backend/tests/test_keycloak_theme_files.py:506@3280994+WT`, `backend/tests/test_keycloak_theme_files.py:528@3280994+WT`
- `backend/tests_e2e/test_keycloak_error_pages_e2e.py:55@3280994+WT`, `backend/tests_e2e/test_keycloak_error_pages_e2e.py:57@3280994+WT`
- `keycloak/themes/gustav/login/_gustav_error_components.ftl:60@3280994+WT`, `keycloak/themes/gustav/login/_gustav_error_components.ftl:61@3280994+WT`, `keycloak/themes/gustav/login/_gustav_error_components.ftl:72@3280994+WT`
- `keycloak/themes/gustav/login/info.ftl:48@3280994+WT`, `keycloak/themes/gustav/login/error.ftl:35@3280994+WT`, `keycloak/themes/gustav/login/footer.ftl:2@3280994+WT`
- `keycloak/themes/gustav/login/messages/messages_de.properties:18@3280994`
- `docker-compose.yml:293@3280994`

### Kurzfazit

Die FreeMarker-Runtime-Härtung behebt den 500er-Kern sichtbar: die neuen E2E-Flows rendern gethemtes HTML. Vor Freigabe bleiben aber Security- und UX-Lücken offen: Das Error-Flow-Cookie erfüllt die dokumentierte Cookie-Policy nicht, und die Recovery-Links führen in eine Fehler-Schleife statt aus ihr heraus.

### MUST-FIX

- [SEC] Error-Flow-Cookie wird nicht gegen die GUSTAV-Cookie-Policy abgesichert — `backend/tests_e2e/test_keycloak_error_pages_e2e.py:57@3280994+WT` — Impact: Die neue Runtime-Regression akzeptiert Antworten, die `KC_STATE_CHECKER` ohne `Secure` und `SameSite` setzen; das widerspricht der Sicherheitsdokumentation für authnahe Cookies und ist wegen „lokal = prod“ kein lokaler Sonderfall. — Fix: Konfiguriere Keycloak/Proxy so, dass Error-Flow-Cookies `HttpOnly; Secure; SameSite=Lax` tragen, und ergänze im E2E-Test eine Set-Cookie-Assertion ohne Cookie-Werte zu loggen.
- [BUG] Recovery-Links zeigen in cookie-losen Fehlerflows wieder auf `login-actions/*` und erzeugen eine Schleife — `keycloak/themes/gustav/login/_gustav_error_components.ftl:60@3280994+WT` — Impact: Die Seite rendert zwar nicht mehr als HTTP 500, aber „Erneut anmelden“ und „Registrieren“ führen wieder zur gleichen Fehlerseite; damit ist der versprochene Wiederanmelde- oder Rückkehrpfad fachlich nicht stabil. — Fix: Rendere `url.loginUrl`/`url.registrationUrl` nicht, wenn sie auf `login-actions/*` zeigen, oder verwende einen explizit vertrauenswürdigen App-Restart-Link; sichere das mit einem E2E-Assert gegen diese Loop-Hrefs ab.

### SHOULD-FIX

- [TEST] Neuer E2E-Test prüft nur „nicht 500“, nicht den eigentlichen Recovery-Vertrag — `backend/tests_e2e/test_keycloak_error_pages_e2e.py:57@3280994+WT` — Impact: Ein generisches 400-HTML, eine JSON-Fehlerseite oder eine gethemte Seite mit kaputten CTAs kann grün bleiben, obwohl die User Story „gethemte GUSTAV-Recovery-Seite“ verlangt. — Fix: Prüfe `Content-Type: text/html`, `kc-gustav`, den erwarteten Titel/Hinweis, keine FreeMarker-Ausgabe, keine Loop-Hrefs und die relevanten Security-Header/Cookie-Attribute.

### NICE-TO-HAVE

- [CONSISTENCY] Deutsche Error-Hinweise verwenden ASCII-Umschreibungen statt Umlauten — `keycloak/themes/gustav/login/messages/messages_de.properties:18@3280994` — Impact: Sichtbare UI-Texte zeigen `ueber`, `Pruefe`, `zusaetzlich`, `oeffne` und `ungueltig`; das widerspricht der GUSTAV-Sprachvorgabe und wirkt auf der jetzt sichtbaren Recovery-Seite unfertig. — Fix: Ersetze die deutschen Hinweise durch korrektes Deutsch mit Umlauten und ergänze einen kleinen statischen Test gegen diese Umschreibungen in sichtbaren Keycloak-Texten.

### TESTS

- Ausgeführt: `git diff --check` -> keine Ausgabe.
- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_keycloak_theme_files.py` -> `39 passed in 0.53s`.
- Ausgeführt: `RUN_E2E=1 .venv/bin/pytest -q -m e2e backend/tests_e2e/test_keycloak_error_pages_e2e.py` in der Sandbox -> blockiert durch lokale Netzwerkrestriktion (`Operation not permitted`).
- Erneut ausgeführt außerhalb der Sandbox gegen laufende lokale Docker-Services: `RUN_E2E=1 .venv/bin/pytest -q -m e2e backend/tests_e2e/test_keycloak_error_pages_e2e.py` -> `2 passed`, mit erwarteten lokalen TLS-Warnungen.
- Manuell geprüft: GET auf beide Error-Flow-URLs liefert gethemtes HTML ohne HTTP 500, aber mit Loop-Links und ohne sichtbare `Secure`-/`SameSite`-Cookie-Attribute.

### Refactor-Budget

Ca. 0,5 Tag. Die Link-Schleife und die E2E-Assertions sind klein. Die Cookie-Policy kann je nach Keycloak-Version/Proxy-Konfiguration etwas Recherche brauchen, sollte aber als authnaher Security-Fix vor PR-Freigabe geschlossen werden.

### Freigabe

Keine Freigabe vor den beiden MUST-FIX-Punkten. Danach reicht eine gezielte erneute Review-Runde mit dem Keycloak-E2E-Test und den statischen Theme-Tests; bei Keycloak-/Compose-Konfigurationsänderungen zusätzlich Docker-Validierung.

## Reparaturplan – 2026-05-26 18:24 CEST

- MUST-FIX `[SEC]` Error-Flow-Cookie-Policy: weiterhin offen. Der aktuelle E2E-Test akzeptiert `KC_STATE_CHECKER` ohne sichtbares `Secure`/`SameSite`, und die laufende Keycloak-Konfiguration nutzt noch `KC_PROXY=edge` statt der von Keycloak 24 angebotenen `proxy-headers`-Option. Regeln: Security first, lokal = prod, Cookie-Policy aus `docs/references/security_checklist.md`. Owner: `identity_access`/Keycloak-Infra. Wahrscheinliche Dateien: `docker-compose.yml`, `backend/tests/test_compose_keycloak_postgres.py`, `backend/tests_e2e/test_keycloak_error_pages_e2e.py`. Kein OpenAPI-/Migrationsbedarf. Test-first: Compose-Contract-Test für `KC_PROXY_HEADERS=xforwarded` + `KC_HTTP_ENABLED=true` und E2E-Assertion für `KC_STATE_CHECKER` mit `HttpOnly; Secure; SameSite=Lax`. Minimaler Fix: Keycloak-Compose-Konfiguration auf explizite Proxy-Header umstellen und, falls Keycloak weiterhin kein `SameSite` setzt, Caddy-Response-Header für IdP-Cookies hart absichern. Akzeptanz: statischer Compose-Test, Caddy-Adapt-Check und Keycloak-E2E grün.
- MUST-FIX `[BUG]` Recovery-Link-Schleife: weiterhin offen. `render_recovery_links` übernimmt `url.loginUrl` und `url.registrationUrl` ungefiltert; im cookie-losen Error-Flow zeigen beide auf `login-actions/*` und reproduzieren den Fehler. Regeln: KISS und verständliche Recovery-UX. Owner: `identity_access`/Keycloak-Theme. Dateien: `_gustav_error_components.ftl`, `backend/tests/test_keycloak_theme_files.py`, `backend/tests_e2e/test_keycloak_error_pages_e2e.py`. Test-first: statischer Template-Test verlangt einen Guard gegen `login-actions`-Recovery-Links; E2E-Test verbietet `href` auf `/login-actions/authenticate` und `/login-actions/registration` auf Error-Recovery-Seiten. Minimaler Fix: kleine Helper-Funktion `is_login_action_link(...)` und Links nur rendern, wenn sie nicht auf login-actions zeigen. Akzeptanz: Theme-Tests und E2E grün.
- SHOULD-FIX `[TEST]` E2E-Recovery-Vertrag: weiterhin offen und wird mit den beiden MUST-FIX-Paketen direkt geschlossen. Test-first: E2E prüft `text/html`, `kc-gustav`, GUSTAV-Titel/Hinweis, keine FreeMarker-Ausgabe, keine Loop-Hrefs, Cache-/Security-Header und Cookie-Attribute ohne Cookie-Werte zu loggen. Minimaler Fix: Assertions im bestehenden E2E-Test erweitern.
- NICE-TO-HAVE `[CONSISTENCY]` Deutsche Error-Hinweise: weiterhin offen. Sichtbare Keycloak-Texte enthalten ASCII-Umschreibungen (`ueber`, `Pruefe`, `zusaetzlich`, `oeffne`, `ungueltig`, `bestaetigen`, `zurueck`). Regeln: AGENTS-Zeichensatzvorgabe für sichtbare UI-Texte. Owner: `identity_access`/Keycloak-Theme. Dateien: `messages_de.properties`, `backend/tests/test_keycloak_theme_files.py`. Test-first: statischer Test verbietet typische ASCII-Umschreibungen in sichtbaren deutschen Theme-Texten. Minimaler Fix: betroffene Strings auf korrektes Deutsch mit Umlauten umstellen.

## Umsetzungsstand – 2026-05-26 18:33 CEST

Geschlossen:

- MUST-FIX `[SEC]` Error-Flow-Cookie-Policy: Keycloak nutzt im Compose-Vertrag jetzt explizit `KC_PROXY_HEADERS=xforwarded` und `KC_HTTP_ENABLED=true`; der `id.localhost`-Reverse-Proxy setzt für Keycloak-`Set-Cookie`-Antworten zusätzlich `Secure; SameSite=Lax`. Der E2E-Test prüft `KC_STATE_CHECKER` ohne Cookie-Werte zu loggen.
- MUST-FIX `[BUG]` Recovery-Link-Schleife: `_gustav_error_components.ftl` erkennt `login-actions`-Links und rendert `url.loginUrl`/`url.registrationUrl` nicht als Recovery-CTA, wenn sie den selben Fehlerflow erneut auslösen würden.
- SHOULD-FIX `[TEST]` E2E-Recovery-Vertrag: Der E2E-Test prüft jetzt `text/html`, GUSTAV-Theme-Marker, Security-/Cache-Header, fehlende FreeMarker-Ausgabe, keine Loop-Hrefs und Cookie-Attribute.
- NICE-TO-HAVE `[CONSISTENCY]` Deutsche Error-Hinweise: Sichtbare deutsche Keycloak-Recovery-Texte verwenden jetzt korrekte Umlaute.

Stale Findings: keine.

Verifikation:

- Red-Phase ausgeführt und bestätigt: neuer Compose-Proxy-Test rot, neue Theme-Loop-/Umlaut-Tests rot, verschärfter Keycloak-E2E rot.
- Gezielte statische Suite: `.venv/bin/pytest -q backend/tests/test_keycloak_theme_files.py backend/tests/test_compose_keycloak_postgres.py` -> `48 passed`.
- Runtime-E2E nach Neubau von Keycloak und Neustart von Caddy: `RUN_E2E=1 .venv/bin/pytest -q -m e2e backend/tests_e2e/test_keycloak_error_pages_e2e.py` -> `2 passed`, mit erwarteten lokalen TLS-Warnungen.
- Syntax/Config: `git diff --check` -> keine Ausgabe; `docker compose config --quiet` -> grün; `docker run --rm -v "$(pwd)/reverse-proxy/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:2-alpine caddy adapt --config /etc/caddy/Caddyfile` -> Exit 0.

Geänderte Subsysteme: `identity_access`/Keycloak-Theme, Keycloak-Compose-Konfiguration, Caddy-IdP-Reverse-Proxy und zugehörige statische/E2E-Tests. Keine OpenAPI- oder Supabase-Migration nötig.

Restrisiko: gering. Die Caddy-Härtung hängt Cookie-Attribute an alle Keycloak-`Set-Cookie`-Header am IdP-Host an; das entspricht der GUSTAV-Cookie-Policy, kann aber bei einer zukünftigen Keycloak-Version mit bereits gesetztem `SameSite` doppelte Attribute erzeugen. Der E2E-Test würde eine sichtbare Vertragsverletzung bei den aktuellen Error-Flows wieder fangen.

## Review Round – 18:35 CEST

Datum/Zeit: 2026-05-26 18:35 CEST

Branch: `keycloak-error-template-runtime-hardening-fix`

Base ref: `master` @ `32809941141df55574150aef1a1dd56d065e5ec1`

Merge-Base: `32809941141df55574150aef1a1dd56d065e5ec1`

HEAD: `32809941141df55574150aef1a1dd56d065e5ec1`

Review-Modus: aktuelle uncommitted Arbeitskopie gegen `master`, da `HEAD`, `master` und Merge-Base identisch sind.

Scope:

- Geprüft: Keycloak-Theme-Härtung, neue E2E-Regression, Compose-Keycloak-Proxy-Konfiguration, Caddy-IdP-Cookie-Härtung, deutsche Theme-Texte und zugehörige Plan-Dokumentation.
- Nicht betroffen: `api/openapi.yml`, Supabase/PostgreSQL-Migrationen, fachliche Teaching-/Learning-Use-Cases.

Kontextnotizen:

- Die Review-Runde prüft gezielt die Reparaturen aus `Reparaturplan – 2026-05-26 18:24 CEST`.
- Keycloak und Caddy wurden neu gebaut/gestartet, damit Theme- und Reverse-Proxy-Änderungen in der Runtime sichtbar sind.
- Der E2E-Test prüft die Cookie-Attribute ohne Cookie-Werte zu protokollieren.

Verwendete Code-Referenzen:

- `reverse-proxy/Caddyfile:40@3280994+WT`, `reverse-proxy/Caddyfile:45@3280994+WT`
- `docker-compose.yml:293@3280994+WT`
- `backend/tests_e2e/test_keycloak_error_pages_e2e.py:26@3280994+WT`, `backend/tests_e2e/test_keycloak_error_pages_e2e.py:69@3280994+WT`
- `keycloak/themes/gustav/login/_gustav_error_components.ftl:18@3280994+WT`, `keycloak/themes/gustav/login/_gustav_error_components.ftl:75@3280994+WT`
- `backend/tests/test_keycloak_theme_files.py:468@3280994+WT`, `backend/tests/test_compose_keycloak_postgres.py:89@3280994+WT`
- `docs/plan/2026-05-26-keycloak-error-template-runtime-hardening.md:23@3280994+WT`

### Kurzfazit

Die vorherigen Findings sind im aktuellen Worktree geschlossen. Die Keycloak-Error-Flows rendern gethemtes HTML ohne Loop-Links, die Cookie-Policy wird am IdP-Proxy erzwungen, und die relevanten statischen sowie E2E-Tests decken die Regressionen ab.

### MUST-FIX

Keine.

### SHOULD-FIX

Keine.

### NICE-TO-HAVE

Keine.

### TESTS

- Ausgeführt: `.venv/bin/pytest -q backend/tests/test_keycloak_theme_files.py backend/tests/test_compose_keycloak_postgres.py` -> `48 passed in 0.60s`.
- Ausgeführt nach Keycloak-/Caddy-Neustart: `RUN_E2E=1 .venv/bin/pytest -q -m e2e backend/tests_e2e/test_keycloak_error_pages_e2e.py` -> `2 passed`, mit erwarteten lokalen TLS-Warnungen.
- Ausgeführt: `git diff --check` -> keine Ausgabe.
- Ausgeführt: `docker compose config --quiet` -> Exit 0.
- Ausgeführt: `docker run --rm -v "$(pwd)/reverse-proxy/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:2-alpine caddy adapt --config /etc/caddy/Caddyfile` -> Exit 0.

### Refactor-Budget

0 Tage für PR-Blocker. Optional später: Wenn Keycloak in einer zukünftigen Version selbst konsistent `Secure; SameSite=Lax` setzt, kann die Caddy-Härtung zu einem spezifischeren Cookie-Rewrite oder ganz zurückgebaut werden; der aktuelle E2E-Test gibt dafür die Sicherheitsgrenze vor.

### Freigabe

Freigabe aus Review-Sicht für diesen Scope. Vor Commit/PR bleibt der übliche formale Gate `make verify`; wegen Keycloak-/Caddy-Änderungen ist zusätzlich Docker-Validierung sinnvoll.
