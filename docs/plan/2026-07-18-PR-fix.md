# PR-Review: `master` gegen `origin/master`

## Review-Runde – 09:46 CEST

### Review-Metadaten

- Review-Zeitpunkt: 2026-07-18 09:46:51 CEST
- Branch: `master`
- Basis-Ref: `origin/master` (`1407ba727307bb3694899af4ced97856015f9f2c`)
- Merge-Base: `1407ba727307bb3694899af4ced97856015f9f2c`
- HEAD: `b8050513683270ddb85cc8c9fb91dc8de3809778`
- Scope: `origin/master..master`; 228 Commits, 648 Dateien, 59.390 Einfügungen und 40.325 Löschungen
- Geprüfter Umfang: Gesamtstand mit Fokus auf den Harness-/Router-/Repository-Refactor, `api/openapi.yml`, geänderte Tests, Docker-/Build-Parität, H5P-Review, Auth-/Privacy-Grenzen und die zuvor dokumentierten Reparaturrunden; keine Supabase-Migration im Diff
- Kontext: `origin/master` ist der lokal vorhandene Tracking-Ref und wurde für dieses Review nicht über das Netzwerk aktualisiert. Die Befunde aus `docs/plan/2026-07-17-PR-fix.md` sind im aktuellen Stand geschlossen und wurden nicht erneut als offen gewertet.
- Verwendete Code-Referenzen: `backend/web/routes/teaching_units.py`, `backend/web/routes/teaching_guards.py`, `backend/tests/test_privacy_logging_contract.py`, `h5p-service/lib/review_tokens.mjs`, `h5p-service/lib/review_mode_middleware.mjs`, `h5p-service/server.mjs`, `api/openapi.yml` und die jeweils zugehörigen Tests

### 1. Kurzfazit

Der Refactor ist strukturell stark und `make verify` vollständig grün. Die neue Teaching-Fehlergrenze greift jedoch nicht zuverlässig: breite Catch-Blöcke übersetzen Datenbankausfälle weiterhin in `403`, `404` oder leere Antworten und widersprechen damit dem ergänzten `503`-Vertrag. Zwei kleinere, aber konkrete Risiken betreffen ein korrelierbares Subject-Suffix im Log sowie konkurrierende H5P-Reviews. Vor Freigabe muss mindestens das Fehlermapping korrigiert werden.

### 2. MUST-FIX

- [API] Ausgelagerte Teaching-Routen verschlucken `TeachingRepositoryUnavailable` und liefern fachlich falsche Auth-/Not-found-Antworten — `backend/web/routes/teaching_units.py:61@b8050513`, `backend/web/routes/teaching_guards.py:88@b8050513` — Impact: Ein reproduzierter Repository-Ausfall auf `GET /api/teaching/units` ergibt `403 {"error":"forbidden"}` statt des in `api/openapi.yml` dokumentierten privaten `503`; weitere breite Catch-Blöcke erzeugen `404`, `204` oder leere `200`-Antworten. Clients, Monitoring und Lehrkräfte können einen Infrastrukturfehler dadurch nicht von fehlender Berechtigung oder fehlenden Daten unterscheiden. — Fix: Schreibe zuerst fehlschlagende Regressionstests für mindestens Listen-, Guard-, Live- und Schreibpfad, reiche `TeachingRepositoryUnavailable` anschließend vor jedem generischen Catch unverändert an den zentralen Handler weiter und zentralisiere diese Regel, damit neue Router sie nicht erneut umgehen.

### 3. SHOULD-FIX

- [SEC] Die Unit-Liste protokolliert weiterhin die letzten sechs Zeichen des OIDC-Subjects, während der Privacy-Test nur zwei ausgewählte Teaching-Dateien scannt — `backend/web/routes/teaching_units.py:62@b8050513`, `backend/tests/test_privacy_logging_contract.py:48@b8050513` — Impact: Das Suffix ist ein dauerhaft korrelierbares Benutzerpseudonym; die lokale Reproduktion schrieb `sub=123456` ins Log, obwohl die dokumentierte Privacy-Baseline keine Benutzerkennungen zulässt. Der unvollständige Test lässt denselben Fehler in ausgelagerten Routern unbemerkt. — Fix: Erweitere zuerst den AST-Vertrag auf alle produktiven Teaching-Router, entferne anschließend Subject- und andere personenbeziehbare ID-Suffixe aus Logs und verwende ausschließlich niedrig-kardinale Reason-Codes sowie Exception-Klassen.
- [BUG] Alle H5P-Reviews eines Browsers teilen sich genau einen Review-Cookie — `h5p-service/lib/review_tokens.mjs:5@b8050513`, `h5p-service/server.mjs:775@b8050513`, `h5p-service/lib/review_mode_middleware.mjs:29@b8050513` — Impact: Öffnet eine Lehrkraft zwei Schüler-Reviews parallel, überschreibt der zweite Modellaufruf das Credential des ersten; dessen nächster User-State-Read wird wegen Content-, Task- oder Schülerbindung mit `403` abgewiesen. Mehrere Tabs beziehungsweise schnell wechselnde Detailansichten sind dadurch nicht robust. — Fix: Schreibe zuerst einen Interleaving-Test mit zwei gültigen Reviews, führe dann einen nicht personenbezogenen Review-Handle mit getrennt auswählbaren Credentials ein und binde beziehungsweise lösche jeden Cookie-Eintrag eng an Handle, Ablaufzeit und Review-Kontext.

### 4. NICE-TO-HAVE

Keine zusätzlichen Befunde.

### 5. TESTS

- `git diff --check origin/master..master`: bestanden.
- `make verify`: bestanden; DB-Preflight, Import-, OpenAPI-, Architektur-, Route-Map-, DB-Inventar-, Offline-Supply-Chain-, Ruff- und Docker-Image-Gates grün; Backend 2.047 bestanden und 73 übersprungen; Svelte Check 0 Fehler und 0 Warnungen; Frontend 312 Tests plus 3 Build-Policy-Tests; produktiver Frontend-Build bestanden; H5P 59 Tests bestanden.
- Isolierte Runtime-Probe: Ein `TeachingRepositoryUnavailable` aus `list_units_for_author` lieferte reproduzierbar `403 {"error":"forbidden"}` und protokollierte das Subject-Suffix.
- `make dependency-audit`, `make test-e2e`, `make test-visual-smoke` und das vollständige `make test-full-prod-like` wurden in dieser Runde nicht ausgeführt. Das Online-Audit würde Dependency-Metadaten an die npm-Registry übermitteln; die produktnahen Browser-/E2E-Läufe bleiben daher als Restrisiko offen.

### 6. Refactor-Budget

Etwa ein bis zwei Entwicklertage: bis zu ein Tag für die systematische `TeachingRepositoryUnavailable`-Propagation samt repräsentativer Route-Tests, wenige Stunden für die Privacy-Log-Grenze und bis zu ein Tag für einen paralleltauglichen H5P-Review-Kontext einschließlich Node- und Browser-Regressionstest.

### 7. Freigabe

Nicht freigegeben. Vor der Freigabe muss der MUST-FIX-Befund test-first geschlossen und mit `make verify` sowie gezielten Ausfalltests über mehrere Teaching-Router nachgewiesen werden. Die beiden SHOULD-FIX-Punkte sollten wegen DSGVO-Konformität und zuverlässiger H5P-Lehreransicht im selben Reparaturblock erledigt werden; anschließend sind produktnahe H5P-/Browser-Smokes sinnvoll.

## Reparaturplan – 2026-07-18 09:55 CEST

### User Story und BDD-Szenarien

Als Lehrkraft möchte ich bei einem Datenbankausfall eine eindeutige temporäre Fehlermeldung erhalten und mehrere H5P-Abgaben parallel prüfen können, ohne dass Benutzerkennungen protokolliert oder Review-Berechtigungen gegenseitig überschrieben werden.

- **Given** eine Teaching-Repository-Operation verliert die PostgreSQL-Verbindung, **when** eine Listen-, Guard-, Live- oder Schreibroute den Fehler verarbeitet, **then** erreicht `TeachingRepositoryUnavailable` den zentralen HTTP-Handler und die Antwort entspricht dem bestehenden privaten `503`-Vertrag statt `403`, `404`, `204` oder einer leeren `200`-Antwort.
- **Given** ein unerwarteter, nicht infrastruktureller Fehler tritt in einem Legacy-Fallback auf, **when** die Route ihn verarbeitet, **then** bleibt das bisherige fail-closed Verhalten erhalten; nur `TeachingRepositoryUnavailable` wird gezielt weitergereicht.
- **Given** ein Teaching-Request schlägt fehl, **when** der Web-Adapter diagnostische Metadaten protokolliert, **then** enthält der Logeintrag weder ein vollständiges noch ein gekürztes OIDC-Subject.
- **Given** eine Lehrkraft öffnet zwei gültige H5P-Reviews in verschiedenen Tabs, **when** beide Player anschließend ihren jeweiligen User-State laden, **then** wählt jeder Request sein eigenes kurzlebiges Credential über einen opaken Review-Handle aus und beide Reads bleiben autorisiert.
- **Given** ein Review-Handle fehlt, ist syntaktisch ungültig oder passt nicht zum verschlüsselten Credential, **when** der H5P-User-State geladen wird, **then** antwortet der Sidecar fail-closed mit `403` und führt keine Schüler-Impersonation aus.

### Arbeitspaket 1: Teaching-Infrastrukturfehler durchreichen (MUST-FIX)

- **Warum offen:** `DBTeachingRepo` übersetzt Verbindungsfehler korrekt an der Adaptergrenze, aber mehrere Web-Router und Ownership-Guards fangen den Kontextfehler anschließend als generische Exception ab.
- **Regeln und Kontext:** `AGENTS.md` verlangt Contract-first und TDD; `docs/ARCHITECTURE.md` ordnet die Treiberübersetzung `teaching` und die HTTP-Abbildung dem Web-Adapter zu. Owner ist der Bounded Context `teaching`; der zentrale Handler in `backend/web/runtime_errors.py` bleibt die einzige HTTP-Übersetzungsstelle.
- **Dateien:** `backend/tests/test_teaching_repository_readiness.py`, ein source-level Boundary-Contract unter `backend/tests/`, sowie betroffene `backend/web/routes/teaching*.py`-Adapter.
- **Vertrag/Migration:** `api/openapi.yml` dokumentiert `TeachingRepositoryUnavailable503` bereits; keine Vertrags- oder Schemaänderung und keine Migration nötig.
- **Test-first:** Ergänze rote Verhaltensfälle für Unit-Liste, Ownership-Guard, Live-Read und Membership-Write sowie einen AST-Vertrag, der generische Catch-Blöcke mit direkten Repository-Aufrufen nur bei explizitem `TeachingRepositoryUnavailable`-Pass-through akzeptiert.
- **Minimaler Fix:** Fange `TeachingRepositoryUnavailable` vor den jeweiligen generischen Legacy-Fallbacks explizit ab und reiche ihn unverändert weiter; behalte alle übrigen Fehlerabbildungen unverändert.
- **Akzeptanz:** Die repräsentativen Routen liefern über den zentralen Handler `503`; der Boundary-Contract findet keinen direkten Repository-Catch ohne Pass-through.

### Arbeitspaket 2: Personenbeziehbare Logging-Suffixe entfernen (SHOULD-FIX)

- **Warum offen:** `teaching_units.py` protokolliert `sub[-6:]`; der bestehende Privacy-Vertrag scannt nur `teaching.py` und `teaching_live.py` und übersieht ausgelagerte Router.
- **Regeln und Kontext:** Security-first, DSGVO-Datenminimierung und der `identity_access`-Vertrag behandeln `sub` als stabilen Identifikator. Die Diagnoseentscheidung liegt im Teaching-Web-Adapter; es wird keine fachliche Identität verändert.
- **Dateien:** `backend/tests/test_privacy_logging_contract.py`, `backend/web/routes/teaching_units.py` und gegebenenfalls weitere durch den erweiterten Test gefundene Teaching-Router.
- **Vertrag/Migration:** Keine API-, Schema-, Konfigurations- oder Migrationsänderung.
- **Test-first:** Erweitere den AST-Test rot auf alle produktiven `teaching*.py`-Router und verbiete Subject-Namen auch innerhalb von Slices oder Formatargumenten.
- **Minimaler Fix:** Entferne Subject-Argument und Platzhalter; protokolliere ausschließlich einen niedrig-kardinalen Reason-Code und die Exception-Klasse.
- **Akzeptanz:** Der Privacy-Contract ist grün und eine gezielte Laufzeitprobe enthält keinen Teil des Subjects.

### Arbeitspaket 3: Paralleltaugliche H5P-Review-Credentials (SHOULD-FIX)

- **Warum offen:** Der Sidecar speichert jedes Review-Credential unter demselben Cookie-Namen; ein zweiter Modellaufruf überschreibt deshalb das erste Review im selben Browser.
- **Regeln und Kontext:** H5P bleibt technischer Adapter für den Teaching-Review. Credentials und Personenkennungen dürfen nicht in URLs stehen; ein zufälliger, nicht personenbezogener Handle darf lediglich den passenden HttpOnly-Cookie auswählen. Die Oberfläche und das Svelte-Design ändern sich nicht, daher ist kein neuer visueller Designvertrag nötig.
- **Dateien:** zuerst `api/openapi.yml`; danach `h5p-service/lib/review_tokens.mjs`, `h5p-service/lib/review_mode_middleware.mjs`, `h5p-service/server.mjs`, zugehörige Node-/pytest-Contracts sowie `docs/ARCHITECTURE.md` und `docs/references/teaching_live.md`.
- **Vertrag/Migration:** Präzisiere zuerst den bestehenden H5P-Review-Vertrag auf einen pro Review benannten HttpOnly-Cookie und einen opaken `review_id` ausschließlich in der nachgelagerten H5P-User-State-URL. Der initiale Endpunkt und sein Request bleiben unverändert; keine Migration.
- **Test-first:** Ergänze rote Tests für Handle-Ableitung, sichere Cookie-Namen, zwei gleichzeitig vorhandene Review-Cookies, falsche Handle-Credential-Paare und die serverseitige Weitergabe von `review_id` ohne Credential oder PII in der URL.
- **Minimaler Fix:** Leite den Review-Handle aus dem zufälligen AES-GCM-Nonce-Teil ab, validiere ihn streng, verwende einen handle-spezifischen `__Secure-`-Cookie und wähle ihn im Middleware-Read über `review_id` aus; Token-Ablauf und alle bestehenden Lehrer-/Inhalts-/Aufgabenbindungen bleiben unverändert.
- **Akzeptanz:** Zwei interleaved Reviews laden jeweils den richtigen Schülerzustand; fehlende, manipulierte oder vertauschte Handles liefern `403`; Credential und Subjects erscheinen weiterhin in keiner URL.

### Verifikation und Abschluss

- Zuerst die neuen roten pytest-/Node-Contracts einzeln nachweisen, danach minimale Implementierung und Refactor.
- Anschließend gezielte Teaching-, Privacy-, OpenAPI- und H5P-Suites, `git diff --check` und wegen des querschnittlichen Router-/Sidecar-Risikos `make verify` ausführen.
- Produktnahe H5P-/Browser-Smokes werden ausgeführt, sofern die vorhandene lokale Compose-/Browser-Infrastruktur ohne neue externe Autorisierung verfügbar ist; andernfalls wird das Restrisiko ausdrücklich dokumentiert.

## Umsetzungsstand – 2026-07-18 11:14 CEST

### Geschlossene Befunde

- **[API] Teaching-Infrastrukturfehler:** geschlossen. Alle direkten Repository-Catches in den produktiven `teaching*.py`-Routern reichen `TeachingRepositoryUnavailable` vor generischen Fallbacks unverändert weiter. Der zentrale HTTP-Handler bleibt die einzige Übersetzung auf den bestehenden privaten `503`-Vertrag. Repräsentative Verhaltenstests decken Unit-Liste, Ownership-Guard, Live-Summary und Membership-Write ab; ein AST-Vertrag schützt die gesamte Teaching-Routerfläche vor Regressionen.
- **[SEC] Subject-Suffix im Logging:** geschlossen. `teaching_units.py` protokolliert weder vollständige noch gekürzte OIDC-Subjects. Der Privacy-Vertrag scannt jetzt alle produktiven Teaching-Router und erkennt Subject-Variablen auch innerhalb zusammengesetzter Logging-Ausdrücke.
- **[BUG] Globaler H5P-Review-Cookie:** geschlossen. Jeder Review erhält einen streng validierten, aus dem zufälligen AES-GCM-Nonce abgeleiteten opaken Handle und einen eigenen `__Secure-`-Cookie. Die User-State-URL enthält nur `review_id`; Middleware und Tokenparser prüfen Cookie-/Handle-Zuordnung fail-closed. Zwei parallel vorhandene Reviews wurden als Interleaving-Szenario getestet.

### Vertrag, Architektur und Datenbank

- `api/openapi.yml` beschreibt den review-spezifischen HttpOnly-Cookie, die opake `review_id` und das Verbot von Credentials beziehungsweise Personenkennungen in URLs.
- `docs/ARCHITECTURE.md` und `docs/references/teaching_live.md` dokumentieren die zentrale Teaching-Fehlergrenze sowie die paralleltaugliche H5P-Review-Auswahl.
- Keine Schemaänderung, keine Supabase-Migration und keine neue Konfiguration waren erforderlich.
- Keine sichtbare Svelte-/UI-Änderung; ein visueller Designvertrag war daher nicht erforderlich.

### Verifikation

- Rot-Nachweis: Die neuen Teaching-Verhaltens-/Boundary-Tests, der erweiterte Privacy-Vertrag und die H5P-Handle-/Interleaving-Tests schlugen vor der Implementierung gezielt an den drei dokumentierten Befunden fehl.
- Fokussierte Verträge: 20 bestanden, 2 in der eingeschränkten Prozess-/Netzwerksandbox abgewählt; dieselben Health-Pfade bestanden anschließend im vollständigen `make verify`.
- Bestehende betroffene Router-Suites: 33 bestanden, 62 datenbankabhängig übersprungen.
- `make verify`: bestanden. DB-Preflight, Import-, OpenAPI-, Architektur-, Route-Map-, DB-Inventar-, Offline-Supply-Chain-, Ruff- und Docker-Image-Gates grün; Backend 2.052 bestanden und 73 übersprungen; Svelte Check 0 Fehler und 0 Warnungen; Frontend 312 Tests plus 3 Build-Policy-Tests; Produktionsbuild bestanden; H5P 62 Tests bestanden.
- Produktnaher Compose-Smoke: H5P-Service neu gebaut; `test_h5p_service_auth_e2e.py` und `test_h5p_roundtrip_e2e.py` mit aktivierter TLS-Prüfung bestanden (2 Tests). Ein erster Lauf innerhalb der Netzwerksandbox konnte `id.localhost` nicht erreichen; der identische freigegebene Lauf gegen den lokalen Compose-Stack war grün.
- `git diff --check`: bestanden.

### Verbleibendes Risiko und Freigabe

- Es existiert kein dedizierter Browser-E2E-Test für zwei gleichzeitig geöffnete H5P-Review-Tabs. Die Cookie-Isolation ist durch Node-Interleaving-Tests, Python-Quellcode-/OpenAPI-Verträge und die produktnahen H5P-Auth-/Roundtrip-Smokes abgesichert; eine manuelle oder browserautomatisierte Zwei-Tab-Abnahme bleibt als zusätzliche Absicherung sinnvoll.
- Alle drei Befunde aus dieser Review-Runde sind geschlossen. Empfehlung: **technisch freigeben**, vorbehaltlich der üblichen PR-Durchsicht des finalen Diffs.

## Review-Runde – 11:43 CEST

### Review-Metadaten

- Review-Zeitpunkt: 2026-07-18 11:43:09 CEST
- Branch: `master`
- Basis-Ref: `origin/master` (`1407ba727307bb3694899af4ced97856015f9f2c`)
- Merge-Base: `1407ba727307bb3694899af4ced97856015f9f2c`
- HEAD: `8e3b0d30ceb30f5cdfbee9f20599c78a4e879ab4`
- Scope: `origin/master..master`; 229 Commits, 650 Dateien, 59.894 Einfügungen und 40.330 Löschungen
- Geprüfter Umfang: Gesamtstand mit Fokus auf die seit der Review-Runde um 09:46 ergänzten Teaching-Fehlergrenzen, Privacy-Logs und paralleltauglichen H5P-Review-Cookies; zusätzlich `api/openapi.yml`, zugehörige Tests, Architektur-/Referenzdokumentation und der vollständige Verify-Gate; keine Supabase-Migration im Diff
- Kontext: `origin/master` ist der lokal vorhandene Remote-Tracking-Ref und wurde nicht über das Netzwerk aktualisiert. Die drei früheren Befunde wurden erneut am finalen Commit geprüft; die beabsichtigten Fixes greifen für Repository-Methoden und parallele Cookies, lassen aber direkte SQL-Fallbacks und einen H5P-Kontext-Randfall offen.
- Verwendete Code-Referenzen: `backend/web/routes/teaching_live.py`, `backend/web/db_cursor.py`, `backend/tests/test_teaching_repository_error_boundary_contract.py`, `backend/tests/test_privacy_logging_contract.py`, `h5p-service/lib/review_mode_middleware.mjs`, `h5p-service/server.mjs`, `api/openapi.yml` sowie die jeweils zugehörigen Tests

### 1. Kurzfazit

Der große Refactor ist strukturell nachvollziehbar und `make verify` vollständig grün. Die jüngste Reparatur schließt die ursprünglichen Befunde jedoch nicht vollständig: Direkte SQL-Fallbacks umgehen weiterhin die Teaching-Fehlergrenze, verschleiern Datenbankausfälle als „keine Abgabe“ und protokollieren rohe Exception-Texte. Zusätzlich ist die H5P-Taskbindung bei nachgelagerten User-State-Reads optional. Vor Freigabe müssen die beiden Teaching-Befunde test-first geschlossen werden.

### 2. MUST-FIX

- [SEC] Die Latest-Submission-Fallbacks protokollieren rohe Exception-Texte — `backend/web/routes/teaching_live.py:783@8e3b0d30`, `backend/web/routes/teaching_live.py:851@8e3b0d30` — Impact: Die Laufzeitprobe schrieb den vollständigen simulierten Fehlertext einschließlich privatem DB-Host und Schülerpseudonym ins Log; der grüne Privacy-Vertrag erkennt nur Variablennamen wie `student_sub`, nicht über `exc` transportierte PII oder Infrastrukturdetails. Das verletzt Datenminimierung und die dokumentierte Privacy-Baseline. — Fix: Schreibe zuerst einen Regressionstest mit einer PII-haltigen Exception, protokolliere anschließend ausschließlich einen niedrig-kardinalen Reason-Code und die Exception-Klasse und erweitere den Logging-Vertrag so, dass rohe Exception-Objekte in produktiven Teaching-Routern abgewiesen werden.
- [API] Direkte SQL-Zugriffe umgehen die kontextgebundene Fehlerübersetzung und machen einen Datenbankausfall zu `204` beziehungsweise `404` — `backend/web/routes/teaching_live.py:850@8e3b0d30`, `backend/web/routes/teaching_live.py:966@8e3b0d30`, `backend/web/routes/teaching_live.py:1050@8e3b0d30`, `backend/web/db_cursor.py:48@8e3b0d30` — Impact: Die reproduzierte `psycopg.OperationalError` auf dem in OpenAPI mit `TeachingRepositoryUnavailable503` beschriebenen Latest-Submission-Endpunkt ergab `204`; der File-Pfad setzt bei demselben Fehler `row = None` und liefert `404`. Clients und Lehrkräfte unterscheiden dadurch einen Ausfall nicht von fehlenden Daten. Der neue AST-Test bleibt grün, weil er nur `_get_repo()` und `repo.*`, nicht `open_repo_cursor()`, erfasst. — Fix: Schreibe zuerst Ausfalltests für Detail- und Datei-Route, verschiebe die SQL-Abfragen anschließend hinter Teaching-Repository-Methoden mit `OperationalError -> TeachingRepositoryUnavailable` an der Adaptergrenze und erweitere den Boundary-Vertrag auf direkte Cursorpfade.

### 3. SHOULD-FIX

- [SEC] Der H5P-User-State-Read akzeptiert ein gültiges Review ohne Task-Kontext — `h5p-service/lib/review_mode_middleware.mjs:73@8e3b0d30`, `h5p-service/server.mjs:774@8e3b0d30` — Impact: Eine Laufzeitprobe ohne `contextId` rief `next()` auf und impersonierte den Schüler. Zugleich ergänzt der Server `context_id`, während der eingesetzte `@lumieducation/h5p-express`-Controller ausschließlich `contextId` an den kontextgebundenen State-Speicher weiterreicht. Ein manipulierter oder künftig anders erzeugter URL-Pfad kann damit die im Credential vorgesehene Taskbindung verlieren. — Fix: Schreibe zuerst einen fehlenden-Kontext-Test, verlange in Review-Mode exakt einen zum Token passenden `contextId` und normalisiere die umgeschriebene User-State-URL auf diesen vom H5P-Controller ausgewerteten Parameternamen.

### 4. NICE-TO-HAVE

Keine zusätzlichen Befunde.

### 5. TESTS

- `git diff --check origin/master..master`: bestanden.
- Fokussierte Python-Verträge ohne den anschließend im Gesamt-Gate ausgeführten Readiness-Teil: 11 bestanden.
- `make verify`: bestanden; DB-Preflight, Import-, OpenAPI-, Architektur-, Route-Map-, DB-Inventar-, Offline-Supply-Chain-, Ruff- und Docker-Image-Gates grün; Backend 2.052 bestanden und 73 übersprungen; Svelte-Check 0 Fehler und 0 Warnungen; Frontend 312 Tests plus 3 Build-Policy-Tests; Produktionsbuild bestanden; H5P 62 Tests bestanden.
- Laufzeitprobe Teaching: Eine simulierte `psycopg.OperationalError` im direkten Cursorpfad lieferte `204 private, no-store` und protokollierte den vollständigen Exception-Text.
- Laufzeitprobe H5P: Ein gültiges Review-Cookie ohne `contextId` passierte die Middleware und setzte `req.user` auf den Schüler.
- `make dependency-audit`, `make test-e2e`, `make test-visual-smoke` und `make test-full-prod-like` wurden nicht ausgeführt; Browser-E2E und vollständige produktnahe Ausfallpfade bleiben Restrisiken.

### 6. Refactor-Budget

Etwa ein bis zwei Entwicklertage: bis zu ein Tag für zwei Repository-Methoden, deren Contract-/DB-Ausfalltests und die Entfernung der direkten SQL-Fallbacks; wenige Stunden für PII-sicheres Logging samt statischem Vertrag; wenige Stunden für die verpflichtende H5P-Kontextbindung und Node-Regressionstests.

### 7. Freigabe

Nicht freigegeben. Die beiden MUST-FIX-Befunde müssen test-first geschlossen und mit gezielten Ausfalltests sowie `make verify` nachgewiesen werden. Die H5P-Kontextbindung sollte wegen des explizit taskgebundenen Review-Credentials im selben Reparaturblock gehärtet werden; anschließend ist mindestens ein produktnaher H5P-Review-Smoke sinnvoll.

## Reparaturplan – 2026-07-18 11:48 CEST

### Arbeitspaket 1: Latest-Submission-SQL an die Teaching-Repositorygrenze verschieben (MUST-FIX)

- **Warum weiterhin offen:** Detail- und Datei-Route öffnen über `backend.web.db_cursor.open_repo_cursor` selbst PostgreSQL-Cursor. Dadurch greift die zentrale Übersetzung von `psycopg.OperationalError` zu `TeachingRepositoryUnavailable` nicht; Ausfälle werden als `204` oder `404` verschleiert.
- **Regeln und Kontext:** Contract-first, Red-Green-Refactor, Security-first und die in `docs/ARCHITECTURE.md` dokumentierte Treiberübersetzung gelten. Owner ist `teaching`; `learning_submissions` bleibt fachlich im Kontext `learning`, wird hier aber ausschließlich als owner-geprüfte Teaching-Read-Projection gelesen.
- **Dateien:** zuerst `api/openapi.yml` und Tests unter `backend/tests/`; danach `backend/teaching/repo_live_queries.py`, `backend/teaching/repo_db.py` und `backend/web/routes/teaching_live.py`.
- **Vertrag/Migration:** Der Detail-Endpunkt dokumentiert `503` bereits; der Datei-Endpunkt erhält dieselbe bestehende `TeachingRepositoryUnavailable503`-Response. Keine Schemaänderung, Migration oder Konfiguration.
- **Test-first:** Ergänze Ausfalltests für beide Routen und erweitere den AST-Grenzvertrag so, dass produktive Teaching-Router keine direkten Cursorzugriffe enthalten.
- **Minimaler Fix:** Kapsle Detail- und Datei-Read in zwei `DBTeachingRepo`-Methoden, behalte Relation-, Ownership-, RLS- und Helper-Fallback-Semantik bei und lasse den zentralen Decorator Verbindungsfehler übersetzen. Der Web-Adapter baut nur noch HTTP- und Domain-Payloads.
- **Akzeptanz:** Ein simulierter `psycopg.OperationalError` liefert auf beiden Endpunkten den privaten `503`-Vertrag; fehlende Abgaben beziehungsweise Dateien bleiben `204`/`404`.

### Arbeitspaket 2: Exception-Inhalte aus Teaching-Logs entfernen (MUST-FIX)

- **Warum weiterhin offen:** Zwei Warning-Logs formatieren das rohe Exception-Objekt und können dadurch PII oder interne Infrastrukturdetails aus Treibertexten veröffentlichen.
- **Regeln und Kontext:** DSGVO-Datenminimierung und die Logging-Leitlinie aus `docs/ARCHITECTURE.md`; Owner ist der Teaching-Web-Adapter, fachliche Daten oder Identitäten werden nicht verändert.
- **Dateien:** zuerst `backend/tests/test_privacy_logging_contract.py` und ein Laufzeit-Regressionsfall; danach `backend/web/routes/teaching_live.py` beziehungsweise die neue Repository-Read-Projection.
- **Vertrag/Migration:** Keine API-, Schema-, Konfigurations- oder Migrationsänderung.
- **Test-first:** Verbiete in produktiven Teaching-Routern rohe Exception-Objekte als Logging-Argumente und prüfe mit PII-haltigem simuliertem Fehlertext, dass dieser nicht protokolliert wird.
- **Minimaler Fix:** Protokolliere ausschließlich einen konstanten Reason-Code und den Klassennamen über `type(exc).__name__`; Inhalte, Subjects und IDs bleiben aus dem Log.
- **Akzeptanz:** Statischer Privacy-Vertrag und Laufzeitprobe sind grün; der vertrauliche Exception-Text fehlt vollständig im Log.

### Arbeitspaket 3: H5P-Review zwingend an `contextId` binden (SHOULD-FIX)

- **Warum weiterhin offen:** Die Middleware akzeptiert einen fehlenden Task-Kontext, während die vom H5P-Controller ausgewertete Query `contextId` heißt; der Server schreibt derzeit `context_id` in die User-State-URL.
- **Regeln und Kontext:** Fail-closed, minimale Review-Credentials und die dokumentierte Aufgabenbindung gelten. H5P ist technischer Adapter der Teaching-Review-Ansicht; keine sichtbare UI und damit kein `docs/DESIGN.md`-Eingriff.
- **Dateien:** zuerst `h5p-service/test/review_mode_middleware.test.mjs` und ein Server-Quellvertrag unter `backend/tests/`; danach `h5p-service/lib/review_mode_middleware.mjs` und `h5p-service/server.mjs`.
- **Vertrag/Migration:** Der initiale `/h5p/player/review`-Vertrag bleibt unverändert; angepasst wird nur die interne User-State-URL. Keine Migration oder neue Konfiguration.
- **Test-first:** Ergänze einen fehlenden-`contextId`-Fall und einen Vertrag, der für die umgeschriebene User-State-URL exakt `contextId` verlangt.
- **Minimaler Fix:** Akzeptiere ausschließlich einen nichtleeren `contextId`, der exakt `payload.taskId` entspricht, und schreibe denselben Parameternamen in die nachgelagerte URL.
- **Akzeptanz:** Fehlender oder abweichender Kontext liefert `403` ohne Impersonation; gültige und interleaved Reviews bleiben autorisiert.

### Verifikation und Abschluss

- Rote Tests werden vor der Implementierung gezielt ausgeführt und dokumentiert.
- Danach folgen die betroffenen Python-/Node-Suites, OpenAPI- und Architekturverträge, `git diff --check` und wegen der gemeinsamen Teaching-/H5P-Grenzen `make verify`.
- Ein produktnaher H5P-Smoke wird ausgeführt, sofern der lokale Compose-Stack ohne zusätzliche externe Autorisierung verfügbar ist; andernfalls wird dieses Restrisiko explizit festgehalten.

## Umsetzungsstand – 2026-07-18 12:15 CEST

### Geschlossene Befunde

- **[API] Direkte Latest-Submission-SQL-Pfade:** geschlossen. Detail- und Datei-Read liegen jetzt in `backend.teaching.repo_live_queries` und werden ausschließlich über öffentliche Methoden von `DBTeachingRepo` aufgerufen. Damit übersetzt dessen bestehender Decorator `psycopg.OperationalError` an der Teaching-Adaptergrenze zu `TeachingRepositoryUnavailable`; der zentrale HTTP-Handler bleibt die einzige Übersetzung auf den privaten `503`-Fehler. Der Web-Router enthält keinen `open_repo_cursor`-Aufruf mehr.
- **[SEC] Rohe Exception-Texte in Teaching-Logs:** geschlossen. Produktive Teaching-Router protokollieren in den betroffenen Fallbacks nur feste Reason-Codes und Exception-Klassen. Der erweiterte AST-Vertrag verbietet rohe Exception-Werte als Logging-Argumente; die Laufzeitprobe bestätigt, dass ein simulierter PII-/Host-haltiger Treibertext nicht im Log erscheint. Drei bestehende DB-Fallback-Tests wurden auf dieses datensparsame Diagnoseformat umgestellt.
- **[SEC] Optionale H5P-Taskbindung:** geschlossen. Review-User-State-Reads benötigen jetzt zwingend einen nichtleeren `contextId`, der exakt `payload.taskId` entspricht. Die vom Review-Modell erzeugte User-State-URL verwendet denselben vom Lumi-Controller ausgewerteten Parameternamen. Fehlender oder abweichender Kontext liefert `403`, bevor eine Schüler-Impersonation erfolgt.
- **Veraltete Befunde:** keine. Alle drei Findings waren am Ausgangs-HEAD reproduzierbar und wurden umgesetzt.

### Vertrag, Architektur und Datenbank

- Der Detail-Endpunkt dokumentierte `TeachingRepositoryUnavailable503` bereits. Der Datei-Endpunkt besaß bereits eine allgemeine `503`-Response für Datenbank- oder Storage-Ausfälle; nach erneuter Contract-Prüfung war deshalb keine OpenAPI-Änderung erforderlich. Ein Contract-Test schützt die vorhandene `503`-Dokumentation.
- Die Teaching-Read-Projection liest weiterhin owner-geprüft und unter RLS aus `learning_submissions`; SQL und Treiberfehler liegen nun jedoch korrekt in der technischen Teaching-Adaptergrenze statt im FastAPI-Router. Fachlogik wurde nicht in den Web-Adapter verschoben.
- Keine Schemaänderung, keine Supabase-Migration und keine neue Konfiguration. Lokal und Produktion verwenden denselben Codepfad.
- Keine sichtbare Svelte-/UI-Änderung; `docs/DESIGN.md` war daher nicht betroffen. Die Begriffe Aufgabe, Abgabe, Auswertung und Rückmeldung bleiben unverändert glossary-konform.

### Red-Green-Refactor-Nachweis

- **Red:** Der neue Cursor-Grenzvertrag fand drei `open_repo_cursor`-Aufrufe; der Privacy-Vertrag fand neun rohe Exception-Loggingstellen; die beiden simulierten Treiberausfälle wurden als `204`/`404` verschleiert; der H5P-Test ließ einen fehlenden Kontext passieren und der Server-Vertrag fand `context_id` statt `contextId`.
- **Green:** Zwei kleine Repository-Methoden kapseln Detail- und Datei-Read, die Routen delegieren nur noch, Logs verwenden niedrig-kardinale Diagnosedaten, und die H5P-Middleware prüft die Taskbindung fail-closed.
- **Refactor:** Doppelte SQL- und Payload-Fallbackblöcke wurden aus `teaching_live.py` entfernt. Ein erster Gesamt-Lauf zeigte drei veraltete Tests, die rohe SQL-Exception-Inhalte verlangten; deren Assertions wurden auf Reason-Code, Exception-Klasse und explizite Nicht-Offenlegung umgestellt.

### Verifikation

- Neue fokussierte Python-Verträge einschließlich Ausfall-, Privacy-, OpenAPI-, Cursor- und H5P-Quellvertrag: 12 bestanden.
- Bestehender Teaching-Latest-Detail-Test in der eingeschränkten Einzelausführung: 1 bestanden, 11 DB-bedingt übersprungen; dieselben DB-Pfade liefen anschließend im vollständigen Gate.
- H5P-Einzelsuite: 16 Testdateien bestanden; im vollständigen Gate 62 Tests bestanden.
- `make verify`: im finalen Lauf vollständig bestanden. DB-Preflight, Import-, OpenAPI-, Architektur-, Route-Map-, DB-Inventar-, Offline-Supply-Chain-, projektdefinierter Ruff- und Docker-Image-Gate grün; Backend 2.058 bestanden und 73 übersprungen; Svelte-Check 0 Fehler und 0 Warnungen; Frontend 312 Tests plus 3 Build-Policy-Tests; Produktionsbuild bestanden; H5P 62 Tests bestanden.
- Produktnaher Compose-Smoke: H5P-Service mit aktuellem Code neu gebaut; `test_h5p_service_auth_e2e.py` und `test_h5p_roundtrip_e2e.py` mit `E2E_VERIFY_TLS=1` und lokalem CA-Bundle bestanden (2 Tests). Der erste Netzwerksandbox-Lauf konnte `id.localhost` erwartungsgemäß nicht erreichen; der identische freigegebene Lauf war in 2,60 Sekunden grün.
- `git diff --check`: bestanden.

### Verbleibendes Risiko und Freigabe

- Es existiert weiterhin kein dedizierter Browser-E2E-Test, der einen manipulierten H5P-Review-User-State-Request ohne `contextId` oder zwei parallele Review-Tabs im echten Browser ausführt. Middleware-Unit-Test, Server-Quellvertrag, Cookie-Interleaving-Test und produktnahe H5P-Auth-/Roundtrip-Smokes decken die beteiligten Grenzen ab; das verbleibende Risiko ist gering.
- Alle MUST-FIX- und SHOULD-FIX-Befunde der Review-Runde um 11:43 CEST sind geschlossen. Empfehlung: **technisch freigeben**, vorbehaltlich der üblichen menschlichen Durchsicht des finalen Diffs.

## Review-Runde – 13:53 CEST

### Review-Metadaten

- Review-Zeitpunkt: 2026-07-18 13:53 CEST
- Branch: `master`
- Basis-Ref: `origin/master` (`1407ba727307bb3694899af4ced97856015f9f2c`)
- Merge-Base: `1407ba727307bb3694899af4ced97856015f9f2c`
- HEAD: `95f05595f82fe22d9d45f43512faa9f5434ca9bc`
- Scope: `origin/master..master`; 230 Commits, 651 Dateien, 60.166 Einfügungen und 40.321 Löschungen
- Geprüfter Umfang: Gesamtstand mit inkrementellem Schwerpunkt auf den zwei Commits seit der Review-Runde zu `8e3b0d30`; Teaching-Latest-Submission-Repositorygrenze, RLS-/Helper-Semantik, Fehlerabbildung, geänderte DB-/Privacy-/OpenAPI-Tests und H5P-Kontextbindung; keine Supabase-Migration im Diff
- Kontext: `origin/master` ist der lokal vorhandene Remote-Tracking-Ref und wurde nicht über das Netzwerk aktualisiert. Die früheren Teaching- und H5P-Befunde sind im aktuellen Code geschlossen; die Abschlussreparatur führt jedoch einen nicht prod-paritätischen SQL-Fallback ein und lässt vorhandene Fallback-Tests die falsche Verzweigung prüfen.
- Verwendete Code-Referenzen: `backend/teaching/repo_live_queries.py`, `backend/teaching/repo_db.py`, `backend/web/routes/teaching_live.py`, `backend/tests/test_teaching_live_detail_api.py`, `supabase/migrations/20260111130000_teaching_latest_submission_owner_helper_hardening.sql`, `supabase/migrations/20251023093421_learning_rls_policies.sql`, `api/openapi.yml` und die fokussierten H5P-/Privacy-/Readiness-Tests

### 1. Kurzfazit

Die jüngsten Teaching- und H5P-Härtungen greifen, und `make verify` ist vollständig grün. Die neue Latest-Submission-Projection enthält jedoch einen direkten SQL-Fallback, der die Autorisierungssemantik des gehärteten Helpers nicht erhält und unter der produktiven Limited-Role-RLS gar keine fremde Schülerabgabe lesen kann. Zugleich maskieren breite Router-Catches unerwartete Defekte als „keine Abgabe“ beziehungsweise „keine Datei“. Die vorhandenen Fallback-Tests prüfen wegen eines zu frühen Mocks nicht den behaupteten Pfad.

### 2. MUST-FIX

- [SEC] Der neue Kompatibilitätsfallback ersetzt bei jeder Helper-Exception die gehärtete Funktion durch einen semantisch schwächeren direkten Tabellenzugriff — `backend/teaching/repo_live_queries.py:517@95f05595`, `supabase/migrations/20260111130000_teaching_latest_submission_owner_helper_hardening.sql:47@95f05595`, `supabase/migrations/20251023093421_learning_rls_policies.sql:19@95f05595` — Impact: Der SECURITY-DEFINER-Helper bindet Kurs-Owner, Unit-Task-Relation und aktuelle Kursmitgliedschaft; die Ersatzabfrage filtert nur Kurs, Aufgabe und Subject. Mit der produktiven Limited Role blockiert die Student-only-RLS den Lehrerzugriff vollständig, während ein erlaubter erhöhter Test-DSN die fehlenden Helper-Prüfungen umgeht. Verhalten und Schutz hängen damit vom DSN ab und widersprechen „lokal = prod“. — Fix: Schreibe zuerst Limited-Role- und Removed-Member-Regressionstests, entferne danach den direkten Tabellenfallback und behandle einen fehlenden/inkompatiblen Helper als Migrations-/Readinessfehler über die bestehende `503`-Grenze.
- [TEST] Die zwei DB-Fallbacktests stören den ersten Verbindungsaufbau des Endpunkts statt den Latest-Submission-Helper — `backend/tests/test_teaching_live_detail_api.py:988@95f05595`, `backend/tests/test_teaching_live_detail_api.py:1109@95f05595`, `backend/teaching/repo_db.py:1338@95f05595` — Impact: `course_exists_for_owner` fängt genau diesen ersten Fehler ab und wiederholt seine Ownership-Prüfung; anschließend läuft der Latest-Submission-Helper normal. Beide Tests sind daher grün, ohne den im Namen und BDD-Text versprochenen Fallback zu erreichen; einer davon ist Teil des harten `test-db-security`-Gates. — Fix: Injiziere den Fehler gezielt beim Aufruf von `get_latest_submission_for_owner` beziehungsweise beim Helper-Statement, belege die tatsächlich ausgeführte Verzweigung und teste Limited-Role-RLS statt einer bloßen globalen `psycopg.connect`-Aufrufnummer.
- [BUG] Unerwartete Repository- oder Mappingfehler werden weiterhin als fachlich leere Antworten ausgegeben — `backend/web/routes/teaching_live.py:803@95f05595`, `backend/web/routes/teaching_live.py:864@95f05595` — Impact: Eine gezielte Laufzeitprobe mit `ValueError` ergab `204` für den Detail-Endpunkt und `404` für die Datei-Route. Lehrkräfte und Clients sehen dadurch fälschlich „keine Abgabe“ beziehungsweise „keine Datei“, obwohl ein interner Defekt vorliegt; das verfälscht lernrelevante Daten und erschwert Monitoring. — Fix: Schreibe zuerst Regressionstests für unerwartete Fehler, lasse danach ausschließlich echte leere Repository-Ergebnisse zu `204`/`404` werden und reiche unerwartete Exceptions an den zentralen privaten `500`-Fehlerpfad weiter.

### 3. SHOULD-FIX

Keine zusätzlichen Befunde.

### 4. NICE-TO-HAVE

Keine zusätzlichen Befunde.

### 5. TESTS

- `git diff --check origin/master..master`: bestanden.
- Fokussierte Python-Verträge: 10 statische/API-/Privacy-Tests und 2 gezielte Repository-Ausfalltests bestanden.
- Fokussierte H5P-Tests: 2 Node-Testsuites bestanden.
- Gezielte DB-Fallbacktests: 2 bestanden; die Quellpfadanalyse zeigt jedoch, dass der injizierte erste Connect-Fehler in `course_exists_for_owner` verbraucht wird und nicht den behaupteten Latest-Submission-Fallback prüft.
- `make verify`: vollständig bestanden; DB-Preflight, Import-, OpenAPI-, Architektur-, Route-Map-, DB-Inventar-, Offline-Supply-Chain-, Ruff- und Docker-Image-Gates grün; Backend 2.058 bestanden und 73 übersprungen; Svelte-Check 0 Fehler und 0 Warnungen; Frontend 312 Tests plus 3 Build-Policy-Tests; Produktionsbuild bestanden; H5P 62 Tests bestanden.
- Laufzeitprobe der Router-Fehlerabbildung: Ein unerwarteter `ValueError` wurde reproduzierbar zu `204` im Detailpfad und `404` im Dateipfad.
- `make dependency-audit`, Browser-E2E, Visual-Smoke und vollständige produktnahe E2E wurden nicht ausgeführt; diese Lücken ändern die drei lokal reproduzierten Befunde nicht.

### 6. Refactor-Budget

Etwa ein Entwicklertag: wenige Stunden für präzise rote Limited-Role-/Helper-Tests, wenige Stunden zum Entfernen des direkten SQL-Fallbacks und Bereinigen der Fehlerabbildung sowie ein abschließender vollständiger Gate-Lauf.

### 7. Freigabe

Nicht freigegeben. Entferne den DSN-abhängigen direkten SQL-Fallback, korrigiere die zwei falsch zielenden DB-Tests und verhindere fachlich leere Antworten bei unerwarteten Fehlern test-first. Danach sind `make verify` und gezielte Limited-Role-Ausfall-/Autorisierungsproben erneut auszuführen.

## Reparaturplan – 2026-07-18 14:12 CEST

### User Story und BDD-Szenarien

Als Lehrkraft möchte ich in der Detailansicht zuverlässig zwischen „keine Abgabe“, ungültiger Relation, temporär fehlender Datenbankfunktion und einem unerwarteten internen Fehler unterscheiden, damit keine Abgabe fälschlich als nicht vorhanden erscheint und alle Zugriffe dieselben RLS-Regeln wie in Produktion verwenden.

- **Given** der migrationspflichtige Helper `get_latest_submission_for_owner` fehlt, **when** die Lehrkraft Detail oder Datei der letzten Abgabe anfordert, **then** liefert die Teaching-Fehlergrenze den privaten `503`-Vertrag und führt keinen direkten Tabellenfallback aus.
- **Given** der Helper ist vorhanden, aber der Schüler ist nicht mehr Kursmitglied, **when** die Lehrkraft die letzte Abgabe anfordert, **then** bleibt die frühere Abgabe unter der sessiongebundenen Helper-Autorisierung unsichtbar.
- **Given** Repository-Mapping oder Payload-Aufbereitung wirft unerwartet einen Fehler, **when** Detail- oder Datei-Route ihn verarbeitet, **then** antwortet die Route privat mit `500 {"error":"internal_error"}` statt `204` oder `404`.
- **Given** Aufgabe und angefragte Lerneinheit passen nicht zusammen, **when** die Detail-Route aufgerufen wird, **then** liefert sie `404`, ohne den Latest-Submission-Helper aufzurufen.

### Arbeitspaket 1: API-Fehlervertrag präzisieren

- **Finding:** [BUG] Unerwartete Fehler wurden als fachlich leere Antworten ausgegeben.
- **Warum offen:** Die beiden Endpunkte dokumentieren `204`/`404` und `503`, aber keinen kontrollierten privaten `500`-Fehler.
- **Regeln und Kontext:** Contract-first aus `AGENTS.md`; Owner ist `teaching`. Der Web-Adapter darf HTTP-Fehler formen, ohne Learning-Geschäftslogik zu übernehmen.
- **Dateien:** zuerst `api/openapi.yml`, danach `backend/tests/test_openapi_teaching_live_detail_contract.py`.
- **Vertrag/Migration:** Gemeinsame Response `InternalServerError500` und Referenzen an beiden Latest-Submission-Endpunkten ergänzen. Keine Schemaänderung, Migration oder Konfiguration.
- **Test-first:** Fordere für Detail und Datei den gemeinsamen privaten `500`-Vertrag mit `Error`-Schema und `internal_error`-Beispiel.
- **Minimaler Fix:** Nutze in den Teaching-Routen den bestehenden `_private_error`-Helper für unerwartete Fehler.
- **Akzeptanz:** Beide Endpunkte unterscheiden echte leere Ergebnisse (`204`/`404`), Repository-Ausfall (`503`) und unerwarteten Fehler (`500`).

### Arbeitspaket 2: SECURITY-DEFINER-Helper als einzige Abgabe-Lesegrenze

- **Finding:** [SEC] Der direkte SQL-Kompatibilitätsfallback erhält die Helper-Autorisierung nicht.
- **Warum offen:** `repo_live_queries.py` fängt jede Helper-Exception und liest danach direkt aus `learning_submissions`; dieser Pfad ist unter Student-only-RLS nicht prod-paritätisch.
- **Regeln und Kontext:** Security-first, Migrationen als einzige Schemaquelle und „lokal = prod“. `teaching` besitzt die lehrkraftseitige Read-Projection; die Abgabe selbst bleibt ein Begriff aus `learning`.
- **Dateien:** `backend/tests/test_teaching_repo_live_queries_contract.py`, `backend/teaching/repo_live_queries.py`, `backend/teaching/errors.py`, `docs/ARCHITECTURE.md` und `docs/references/teaching_live.md`.
- **Vertrag/Migration:** Bestehender `503`-Vertrag genügt; der erforderliche Helper existiert bereits per Migration, daher keine neue Migration.
- **Test-first:** Simuliere gezielt `UndefinedFunction` am Helper-Statement, erwarte `TeachingRepositoryUnavailable` und verbiete jede direkte `learning_submissions`-Abfrage; sichere zusätzlich die Mitgliedschaftsbindung des bestehenden Helpers ab.
- **Minimaler Fix:** Entferne Savepoint und direkten Tabellenfallback; übersetze ausschließlich den fehlenden Helper in den kontexteigenen Verfügbarkeitsfehler und lasse andere unerwartete Fehler unverändert weiterlaufen.
- **Akzeptanz:** Der Adapter liest letzte Abgaben nur über den gehärteten Helper; fehlende Migration ergibt `503`, nicht leere Daten oder einen DSN-abhängigen Ersatzpfad.

### Arbeitspaket 3: Fallbacktests auf echte Verzweigungen ausrichten

- **Finding:** [TEST] Zwei grüne DB-Tests stören nur den vorgelagerten Ownership-Connect.
- **Warum offen:** Die globale „erster Connect“-Simulation wird von `course_exists_for_owner` verbraucht und erreicht die im Testnamen behauptete Abgabe-Verzweigung nicht.
- **Regeln und Kontext:** Red-Green-Refactor, echte lokale Testdatenbank und lehrbare BDD-Namen. Owner ist `teaching`; keine UI-/Designänderung.
- **Dateien:** `backend/tests/test_teaching_live_detail_api.py`, `backend/tests/test_teaching_repository_readiness.py`, `Makefile` und `backend/tests/test_makefile_targets.py`.
- **Vertrag/Migration:** Keine weitere Vertrags- oder Schemaänderung.
- **Test-first:** Ersetze die globale Connect-Aufrufnummer durch gezielte Repository-/Helper-Injektion; prüfe Removed-Member-Verhalten, Relation-Short-Circuit und unerwartete Fehlerantworten.
- **Minimaler Fix:** Reiche unerwartete Fehler in den gemeinsamen privaten `500`-Response-Helper und aktualisiere den benannten DB-Security-Gate-Test.
- **Akzeptanz:** Jeder Test beweist anhand eines gezielten Mocks beziehungsweise echter RLS-Daten genau die im Namen beschriebene Verzweigung.

### Verifikation und Abschluss

- Zuerst OpenAPI und neue Tests rot nachweisen, danach minimale Implementierung und Dokumentationsrefactor.
- Anschließend fokussierte OpenAPI-, Repository-, Readiness-, Privacy- und DB-Detailtests sowie `git diff --check` ausführen.
- Wegen der gemeinsamen Repository-/Web-Adaptergrenze abschließend `make verify` gegen den lokal migrierten Supabase-Stack ausführen. H5P, Compose und sichtbare UI werden nicht geändert; ein zusätzlicher Design- oder H5P-E2E-Lauf ist deshalb nicht erforderlich.

## Umsetzungsstand – 2026-07-18 14:32 CEST

### Geschlossene Findings

- **[SEC] geschlossen:** `get_latest_submission_for_owner` und die Datei-Projection lesen ausschließlich über den migrationspflichtigen SECURITY-DEFINER-Helper. Der direkte SELECT gegen `public.learning_submissions` sowie der Savepoint-Fallback wurden entfernt. Eine fehlende Funktion wird an der Teaching-Repositorygrenze als `TeachingRepositoryUnavailable` und damit als privater `503`-Vertrag behandelt; andere Exceptions bleiben für die private `500`-Abbildung unterscheidbar.
- **[TEST] geschlossen:** Die beiden DB-Tests verwenden keine globale „erster Connect schlägt fehl“-Simulation mehr. Der Helper-Ausfall wird gezielt am Repository-Aufruf injiziert. Der Relationsfall verfolgt den echten owner-scoped Projection-Aufruf und belegt dessen `404`; ein zusätzlicher echter DB-/RLS-Test belegt, dass eine vor dem Mitgliedschaftsende gespeicherte Abgabe danach als `204` unsichtbar bleibt.
- **[BUG] geschlossen:** Unerwartete Repository- und Mappingfehler ergeben in Detail- und Datei-Route nun `500 {"error":"internal_error"}` mit `Cache-Control: private, no-store` und `Vary: Origin`, statt als `204` oder `404` fachlich falsche Leerergebnisse zu erscheinen. Die Logs nennen nur Fehlerklasse, Kurs-ID und Aufgaben-ID, nicht Schülerkennung oder Abgabeinhalt.

### Vertrag und Architektur

- `api/openapi.yml` definiert die wiederverwendbare Response `InternalServerError500`; beide Latest-Submission-Endpunkte referenzieren sie. Die Datei-Route dokumentiert `503` nun für Datenbank- oder Storage-Ausfälle.
- `docs/ARCHITECTURE.md` und `docs/references/teaching_live.md` beschreiben den Helper als verbindlichen Schema- und Autorisierungsvertrag sowie die Trennung zwischen `204`/`404`, `503` und `500`.
- Gegenüber dem vorläufigen BDD-Text wurde der Relationsnachweis präzisiert: Die Route kann die atomare Beziehung `task ∈ unit ∈ course` nicht ohne Repositoryzugriff beweisen. Der Test verfolgt deshalb bewusst den echten Projection-/Helper-Pfad; dort wird die Relation konsistent und ohne TOCTOU-Lücke geprüft.
- Es waren keine neue Migration, keine Konfigurationsänderung und keine UI-/Designänderung erforderlich. Der bestehende Helper enthält bereits Kurs-Ownership, Unit-/Task-Bindung und aktuelle Kursmitgliedschaft; der Migrationstest sichert diese Definition zusätzlich ab.

### Red-Green-Refactor-Nachweis

- **Red 1:** Vier neue Regressionstests schlugen erwartungsgemäß fehl: fehlender Helper fiel noch auf Direkt-SQL zurück, ein unerwarteter Helperfehler wurde verschluckt und die Routen antworteten fälschlich mit `204` beziehungsweise `404` (`4 failed, 5 passed, 13 deselected`).
- **Green 1:** Nach dem minimalen Repository- und Router-Fix bestand derselbe Fokus (`9 passed, 13 deselected`).
- **Red 2:** Der anschließend ergänzte Datei-Helper-Vertrag reproduzierte die noch nicht übersetzte `UndefinedFunction` (`1 failed`).
- **Green 2:** Detail-Helper, Datei-Helper und unerwarteter Helperfehler bestanden gemeinsam (`3 passed`).
- **DB/RLS:** Helper-Ausfall, Relationsmismatch und Helper-Härtung bestanden gegen die lokal migrierte Datenbank (`3 passed`); der separate Removed-Member-Test bestand ebenfalls (`1 passed`).

### Abschlussverifikation und Freigabe

- `git diff --check`: bestanden.
- Fokussierte OpenAPI-, Repository- und Makefile-Verträge: `24 passed`.
- `make verify`: vollständig bestanden. DB-Preflight, Import-, OpenAPI-, Architektur-, Route-Map-, DB-Inventar-, Offline-Supply-Chain-, Ruff- und Docker-Image-Gates sind grün; Backend `2065 passed, 73 skipped`; Svelte-Check `0 errors, 0 warnings`; Frontend `312 passed` plus 3 Build-Policy-Tests; Produktionsbuild bestanden; H5P `62 passed`.
- Branch blieb während der gesamten Reparatur `master`; es wurde kein Commit erzeugt.
- Alle drei MUST-FIX-Befunde der Review-Runde um 13:53 CEST sind geschlossen. Empfehlung: **technisch freigeben**, vorbehaltlich der üblichen menschlichen Durchsicht des finalen Diffs.
