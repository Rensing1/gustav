# PR-Review: Kursregistrierung über QR-Code und E-Mail-Einladung

## Review Round – 21:33

- Datum: 15. August 2026, 21:33 CEST
- Branch: `codex/course-invite-registration`
- Base-Ref: lokales `master` (`1209a8c8a8f70687cddf9595480464a661e8582a`)
- Merge-Base: `1209a8c8a8f70687cddf9595480464a661e8582a`
- Head: `1209a8c8a8f70687cddf9595480464a661e8582a`
- Scope: vollständiger uncommitted Arbeitsbaum gegen `master`; `HEAD` und `master` zeigen auf denselben Commit.
- Kontext: Geprüft wurden OpenAPI-Vertrag, FastAPI-Routen, PostgreSQL-Migrationen und RLS-Grenzen, Einladungs- und Mail-Domäne, bestehender Worker, SvelteKit-Intent-/Auth-Fluss, QR-Vollbildoberfläche, Compose, Dokumentation sowie neue Unit-, Integrations- und Playwright-Tests. Referenzen mit `+WT` beziehen sich auf den uncommitted Arbeitsbaum auf Basis des genannten SHA.

## Kurzfazit

Die vertikale Umsetzung ist umfangreich und grundsätzlich gut geschnitten; Token, RLS, Keycloak-Fortsetzung und QR-Fallback sind nachvollziehbar aufgebaut. Vor einer Freigabe müssen jedoch Transport- und Seitenheader gehärtet, mehrere OpenAPI-Abweichungen behoben und die fehlerhafte Retry-Semantik korrigiert werden. Der grüne Gesamtlauf übersieht diese Punkte, weil wichtige Negativ- und Datenbanktests fehlen. Unverwandte Practice-Snapshots gehören nicht in diesen Feature-Stand.

## MUST-FIX

- [SEC] Erzwinge STARTTLS statt unverschlüsselten SMTP-Versand zu erlauben — `backend/teaching/course_invitation_mail.py:66@1209a8c+WT` — Impact: Bei `KC_SMTP_STARTTLS=false` sendet der Worker Einladungstoken und Empfängeradresse im Klartext; `validate()` akzeptiert diese Konfiguration trotz zugesagter TLS-Pflicht. — Fix: Lehne `use_starttls=false` im produktiven Mail-Use-Case ab und teste den Fail-closed-Pfad; verwende für lokale Protokolltests einen TLS-fähigen Testserver beziehungsweise eine ausdrücklich injizierte Testgrenze.
- [SEC] Setze Schutzheader auf den HTML-Einladungsseiten, nicht nur als Meta-Element und auf JSON-Endpunkten — `frontend/src/routes/invite/+page.svelte:47@1209a8c+WT` — Impact: Eine reale Abfrage von `/invite` und `/invite/result` liefert weder `Cache-Control: private, no-store` noch den zugesagten HTTP-Header `Referrer-Policy: no-referrer`; der Kurstitel kann dadurch im Browser-Cache verbleiben und der Sicherheitsvertrag ist nicht erfüllt. — Fix: Setze beide Header über einen gemeinsamen `+layout.server.ts`/Hook für `/invite/**` und ergänze einen Server- beziehungsweise Browser-Vertragstest der tatsächlichen Response-Header.
- [API] Trenne Einladungs-Kurzstatus und vollständigen Mailstatus und dokumentiere die Retry-Antwort korrekt — `api/openapi.yml:1931@1209a8c+WT` — Impact: `CourseInvitation.email_status` referenziert ein Schema, das `failed_recipients` zwingend verlangt, während Create/Active nur drei Zähler liefern; der Retry-Endpunkt verspricht dasselbe Statusschema, sendet aber `{queued}`. Generierte Clients und Response-Validatoren brechen. — Fix: Führe getrennte Schemas für Zähler, vollständigen Status und `{queued}` ein und validiere echte Handler-Antworten gegen OpenAPI.
- [API] Richte Request- und Fehlerverträge an der tatsächlichen API aus — `frontend/src/routes/teaching/courses/[courseId]/+page.server.ts:242@1209a8c+WT` — Impact: Die BFF sendet `recipients` als String, obwohl OpenAPI ausschließlich ein Array erlaubt; unvollständige Kursdaten liefern implementiert `409`, dokumentiert ist `400`, und ein unbekannter Retry-Zielwert wird als `202 {queued: 0}` statt `404` beantwortet. — Fix: Sende aus der BFF eine normalisierte Liste und vereinheitliche Statuscodes sowie Not-found-Erkennung in Vertrag, Handler und Tests.
- [TEST] Entferne die drei unverwandten Practice-Snapshot-Änderungen aus diesem Feature — `frontend/e2e/practice-session.spec.ts-snapshots/practice-feedback-light-desktop-chromium-linux.png:1@1209a8c+WT` — Impact: Ohne Änderung am Practice-Code akzeptiert der Branch neue Bildbaselines; dadurch kann eine bestehende visuelle Regression unbemerkt mit dem Einladungsfeature freigegeben werden. — Fix: Stelle alle drei Practice-Baselines auf `master` zurück oder begründe und teste die visuelle Änderung in einem separaten, fachlich passenden PR.
- [BUG] Korrigiere die invertierte Retry-Semantik der Mailzustellung — `supabase/migrations/20260815120000_course_invitations.sql:336@1209a8c+WT` — Impact: Temporäre Fehler bleiben bis zum fünften Versuch `pending` und sind danach wegen `retry_count = 5` nicht manuell retryfähig; permanente Fehler werden dagegen sofort `failed` und vom Retry-Endpunkt erneut eingeplant. Die Aktion „Fehlgeschlagene erneut senden“ wiederholt damit gerade die nicht retryfähigen Fehler. — Fix: Persistiere die Retryfähigkeit explizit und definiere Statusübergänge samt Gesamtlimit so, dass nur temporäre Fehler erneut eingeplant werden; sichere dies mit echten DB-Tests für 4xx, 5xx, Backoff und Versuchslimit ab.

## SHOULD-FIX

- [TEST] Ergänze die im Plan zugesagten Negativ- und Datenbanknachweise — `backend/tests/test_course_invitations_api.py:56@1209a8c+WT` — Impact: Owner-/Fremdowner-, Rollen- und CSRF-Fälle, tatsächlicher Ablauf, Widerruf/Archivierung, direkte Tabellenrechte, Retry/Backoff und Sieben-Tage-Bereinigung werden nicht gegen die echte Datenbank geprüft; der Migrationstest sucht überwiegend nur Textfragmente. — Fix: Schreibe vor den Korrekturen fehlschlagende API- und PostgreSQL-Integrationstests für diese Übergänge und prüfe außerdem reale Responses gegen OpenAPI.
- [TEST] Verlasse dich im neuen Akzeptanztest nicht auf deaktivierte TLS-Prüfung — `frontend/e2e/course-invite-registration.spec.ts:33@1209a8c+WT` — Impact: `ignoreHTTPSErrors: true` maskiert genau den lokalen Zertifikatsfehler, vor dem die Repository-Regeln warnen, und schwächt die Aussage „lokal = Prod“. — Fix: Prüfe die lokale CA im Test-Preflight und erzeuge die Browserkontexte ohne diese Ausnahme; bereinige anschließend die gleichartige globale Playwright-Voreinstellung separat.
- [BUG] Räume den künstlichen History-Eintrag beim Schließen des Vollbilds auf — `frontend/src/lib/components/teacher-course/CourseInvitationPanel.svelte:65@1209a8c+WT` — Impact: `openFullscreen()` führt immer `pushState()` aus, aber Schließen, `Esc` und `fullscreenchange` entfernen den Eintrag nicht; der nächste Druck auf Browser-Zurück scheint deshalb wirkungslos und wiederholtes Öffnen füllt die Historie. — Fix: Synchronisiere Close und Popstate mit einem eindeutigen History-Marker und ergänze einen Test „öffnen → Esc/Schließen → Zurück navigiert wirklich“.
- [BUG] Unterscheide „keine aktive Einladung“ von Ladefehlern — `frontend/src/routes/teaching/courses/[courseId]/+page.server.ts:140@1209a8c+WT` — Impact: Jeder nicht erfolgreiche Active-Request wird wie `404` als fehlender Link dargestellt; bei einem vorübergehenden `503` bietet die UI dadurch fälschlich „Klassenlink erstellen“ an und kann bei Erholung einen bestehenden Link rotieren. — Fix: Behandle ausschließlich `404` als leeren Zustand und zeige beziehungsweise wirf alle übrigen Fehler explizit.
- [BUG] Isoliere Mailkonfiguration und Mailfehler vom gemeinsamen Learning-Worker — `backend/teaching/course_invitation_mail.py:244@1209a8c+WT` — Impact: Fehler in `purge()`, `claim()` oder bereits `int(KC_SMTP_PORT)` liegen außerhalb des Catch-Blocks; sie beenden die gemeinsame Worker-Schleife und stoppen damit auch Lernfeedback und Lifecycle-Jobs. — Fix: Fasse den gesamten Mail-Tick in eine adressfreie Fehlergrenze und lasse `run_forever()` nach einem begrenzten Backoff weiterarbeiten; teste eine ungültige Portangabe und Repository-Ausfälle.
- [BUG] Sperre den Fokus im seitenfüllenden Vollbild-Fallback — `frontend/src/lib/components/teacher-course/CourseInvitationPanel.svelte:153@1209a8c+WT` — Impact: Das Overlay ist visuell modal, aber darunterliegende Link-, Mail- und Kursaktionen bleiben per Tab erreichbar; `aria-modal` allein macht sie nicht inert. — Fix: Verwende die vorhandene Modalabstraktion mit Fokusfalle oder setze den restlichen Drawer während des Fallbacks `inert`; teste Tab-/Shift-Tab-Zyklen und Fokuswiederherstellung.

## NICE-TO-HAVE

- [CONSISTENCY] Entferne die nicht mehr verwendete Keycloak-Hostzuordnung für den SMTP-Test — `docker-compose.yml:269@1209a8c+WT` — Impact: Der Testserver läuft inzwischen im `gustav-frontend`-Container und wird über dessen Servicenamen erreicht; der Kommentar und `host.docker.internal` sind tote, testbezogene Produktionskonfiguration. — Fix: Entferne `extra_hosts` an Keycloak und sichere nur die tatsächlich benötigte Container-zu-Container-Verbindung im Test.

## TESTS

- Bereits erfolgreich: `make verify-feature` mit 2.396 Backend-Tests, 527 Frontend-Tests und 17/17 `@feature-acceptance`-Chromium-Tests.
- Bereits erfolgreich: `make docker-validate` und `git diff --check`.
- Im Review zusätzlich geprüft: reale Header von `https://app.localhost/invite`, `/invite/result` und `/invite/intent` mit der lokalen Caddy-CA; die HTML-Seiten bestätigen das Header-Finding, der JSON-Intent liefert die erwarteten Header.
- Nicht belastbar abgeschlossen: Online-`npm audit` war wegen der gesperrten Registry-/DNS-Verbindung nicht abrufbar; der repository-eigene Supply-Chain-Gate im erfolgreichen `make verify-feature` blieb grün.

## Refactor-Budget

Etwa 2 bis 3 konzentrierte Entwicklungstage: ein Tag für OpenAPI, Retry-Migration und Tests; ein halber bis ganzer Tag für Header, TLS und Worker-Isolation; ein halber Tag für Vollbild-Historie, Fokus und PR-Bereinigung. Danach den vollständigen `make verify-feature`- und `make docker-validate`-Lauf erneut ausführen.

## Freigabe

Nicht freigegeben. Sechs MUST-FIX-Findings blockieren den Merge gegen `master`; insbesondere unverschlüsselbarer Mailtransport, fehlende Seitenheader, widersprüchliche API-Verträge und die invertierte Retry-Logik müssen vor einer erneuten Review-Runde behoben werden.

## Reparaturplan – 2026-08-15 21:39

### 1. Vertrag und öffentliche Sicherheitsgrenzen

- Findings: OpenAPI-Request/-Response-Drift, fehlende Einladungsseiten-Header und deaktivierte TLS-Prüfung im Browserlauf.
- Weiterhin offen: Die im Review belegten Schemas, Statuscodes und HTML-Header sind unverändert; die lokale Caddy-CA ist inzwischen für System, Chromium/Codex und Firefox als vertraut bestätigt.
- Regeln: Contract-first, Security-first, Browser-BFF aus `docs/ARCHITECTURE.md`; Einladungen bleiben im Bounded Context `teaching`, die Auth-Fortsetzung in `identity_access`.
- Zuerst: `api/openapi.yml` in Zähler-, Vollstatus- und Retry-Schemas trennen; fehlschlagende Vertrags-, Response-Header- und BFF-Tests ergänzen.
- Minimal: BFF sendet ein Array, Handler unterscheiden `404`/`409` korrekt, `/invite/**` setzt `private, no-store` und `no-referrer`, Playwright prüft wieder die vertraute CA.
- Abnahme: gezielte pytest-/Vitest-/Playwright-Tests validieren echte Payloads, Statuscodes und HTTP-Header.

### 2. Retry-Zustandsmaschine, RLS und Worker-Isolation

- Findings: invertierte Retryfähigkeit, fehlende DB-Negativtests, optionales STARTTLS und Mailfehler, die den gemeinsamen Worker beenden können.
- Weiterhin offen: Die Migration speichert Retryfähigkeit nicht; permanente Fehler werden erneut eingeplant, temporäre nach Ausschöpfen des Limits nicht. Konfiguration, Claim und Cleanup liegen teilweise außerhalb der Fehlergrenze.
- Regeln: Schema ausschließlich per neuer Supabase-Migration; feste `search_path`- und Least-Privilege-Grenzen; fachliche Mailzustände bleiben in `teaching`, der bestehende Learning-Worker ist nur Ausführungsort.
- Zuerst: echte PostgreSQL-Tests für temporär/permanent, Backoff, Limit, Ablauf, Archivierung, Bereinigung und direkte Tabellenrechte sowie Worker-Tests für TLS- und Infrastrukturfehler rot schreiben.
- Minimal: `retryable` explizit persistieren, nur temporäre Fehler automatisch/manuell einplanen, unbekannte Einladungen als `404` abbilden, STARTTLS erzwingen und den Mail-Tick vollständig isolieren.
- Abnahme: Migration lokal anwenden; gezielte DB-, API- und Worker-Slices gegen die produktionsgleiche Datenbank ausführen.

### 3. Vollbild, Fehler-UX und PR-Hygiene

- Findings: liegenbleibender History-Eintrag, fehlende Fokusisolation, verschleierte Active-API-Fehler, fachfremde Practice-Baselines und tote Compose-Konfiguration.
- Weiterhin offen: Schließen konsumiert den Vollbild-History-Marker nicht; das Fallback setzt den Hintergrund nicht inert; der Loader behandelt jedes Nicht-2xx wie `404`.
- Regeln: gemeinsame Drawer-/Fokusprinzipien aus `docs/DESIGN.md`, keine neue visuelle Sprache und keine Dev-only-Infrastruktur.
- Zuerst: Komponenten- und Loader-Vertragstests für History, Tab-Fokus und `503` schreiben.
- Minimal: History-Marker deterministisch konsumieren, außerhalb des QR-Containers liegende DOM-Zweige während des Fallbacks `inert` setzen, nur `404` als leeren Zustand behandeln, tote Keycloak-Hostzuordnung entfernen und Practice-Baselines auf `master` zurückstellen.
- Abnahme: Komponenten-, Seiten- und Browser-Slices; danach `make docker-validate`, `make verify-feature` und `git diff --check`.

## Ausführung – 2026-08-15 22:14

### Umgesetzte Korrekturen

- MUST-FIX abgeschlossen: Der Teaching-Mailpfad akzeptiert nur noch aktiviertes STARTTLS mit normaler Zertifikatsprüfung und least keine Nachricht bei ungültiger Transportkonfiguration.
- MUST-FIX abgeschlossen: `/invite` und `/invite/result` erhalten über ein gemeinsames SvelteKit-Serverlayout `Cache-Control: private, no-store` und `Referrer-Policy: no-referrer`; Unit- und echter Browsertest prüfen die Header.
- MUST-FIX abgeschlossen: OpenAPI trennt Einladungszähler, vollständigen Mailstatus und Retry-Quittung. Create-Konflikte, malformed Capability-404 und Retry-404 entsprechen den Handlern.
- MUST-FIX abgeschlossen: Die BFF übergibt Empfänger als Array; der FastAPI-Payload akzeptiert ausschließlich diese Vertragsform. Validierung, Normalisierung und Deduplizierung bleiben zentral im Backend.
- MUST-FIX abgeschlossen: Alle drei fachfremden Practice-Baselines sind exakt auf `master` zurückgestellt.
- MUST-FIX abgeschlossen: Die neue vorwärtsgerichtete Migration `20260815214500_course_invitation_mail_retry_hardening.sql` persistiert `retryable`. Nur transiente Fehler werden automatisch oder manuell erneut eingeplant; permanente und nach fünf Versuchen ausgeschöpfte Fehler bleiben final.
- SHOULD-FIX abgeschlossen: Echte PostgreSQL- und API-Tests decken Owner/Fremdowner, Rollen, CSRF, Metadatenkonflikt, Ablauf, Widerruf, Archivierung/Wiederherstellung, direkte Tabellenrechte, transient/permanent, Fünfversuchsgrenze und Adressbereinigung ab.
- SHOULD-FIX abgeschlossen: Die neue Akzeptanzprüfung und die globale Playwright-Konfiguration deaktivieren die TLS-Prüfung nicht mehr. `make local-ca-status` bestätigt die aktuelle CA für System, Chromium/Codex und Firefox als vertraut.
- SHOULD-FIX abgeschlossen: Der QR-Vollbildmodus konsumiert genau einen temporären History-Eintrag. Browser-Zurück schließt ohne zweiten Rücksprung; Schließen, Escape und `fullscreenchange` räumen den Eintrag auf.
- SHOULD-FIX abgeschlossen: Nur `404` bedeutet „keine aktive Einladung“; andere Active- und Statusfehler werden als `BackendRequestError` weitergereicht.
- SHOULD-FIX abgeschlossen: Konfigurations-, Purge-, Claim- und Ack-Fehler des Teaching-Mailpfads werden PII-frei isoliert. Zusätzlich schützt die gemeinsame Worker-Schleife die Lern- und Lifecycle-Verarbeitung vor unerwarteten Mailprozessorfehlern.
- SHOULD-FIX abgeschlossen: Der Vollbild-Fallback setzt alle außerhalb des QR-Pfads liegenden DOM-Zweige `inert`, hält Tab und Shift-Tab auf der Schließen-Aktion und stellt anschließend den Fokus wieder her.
- NICE-TO-HAVE abgeschlossen: Die nicht mehr verwendete Keycloak-Zuordnung für `host.docker.internal` ist entfernt; der SMTP-Capture läuft im `gustav-frontend`-Container und wird per Servicename erreicht.

### Nachweise

- `40 passed`: gezielte OpenAPI-, Migrations-, Domänen-, Worker-, PostgreSQL- und API-Tests; darunter ungültiger SMTP-Port, realer Backoff und die Sieben-Tage-Grenze.
- `23 passed` sowie `svelte-check found 0 errors and 0 warnings`: gezielte Frontend- und Komponentenprüfungen.
- `make verify` als erster Teil von `make verify-feature`: 2.412 Backendtests bestanden, 78 übersprungen; 533 Frontendtests bestanden; Produktionsbuild, OpenAPI-, Architektur-, Supply-Chain-, Lint-, Image- und H5P-Prüfungen grün.
- `make docker-validate`: grün.
- `git diff --check`: grün.
- Vollständiger neu gebauter Einladungs-Browserrundlauf: `1 passed`; echte Keycloak-Registrierung, SMTP-Verifikation, Kursbeitritt, QR-Fallback, Fokus/Schließen und HTTP-Sicherheitsheader sind bestätigt. Eine zusätzliche In-App-Browserprüfung bestätigt den fail-closed Zustand einer ungültigen Capability über die vertraute lokale CA.

### Restrisiko und Gate-Status

`make verify-feature` endet weiterhin mit einem fachfremden visuellen Practice-Fehler, nachdem sämtliche Produkt-, Build- und Unit-Suiten grün waren. Der isolierte Test reproduziert auf dem frisch gebauten Stack eine Abweichung von 17.840 Pixeln (`0,02`) in `practice-feedback-light-desktop.png`. Gegen `master` existiert keine Änderung am Practice-Quellcode; die drei zuvor geänderten Baselines wurden entsprechend dem MUST-FIX bewusst nicht erneut akzeptiert. Der Einladungs-`@feature-acceptance`-Test selbst ist danach isoliert vollständig grün gelaufen. Eine Baseline-Aktualisierung gehört in einen getrennten, fachlich begründeten Practice-PR; bis dahin bleibt das repositoryweite Feature-Gate formal rot, obwohl kein offenes Einladungs-Finding verbleibt.
