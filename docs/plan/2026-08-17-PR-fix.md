# PR-Review: Korrekturen aus dem Live-Test zum Schulstart

## Review Round – 17:37

- Datum und Zeit: 2026-08-17, 17:37 CEST
- Branch: `codex/live-test-schultag`
- Base-Ref: lokaler `master`
- Base-SHA: `bb27cf868ab0ead8de0345b873d0921d7fc03094`
- Merge-Base-SHA: `bb27cf868ab0ead8de0345b873d0921d7fc03094`
- Head-SHA: `0076df7270c9f6a645335a053246bf2eef18ebed`
- Review-Diff: `bb27cf868ab0ead8de0345b873d0921d7fc03094..0076df7270c9f6a645335a053246bf2eef18ebed`

### Geprüfter Umfang

Der Branch umfasst 47 geänderte Dateien mit fünf Implementierungsplänen, Frontend-Logik für Responsive Layout, aufgabenbezogene Entwürfe, sichtbare Finalisierung, Materialentwurfsvergleich und Aufgabenstellungsvorschauen sowie Komponenten-, Server-, Browser- und visuelle Tests. `api/openapi.yml`, Migrationen und Backend-Fachlogik sind im Diff unverändert; der vorhandene Finalisierungsvertrag und seine Idempotenzimplementierung wurden als direkte Abhängigkeiten mitgeprüft.

### Kontextnotizen

- OIDC, Login, Cookies, Redirects und Rate Limits sind nicht geändert.
- Same-Origin-/CSRF-Weitergabe, bestehende RLS-Prüfungen, bereinigte Fehlermeldungen und private Browserentwürfe bleiben erhalten.
- Der Arbeitsbaum war zu Review-Beginn sauber. `make verify` war auf dem geprüften Head vollständig grün: 2.418 Backend-Tests, 78 übersprungen, 562 Frontend-Tests, Svelte-Typprüfung, Produktions-Build sowie Architektur-, OpenAPI-, Supply-Chain- und Image-Prüfungen. Der zuvor ausgeführte Feature-Nachweis hatte 20 von 20 authentifizierten Browser-Rundläufen bestanden.
- Die geänderten Dialog- und Practice-Referenzbilder wurden visuell kontrolliert.

### Verwendete Codereferenzen

- `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte:1140-1143`
- `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.server.ts:240-253`
- `frontend/src/lib/learning-unit/submission-finalization.ts:14-30`
- `frontend/src/lib/learning-unit/task-preview.ts:20-31`
- `frontend/e2e/learner-task-drafts.spec.ts:10-12`
- `frontend/e2e/learner-task-finalization.spec.ts:11-13,48-75`
- `frontend/e2e/learner-task-responsive.spec.ts:11-13,30-69`
- `frontend/e2e/practice-session.spec.ts:198-204`
- `backend/tests/test_current_documentation_contract.py:73-82`
- `docs/plan/2026-08-17-zuverlaessige-endgueltige-aufgabenabgabe.md:48-52`
- `docs/plan/2026-08-17-schueleraufgaben-ipad-querformat.md:22-26`
- `api/openapi.yml:7009-7088`
- `backend/learning/repo_submission_command_queries.py:601-785`

### Kurzfazit

Der Branch löst die fünf beobachteten Probleme grundsätzlich verständlich und mit guter Testbreite. Vor der Freigabe bleiben jedoch vier relevante Lücken: neue Browsertests umgehen TLS-Prüfungen, Wiederholungen einer bereits gespeicherten Finalisierung sind nicht stabil idempotent, eine parallele Aufgabe kann Klicks kommentarlos verlieren und der Kürzungshinweis erkennt reale Zweizeilen-Clips nicht zuverlässig. API, Datenmodell und Sicherheitsgrenzen mussten ansonsten nicht verändert werden.

### MUST-FIX

- [SEC] Entferne `ignoreHTTPSErrors: true` aus allen drei neuen Akzeptanztests — frontend/e2e/learner-task-drafts.spec.ts:11@0076df72 — Impact: Die Tests können trotz defekter lokaler CA bestehen und prüfen damit TLS und Secure-Cookies nicht mehr produktionsnah; derselbe Bypass steht auch in `learner-task-finalization.spec.ts:12` und `learner-task-responsive.spec.ts:12`. — Fix: Vertraue die lokale Caddy-CA gemäß `make local-ca-status`/`make trust-local-ca` und erzeuge die Browserkontexte ohne abgeschaltete Zertifikatsprüfung.
- [API] Verwende für Wiederholungen derselben Finalisierung einen stabilen Idempotenzschlüssel — frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.server.ts:251@0076df72 — Impact: Hat das Backend bereits gespeichert, aber die Erfolgsantwort geht verloren, erzeugt der nächste Klick mit einem neuen UUID-Schlüssel eine weitere finale Abgabe und kann einen Versuch verbrauchen. — Fix: Binde einen Schlüssel bis zum bestätigten Erfolg an den konkreten Rückmeldungsentwurf und prüfe den Lost-Response-/Retry-Fall gegen den vorhandenen Finalisierungsendpunkt.
- [BUG] Verwirf Abgaben in einer zweiten Aufgabe nicht kommentarlos — frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte:1140@0076df72 — Impact: Während Aufgabe A verarbeitet wird, erscheinen die Aktionen in Aufgabe B aktiv; `cancel()` beendet dort den Klick ohne Meldung und reproduziert damit genau den Eindruck „nichts geschieht“. — Fix: Zeige und sperre den globalen laufenden Zustand in jeder geöffneten Aufgabe oder verwalte Pending-Zustände aufgabenbezogen; ergänze einen Test für A-pending → B-abgeben.
- [BUG] Erkenne die tatsächliche visuelle Kürzung statt einer festen 120-Zeichen-Heuristik — frontend/src/lib/learning-unit/task-preview.ts:30@0076df72 — Impact: Eine einzeilige Aufgabenstellung unter 121 Zeichen kann auf einem Smartphone mehr als zwei Zeilen benötigen, wird durch CSS abgeschnitten und erhält dennoch keinen Hinweis auf weitere Angaben. — Fix: Leite den Hinweis von der realen Überlaufmessung der Vorschau ab oder verwende eine Darstellung, die niemals ohne sichtbaren Hinweis gekürzt wird; teste einen schmalen Viewport mit einer langen logischen Zeile.

### SHOULD-FIX

- [TEST] Decke die endgültige Abgabe einer Datei über die echte Oberfläche ab — docs/plan/2026-08-17-zuverlaessige-endgueltige-aufgabenabgabe.md:52@0076df72 — Impact: Der Plan verspricht einen Playwright-Nachweis für Text und Datei, der neue Browsertest finalisiert aber ausschließlich Text; der abweichende Upload-Formularpfad bleibt unbewiesen. — Fix: Ergänze den vorhandenen Finalisierungs-Rundlauf um eine vollständig rückgemeldete Datei oder korrigiere den Plan ausdrücklich auf den tatsächlich geprüften Umfang.
- [TEST] Maskiere nicht die gesamte zentrale Practice-Arbeitsfläche — frontend/e2e/practice-session.spec.ts:203@0076df72 — Impact: Das neue Referenzbild besteht großflächig nur noch aus der Maskenfarbe und kann Layoutregressionen an Aufgabe, Rückmeldung und Hauptinteraktion nicht mehr erkennen. — Fix: Stabilisiere die dynamische Rückmeldung oder maskiere nur deren variablen Inhalt bei erhaltener Geometrie.
- [TEST] Prüfe die dokumentierten größeren iPad-Breiten oder passe den Plan an — docs/plan/2026-08-17-schueleraufgaben-ipad-querformat.md:24@0076df72 — Impact: Der Plan nennt Geometrie- und Screenshot-Prüfungen bei 1080 beziehungsweise 1180 Pixeln, der neue Rundlauf prüft nur 1024, 820 und 390 Pixel. — Fix: Ergänze mindestens eine größere Querformatbreite ohne horizontalen Überlauf oder entferne den nicht erbrachten Nachweis aus dem Plan.
- [TEST] Entkopple den Datumsformat-Vertrag von den beiden aktuellen Tagen — backend/tests/test_current_documentation_contract.py:81@0076df72 — Impact: Jeder korrekte neue Tagesabschnitt lässt den Test fehlschlagen und erzwingt eine sachlich unnötige Teständerung. — Fix: Prüfe ausschließlich gültige, eindeutige und absteigend sortierte ISO-Daten.

### NICE-TO-HAVE

Keine weiteren findings. Die neuen frameworkfreien Helfer sind klein, verständlich benannt und angemessen dokumentiert.

### TESTS

- Bestanden: `make verify` mit 2.418 Backend- und 562 Frontend-Tests, Typprüfung, Produktions-Build und statischen Verträgen.
- Bestanden: zuvor 20/20 `@feature-acceptance`-Rundläufe für denselben Produktcode.
- Ergänzen: Cross-Task-Pending, Lost-Response-Retry mit stabilem Idempotenzschlüssel, reale mobile Vorschaukürzung und Upload-Finalisierung.
- Nach Korrektur der TLS-Bypässe `make local-ca-status` und anschließend `make verify-feature` erneut ausführen.

### Refactor-Budget

Klein bis mittel: ungefähr ein Arbeitstag. Die Korrekturen bleiben im Browser-BFF, in den kleinen UI-Helfern und in Tests; weder Migration noch neue Backend-Schicht sind erforderlich.

### Freigabe

Noch nicht freigeben. Die vier MUST-FIX-Punkte betreffen einen expliziten Sicherheitsgrundsatz und drei Kernversprechen der gemeldeten Schülerprobleme. Nach deren Korrektur und einem grünen produktionsnahen Feature-Nachweis ist der Branch aus Review-Sicht freigabefähig.

## Reparaturplan – 2026-08-17 17:43

Die erneute Prüfung am unveränderten Head `0076df7270c9f6a645335a053246bf2eef18ebed` bestätigt alle acht Findings als offen. Keines ist veraltet. Die Änderungen bleiben in vorhandenen Verträgen; `api/openapi.yml`, Migrationen, Glossar und Laufzeitkonfiguration müssen nicht vorab geändert werden.

### Arbeitspaket 1: Verifizierte TLS-Browserprofile

- Findings: `[SEC] ignoreHTTPSErrors`.
- Warum offen: Alle drei neuen Akzeptanztests erzeugen weiterhin Browserkontexte mit abgeschalteter Zertifikatsprüfung.
- Regeln und Eigentümer: Security first, lokal = Produktion und Browser-CA-Regel aus `AGENTS.md`; querschnittliche Testinfrastruktur mit Bezug zu `identity_access`.
- Dateien: `backend/tests/test_e2e_tls_contract.py`, die drei neuen `frontend/e2e/learner-task-*.spec.ts`.
- Contract/Migration/Docs/Config: keine Änderung an OpenAPI, Schema oder Konfiguration.
- Test zuerst: Repository-Vertrag rot erweitern, sodass kein produktionsnaher Playwright-Test `ignoreHTTPSErrors: true` enthalten darf.
- Minimaler Fix: TLS-Bypässe entfernen und vor dem Browserlauf den bestehenden CA-Preflight verwenden.
- Akzeptanz: TLS-Vertrag grün, `make local-ca-status` grün und die betroffenen Playwright-Tests laufen mit geprüfter lokaler CA.

### Arbeitspaket 2: Wiederholbare und sichtbare Finalisierung

- Findings: `[API] stabiler Idempotenzschlüssel`, `[BUG] kommentarloser Cross-Task-Abbruch`, `[TEST] Datei-Finalisierung`.
- Warum offen: Der BFF erzeugt pro POST eine neue UUID; ein anderer laufender Task führt nur zu `cancel()`; der Browsernachweis deckt ausschließlich Text ab.
- Regeln und Eigentümer: `learning`; unveränderliche Abgaben, bestehender OpenAPI-Finalisierungsvertrag, sofort sichtbare Statusmeldungen aus `DESIGN.md`.
- Dateien: `submission-finalization.ts` und Test, `LearningTaskCard.svelte` und Test, Learning-`+page.svelte`, `+page.server.ts` und deren Vertragstests, Finalisierungs-E2E samt Fixture.
- Contract/Migration/Docs/Config: Der vorhandene optionale `Idempotency-Key` reicht aus; keine API- oder Schemaänderung. Der Implementierungsplan wird an den tatsächlich erbrachten Dateinachweis angeglichen.
- Test zuerst: Tests für einen aus der rückgemeldeten Submission abgeleiteten stabilen Schlüssel, wiederholte BFF-Weitergabe, sichtbare Cross-Task-Ablehnung und Datei-Finalisierung schreiben.
- Minimaler Fix: `finalize-<feedback-submission-id>` als validierten Schlüssel weiterreichen; bei globaler Sperre eine aufgabenbezogene Fehlermeldung anzeigen; Datei-Fixture um einen vollständig rückgemeldeten PDF-Versuch ergänzen.
- Akzeptanz: derselbe Rückmeldungsversuch verwendet bei Wiederholung denselben Header, Cross-Task-Klick zeigt eine Meldung, Text und Datei finalisieren im echten Browserlauf genau einmal.

### Arbeitspaket 3: Echte Vorschaukürzung und vollständige iPad-Breiten

- Findings: `[BUG] 120-Zeichen-Heuristik`, `[TEST] größere iPad-Breiten`.
- Warum offen: Der fachliche Vorschauhelfer kennt die tatsächliche Zweizeilen-Geometrie nicht; der Browsertest endet bei 1024 Pixeln.
- Regeln und Eigentümer: `learning`; höchstens zwei Vorschauzeilen mit sichtbarem Hinweis sowie 60-rem-Zweispaltenvertrag aus `DESIGN.md`.
- Dateien: `task-preview.ts` und Test, `LearningTaskCard.svelte` und Test, `learner-task-responsive.spec.ts`, zugehöriger Implementierungsplan.
- Contract/Migration/Docs/Config: keine API- oder Schemaänderung.
- Test zuerst: Komponenteninteraktion mit künstlichem `scrollHeight > clientHeight` sowie Browsergeometrie bei 1180 Pixeln ergänzen.
- Minimaler Fix: Überlauf der realen Vorschaufläche bei Inhalt und Größenänderung messen; semantischen Mehrinhalt und sichtbaren Clip gemeinsam für den Hinweis verwenden.
- Akzeptanz: eine lange logische Zeile erhält auf schmaler Fläche den Hinweis, kurze vollständige Aufgaben nicht; 1024 und 1180 Pixel bleiben ohne Überlauf zweispaltig.

### Arbeitspaket 4: Aussagekräftiger Practice-Screenshot

- Finding: `[TEST] vollständig maskierte Practice-Hauptfläche`.
- Warum offen: Die aktuelle Maske entfernt den gesamten zentralen visuellen Prüfbereich.
- Regeln und Eigentümer: `learning`/Practice; visuelle Referenzen müssen die in `DESIGN.md` definierte Arbeitsfläche tatsächlich abdecken.
- Dateien: `backend/tests/test_visual_smoke_contract.py`, `frontend/e2e/practice-session.spec.ts`, betroffene Referenzgrafik.
- Contract/Migration/Docs/Config: keine Änderung.
- Test zuerst: Repository-Vertrag rot ergänzen, der die Vollmaskierung von `.practice-session__main` verbietet.
- Minimaler Fix: dynamischen Feedbacktext und Fälligkeit vor dem Screenshot auf feste Testdarstellung setzen und nur globale variable Kontrollen maskieren.
- Akzeptanz: Vertrag und visueller Vergleich grün; Hauptfläche ist im Referenzbild wieder sichtbar.

### Arbeitspaket 5: Wartbarer Datumsvertrag

- Finding: `[TEST] fest verdrahtete Changelog-Tage`.
- Warum offen: Der Dokumentationstest erwartet weiterhin exakt `2026-08-17` und `2026-08-16`.
- Regeln und Eigentümer: querschnittliche FOSS-Dokumentation; datumsbasierter Changelog bleibt unabhängig von der Produktversion.
- Dateien: `backend/tests/test_current_documentation_contract.py`.
- Contract/Migration/Docs/Config: keine Änderung.
- Test zuerst und minimaler Fix: Die bestehende Prüfung auf gültige, eindeutige und streng absteigende ISO-Daten refaktorieren.
- Akzeptanz: heutiger Changelog bleibt grün und ein späterer korrekt einsortierter Tagesabschnitt benötigt keine Teständerung.
