# PR-Review: Produktivauffälligkeiten in der Lernendenansicht

Review-Zeitpunkt: 2026-08-20T14:30:10+02:00

## Git-Kontext

- Branch: `master`
- Lokale Basis `master`: `3a3abf0931b1381fd6abcfcd41f1c631e2e2522f`
- Formale Merge-Base `master...HEAD`: `3a3abf0931b1381fd6abcfcd41f1c631e2e2522f`
- Aktueller HEAD: `3a3abf0931b1381fd6abcfcd41f1c631e2e2522f`
- Vergleichsbasis der eigenen Änderungen: `origin/master` bei `08ea802580b79a35871af449b7d667cf341f1c20`
- Review-Head der eigenen Änderungen: `8cdc3ebb48dc497fa65d1a299f6923599f5aded0`
- Geprüfter Scope: `08ea8025..8cdc3ebb` mit den sechs Commits `9448580b`, `ae8bbc68`, `04c24675`, `804536ef`, `125afa7c` und `8cdc3ebb`
- Nicht im Scope: der nachfolgende fremde Handbuch-Commit `3a3abf09` sowie die unversionierten Handbuchdateien im Arbeitsbaum

Der Standardvergleich `merge-base..HEAD` ist leer, weil die zu prüfenden Änderungen bereits direkt auf `master` liegen. Um ausschließlich die von Codex vorgenommenen Änderungen zu prüfen, verwendet dieses Review deshalb den oben dokumentierten Commitbereich und schließt den späteren Handbuch-Commit aus.

## Referenzpläne

- `docs/plan/2026-08-20-endgueltige-abgabe-entwurfsbindung.md`
- `docs/plan/2026-08-20-ki-dialog-abschlussfeedback.md`
- `docs/plan/2026-08-20-ki-dialog-hauptaktionen.md`
- `docs/plan/2026-08-20-cli-token-lehrkraftautorisierung.md`
- `docs/plan/2026-08-20-ipad-zweispalten-scrollstabilitaet.md`
- `docs/plan/2026-08-20-ipad-eingabeperformance.md`

## Kurzfazit

Die sechs Änderungen sind überwiegend klar geschichtet, sicher autorisiert und breit getestet. Die Lehrkraftprüfung für CLI-Tokens greift korrekt vor dem Store-Zugriff. Vor einer Freigabe muss jedoch der KI-Dialog repariert werden: Bei fehlgeschlagenen KI-Turns verschwindet die Abschlussaktion; nach ausgeschöpften Retries kann ein fortgeschrittener Dialog ohne erreichbaren Ausweg verbleiben. Zusätzlich bestehen Robustheits- und iPad-Testlücken.

## MUST-FIX

- [BUG] Fehlgeschlagene KI-Turns sperren die Abschlussaktion; nach dem dritten Fehlversuch bleibt bei `round_count > 0` weder „Dialog beenden“ noch der serverseitig erlaubte terminale Abbruch erreichbar — frontend/src/lib/components/learning-unit/LearningDialogWorkspace.svelte:149@04c24675 — Impact: Lernende können in einem fortgeschrittenen Dialog dauerhaft festhängen und die Aufgabe weder abgeben noch sauber verlassen. — Fix: `canEndDialog()` nur während einer laufenden Generierung sperren, fehlgeschlagene Turns bei bereits abgeschlossenen Runden als beendbar behandeln und bei terminalem Fehlschlag ohne abgeschlossene Runde die Abbruchaktion im Hauptbereich anbieten; beide Zustände zuerst mit Komponenten- und Feature-Acceptance-Tests absichern.

## SHOULD-FIX

- [BUG] Der Abschluss-Callback ordnet die zurückgegebene Dialogabgabe der beim Callback gerade aktiven Aufgabe statt der tatsächlich abgeschlossenen Dialogaufgabe zu — frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte:1126@ae8bbc68 — Impact: Bei Zurück-Navigation oder Aufgabenwechsel während der Abschlussanfrage wird die Abgabe in die falsche Historie eingefügt und der Poller fragt die falsche Aufgabe potenziell unbegrenzt ab; die Dialogrückmeldung bleibt dann erneut unsichtbar. — Fix: Die unveränderliche Dialog-`task.id` zusammen mit der Submission durch die Callback-Kette führen, vor dem UI-Update gegen den aktuellen Kontext prüfen und einen Navigation-während-Abschluss-Test ergänzen.
- [API] Idempotenzschlüssel und `feedback_submission_id` werden jeweils syntaktisch, aber nicht als zusammengehöriges Paar validiert; ein bereits verwendeter Schlüssel liefert vor Prüfung der neuen Feedback-ID die alte finale Abgabe zurück — backend/learning/repo_submission_command_queries.py:640@9448580b — Impact: Ein manipuliertes oder inkonsistentes Formular kann eine andere Abgabe als die angeforderte Fassung zurückerhalten und verletzt damit die zugesicherte Entwurfsbindung. — Fix: Contract-first den Schlüssel deterministisch an die Feedback-ID binden oder einen Request-Fingerprint speichern und bei abweichender Wiederverwendung mit Konflikt antworten; BFF-, API- und Repository-Test für unterschiedliche IDs bei gleichem Schlüssel ergänzen.
- [TEST] Die beiden iPad-Regressionsprüfungen laufen ausschließlich in Desktop-Chromium mit `hasTouch`; der Scrolltest setzt `scrollTop` per JavaScript und die Eingabeprüfung erfasst entgegen dem Plan keine Long Tasks — frontend/playwright.config.ts:13@8cdc3ebb — Impact: WebKit-spezifisches Momentum-Scrolling und iOS-Tastatur-/Main-Thread-Verhalten, also genau die gemeldete Produktionsumgebung von Chrome auf dem iPad, bleiben ungetestet. — Fix: Einen gezielten WebKit-Landscape-iPad-Lauf ergänzen, die Scrollbewegung über reale Browser-Eingabe auslösen und die zugesagte Long-Task-Messung oder eine gleichwertige Main-Thread-Metrik erfassen.

## NICE-TO-HAVE

- [CONSISTENCY] Der Repository-Kommentar behauptet weiterhin, die „most recent completed draft“ werde finalisiert, obwohl nun explizit die ausgewählte Fassung kopiert wird — backend/learning/repo_submission_command_queries.py:617@9448580b — Impact: Die falsche Erklärung erschwert Lernenden und Reviewern das Verständnis der neuen Sicherheits- und Zuverlässigkeitsgarantie. — Fix: Kommentar auf „selected completed draft“ korrigieren und die Bindung an Kurs, Aufgabe und Lernenden kurz benennen.

## TESTS

- `git diff --check 08ea8025..8cdc3ebb`: erfolgreich.
- `.venv/bin/pytest -q backend/tests/test_learning_submission_finalize_usecase.py backend/tests/test_openapi_learning_finalize_submission_contract.py backend/tests/test_profile_view_api.py backend/tests/test_openapi_profile_contract.py`: 32 bestanden.
- `npx vitest run` für die sieben betroffenen Komponenten- und Routentests: 100 bestanden.
- Ein versehentlicher Aufruf über `npm test -- --run ...` führte zunächst die vollständige Vitest-Suite erfolgreich aus (587 Tests), reichte die Dateinamen danach aber an Node weiter und endete deshalb mit einem Aufruffehler; der korrekte gezielte Vitest-Aufruf war anschließend grün.
- Die Implementierungspläne dokumentieren einen erfolgreichen vollständigen `make verify-feature`-Lauf mit 2.435 bestandenen Backendtests, 587 Frontendtests, 62 H5P-Tests und 22 Feature-Acceptance-Szenarien. Im Review wurde dieser lange Lauf nicht erneut ausgeführt.
- Testlücke: Kein WebKit-/iOS-naher Browserlauf und kein Test für Abschluss/Abbruch nach fehlgeschlagenem KI-Turn.

## Refactor-Budget

M — ungefähr ein halber bis ein Arbeitstag. Der Dialogausweg und die stabile Task-ID sind klein, benötigen aber neue Browserfälle; die saubere Idempotenzbindung ist eine Contract-first-Änderung mit BFF-, API- und Repository-Tests. Der WebKit-Nachweis kann zusätzliche CI-Einrichtung erfordern.

## Freigabe

NOT APPROVED — Freigabe nach Behebung der MUST-FIX-Findings

## Reparaturplan – 2026-08-20 14:35

### Arbeitspaket 1: Erreichbare Dialogauswege

- Befund: MUST-FIX bestätigt. `canEndDialog()` sperrt bei jedem fehlgeschlagenen Turn; nach ausgeschöpften Retries fehlt bei fortgeschrittenen Dialogen ein sicherer Ausweg.
- Regeln und Kontext: `learning`; Red-Green-Refactor, bestehende `workspace-top-action`-Hierarchie und barrierefreie Hauptaktionen. Die ausdrückliche Produktentscheidung für Hauptbereich-Aktionen wird vor der Implementierung in `docs/DESIGN.md` übernommen.
- Betroffene Dateien: `docs/DESIGN.md`, `LearningDialogWorkspace.svelte`, Komponenten- und authentifizierter Dialog-Browsertest sowie dessen DB-Fixture.
- Vertrag/Migration: keine API- oder Schemaänderung; Designvertrag zuerst.
- Test zuerst: Fehlgeschlagener Turn mit verbleibendem Retry zeigt Retry und Abschluss; terminaler Fehlschlag zeigt keinen Retry, aber Abschluss beziehungsweise bei null abgeschlossenen Runden den erlaubten Abbruch.
- Minimale Implementierung: Laufende Generierung, fehlgeschlagene Generierung und terminalen Fehlschlag getrennt modellieren; Aktionen im Hauptbereich aus diesen Zuständen ableiten.
- Akzeptanz: Komponentenfälle grün und der bestehende echte Dialog-Rundlauf passiert zusätzlich einen terminal fehlgeschlagenen Turn.

### Arbeitspaket 2: Dialogabgabe stabil an die Aufgabe binden

- Befund: SHOULD-FIX bestätigt. Der Callback liest die veränderliche `activeTask` erst nach Abschluss der asynchronen Anfrage.
- Regeln und Kontext: `learning`; unveränderliche fachliche IDs werden an der Komponenten- und BFF-Grenze explizit transportiert, ohne Geschäftslogik in Svelte zu duplizieren.
- Betroffene Dateien: `LearningTaskCard.svelte`, `LearnerContentWorkspace.svelte`, Lernraumseite und zugehörige Komponenten-/Seitenvertragstests.
- Vertrag/Migration: keine öffentliche API- und keine Schemaänderung.
- Test zuerst: Der Abschluss-Handler erhält eine konkrete Task-ID und schreibt/pollt auch nach einem simulierten Aktivtask-Wechsel ausschließlich diese Historie.
- Minimale Implementierung: Callback-Signatur um `taskId` erweitern und Dialog-/H5P-Adapter mit der stabilen `task.id` umschließen.
- Akzeptanz: gezielte Vitest-Suite zeigt korrekte Historien- und Pollingbindung.

### Arbeitspaket 3: Idempotenz an die ausgewählte Rückmeldungsfassung koppeln

- Befund: SHOULD-FIX bestätigt. Derselbe gültige Schlüssel kann mit einer anderen `feedback_submission_id` wiederverwendet werden.
- Regeln und Kontext: `learning`; Contract-first, fachliche Validierung im framework-unabhängigen Use Case, BFF nur als frühe UX-Grenze, Repository/RLS unverändert.
- Betroffene Dateien: `api/openapi.yml`, Finalisierungs-Use-Case, BFF-Aktion sowie Contract-, Use-Case-, API- und BFF-Tests.
- Vertrag/Migration: OpenAPI zuerst; keine Migration. Der kanonische Schlüssel lautet `finalize-{feedback_submission_id}`.
- Test zuerst: Vertrag fordert das gebundene Format; Use Case und BFF lehnen unterschiedliche ID/Schlüssel-Paare ab; identische Wiederholung bleibt idempotent.
- Minimale Implementierung: normalisierte Feedback-ID deterministisch in den erwarteten Schlüssel überführen und Abweichungen vor Repository-Zugriff ablehnen.
- Akzeptanz: Vertragstests, echte API-Datenbanktests und SvelteKit-Aktionstests sind grün.

### Arbeitspaket 4: iPad-nahe Browsernachweise

- Befund: SHOULD-FIX bestätigt. `hasTouch` ändert nicht die Chromium-Engine, programmatisches `scrollTop` erzeugt keine echte Eingabe und die zugesagte Long-Task-Diagnostik fehlt.
- Regeln und Kontext: `learning`; lokal entspricht Produktion, TLS bleibt unverändert, keine Dev-only-Produktpfade. Testinfrastruktur darf einen eng begrenzten WebKit-iPad-Projektlauf besitzen.
- Betroffene Dateien: `frontend/playwright.config.ts`, Browser-Preflight, `Makefile`, `docs/references/make_targets.md` sowie die beiden iPad-Feature-Acceptance-Tests.
- Vertrag/Migration: keine API- oder Schemaänderung; Testkonfiguration vor Testausführung.
- Test zuerst: WebKit-Projekt nur für Scroll- und Eingabespezifikation; Scrollen über Browser-Eingabe; Long-Task-Zähler, sofern die Engine ihn unterstützt, plus plattformunabhängiger Gesamtlatenzgrenzwert.
- Minimale Implementierung: WebKit in Bootstrap/Feature-Preflight aufnehmen und die vorhandenen Tests engine-näher instrumentieren.
- Akzeptanz: beide Spezifikationen bestehen in Chromium und WebKit; anschließend `make verify-feature`.

### Arbeitspaket 5: Lehrbarkeit des Repository-Kommentars

- Befund: NICE-TO-HAVE bestätigt und im geöffneten Code trivial.
- Regeln und Kontext: `learning`; englische, präzise Kommentare, keine Verhaltensänderung.
- Betroffene Datei: `backend/learning/repo_submission_command_queries.py`.
- Vertrag/Migration: nicht erforderlich.
- Test zuerst: kein neuer Verhaltenstest nötig, weil ausschließlich eine falsche Erklärung berichtigt wird; bestehende Finalisierungstests bleiben der Regressionsnachweis.
- Minimale Implementierung: „most recent“ durch „selected“ ersetzen und Scope-Bindung nennen.
- Akzeptanz: Diff- und bestehende Finalisierungstests grün.

## Umsetzungsstand – 2026-08-20 15:15

### Geschlossene Findings

- [MUST-FIX][BUG] Geschlossen: Ein fortgeschrittener KI-Dialog bleibt nach einem fehlgeschlagenen KI-Turn beendbar. Nach dem dritten Fehlversuch verschwindet die Wiederholungsaktion; Abschluss und der fachlich erlaubte Abbruch bleiben erreichbar. Sichtbarkeit und Klick verwenden dieselbe Zustandsregel.
- [SHOULD-FIX][BUG] Geschlossen: Der Abschluss-Callback transportiert die unveränderliche `task.id`; Historie und Feedback-Poller hängen nicht mehr von der während der Anfrage veränderlichen aktiven Aufgabe ab.
- [SHOULD-FIX][API] Geschlossen: `Idempotency-Key` ist laut OpenAPI verpflichtend und muss exakt `finalize-{feedback_submission_id}` entsprechen. BFF und framework-unabhängiger Use Case lehnen abweichende Paare vor dem Repository-Zugriff ab.
- [SHOULD-FIX][TEST] Geschlossen: Die iPad-Regressionsfälle laufen zusätzlich gezielt mit WebKit. Scrollen wird über fokussierte Tastatureingabe ausgelöst; die Eingabeprüfung erfasst Long Tasks, sofern die Engine sie bereitstellt, und behält den plattformübergreifenden Gesamtlatenzgrenzwert bei.
- [NICE-TO-HAVE][CONSISTENCY] Geschlossen: Der Repository-Kommentar beschreibt nun die ausgewählte statt der vermeintlich neuesten Rückmeldungsfassung und nennt die Bindung an Lernenden, Kurs und Aufgabe.

### Architektur- und Designentscheidungen

- Die fachliche Idempotenzprüfung liegt im `learning`-Use-Case; die SvelteKit-Aktion spiegelt sie nur früh für eine verständliche Nutzerreaktion. Repository und RLS-Verträge bleiben unverändert.
- Für Dialogabschluss und Feedbackzuordnung werden bereits vorhandene öffentliche Endpunkte und Submission-Modelle verwendet. Eine Datenbankmigration war nicht erforderlich.
- `docs/DESIGN.md` hält die ausdrückliche Produktentscheidung fest: `Dialog beenden` steht bei den Hauptaktionen; die initiale Abbruchaktion bleibt im Partnerbereich, terminale Auswege stehen im Hauptbereich.
- Das WebKit-Projekt ist auf die beiden iPad-Spezifikationen begrenzt. Der normale visuelle Smoke-Test bleibt bei Chromium.

### Verifikation

- `git diff --check`: erfolgreich.
- Gezielte Komponenten-, Routen-, OpenAPI- und Use-Case-Tests: erfolgreich.
- Authentifizierter KI-Dialog-Rundlauf einschließlich terminalem KI-Fehler, endgültiger Abgabe und sichtbarer Rückmeldung: 1 bestanden.
- Vollständiges `make verify`: 2.436 Backendtests bestanden, 78 erwartete Skips; 602 Frontendtests bestanden; Svelte-Check 0 Fehler/0 Warnungen; Produktionsbuild erfolgreich; 62 H5P-Tests bestanden.
- Vollständiges `make test-feature-acceptance`: 24 bestanden, davon 22 Chromium- und 2 WebKit-iPad-Szenarien.
- Ein erster Gesamtlauf deckte den inzwischen korrigierten statischen Browser-Preflight-Vertrag auf. Weitere Wiederholungen waren wegen eines isoliert anschließend grünen Shared-DB-Mail-Worker-Ausreißers sowie eines nachgewiesenen parallelen Frontend-Container-Neustarts (`ERR_NETWORK_CHANGED`, Login 401) nötig. Nach Stabilisierung bestanden die betroffenen Fälle isoliert und der vollständige Browser-Gate.
- Playwright unterstützt Ubuntu 26.04 derzeit nicht als Hostpaket. Der lokale WebKit-Nachweis verwendete deshalb Playwrights Ubuntu-24.04-Build mit ausschließlich unter `/tmp` entpackten Laufzeitbibliotheken. Produktcode, TLS und Containerkonfiguration wurden dafür nicht abgeschwächt.

### Verbleibende Punkte

- Keine offenen Findings aus diesem Review.
- Nicht Bestandteil dieser Reparatur und nicht zu committen: parallel unversionierte Handbuch- und Split-Divider-Arbeiten. Deren Änderungen wurden im gemeinsamen Arbeitsbaum mitverifiziert, bleiben aber eigentumsgetrennt.

### Freigabe

APPROVED — alle Review-Findings sind behoben und die vollständigen Qualitäts- sowie Browser-Gates sind grün.
