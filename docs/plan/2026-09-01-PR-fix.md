# PR-Review: Finale Textabgabe nach History-Restore

## Review Round – 14:52

- Datum und Zeit: 2026-09-01 14:52 CEST
- Branch: `master`
- Base-Ref: `master`
- Merge-Base: `e64f7353695a2b2d4a66a198ab7b483536c0767d`
- Head: `e64f7353695a2b2d4a66a198ab7b483536c0767d`
- Scope: Uncommitteter Working-Tree-Diff für History-Hydration, Finalisierungsbaseline, Direktlink-/Reload-Restore, Retry, Tests, Dokumentation und Entfernung des ungenutzten `LearningSubmissionWorkspace`.
- Kontext: `HEAD`, lokaler `master` und Merge-Base sind identisch; `merge-base..HEAD` ist daher leer. Gegenstand dieses Reviews ist ausdrücklich der vollständige uncommittete Working-Tree-Diff auf diesem Stand. `api/openapi.yml`, Migrationen und Architekturverträge wurden geprüft und sind unverändert, passend zum rein clientseitig geplanten Fix.
- Verwendete Codereferenzen: `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.server.ts:142-155`, `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.svelte:117-136,418-425,968-1010`, `frontend/src/lib/learning-unit/submission-finalization.ts:47-75`, `frontend/src/lib/components/learning-unit/LearningTaskCard.svelte:460-485,627-650,853-890`, `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/page-contract.test.ts:264-280`, `frontend/src/lib/components/learning-unit/LearningTaskCard.test.ts:212-354,427-448,1575-1593`, `frontend/e2e/learner-task-finalization.spec.ts:35-83`.

### Kurzfazit

Die Draft-Reconciliation ist verständlich, sicher gegen das Überschreiben lokaler Änderungen und gut durch Komponenten- sowie Browsertests abgesichert. Ein zentraler Fehlerfall ist jedoch noch nicht geschlossen: Beim direkten Ergebnislink oder Reload verhindert ein serverseitiger History-Fehler das Rendern der neuen Retry-Oberfläche. Zusätzlich sollte die UI bei einer vertragswidrigen Submission-ID fail-closed bleiben. OpenAPI, Datenbank und RLS sind zu Recht unverändert.

### MUST-FIX

- [BUG] Fange einen transienten serverseitigen History-Fehler für direkte Ergebnislinks kontrolliert ab — `frontend/src/routes/learning/courses/[courseId]/units/[unitId]/+page.server.ts:151@e64f7353` — Impact: Bei `5xx` oder einem vergleichbaren Backendfehler wirft der SSR-Loader vor dem Rendern; Lernende sehen dadurch keine Ergebnisansicht und keinen „Erneut versuchen“-Button, obwohl genau dieser Fehlerfall laut Plan abgedeckt sein soll. — Fix: Gib einen expliziten initialen History-Ladezustand `failed` an die Seite weiter, erhalte Auth-Redirects fail-closed und lasse den bestehenden deduplizierten Client-Loader den Retry übernehmen; sichere Direktlink und Reload mit einem verhaltensbasierten Server-/Routentest ab.

### SHOULD-FIX

- [BUG] Verhindere die Finalisierungsfreigabe bei einer ungültigen Submission-ID bereits in der Baseline — `frontend/src/lib/learning-unit/submission-finalization.ts:55@e64f7353` — Impact: Eine abgeschlossene Text-Submission mit vertragswidriger ID erzeugt derzeit eine Baseline und kann den Button aktivieren, während der Idempotenzschlüssel leer bleibt und der BFF die garantiert fehlschlagende Anfrage erst später mit `400` ablehnt. — Fix: Validiere die UUID beim Erzeugen der Baseline oder binde die Buttonfreigabe zusätzlich an einen gültigen atomaren Idempotenzschlüssel; ergänze einen Regressionstest für das fail-closed-Verhalten.

### NICE-TO-HAVE

Keine weiteren Findings. Die Entfernung der ungenutzten Komponente und ihrer exklusiven CSS-Regeln reduziert den Wartungsumfang sinnvoll.

### TESTS

- 140 Frontend-Testdateien mit 635 Tests sowie die drei zentralen Regressiondateien mit 100 Tests bestanden.
- Der authentifizierte Direktlink-/Reload-/Finalisierungstest bestand und wies genau eine finale Submission nach.
- `make verify-feature` ist nicht grün: 22 von 24 Feature-Acceptance-Szenarien bestanden; zwei bestehende KI-Ausgabeverträge außerhalb dieses Diffs scheitern reproduzierbar.
- Der neue Route-Retry-Test prüft aktuell nur Quelltextfragmente und hätte den SSR-Fehlerpfad deshalb nicht erkannt.

### Refactor-Budget

Etwa ein halber Arbeitstag: initialen Fehlerzustand durch den SSR-/Client-Vertrag reichen, zwei verhaltensbasierte Fehlerpfadtests ergänzen und die UUID-Prüfung in der Baseline schließen.

### Freigabe

Nicht freigegeben. Zuerst den MUST-FIX beheben und den ticketspezifischen Fehlerpfad grün nachweisen. Vor Merge muss außerdem das verbindliche Gesamt-Gate `make verify-feature` vollständig erfolgreich sein.

## Reparaturplan – 2026-09-01 14:57

### Arbeitspaket 1: SSR-History-Fehler bis zur Retry-Oberfläche tragen

- Finding: MUST-FIX „serverseitigen History-Fehler für direkte Ergebnislinks kontrolliert abfangen“; der Befund ist am aktuellen Working Tree weiterhin offen, weil `+page.server.ts` jeden History-Fehler außer `404` vor dem Rendern weiterwirft.
- Regeln und Design: TDD und KISS aus `AGENTS.md`; SvelteKit bleibt View-Model-/BFF-Adapter gemäß `docs/ARCHITECTURE.md`; Eigentümer ist der Bounded Context `learning`; Auth-Redirects bleiben gemäß `docs/references/auth_sessions_and_cookies.md` fail-closed; die UI verwendet unverändert die gemeinsame `StatusMessage`-Aktion aus `docs/DESIGN.md`.
- Voraussichtliche Dateien: `+page.server.ts`, `+page.svelte`, `page.server.test.ts`, `page-contract.test.ts` und der zentrale Learning-Typ.
- Verträge: Keine Änderung an `api/openapi.yml`, Migrationen, Referenzdokumenten oder Konfiguration; neu ist nur ein internes SvelteKit-View-Model-Feld für den initialen History-Ladezustand.
- Test zuerst: Einen direkten `panel=result`-Load mit einer synthetischen `503`-Antwort der History als fehlschlagenden Routentest ergänzen und zusätzlich nachweisen, dass ein Auth-Redirect unverändert weitergeworfen wird.
- Minimale Implementierung: Einen expliziten initialen History-Ladezustand zurückgeben, ausschließlich retryfähige Backendfehler als `failed` abbilden und diesen Zustand synchron in den vorhandenen Client-Loader einspeisen.
- Abnahme: Die Ergebnisansicht kann trotz initialem `503` rendern; der Client versucht den deduplizierten Load und zeigt bei erneutem Fehler „Erneut versuchen“. Redirects, `404`-Semantik und erfolgreiche History bleiben unverändert.

### Arbeitspaket 2: Finalisierungsbaseline bei ungültiger ID fail-closed halten

- Finding: SHOULD-FIX „Finalisierungsfreigabe bei ungültiger Submission-ID verhindern“; der Befund ist weiterhin offen, weil `reviewedSubmissionBaseline()` Status und Intent prüft, aber die ID nicht validiert.
- Regeln und Design: Security first und lehrbare reine Logik aus `AGENTS.md`; Eigentümer ist `learning`; die Abgabe bleibt gemäß Glossar unveränderlich und konkret gebunden; es entsteht keine neue sichtbare UI oder Designsprache.
- Voraussichtliche Dateien: `submission-finalization.ts`, `submission-finalization.test.ts` und `LearningTaskCard.test.ts`.
- Verträge: OpenAPI bleibt unverändert und definiert `LearningSubmission.id` bereits als UUID; Datenbank und RLS sind nicht betroffen.
- Test zuerst: Eine abgeschlossene Feedback-Submission mit ungültiger ID muss als Baseline abgelehnt werden; die Aufgabenkarte darf dafür keine Finalisierungsaktion freigeben.
- Minimale Implementierung: Die bereits zentrale UUID-Validierung beim Erzeugen der Baseline wiederverwenden und nur die validierte ID in den Snapshot übernehmen.
- Abnahme: Baseline, Button, Hidden Field und Idempotenzschlüssel sind entweder gemeinsam gültig oder gemeinsam nicht vorhanden; gültige Text- und Uploadpfade bleiben grün.

### Reihenfolge und Abschluss

1. Beide Regressionen rot nachweisen.
2. Arbeitspaket 1 minimal grün umsetzen und Auth-/Retry-Verträge gezielt prüfen.
3. Arbeitspaket 2 minimal grün umsetzen und bestehende vertragswidrige Testfixtures bereinigen.
4. Gemeinsame Namen und Zustandsableitung vereinfachen, ohne neue UI oder öffentliche Verträge einzuführen.
5. Gezielte Svelte-/Vitest-Suiten, die vollständige Frontend-Suite und anschließend das verbindliche `make verify-feature` ausführen; Ergebnis und Restrisiko in diesem Dokument ergänzen.

## Umsetzungsstand – 2026-09-01 15:10

### Findings

- **MUST-FIX geschlossen:** Der SSR-Loader bildet `404` weiterhin als nicht verfügbare History und ausschließlich retryfähige Fehler (`408`, `429`, `5xx`) als initialen Ladezustand `failed` ab. Auth-Redirects und andere unerwartete Fehler werden unverändert fail-closed weitergeworfen. Die Seite übernimmt den serverseitigen Zustand synchron; der vorhandene deduplizierte Loader und seine Retry-Aktion bleiben die einzige Nachlade-Implementierung.
- **SHOULD-FIX geschlossen:** `reviewedSubmissionBaseline()` übernimmt nur noch eine zentral validierte UUID. Baseline, Finalisierungsbutton, `feedback_submission_id` und `finalize-{id}` sind dadurch gemeinsam gültig oder gemeinsam nicht vorhanden.
- Beide Reviewbefunde waren am Ausgangsstand reproduzierbar und wurden test-first behoben; es wurden keine Findings als veraltet verworfen.

### Red–Green–Refactor-Nachweis

- Red: Drei neue Regressionen schlugen gezielt fehl: SSR-History-`503` renderte keine Retry-fähige Seite, eine ungültige Submission-ID erzeugte eine Baseline und die Aufgabenkarte bot dafür die Finalisierung an. Der ergänzte Auth-Redirect-Test blieb erwartungsgemäß bereits grün und sichert die Sicherheitsgrenze.
- Green: Die drei zuerst betroffenen Testdateien bestanden anschließend mit 93 Tests. Die erweiterte gezielte Suite aus Serverroute, Seitenvertrag, Finalisierungshelper und Aufgabenkarte bestand mit 120 Tests.
- Refactor: Der initiale History-Ladezustand ist nun Teil des gemeinsamen internen Learning-View-Models; die Seite leitet ihn ohne parallele Sonderlogik in ihren bestehenden Zustandsautomaten weiter. Die vorhandene UUID-Validierung wird wiederverwendet.

### Verifikation und Qualitätsprüfung

- Vollständige Frontend-Suite: 140 Testdateien mit 638 Tests bestanden.
- Svelte-Prüfung: 0 Fehler und 0 Warnungen.
- Produktionsbuild und Build-Output-Policy bestanden; die 16 gemeldeten Warnungen sind dokumentierte Upstream-Warnungen.
- Backend: 2477 Tests bestanden, 78 wurden übersprungen. H5P: 62 Tests bestanden.
- Lokale CA: System, Chromium/Codex und Firefox vertrauen der aktuellen Caddy-CA.
- Ticketbezogener authentifizierter Browserlauf: Beide Finalisierungsszenarien bestanden, einschließlich Text-Direktlink, Reload, genau einer finalen Submission und Upload-Regression.
- `make verify-feature`: 22 von 24 Feature-Acceptance-Szenarien bestanden. Die beiden ticketbezogenen Finalisierungsszenarien sind grün. Unverändert rot und außerhalb dieses Diffs sind `learner-navigation.spec.ts` wegen der Ausgabe des synthetischen Markers `GUSTAV-INTERN-NAVIGATION` sowie `practice-session.spec.ts` wegen einer Rückmeldung mit zwei statt genau eines Satzes.

### Architektur, Grenzen und Restrisiko

- Der Fix bleibt im Bounded Context `learning` und respektiert die bestehende Trennung zwischen SvelteKit-View-Model, Clientzustand und Backendvertrag. OpenAPI, Persistenz, Migrationen, RLS, Worker, Konfiguration und sichtbares Design wurden nicht geändert.
- Die Fehlerabbildung ist bewusst eng: Nur als vorübergehend behandelbare Backendstatus werden im Ergebnisbereich retryfähig; Authentifizierungs- und unbekannte Fehler werden nicht verschluckt.
- Die beiden Reviewfindings sind fachlich geschlossen. Das repositoryweite Pflicht-Gate ist wegen der zwei unabhängigen KI-Ausgabeverträge dennoch nicht grün; eine Freigabe oder ein Commit ist deshalb weiterhin nicht empfohlen.
