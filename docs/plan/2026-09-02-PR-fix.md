# PR-Review: CLI-Diagnostik-Erweiterung

## Review Round – 08:14

- Datum: 2026-09-02 08:14 CEST
- Branch: `master`
- Base-Ref: `master` (`377606cfa70b3c2434045681600339e3600a0aa2`)
- Merge-Base: `377606cfa70b3c2434045681600339e3600a0aa2`
- Head: `377606cfa70b3c2434045681600339e3600a0aa2`
- Tatsächlicher Review-Scope: Feature-Commit `f07c02ede640fba41678f8b34b11f143feb72934..377606cfa70b3c2434045681600339e3600a0aa2`
- Umfang: OpenAPI-Vertrag, CLI-Operations- und Capability-Register, Authentifizierung, Diagnostik-Read-Models, CLI-Ausgabe und Dateidownload, Python- und Playwright-Tests sowie Dokumentation; keine Migration.
- Kontext: Der formale Skill-Diff `merge-base..HEAD` ist leer, weil das Feature bereits direkt auf `master` committed wurde. Deshalb wurde ausschließlich der einzelne, unmittelbar vorherige Feature-Commit geprüft.
- Verwendete Codeverweise: `backend/web/auth_middleware.py`, `backend/web/cli_authoring.py`, `backend/web/cli_capabilities.py`, `backend/web/routes/app_diagnostics_routes.py`, `backend/web/routes/teaching_live.py`, `backend/tools/gustav_cli/diagnostics_commands.py`, `backend/tests/test_gustav_cli_diagnostics.py`, `api/openapi.yml`.

### Kurzfazit

Die Erweiterung ist funktional breit umgesetzt, contract-first dokumentiert und durch gute CLI-, API-, RLS- und Acceptance-Tests gestützt. Zwei Punkte verhindern die Freigabe: Slash-haltige Schülerkennungen werden trotz Vertrag vor der Route abgewiesen, und ungefilterte Fremdtexte können Terminal-Steuersequenzen ausgeben. Die Paginierung des Lernendenprofils braucht außerdem eindeutigere HTTP-Semantik und eine skalierbare Datenabfrage.

### MUST-FIX

- [API] Verwende für den Capability-Abgleich nicht den bereits dekodierten `request.url.path`: `%2F` in `student_sub` wird zu einem zusätzlichen Pfadsegment und deshalb mit `401` abgewiesen, obwohl OpenAPI solche Kennungen ausdrücklich zusagt — `backend/web/auth_middleware.py:150@377606cf` — Impact: `diagnostics student`, `submission`, `download` und Dialogabrufe funktionieren für slash-haltige OIDC-Subjects nicht; der Dialogroute fehlt zusätzlich ein `path`-Converter — Fix: Gleiche gegen den kontrolliert dekodierten Raw-Pfad oder mit einem converter-bewussten Matcher ab, passe die Dialogroute an und sichere alle betroffenen Endpunkte mit echten ASGI-Tests ab.
- [SEC] Bereinige alle menschenlesbaren Terminalwerte zentral von C0/C1- und ANSI-Steuerzeichen — `backend/tools/gustav_cli/diagnostics_commands.py:384@377606cf` — Impact: Schülertexte, KI-Rückmeldungen, Namen und API-Details können Tabellen fälschen oder Terminalfunktionen wie Titel beziehungsweise Zwischenablage beeinflussen; JSON darf dagegen unverändert maschinenlesbar bleiben — Fix: Führe einen gemeinsamen Plain-Terminal-Sanitizer ein, behalte sinnvolle Zeilenumbrüche nur in Detailtexten bei und gib bei API-Fehlern ausschließlich bekannte Fehlercodes aus.

### SHOULD-FIX

- [API] Validiere `limit` und `offset` vor der FastAPI-Typkonvertierung — `backend/web/routes/app_diagnostics_routes.py:220@377606cf` — Impact: Werte wie `limit=abc` erzeugen einen frameworkseitigen `422` statt des dokumentierten privaten `400 invalid_pagination`; derselbe Bruch besteht im Unit-Summary — Fix: Nimm Querywerte zunächst lose entgegen, normalisiere sie in einem gemeinsamen Helper und antworte bei jedem Fehler mit den vorgesehenen privaten Cache-Headern.
- [API] Verwende `404` nicht gleichzeitig als „Lernender unsichtbar“ und als reguläres Ende einer späteren Seite — `backend/tools/gustav_cli/diagnostics_commands.py:133@377606cf` — Impact: Die CLI behandelt jeden `404` nach Seite eins als Erfolg und kann dadurch einen echten Relations- oder Routingfehler als vollständiges Profil ausgeben — Fix: Liefere für eine gültige leere Folgeseite `200` mit `courses: []` oder ein explizites `has_more`/Cursor-Feld und entferne `allow_terminal_404`.
- [BUG] Verschiebe die sichtbare Kurspaginierung in eine owner- und mitgliedschaftsgebundene Repository-Abfrage — `backend/web/routes/app_diagnostics_routes.py:138@377606cf` — Impact: Jede CLI-Seite scannt erneut ab Offset null in 200er-Blöcken und führt pro Kurs eine Mitgliedschaftsabfrage aus; große Bestände verursachen quadratisch wachsende Wiederholungsarbeit und viele DB-Roundtrips — Fix: Paginiere die Schnittmenge aus Kurs-Ownership, aktiver Mitgliedschaft und Lernendem direkt in SQL beziehungsweise einem gebündelten Repository-Helper.
- [API] Ergänze in der kursbezogenen Schülerübersicht die neuesten H5P-Punkte — `backend/tools/gustav_cli/diagnostics_commands.py:303@377606cf` — Impact: Die angekündigte tabellarische Langform enthält dort nur `h5p_abschluss`, aber weder `score_raw` noch `score_max`; damit ist die H5P-Scoreausgabe zwischen Unit-, Schüler- und Detailperspektive inkonsistent — Fix: Erweitere Read Model und OpenAPI-Schema um die beiden Punktefelder und gib sie als `h5p_punkte` aus.
- [TEST] Ersetze den reinen URL-String-Test durch echte Laufzeittests für besondere Subjects und ergänze den kurslosen Schülerpfad — `backend/tests/test_gustav_cli_diagnostics.py:198@377606cf` — Impact: Der Test bestätigt nur `%2F` im erzeugten String und übersieht die anschließende ASGI-Dekodierung; auch automatische Mehrseitenaggregation von `diagnostics student` ohne Kurs ist nicht als CLI-Ablauf abgedeckt — Fix: Teste ein slash-haltiges Subject durch Middleware und Route sowie ein kursübergreifendes Profil mit mehr als 50 sichtbaren Kursen und fehlerhafter Folgeseite.

### NICE-TO-HAVE

- [CONSISTENCY] Löse den allgemeinen Capability-Kern vollständig aus dem authoring-spezifischen Modul — `backend/web/cli_capabilities.py:11@377606cf` — Impact: Typ, Matcher und Basistabelle bleiben trotz allgemeinem Register in `cli_authoring.py`; das erschwert die begriffliche Zuordnung und spätere Erweiterungen — Fix: Verschiebe gemeinsame Definitionen nach `cli_capabilities.py` oder ein neutrales Kernmodul und lasse höchstens schmale Kompatibilitäts-Re-Exports zurück.

### TESTS

- Bestanden auf `377606cf`: 45 CLI-/OpenAPI-/Capability-Tests in 11,68 Sekunden.
- Bestanden auf `377606cf`: 5 gezielte API- und Paginierungstests in 0,49 Sekunden.
- Der DB-gestützte Owner-/RLS-Test blockierte im Reviewlauf über zwei Minuten ohne Ausgabe und wurde abgebrochen. Laut Featureplan waren `make verify-feature FEATURE=cli-diagnostics` und `make verify` vor dem Review erfolgreich; dieser Reviewlauf bestätigt den DB-Test nicht erneut.
- Statische Reproduktion: Ein als `school%2Fstudent` kodiertes Subject erscheint im ASGI-Pfad als `school/student`; `cli_capability_for_request` liefert dafür aktuell `None`.

### Refactor-Budget

Etwa 2–3 Entwicklungstage: bis zu 1 Tag für Raw-Path-Matching, Routenanpassungen und Terminal-Sanitizing; 1 Tag für eindeutige Profilpaginierung samt Repository-Query; bis zu 1 Tag für H5P-Felder, Contracttests und den erweiterten Acceptance-Nachweis.

### Freigabe

Nicht freigegeben. Die beiden MUST-FIX-Punkte müssen vor einem Push behoben und anschließend mit `make verify-feature FEATURE=cli-diagnostics` sowie `make verify` auf dem endgültigen Commit erneut geprüft werden.

## Reparaturplan – 2026-09-02 08:26 CEST

### Erneute Prüfung der Befunde

- MUST `[API]`: bestätigt. Starlette stellt `request.url.path` bereits dekodiert bereit; dadurch wird ein als `%2F` übertragenes Zeichen vor dem segmentbasierten Capability-Abgleich wieder zu `/`. Die Dialogroute verwendet zudem noch keinen `path`-Converter.
- MUST `[SEC]`: bestätigt. Namen, Aufgabenstellungen, Abgaben, Rückmeldungen, Dialognachrichten und API-Details gelangen in der menschenlesbaren Ausgabe ohne gemeinsame Neutralisierung von Terminal-Steuerzeichen auf stdout/stderr. Die JSON-Ausgabe soll davon ausdrücklich unberührt bleiben.
- SHOULD `[API]` Paginierungswerte: bestätigt. FastAPI konvertiert die als `int` annotierten Queryparameter vor der Handlerlogik und liefert bei nichtnumerischen Werten `422`, obwohl der Vertrag `400 invalid_pagination` vorsieht.
- SHOULD `[API]` Seitenende: bestätigt. Der Client deutet derzeit jeden `404` nach der ersten Profilseite als normales Ende und kann deshalb einen echten Fehler als vollständigen Erfolg ausgeben.
- SHOULD `[BUG]` Repository-Paginierung: bestätigt. Jede Profilseite scannt die owner-eigenen Kurse erneut ab Offset null und prüft die Mitgliedschaft kursweise. Das erzeugt unnötige Wiederholungsarbeit und ein N+1-Abfragemuster.
- SHOULD `[API]` H5P-Punkte: bestätigt. Das kursbezogene Schüler-Read-Model enthält den Abschlussstatus, aber noch nicht `score_raw` und `score_max`; die CLI kann daher keine konsistente Punktedarstellung ausgeben.
- SHOULD `[TEST]`: bestätigt. Der bisherige CLI-Test prüft nur die URL-Zeichenfolge. Ein echter ASGI-Lauf mit slash-haltigem Subject sowie die kurslose Mehrseitenaggregation einschließlich Fehlerfall fehlen.
- NICE `[CONSISTENCY]`: bestätigt, aber für diesen Fix zurückgestellt. Der Umbau der Modulgrenzen verändert keine der festgestellten Laufzeit- oder Sicherheitslücken und würde das Refactor-Budget unnötig ausweiten.

### Arbeitspakete und Reihenfolge

1. **Identity & Access: URL-sichere Subjects**
   - Verantwortlicher Kontext: Identity & Access; konsumierende Routen in Diagnostics und Teaching.
   - Vertrag zuerst: Die Beschreibungen der betroffenen `student_sub`-Parameter präzisieren, ohne Cookie-/Bearer-Flows oder Antwortformen zu ändern.
   - Roter Test: Echte ASGI-Aufrufe mit einem als `%2F` kodierten Subject müssen die CLI-Capability-Prüfung passieren; die Dialogroute muss dasselbe Subject als einen Pfadwert binden.
   - Minimaler Fix: Für den Capability-Abgleich den unveränderten ASGI-Rohpfad verwenden und nur bei fehlendem Rohpfad sicher auf den dekodierten URL-Pfad zurückfallen; die Dialogroute erhält den `path`-Converter. Die exakte Methoden-/Pfadfreigabe bleibt fail-closed.

2. **CLI-Adapter: sichere menschenlesbare Ausgabe**
   - Verantwortlicher Kontext: Diagnostics-CLI-Adapter; keine Änderung an fachlichen Read Models.
   - Roter Test: ANSI-, C0-/C1-, Zeilen- und Tabulatorzeichen in Namen, Inhalten und Fehlerdetails dürfen keine Tabellenzeilen oder Terminalfunktionen einschleusen; JSON muss die ursprünglichen Werte bytegetreu als JSON-Zeichenketten behalten.
   - Minimaler Fix: Einen zentralen Plain-Terminal-Sanitizer und einen gemeinsamen Tabellenzeilen-Writer einsetzen. Nur Detailblöcke dürfen bereinigte Zeilenumbrüche behalten. Fehlermeldungen zeigen ausschließlich bekannte öffentliche Fehlercodes, niemals beliebige Serverdetails.

3. **Diagnostics: eindeutige und skalierbare Paginierung**
   - Verantwortlicher Kontext: Diagnostics; Course Management liefert die owner- und mitgliedschaftsgebundene Kursprojektion.
   - Vertrag zuerst: Nichtnumerische oder außerhalb der Grenzen liegende `limit`-/`offset`-Werte ausdrücklich dem privaten `400 invalid_pagination` zuordnen und leere gültige Folgeseiten als `200` mit leerer Liste dokumentieren.
   - Roter Test: API-Tests decken nichtnumerische Werte, leere gültige Folgeseiten und unsichtbare Lernende bei positivem Offset ab. CLI-Tests laden ein Profil mit mehr als 50 Kursen vollständig und geben bei einer fehlerhaften Folgeseite keinerlei Teilergebnis aus.
   - Minimaler Fix: Querywerte vor der Framework-Typkonvertierung gemeinsam und strikt parsen. Eine gebündelte Repository-Abfrage paginiert direkt die Schnittmenge aus aktivem owner-eigenem Kurs und aktiver Mitgliedschaft. `404` bleibt ausschließlich der unsichtbaren Relation vorbehalten; der Client entfernt die Sonderbehandlung später `404`-Antworten.
   - Datenbank: keine Migration. Die Abfrage nutzt vorhandene Tabellen, RLS-Helfer und Indizes.

4. **Learning/Teaching → Diagnostics: neueste H5P-Punkte**
   - Verantwortlicher Kontext: Learning erzeugt die Punkte; Teaching stellt die owner-geschützte Schülerprojektion bereit; Diagnostics formatiert sie.
   - Vertrag zuerst: `score_raw` und `score_max` als nullable Integer in `TeachingStudentLiveOverviewTask` ergänzen und ausdrücklich als H5P-Punkte, nicht als Note, beschreiben.
   - Roter Test: Contract-, Service- und CLI-Tests belegen die beiden Felder und die Spalte `h5p_punkte`.
   - Minimaler Fix: Die bereits gespeicherten Werte der neuesten Abgabe in der bestehenden Repository-Abfrage mitlesen, unverändert durch Service und API reichen und nur bei vollständig vorhandenen Werten als `raw/max` darstellen.

5. **Regression, Dokumentation und Abschluss**
   - Betroffene Referenzen und Changelog bei geänderten öffentlichen Details nachziehen; keine Diagnosecaches, Inhaltslogs oder Migrationen anlegen.
   - Zuerst die eng zugeordneten Contract-, Middleware-, API-, Repository-/Service- und CLI-Tests ausführen, danach `make verify-feature FEATURE=cli-diagnostics` und `make verify`.
   - Nach der Verifikation diesen Abschnitt um erledigte Befunde, Testergebnisse und verbleibende Risiken ergänzen. Der NICE-Punkt bleibt transparent als nicht blockierende Restarbeit dokumentiert.

### Erwartete Sicherheitsgrenzen

- Rollen- und Ownership-Prüfungen werden bei jedem Aufruf weiterhin serverseitig ausgewertet; ein CLI-Token ersetzt weder Lehrerrolle noch Kursbesitz.
- Der Raw-Pfad wird ausschließlich für den Capability-Abgleich verwendet und nicht als bereits vertrauenswürdiger fachlicher Wert behandelt.
- Keine Schülerkennung und kein Diagnoseinhalt wird neu geloggt oder in Fehlerdetails gespiegelt.
- Mehrseitige CLI-Ausgaben werden weiterhin erst nach vollständigem Erfolg gerendert; Downloads bleiben atomar und auf `0600` begrenzt.

## Umsetzungsergebnis – 2026-09-02 09:15 CEST

### Erledigte Befunde

- [x] MUST `[API]`: Der Capability-Abgleich verwendet den unveränderten ASGI-Rohpfad. Echte ASGI-Tests belegen, dass `%2F` innerhalb einer Schülerkennung für Profil, Schülerübersicht, neueste Abgabe, Datei und Dialog nicht mehr als zusätzliches Pfadsegment fehlinterpretiert wird. Die Dialogroute bindet `student_sub` nun ebenfalls mit einem `path`-Converter.
- [x] MUST `[SEC]`: Ein zentraler Plain-Terminal-Sanitizer neutralisiert C0-/C1-, ANSI- und weitere Unicode-Steuerzeichen in allen menschenlesbaren Tabellen und Detailblöcken. Mehrzeilige Inhalte behalten ausschließlich sichere Zeilenumbrüche. API-Fehler zeigen nur bekannte öffentliche Codes; JSON bleibt unverändert.
- [x] SHOULD `[API]` Paginierungswerte: Ein gemeinsamer, frameworkunabhängiger Helper prüft `limit` und `offset`, bevor FastAPI eine Integerkonvertierung erzwingt. Nichtnumerische und außerhalb der Grenzen liegende Werte liefern jetzt privat `400 invalid_pagination` für Kursmatrix, Lernendenprofil und Unit-Summary.
- [x] SHOULD `[API]` Seitenende: Gültige leere Profilfolgeseiten liefern `200` mit `courses: []`; `404` bezeichnet weiterhin ausschließlich eine unsichtbare Relation. Die CLI besitzt keine `allow_terminal_404`-Sonderbehandlung mehr und verwirft bei jedem Folgeseitenfehler das gesamte Teilergebnis.
- [x] SHOULD `[BUG]` Repository-Paginierung: Eine einzige owner-, Status- und Mitgliedschafts-gebundene Repository-Abfrage paginiert die sichtbaren Kurse direkt. Das wiederholte Scannen ab Offset null und die kursweise Mitgliedschaftsabfrage entfallen. DB- und In-Memory-Adapter teilen dieselbe fachliche Grenze; keine Migration war nötig.
- [x] SHOULD `[API]` H5P-Punkte: `TeachingStudentLiveOverviewTask` enthält nun nullable `score_raw` und `score_max` der neuesten H5P-Abgabe. Die CLI-Spalte `h5p_punkte` rendert `erreicht/maximal` einschließlich des gültigen Randfalls `0/0`; die Dokumentation bezeichnet die Werte ausdrücklich nicht als Note.
- [x] SHOULD `[TEST]`: Laufzeittests decken slash-haltige Subjects durch Middleware und Route, kurslose Profile mit mehr als 50 Kursen, fehlerhafte Folgeseiten ohne Teilausgabe, Terminalsteuerzeichen, H5P-Punkte sowie strikte Paginierungsfehler ab.

### Verifikation

- Red-Nachweis vor der Implementierung: 15 gezielte Tests scheiterten an den erwarteten Reviewbefunden; der zusätzlich beim Refactor entdeckte H5P-Randfall `0/0` wurde ebenfalls zuerst mit einem fehlschlagenden Test reproduziert.
- Gezielte Abschlussregression: 91 bestanden, 1 bestehender umgebungsabhängiger Test übersprungen.
- `make verify-feature FEATURE=cli-diagnostics`: erfolgreich auf dem finalen Code mit 2.556 bestandenen Python-Tests, 78 übersprungenen Python-Tests, 642 bestandenen Frontend-Tests, fehlerfreiem Svelte-Check, Produktionsbuild, 62 bestandenen H5P-Tests und einem bestandenen authentifizierten Chromium-Feature-Acceptance-Test. Die test-eigenen Daten wurden vollständig bereinigt.
- Anschließendes `make verify`: erfolgreich mit denselben 2.556 Python-, 642 Frontend- und 62 H5P-Tests sowie allen Architektur-, OpenAPI-, Supply-Chain-, Repository-Inventar- und Produktions-Container-Prüfungen.
- `git diff --check`: erfolgreich.

### Verbleibende Risiken und Restarbeit

- Der NICE-Punkt zur vollständigen begrifflichen Entkopplung des allgemeinen Capability-Kerns vom authoring-spezifischen Modul bleibt offen. Er hat keine Laufzeit- oder Sicherheitsauswirkung auf diesen Fix und sollte als eigener, kleiner Refactor mit Kompatibilitäts- und Registertests umgesetzt werden.
- Es verbleiben keine offenen MUST- oder SHOULD-Befunde aus diesem Review. Es wurden keine Datenbankmigration, kein lokaler Diagnosecache und kein Inhaltslogging ergänzt.

### Freigabestatus

Die zuvor blockierenden Reviewbefunde sind behoben. Der Stand ist nach Feature-Gate und Gesamtgate zur erneuten Review-Freigabe bereit.

## Review Round – 09:29

- Datum: 2026-09-02 09:29 CEST
- Branch: `master`
- Base-Ref: `master` (`594473df3255e42e5fd5096f090ae12a2db03cc5`)
- Merge-Base: `594473df3255e42e5fd5096f090ae12a2db03cc5`
- Head: `594473df3255e42e5fd5096f090ae12a2db03cc5`
- Tatsächlicher Review-Scope: Feature und Review-Fix `f07c02ede640fba41678f8b34b11f143feb72934..594473df3255e42e5fd5096f090ae12a2db03cc5`
- Umfang: OpenAPI-Vertrag, CLI-Operations- und Capability-Register, Authentifizierung, Diagnostik-Read-Models, Repository-/Service-Erweiterungen, CLI-Ausgabe und Dateidownload, Python- und Playwright-Tests sowie Dokumentation; keine Migration.
- Kontext: Der formale Skill-Diff `merge-base..HEAD` ist leer, weil Feature und Fix bereits direkt auf `master` committed wurden. Für die erneute Prüfung wurde deshalb der zusammenhängende Feature-und-Fix-Bereich seit dem vorherigen Master-Stand geprüft.
- Verwendete Codeverweise: `backend/tools/gustav_cli/diagnostics_commands.py`, `backend/web/auth_middleware.py`, `backend/web/cli_authoring.py`, `backend/web/cli_capabilities.py`, `backend/web/query_validation.py`, `backend/web/routes/app_diagnostics_routes.py`, `backend/web/routes/teaching_live.py`, `backend/teaching/repo_member_queries.py`, `backend/teaching/repo_task_queries.py`, `backend/teaching/services/live_student_overview.py`, `backend/tests/test_gustav_cli_diagnostics.py`, `backend/tests/test_auth_middleware.py`, `api/openapi.yml`.

### Kurzfazit

Die vorherigen Funktions-, Paginierungs- und URL-Befunde sind sauber behoben; gezielte Regressionen sind grün. Die Freigabe bleibt jedoch blockiert, weil `criteria_results[].max_score` als einziger Wert der Detailausgabe den zentralen Terminal-Sanitizer umgeht. Zusätzlich dokumentiert OpenAPI die privaten Cache-Header der neuen `400`-Antworten noch nicht. Der allgemeine Capability-Kern ist weiterhin nur teilweise aus dem Authoring-Modul gelöst.

### MUST-FIX

- [SEC] Bereinige auch `criteria_results[].max_score` vor der menschenlesbaren Detailausgabe — `backend/tools/gustav_cli/diagnostics_commands.py:446@594473df` — Impact: Eine fehlerhafte oder präparierte Read-Model-Antwort kann weiterhin ESC-/BEL-Sequenzen direkt ins Lehrkraft-Terminal schreiben; damit ist die zugesagte zentrale Neutralisierung unvollständig — Fix: Rendere `maximum` über `_score_text(maximum)` und erweitere den Steuerzeichen-Regressionsfall ausdrücklich um einen nichtnumerischen, kontrollzeichenhaltigen `max_score`, während `--json` unverändert bleibt.

### SHOULD-FIX

- [API] Dokumentiere die privaten Cache-Header der Paginierungsfehler in beiden Diagnostik-Read-Models — `api/openapi.yml:9058@594473df` — Impact: Laufzeit und Tests liefern für `400 invalid_pagination` korrekt `Cache-Control: private, no-store`, der öffentliche Vertrag beschreibt diese Datenschutzgarantie anders als bei `200/401/403/404` jedoch nicht; generierte Clients und Contractprüfungen sehen dadurch ein unvollständiges Fehlerverhalten — Fix: Ergänze `Cache-Control` bei den `400`-Antworten von Kursmatrix und Lernendenprofil und sichere die Headerdeklaration in den beiden OpenAPI-Contracttests ab.

### NICE-TO-HAVE

- [CONSISTENCY] Löse Typ und Pfadmatcher des allgemeinen Capability-Registers aus `cli_authoring.py` — `backend/web/cli_capabilities.py:11@594473df` — Impact: Das öffentliche Register ist allgemein benannt, importiert seinen Kern aber weiterhin aus dem authoring-spezifischen Modul; das erschwert Orientierung und spätere Erweiterungen ohne derzeitige Laufzeitwirkung — Fix: Verschiebe `CLICapability` und `path_matches_template` in ein neutrales Kernmodul beziehungsweise nach `cli_capabilities.py` und belasse in `cli_authoring.py` nur Kompatibilitäts-Re-Exports.

### TESTS

- Bestanden auf `594473df`: 91 gezielte CLI-, API-, OpenAPI-, Capability-, Repository- und Service-Tests; 1 umgebungsabhängiger Test übersprungen.
- Bestanden auf `594473df`: 9 echte ASGI-Middlewaretests für sämtliche Diagnostikrouten, URL-kodierte Slash-Subjects und die Ablehnung eines Write-only-Tokens.
- Reproduziert auf `594473df`: `_render_submission` gibt bei `max_score='3\\x1b]0;OWNED\\x07'` sowohl ESC als auch BEL unverändert aus; der vorhandene Steuerzeichen-Test setzt `max_score` nur numerisch.
- Die bereits auf demselben Head dokumentierten vollständigen Läufe `make verify-feature FEATURE=cli-diagnostics` und `make verify` waren erfolgreich. Im Review wurden sie nicht erneut ausgeführt; ein breiterer Sammellauf mit zusätzlichen, für den Scope irrelevanten Auth-Tests wurde wegen eines umgebungsabhängig wartenden Tests ohne Ergebnis abgebrochen und anschließend durch die gezielte grüne Auswahl ersetzt.

### Refactor-Budget

Etwa ein halber Entwicklungstag: deutlich unter zwei Stunden für den Terminal-Fix samt rotem Regressionstest, anschließend die OpenAPI-Header und Contracttests. Die neutrale Capability-Modulgrenze kann separat in etwa einem weiteren halben Tag refaktoriert werden.

### Freigabe

Nicht freigegeben. Der verbleibende Terminal-Injection-Pfad muss geschlossen und mit einem expliziten Regressionstest abgesichert werden. Danach sollten die gezielten Diagnostiktests sowie wegen der sicherheitsrelevanten Ausgabeänderung erneut `make verify-feature FEATURE=cli-diagnostics` und `make verify` auf dem endgültigen Commit laufen.

## Reparaturplan – 2026-09-02 14:09 CEST

### Erneute Prüfung der offenen Befunde

- MUST `[SEC]`: bestätigt. `criteria_results[].max_score` wird in der menschenlesbaren Detailausgabe direkt interpoliert und umgeht als einziger Kriterienwert `_score_text`; der vorhandene Steuerzeichen-Test verwendet dort nur einen Integer. Die JSON-Ausgabe muss fachlich unverändert bleiben.
- SHOULD `[API]`: bestätigt. Beide Diagnostik-Handler setzen bei `400 invalid_pagination` bereits private Cache-Header, während deren OpenAPI-Antworten im Gegensatz zu den übrigen privaten Antworten keinen `Cache-Control`-Header dokumentieren.
- NICE `[CONSISTENCY]`: weiterhin offen, aber nicht Bestandteil dieses Fix-Durchlaufs. Die Verschiebung des Capability-Kerns ist weder für die Sicherheitskorrektur noch für die Vertragsangleichung erforderlich und soll als eigener Refactor mit breiter Import-Kompatibilitätsprüfung erfolgen.
- Git-Kontext: Dokument und Code beziehen sich weiterhin auf Branch `master` und Head `594473df3255e42e5fd5096f090ae12a2db03cc5`; es liegt kein Kontextkonflikt vor. Die uncommitted Änderung ist ausschließlich die zuvor angehängte Review-Runde in diesem PR-Fix-Dokument.

### Arbeitspakete

1. **Diagnostics-CLI: vollständige Terminalneutralisierung**
   - Finding: MUST `[SEC]`; weiterhin offen wegen der direkten `max_score`-Interpolation.
   - Vorgaben: Security first, KISS und Red–Green–Refactor aus `AGENTS.md`; Terminalformatierung bleibt im CLI-Adapter und verändert weder Learning-Auswertungen noch Teaching-Read-Models.
   - Bounded Context: `diagnostics`; konsumierte Kriterienwerte stammen aus `learning`, werden aber fachlich nicht umgedeutet.
   - Dateien: `backend/tests/test_gustav_cli_diagnostics.py`, anschließend `backend/tools/gustav_cli/diagnostics_commands.py`.
   - Vertrag/Migration/Config: keine Änderung erforderlich; JSON- und HTTP-Vertrag bleiben gleich, keine Migration.
   - Test-first: Den bestehenden Steuerzeichen-Test mit kontrollzeichenhaltigem `max_score` erweitern und dessen Fehlschlag nachweisen.
   - Minimale Implementierung: Den bereits vorhandenen `_score_text`-Pfad auch für den Maximalwert verwenden.
   - Abnahme: Der gezielte CLI-Test ist grün, enthält keine ESC-/BEL-/C1-Zeichen in der menschenlesbaren Ausgabe und bestehende JSON-Tests bleiben unverändert grün.

2. **Diagnostics-API: private Fehlerheader vollständig vertraglich festhalten**
   - Finding: SHOULD `[API]`; Laufzeit und Vertrag sind bei den beiden Paginierungsfehlern noch nicht vollständig deckungsgleich.
   - Vorgaben: Contract First und sichere Cache-Header aus `docs/ARCHITECTURE.md`; der HTTP-Adapter dokumentiert bestehendes Verhalten, ohne neue Geschäftslogik einzuführen.
   - Bounded Context: `diagnostics`; OpenAPI ist die öffentliche Adaptergrenze.
   - Dateien: zuerst `api/openapi.yml`, danach `backend/tests/test_openapi_diagnostics_course_matrix_contract.py` und `backend/tests/test_openapi_diagnostics_learner_profile_contract.py`.
   - Vertrag/Migration/Config: OpenAPI zuerst; keine Runtime-, Migrations- oder Konfigurationsänderung.
   - Testschritt: Beide Contracttests verlangen für `400` einen dokumentierten `Cache-Control`-Header und bestätigen anschließend den aktualisierten Vertrag.
   - Minimale Implementierung: Dieselbe private Headerdeklaration wie bei den benachbarten `401`-/`403`-/`404`-Antworten ergänzen.
   - Abnahme: Die beiden OpenAPI-Contracttests sowie die vorhandenen API-Tests für `400 invalid_pagination` sind grün.

3. **Regression und Freigabe**
   - Zuerst die betroffenen CLI-, OpenAPI- und Diagnostik-API-Tests ausführen; anschließend `make verify-feature FEATURE=cli-diagnostics` und `make verify` auf dem finalen Arbeitsstand.
   - `git diff --check` und Repo-Safety bleiben Teil der Abschlusskontrolle. Frontend, Datenbank und visuelles Design werden nicht geändert; eine Prüfung gegen `docs/DESIGN.md` ist daher nicht erforderlich.
   - Danach den Umsetzungsstand, Testergebnisse, den bewusst offenen NICE-Punkt und die aktualisierte Freigabeempfehlung in diesem Dokument ergänzen.

## Umsetzungsstand – 2026-09-02 14:30 CEST

### Geschlossene Befunde

- [x] MUST `[SEC]`: Auch `criteria_results[].max_score` wird in der menschenlesbaren Detailausgabe über `_score_text` und damit den zentralen Terminal-Sanitizer gerendert. Ein erweiterter Regressionstest belegt, dass ESC-, BEL- und C1-Steuerzeichen aus diesem Feld nicht mehr in stdout gelangen. Die JSON-Ausgabe und die fachliche Score-Semantik bleiben unverändert.
- [x] SHOULD `[API]`: Die `400 invalid_pagination`-Antworten von Kursmatrix und Lernendenprofil dokumentieren nun denselben privaten `Cache-Control`-Header wie die bereits abgesicherten Laufzeitantworten. Beide OpenAPI-Contracttests halten diese Deklaration fest.

### Offene und stale Befunde

- Der NICE-Punkt `[CONSISTENCY]` zur neutralen Modulgrenze des Capability-Kerns bleibt bewusst offen. Er hat keine Laufzeit- oder Sicherheitsauswirkung auf diesen Fix und benötigt als eigener Refactor breitere Import- und Kompatibilitätstests.
- Kein MUST- oder SHOULD-Befund erwies sich bei der erneuten Prüfung als stale; beide waren reproduzierbar und wurden geschlossen.

### Red–Green–Refactor-Nachweis

- Red: Der um ein kontrollzeichenhaltiges `max_score` erweiterte CLI-Test scheiterte erwartungsgemäß, weil ESC und BEL unverändert hinter dem Kriterienwert ausgegeben wurden.
- Green: Die minimale Änderung leitet nun auch den Maximalwert durch den vorhandenen `_score_text`-Helfer. Der einzelne Sicherheitstest und anschließend alle 20 CLI-Diagnostiktests bestanden.
- Contract First: Weil die privaten Header zur Laufzeit bereits vorhanden waren, wurde zuerst `api/openapi.yml` vervollständigt und anschließend die dauerhafte Erwartung in beiden OpenAPI-Contracttests ergänzt. Die zugehörigen 15 OpenAPI- und Diagnostik-API-Tests bestanden.
- Refactor: Es entstand keine zweite Bereinigungslogik und keine neue fachliche Abhängigkeit; der bestehende Ausgabepfad wurde wiederverwendet.

### Verifikation

- Gezielte Diagnostikregression: 91 Tests bestanden, 1 umgebungsabhängiger Test übersprungen; zusätzlich bestanden 9 echte ASGI-Middlewaretests.
- `make local-ca-status`: System, Chromium/Codex und Firefox vertrauen der aktuellen lokalen Caddy-CA.
- `make verify-feature FEATURE=cli-diagnostics`: erfolgreich mit 2.556 bestandenen Python-Tests, 78 übersprungenen Python-Tests, 642 bestandenen Frontend-Tests, fehlerfreiem Svelte-Check, Produktionsbuild, 62 bestandenen H5P-Tests und einem bestandenen authentifizierten Chromium-Feature-Acceptance-Test. Die test-eigenen Daten wurden vollständig bereinigt.
- Der erste separate `make verify`-Lauf traf einmalig einen fehlenden globalen Job-Claim in `test_only_transient_failures_can_be_retried_and_addresses_are_purged`, einem unveränderten DB-Test für Einladungsmails. Der Test bestand unmittelbar danach isoliert. Ohne Codeänderung bestand die vollständige Wiederholung von `make verify` mit 2.556 Python-, 642 Frontend- und 62 H5P-Tests sowie allen Architektur-, OpenAPI-, Supply-Chain-, Repository-Inventar- und Produktions-Container-Prüfungen.
- `git diff --check`: erfolgreich.

### Geänderte Bereiche und Architekturentscheidung

- Geändert wurden ausschließlich `api/openapi.yml`, drei Contract-/CLI-Testdateien, der Diagnostik-CLI-Adapter und dieses PR-Fix-Dokument.
- Die Terminalneutralisierung bleibt eine Verantwortung des Diagnostics-CLI-Adapters. Learning-Auswertungen und Teaching-Read-Models werden weder verändert noch in der CLI neu interpretiert.
- Die Cache-Header-Änderung dokumentiert vorhandenes Verhalten an der HTTP-Vertragsgrenze; es gibt keine neue Runtime-Geschäftslogik.
- Keine Datenbankmigration, Konfigurationsänderung, lokale Sonderlogik, Diagnosecaches oder Inhaltslogs wurden ergänzt. Frontend und visuelles Design waren nicht betroffen.

### Restrisiko und Freigabeempfehlung

- Das verbleibende Risiko beschränkt sich auf den nicht blockierenden Capability-Modulrefactor und den beobachteten, nicht reproduzierbaren Einladungsmail-Test-Flake außerhalb dieses Scopes.
- Es bestehen keine offenen MUST- oder SHOULD-Befunde aus der Review-Runde 09:29. Der Arbeitsstand ist nach Feature-Gate und erfolgreichem Gesamtgate zur erneuten Review beziehungsweise zum Commit auf `master` freigegeben.

## Ergänzender Reparaturplan – 2026-09-02 14:37 CEST

Auf ausdrücklichen Wunsch wird auch der bislang zurückgestellte NICE-Punkt `[CONSISTENCY]` umgesetzt.

1. **Allgemeiner CLI-Capability-Kern**
   - Befund: `CLICapability` und `path_matches_template` liegen noch in `cli_authoring.py`, obwohl sie gleichermaßen Authoring und Diagnostik bedienen. `cli_capabilities.py` muss deshalb allgemeine Bausteine aus einem fachlich enger benannten Modul importieren.
   - Architektur: Die Änderung gehört zur technischen Identity-&-Access-Grenze für CLI-Tokens. Sie verändert weder fachliche Berechtigungen noch OpenAPI, Runtime-Routen oder Bounded-Context-Daten.
   - Dateien: `backend/web/cli_capability.py` als neutraler Kern, `backend/web/cli_authoring.py`, `backend/web/cli_capabilities.py` und der bestehende Architektur-/Registertest `backend/tests/test_cli_authoring_contract.py`.
   - Test-first: Den vorhandenen statischen Delegationstest so erweitern, dass Typ und Matcher aus dem neutralen Kern stammen und im Authoring-Modul nur kompatibel re-exportiert werden. Es wird kein zusätzlicher Testfall benötigt.
   - Minimale Implementierung: Dataclass und exakten Pfadmatcher unverändert verschieben; `CLIAuthoringCapability`, `path_matches_template` und der authoring-spezifische Resolver bleiben als kompatible Imports erhalten. Das allgemeine Register importiert aus `cli_authoring.py` danach nur noch die Authoring-Capability-Tabelle.
   - Vertrag/Migration/Docs: Keine OpenAPI-, Migrations-, Konfigurations- oder Nutzer-Dokumentationsänderung; es handelt sich ausschließlich um eine interne Modulgrenze.
   - Abnahme: Bestehende Authoring-, Diagnostik-, OpenAPI- und Middlewaretests bleiben grün; Architektur- und Gesamtgates bestätigen, dass kein Importzyklus und keine Capability-Abweichung entstanden ist.

## Ergänzender Umsetzungsstand – 2026-09-02 14:52 CEST

- [x] NICE `[CONSISTENCY]`: `CLICapability` und `path_matches_template` liegen nun im neutralen Modul `backend/web/cli_capability.py`. `cli_authoring.py` enthält nur noch die Authoring-Tabelle, den authoring-spezifischen Resolver und kompatible Re-Exports; das allgemeine Gesamtregister importiert aus dem Authoring-Modul ausschließlich dessen Tabelle.
- Red–Green: Der erweiterte bestehende Architekturtest scheiterte zunächst am fehlenden neutralen Kernmodul. Nach der Extraktion bestanden der einzelne Test sowie alle 16 Capability-Vertragstests. Es wurde kein zusätzlicher Verhaltenstest angelegt.
- Kompatibilität: Ein Import-Smoke bestätigt, dass `CLICapability`, `CLIAuthoringCapability`, `path_matches_template` und der authoring-spezifische Resolver weiterhin über `cli_authoring.py` nutzbar sind. Typidentität, Matching und Scopeauflösung blieben unverändert.
- Gezielte Regression: 47 Capability-, CLI-Diagnostik-, OpenAPI- und echte ASGI-Middlewaretests bestanden; 1 bestehender Test wurde übersprungen.
- `make verify-feature FEATURE=cli-diagnostics`: erfolgreich auf dem Refactor-Stand mit 2.556 bestandenen Python-Tests, 78 übersprungenen Python-Tests, 642 bestandenen Frontend-Tests, fehlerfreiem Svelte-Check, Produktionsbuild, 62 bestandenen H5P-Tests und einem bestandenen authentifizierten Chromium-Feature-Acceptance-Test. Die test-eigenen Daten wurden vollständig bereinigt.
- Anschließendes `make verify`: erfolgreich mit denselben 2.556 Python-, 642 Frontend- und 62 H5P-Tests sowie OpenAPI-, Architektur-, Importgrenzen-, Supply-Chain-, Repository-Inventar- und Produktions-Container-Prüfungen.
- Architektur: Der gemeinsame CLI-Capability-Kern ist frameworkfrei und gehört zur technischen Identity-&-Access-Grenze. Die Extraktion führt keine Abhängigkeit in Teaching, Learning oder Diagnostics ein und erzeugt keinen Importzyklus.
- Vertrag und Daten: Keine weitere OpenAPI-Änderung, keine Migration, keine Konfigurationsänderung und keine neue Runtime-Semantik waren für den Refactor erforderlich.
- Freigabe: Damit sind auch die NICE-Restarbeit und alle übrigen Befunde aus der Review-Runde 09:29 geschlossen. Der Arbeitsstand ist vollständig geprüft und bereit für Commit beziehungsweise erneute Review.
