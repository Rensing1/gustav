# PR-Review: Frontend-Kompatibilitätskorrekturen

## Review Round – 12:18

- Datum: 2026-09-03 12:18 CEST
- Branch: `master`
- Base-Ref: `master` (`f555b67a5c86670e72b7006af8d77b5b35c8a851`)
- Merge-Base: `f555b67a5c86670e72b7006af8d77b5b35c8a851`
- Head: `f555b67a5c86670e72b7006af8d77b5b35c8a851`
- Tatsächlicher Review-Scope: noch nicht veröffentlichte Frontend-Commits `7a39e067e50fd7e8fb31f27318a2695630b00c37..f555b67a5c86670e72b7006af8d77b5b35c8a851`
- Umfang: globaler CSS-Einstieg, CSS-Kompatibilitätschecker, Build-Integration, Graph-Prioritätskorrektur, Frontend-/Packaging-/Playwright-Tests und Dokumentation; keine API- oder Datenbankänderung.
- Kontext: Der formale Skill-Diff `merge-base..HEAD` ist leer, weil beide Korrekturen bereits direkt auf lokalem `master` committed wurden. Deshalb wurde ergänzend der zusammenhängende, gegenüber `origin/master` noch lokale Bereich mit den Commits `271664d3` und `f555b67a` geprüft.
- Verwendete Codeverweise: `frontend/src/lib/styles/index.css`, `frontend/src/lib/styles/teaching-workspace.css`, `frontend/src/lib/styles/ui-primitives.css`, `frontend/tooling/css-compatibility.mjs`, `frontend/tooling/css-compatibility.test.mjs`, `frontend/tooling/build-with-warning-gate.sh`, `frontend/e2e/ios-15-3-css-compatibility.spec.ts`, `frontend/playwright.config.ts`, `backend/tests/packaging/test_sveltekit_ui_shell_contract.py`, `docs/DESIGN.md`.

### Kurzfazit

Der konkrete Layer-Ausfall unter iPadOS 15.3 und die im ersten Review entdeckte Graph-Prioritätsverschiebung sind verständlich und gezielt behandelt. Build und Frontendtests bestehen. Die Freigabe bleibt dennoch blockiert: Der nachgebesserte Import-Parser übersieht weiterhin gültige Layer-Imports, wenn ein CSS-Kommentar direkt zwischen Quelle und `layer(...)` steht. Damit erfüllt das neue Build-Gate seine dokumentierte Schutzgarantie noch nicht vollständig.

### MUST-FIX

- [BUG] Behandle CSS-Kommentare beim Parsen des `@import`-Präludiums als Token-Trenner — `frontend/tooling/css-compatibility.mjs:15@f555b67a` — Impact: Gültiges CSS wie `@import "theme.css"/**/layer(theme);` liefert keine Treffer; der Produktionsbuild kann dadurch genau die für iPadOS 15.3 unverträgliche Importform freigeben, die das Gate vollständig ausschließen soll — Fix: Tokenisiere das Import-Präludium robust oder überspringe Whitespace und Kommentare gemeinsam, ergänze kompakte Kommentar- sowie direkt anschließende Medienquery-Fälle zuerst als rote Unit-Tests und lasse den Acceptance-Test dieselbe Erkennungslogik statt einer engeren Regex verwenden.

### SHOULD-FIX

Keine weiteren SHOULD-FIX-Befunde.

### NICE-TO-HAVE

- [CONSISTENCY] Aktualisiere den verbliebenen Kommentar mit der alten Layer-Terminologie — `frontend/src/lib/styles/overrides.css:2@f555b67a` — Impact: Der Kommentar spricht weiterhin von „Cross-layer composition“ und „component layers“, obwohl das Designsystem Cascade Layers nun ausdrücklich verbietet; das erschwert Lernenden die Einordnung, verändert aber kein Laufzeitverhalten — Fix: Beschreibe dort die dateiübergreifende Komposition und normale CSS-Spezifität ohne Layer-Begriffe.

### TESTS

- Bestanden auf `f555b67a`: 141 Frontend-Testdateien mit 653 Tests sowie beide Tooling-Testdateien über `npm test`.
- Bestanden auf `f555b67a`: Produktionsbuild einschließlich CSS-Kompatibilitätsgate; 16 bereits erlaubte und dokumentierte Upstream-Warnungen.
- Reproduziert auf `f555b67a`: `findCascadeLayers('@import "a.css"/**/layer(foo);')` und die entsprechende `url(...)`-Form liefern jeweils `[]` statt eines Layer-Treffers.
- Der vorhandene Test enthält zwar einen Kommentar, aber Leerzeichen vor und nach ihm; PostCSS normalisiert diesen Fall zu gewöhnlichem Whitespace. Die kompakte, ebenfalls gültige Schreibweise bleibt deshalb ungetestet.
- Das Playwright-Projekt verwendet WebKit 26.4 mit einem iPad-Geräteprofil. Es prüft Layout und ausgelieferte Assets, emuliert aber nicht die CSS-Engine von iPadOS 15.3.1. Der im Featureplan bereits ausgewiesene Test auf dem betroffenen physischen Gerät bleibt daher das relevante Restrisiko.
- Das zuvor dokumentierte vollständige Gate `make verify-feature FEATURE=ios-15-3-css-compatibility` war auf demselben Head erfolgreich und wurde in dieser Review-Runde nicht erneut ausgeführt.

### Refactor-Budget

Etwa ein halber Entwicklungstag: zuerst parsernahe Regressionstests, danach eine kleine tokenbasierte oder kommentarbewusste Importanalyse, Wiederverwendung im Browsertest, Aktualisierung des Kommentars und abschließend das gezielte Feature-Gate.

### Freigabe

Nicht freigegeben. Die verbleibende Parserlücke muss geschlossen werden, damit das Build-Gate seine zentrale Kompatibilitätsgarantie tatsächlich für alle gültigen Layer-Imports einhält. Danach `make verify-feature FEATURE=ios-15-3-css-compatibility` auf dem endgültigen Stand erneut ausführen; der reale iPadOS-15.3.1-Test bleibt zusätzlich empfohlen.

## Reparaturplan – 2026-09-03 12:23

### Erneute Prüfung

- Der MUST-FIX-Befund ist auf `master` bei `f555b67a` weiterhin offen: Kommentar-getrennte Layer-Imports mit String- und `url(...)`-Quelle liefern jeweils keine Treffer. Auch ein unmittelbar folgender Medientyp wird nach `layer(...)` wegen der Whitespace-Bedingung nicht erkannt.
- Der NICE-TO-HAVE-Befund ist weiterhin offen und direkt verwandt: `overrides.css` beschreibt die normale dateiübergreifende CSS-Kaskade noch als Zusammenspiel von Layers.
- Es gibt keine API-, Datenbank-, Authentifizierungs- oder fachliche Begriffsänderung. `api/openapi.yml`, Migrationen, RLS und `docs/glossary.md` bleiben deshalb unverändert.

### Arbeitspaket 1: vollständige Erkennung von Layer-Imports

- Finding: MUST-FIX `[BUG]`; das dokumentierte Build-Gate kann gültige verbotene Syntax noch übersehen.
- Regeln und Eigentümer: KISS, TDD und FOSS-Lesbarkeit aus `AGENTS.md`; technische Designsystem-Grenze des Frontends gemäß `docs/DESIGN.md`. Die Änderung bleibt im Frontend-Build-Tooling und führt keine Geschäftslogik in Framework- oder Fachschichten ein.
- Dateien: zuerst `frontend/tooling/css-compatibility.test.mjs` und `frontend/e2e/ios-15-3-css-compatibility.spec.ts`, danach `frontend/tooling/css-compatibility.mjs`.
- Vertrag/Migration/Config: keine Änderung erforderlich; der bestehende CSS-Kompatibilitätsvertrag wird lediglich vollständig durchgesetzt.
- Test-first: Kompakte Kommentare zwischen Importquelle und Layer sowie eine direkt an `layer(...)` anschließende Medienquery müssen zunächst im Unit-Test scheitern. Der Acceptance-Test soll dieselbe parserbasierte Prüfung wie das Build-Gate verwenden.
- Minimale Implementierung: Den bestehenden kleinen Parser beibehalten, aber Whitespace und CSS-Kommentare gemeinsam überspringen; den Abschluss einer benannten `layer(...)`-Funktion nicht künstlich von folgendem Whitespace abhängig machen. Der anonyme Bezeichner `layer` bleibt über eine Token-Grenze gegen falsche Treffer wie `layered` geschützt.
- Abnahme: Gezielte Tooling-Tests erkennen alle neuen Fixtures, akzeptieren weiterhin normales CSS und der Produktionsbuild enthält keine gemeldeten Cascade Layers.

### Arbeitspaket 2: verständliche CSS-Dokumentation

- Finding: NICE-TO-HAVE `[CONSISTENCY]`; wegen unmittelbarer Nähe und minimalem Aufwand Bestandteil dieses Durchlaufs.
- Regeln und Eigentümer: FOSS-Lesbarkeit und englische Codekommentare; Designsystem- und Frontend-Infrastruktur.
- Datei: `frontend/src/lib/styles/overrides.css`.
- Test-first: Der bestehende Packaging-Vertrag und das CSS-Gate schützen die technische Grenze; die reine Kommentarbereinigung benötigt keinen eigenen Verhaltenstest.
- Minimale Implementierung: „Cross-layer“ durch „cross-file“ ersetzen und die Prioritätsaussage auf normale Selektorspezifität und Importreihenfolge beziehen.
- Abnahme: Repository-Suche findet im produktiven CSS keine veraltete Layer-Beschreibung mehr; das visuelle Verhalten bleibt unverändert.

### Abschlussprüfung

1. Zuerst die gezielten Node-Tests und den Svelte-/TypeScript-Check ausführen.
2. Den Produktionsbuild mit integriertem CSS-Gate ausführen.
3. Vor dem Browserlauf `make local-ca-status` prüfen und anschließend `make verify-feature FEATURE=ios-15-3-css-compatibility` auf dem endgültigen Stand ausführen.
4. Ergebnisse, geschlossene Befunde und das weiterhin externe Risiko des physischen iPadOS-15.3.1-Tests in diesem Dokument ergänzen; danach `git diff --check`, Public-Repo-Hygiene und Commit auf `master`.

## Umsetzungsstand – 2026-09-03 12:35

### Geschlossene und geprüfte Befunde

- [x] MUST-FIX `[BUG]`: Der Import-Parser behandelt CSS-Kommentare nun wie zulässige Trennzeichen. Benannte Layer werden auch erkannt, wenn nach `layer(...)` ohne zusätzliches Leerzeichen eine Medienquery folgt. String- und `url(...)`-Quellen sind durch Regressionstests abgedeckt.
- [x] Der authentifizierte Acceptance-Test verwendet für ausgelieferte Stylesheets dieselbe `findCascadeLayers`-Funktion wie das Produktionsbuild-Gate. Die zuvor engere, nur auf `@layer` passende Regex ist entfernt.
- [x] NICE-TO-HAVE `[CONSISTENCY]`: Der Kommentar in `overrides.css` erklärt nun die normale dateiübergreifende Kaskade, Selektorspezifität und Importposition ohne widersprüchliche Layer-Terminologie.
- Kein dokumentierter Befund war bei der erneuten Prüfung stale; weitere SHOULD-FIX-Befunde gab es nicht.

### Red–Green–Refactor-Nachweis

- Red: Der erweiterte Parser-Unit-Test bestand zunächst mit vier Fällen und scheiterte bei den beiden neuen Fixtures. Erwartet waren `commented` und `screen`, tatsächlich fehlten beide Treffer.
- Green: Nach der minimalen Trivia- und Token-Grenzen-Anpassung bestehen alle fünf gezielten Parser-Tests.
- Refactor: Build und Browsertest teilen jetzt eine Erkennungsfunktion. Die erklärende CSS-Dokumentation beschreibt den tatsächlichen Mechanismus ohne neue Abhängigkeit oder zweiten Parser.

### Verifikation

- `node tooling/css-compatibility.test.mjs`: 5 bestanden.
- `npm run check`: 0 Fehler, 0 Warnungen.
- `npm test`: 141 Frontend-Testdateien mit 653 Tests sowie acht Tooling-Tests bestanden.
- `npm run build`: Produktionsbuild einschließlich CSS-Kompatibilitätsgate bestanden; 16 erlaubte und bereits dokumentierte Upstream-Warnungen.
- `make local-ca-status`: aktuelle Caddy-CA in System, Chromium/Codex und Firefox als vertrauenswürdig bestätigt.
- `make verify-feature FEATURE=ios-15-3-css-compatibility`: erfolgreich. Enthalten waren 2.556 bestandene Backendtests, 78 übersprungene Backendtests, 653 Frontendtests, acht Tooling-Tests, 62 H5P-Tests, Produktionsbuild sowie je ein bestandener authentifizierter Chromium- und iPad-WebKit-Lauf. Die run-eigenen Benutzer, Kurse, Lerneinheiten, Sitzungen, Tokens und H5P-Inhalte wurden vollständig bereinigt.

### Architektur, Design und verbleibendes Risiko

- Die Änderung bleibt im Frontend-Build-Tooling und im zugehörigen Acceptance-Adapter. Fachliche Use Cases, API-Vertrag, Datenbank, RLS, Authentifizierung und Bounded-Context-Grenzen sind unverändert.
- Die Designregel aus `docs/DESIGN.md` wird nun konsistent im Build und im Browserlauf durchgesetzt. Es wurden keine sichtbaren Stile oder Komponenten verändert.
- Das verbleibende Risiko ist extern: Das Playwright-iPad-Profil prüft modernes WebKit mit iPad-Geometrie, nicht die historische Engine von iPadOS 15.3.1. Ein vollständiges Neuladen und Smoke-Test auf dem konkret betroffenen Gerät bleibt empfohlen.

### Freigabestatus

Alle offenen MUST- und NICE-Befunde dieser Review-Runde sind geschlossen. Der Stand ist nach dem erfolgreichen Feature-Gate für den Commit und eine erneute Review-Freigabe bereit; ein Push erfolgt weiterhin nur auf ausdrücklichen Auftrag.
