# Designkonsistenz: Reparatur und Abnahme

## Auftrag und Grenzen

Freigegebener Implementierungsplan vom 10. September 2026, Ausgangspunkt `23f3fa2d` auf `master`. Grundlage sind die Befundkataloge `2026-09-09-design-befundkatalog.md`, `2026-09-09-designabschluss-restpruefung.md` und `2026-09-09-designaudit-zusatzlandschaft.md`. Alle DS-01 bis DS-35 werden bearbeitet; vorhandene Reparaturen werden überprüft statt ersetzt. Ein Status „behoben“ setzt automatisierte Tests und tatsächlich gesichteten Bildnachweis voraus.

GUSTAV bleibt eine zusammenhängende Plattform mit kantiger Gestaltung und gemeinsamen Regeln für beide Rollen. Unterschiede folgen deren Aufgaben. DSPy, KI-Optimierung und generierte Rückmeldungen sind ausgeschlossen. Diagnostik bleibt aus der Hauptnavigation verborgen. Keine Bibliotheksaktualisierung und kein H5P-Fork. Bestehende Inhalte, Berechtigungen, Bewertungen, Zähl- und Wiederholungslogik bleiben erhalten.

Keine neuen fachlichen Endpunkte, Tabellen oder Migrationen vorgesehen. Korrigierte Diagnostik-Zieladressen werden zuerst in `api/openapi.yml` beschrieben. Auth erhält ausschließlich einen validierten `light`-/`dark`-Darstellungshinweis ohne Identitätsdaten; TLS, sichere Cookies, Sitzungsprüfung und Weiterleitungsschutz bleiben unverändert. Zentrale Designtokens bleiben die einzige Quelle; Auth- und H5P-Ressourcen werden beim Build daraus abgeleitet, ohne zusätzliche Laufzeitkopplung.

## Durchführung

Je Paket: Designvertrag ergänzen → BDD und fehlschlagende Tests → minimale Reparatur → Aufräumen → Feature-Gates und Bildprüfung → Katalog aktualisieren → lokaler Commit. Kein Push. Isolierte synthetische Testdaten und laufbezogene Bereinigung; vorhandene Dev-Lernstände werden nicht zurückgesetzt. Auth-Mailtests ausschließlich mit lokalem Mailfänger und Wiederherstellung der ursprünglichen SMTP-Konfiguration. Vor Auth-Abnahme aktive Konten lesend auf eindeutige E-Mail-Adressen prüfen; Konflikte niemals durch automatische Umbenennung lösen.

## Pakete, User Stories und BDD-Abnahme

| Paket | Befunde | User Story / Umsetzung | Browser-Specs | Status |
| --- | --- | --- | --- | --- |
| 1 | DS-26, DS-33, DS-34 | Als Lehrkraft möchte ich vorhandenes Markdown ohne Verlust speichern und lineare Abschnitte gezielt bearbeiten; als Lernender erkenne ich Abschnittstitel. Inline-Code und Codeblöcke bleiben inerte Texte. Auswahl zeigt „Inhalte bearbeiten“ und „Eigenschaften“, erst Letzteres öffnet das Formular. Rückkehr erhält Auswahl und Ausschnitt, Direktlinks bleiben gültig. | `editor-content-integrity`, `teacher-linear-section-actions` | abgenommen |
| 2 | DS-01–06, DS-08 | Beide Rollen erhalten gemeinsame primäre, sekundäre, dezente und destruktive Aktionen, Felder und Dialoge. Practice, beide Kataloge, Kurserstellung, Mitglieder und Profil verwenden bestehende Komponenten. Normale Bedienelemente mindestens 44 px hoch, mobile Symbole 44 × 44 px; kompakte Desktopwerkzeuge ausgenommen. Checkboxen/Radiofelder nicht vollbreit. Löschen im beschrifteten Menü „Weitere Aktionen“, mit Bestätigung. | `design-system-consistency`, `teacher-course-detail` | offen |
| 3 | DS-10–12, DS-27–28 | Als Nutzer erkenne ich bei Anmeldung/Reset die erforderliche E-Mail-Adresse und behalte meine Darstellung. Registrierung weiterhin Schul-E-Mail; keine Änderung an IServ oder Kontonamen. Gemeinsamer Keycloak-Rahmen, passende Autofill-Felder, border-box, sichtbarer Theme-Umschalter. App-Hinweis vor Auth-Auswahl vor Systemeinstellung; direkte Mailaufrufe ohne App-Hinweis. Fehler mit festem Rückweg über GUSTAV. Einladungen in beiden Modi lesbar. | `auth-design-consistency`, `course-invite-registration` | offen |
| 4 | DS-13–17, DS-32 | Als Lernender kann ich H5P auch mobil lesen und bedienen. Äußere Rahmen und eingebettete Inhalte getrennt korrigieren; Theme-Werte für Player und Editor vollständig auflösen. Standardbedienung deutsch, Autorentexte unverändert. DragQuestion standardmäßig „Lesbare Ansicht“, außerdem „Gesamtansicht“, Vergrößern/Verkleinern. Renderbreite aus Inhaltsskalierung für mindestens 16 px Text, Originalgeometrie erhalten, reguläres H5P-Resize statt optischer Transformation. Nur Aufgabenfläche scrollt, keine Konkurrenz zwischen Verschieben und Antwortziehen; Fokus wird sichtbar. | `h5p-drag-layout`, `h5p-design-consistency` | offen |
| 5 | DS-07, DS-23–25 | Als Nutzer erreiche ich verständliche Graphwerkzeuge und erkenne Sperren. Deutsche Namen, kontrastreiche Hover-/Fokus-/Deaktiviertzustände, Höhe nach verbleibendem Bildschirmplatz. Lesbarer Startfokus, bewusste Gesamtansicht, persistenter Ausschnitt bei Aktualisierung. Echte Freischaltbedingungen anzeigen, keine erfinden. Identische Knotenkoordinaten, Größen und Verbindungen beider Rollen; keine automatische Umordnung. | `graph-role-parity` | offen |
| 6 | DS-09, DS-18–22, DS-30, DS-35 | Als Lehrkraft nutze ich Live, Diagnostik und Mitglieder auch bei langen Namen/schmalem Bildschirm. Gemeinsame Reiter, Rahmen und Aktionen, begrenzte Auswahlfelder, beschriftete Tabellenscrollbereiche, mobile Detailflächen untereinander. Aufgaben nach Abschnitt/Modul gruppiert und eindeutig benannt. Diagnostik mit Kursauswahl und „Kursmatrix öffnen“; Profilverweise führen zu vorhandener Live-Ansicht einschließlich Kurs, Einheit und Lernendem. Keine technischen Personenkennungen oder Entwicklungsformulierungen im Mitgliederbereich. | `live-summary`, `teaching-overview-consistency` | offen |
| 7 | DS-29, DS-31 | Als Lernender erkenne ich die zweite Präsentation als „Wiederholung“ und weiß, dass Antworten erhalten bleiben; dafür bestehendes `presentation_number`. Als Lehrkraft erhalte ich lesbare mehrseitige Druckausgaben mit einheitlichen Abständen und Überschriften beim Folgeinhalt. | `practice-session`, `teacher-unit-print` | offen |

Verbindliche Given–When–Then-Szenarien (jeweils in den zugeordneten Specs):

1. Gegeben vorhandenes Markdown mit `.sb3`, `.hex`, `.fls` und Codeblock, wenn unverändert geöffnet/gespeichert/neu geladen, dann bleibt der Inhalt erhalten und wird nicht ausgeführt (`editor-content-integrity`). Gegeben erste/letzte lineare Abschnitte, wenn ausgewählt, dann Kontextleiste ohne Formular; bei Eigenschaften Formular, beim Inhaltseditor und Rückkehr gleiche Auswahl/Ausschnitt; fremde Lehrkraft und Lernende dürfen nicht bearbeiten (`teacher-linear-section-actions`). Titel und Nummern-Fallback zusätzlich im Projektionstest.
2. Gegeben beide Rollen und beide Darstellungen, wenn Katalogsuche mit/ohne Treffer, Dialog öffnen/abbrechen, Löschmenü/Abbruch oder Themewechsel mit Entwurf, dann gemeinsame lesbare Zustände, korrekter Fokus und unveränderter Entwurf (beide Paket-Specs).
3. Gegeben gültige/ungültige Eingaben und lokale Testmails, wenn Anmeldung, Registrierung, Verifikation, Reset, verbrauchter Link und Logout durchlaufen werden, dann verständliche Rückmeldung und sicherer Rückweg; Light/Dark einschließlich direkter Mailaufrufe und gültiger/ungültiger Einladung (beide Paket-Specs).
4. Gegeben DragQuestion/Multiple Choice, wenn falsche/richtige Antwort, Wiederholung, Tastaturbedienung und Zoom erfolgen, dann lesbare bedienbare Zustände und gespeichertes Ergebnis; Editor speichern/neuladen bewahrt Inhalt (beide Paket-Specs).
5. Gegeben derselbe Graph beider Rollen, wenn Startfokus, Gesamtansicht, Sperre, Rückkehr und Aktualisierung geprüft werden, dann identische Geometrie und beabsichtigter Ausschnitt (`graph-role-parity`).
6. Gegeben 24 Lernende mit langen Namen, wenn letzte Tabellenzeile, keine/vorhandene Abgabe und Profilverweis geöffnet werden, dann kein Seitenüberlauf, erreichbare Details und richtiges Live-Linkziel (beide Paket-Specs).
7. Gegeben erste Aufgabenpräsentation, wenn Musterlösung, Wiederholung und Abschluss erfolgen, dann eindeutige Erklärung ohne geänderte Zählung (`practice-session`). Gegeben Text-, Bild- und PDF-Materialien, wenn mehrseitig gedruckt, dann konsistente Abstände und zusammengehörige Überschriften/Inhalte (`teacher-unit-print`).

## Gemeinsames QA-Inventar und Nachweise

### Konkretisierung der gemeinsamen Basis für Paket 2

Die vorhandene Workspace-Aktionsfamilie bleibt die Basis: sekundär als Standard, primär als ausdrücklich benannte Variante, dezent und destruktiv als Varianten ohne eigene Typografie. Varianten ändern die Gewichtung, nicht automatisch die Größe. Die normale Mindesthöhe gilt auch für dezente Aktionen; kleinere Desktopwerkzeuge erhalten eine ausdrückliche kompakte Ausnahme. Akzentflächen benötigen eine dunkle, kontrastgeprüfte Schrift statt pauschal weißer Schrift. Suchfelder der Kataloge und Mitglieder werden an dieselbe Feldbasis angeschlossen. Radio-/Checkboxzeilen bleiben neben ihrer Beschriftung und erhalten keine Vollbreite.

Dialoge und Drawer behalten ihre unterschiedlichen Platzierungen, teilen aber Rahmen, Abstände und eine wiederverwendete Fokusführung: Fokus hinein, Tab innerhalb des obersten Dialogs, Escape nur dort, Rückgabe an den noch vorhandenen Auslöser. Ein untergeordneter Löschdialog darf den Drawer nicht mit schließen. Löschmenüs bleiben beschriftet und bieten zuerst einen getrennten Bestätigungsschritt. Die Prüfung umfasst auch Abbruch und unveränderte Daten, nicht nur sichtbare Schaltflächen.

Alle sichtbaren Aktionen, Zustandswechsel und vorgenannten Aussagen erhalten funktionale und separate visuelle Nachweise. Viewports 1440, 1024 und 390 px sowie 320 px als Überlauf-Randfall, jeweils Light/Dark. Kein horizontaler Seitenüberlauf; absichtliche innere Tabellen-/H5P-Scrollbereiche erlaubt. Text-, aktive Aktions-, Hover- und Fokuskontraste prüfen, deaktivierte Zustände separat. Screenshots und gerenderte Druckseiten tatsächlich sichten, fehlerhafte Referenzbilder nicht einfach ersetzen. Zusätzlich Rücknavigation/erneutes Öffnen und ungültige/unberechtigte Zugriffe als Fehlerpfade prüfen.

Je zugeordneter Spec `make verify-feature FEATURE=<spec-stem>`, bei Auth-/Buildintegration `make docker-validate`, abschließend `make verify`. Kein Befund gilt allein aufgrund von Unit-Tests als abgenommen. IServ-Verbund, physische Geräte und vollständige Screenreader-Konformität bleiben ausdrücklich gesonderte Abnahmegrenzen.

## Arbeitsprotokoll

- Ausgangsstand sauber, `master` synchronisiert; lokale CA wird von System, Chromium/Codex und Firefox vertraut.
- Interaktiver Browser-Skill gelesen; `js_repl` in dieser Sitzung nicht verfügbar. Ersatz: vorhandene Playwright-Infrastruktur und separat gesichtete Screenshots, ohne Abschwächung von TLS oder Sandbox-Grundsätzen.
- Paket 1, Rot: Adapter-Roundtrip reproduziert den Verlust von Inline-Code und Codeblöcken; realer Browser zeigt „Dateien: , und .“. Kontextleisten- und Titeltests ebenfalls zunächst rot. Browser bestätigt den fehlenden linearen Auswahlbereich.
- Paket 1, Umsetzung: Tiptap-Codeknoten als inerten Text aktiviert; vorhandene Kontextleiste um Abschnitte erweitert; Eigenschaften in denselben reaktiven Panelzustand aufgenommen; Rücklink nach Knotentyp korrigiert; tabbezogene, validierte Kamerakoordinaten gespeichert. Keine Berechtigungs- oder Markdown-Sanitizeränderung.
- Paket 1, zusätzlicher Fehlerpfad: Backend verweigert fremden Editorzugriff korrekt mit 403, Loader machte daraus 500. Loader erhält jetzt 403/404 und zeigt eine verständliche Meldung. Zwei zuerst fehlschlagende Loader-Tests sichern dies ab. Lernende werden weiterhin zur erlaubten Oberfläche umgeleitet; schreibender API-Zugriff bleibt verboten.
- `make verify-feature FEATURE=editor-content-integrity` bestanden: 3035 Backendtests bestanden / 33 übersprungen; 700 Frontendtests, 8 Toolingtests und 67 H5P-Tests bestanden; Svelte-Prüfung ohne Fehler/Warnungen; Produktionsbuild mit 16 bereits dokumentierten Upstreamwarnungen; authentifizierter Speichern-/Neuladen-Rundlauf erfolgreich.
- `teacher-linear-section-actions` als gezielter Browserlauf bestanden: erste/letzte Auswahl, Eigenschaften/Escape/Fokusrückgabe, Editor-Rückkehr mit gleicher Kamera, alter `quick=1`-Link, fremde Lehrkraft/Lernender ohne Schreibzugriff, Lernenden-Abschnittstitel. Zusätzlich bestehende `teacher-graph-module-actions` erfolgreich als Rückfallprüfung der gemeinsamen Leiste.
- Gesichtet: lineare Kontextleiste bei 1440/1024/390/320 px in Light/Dark und Lernenden-Abschnittstitel. Kein horizontaler Seitenüberlauf. Bekannte Graphhöhe und kompakte Werkzeugzustände bleiben ausdrücklich in Paket 5 offen; diese Bilder gelten nicht als Abnahme von DS-07/23–25.
- Zweites vollständiges Gate `make verify-feature FEATURE=teacher-linear-section-actions` ebenfalls bestanden (3035 Backendtests, 700 Frontendtests, 8 Toolingtests, 67 H5P-Tests, Browser-Rundlauf). Editorbilder bei allen vier Breiten und beiden Modi gesichtet; Theme-Übergänge vor der Bildaufnahme abgewartet. Befunde DS-26/33/34 mit Einzelnachweisen in ihren Katalogeinträgen aktualisiert. Bilder unter `assets/2026-09-10-designreparatur-paket1/`.
