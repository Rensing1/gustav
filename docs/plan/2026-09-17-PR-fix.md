# PR-Review: Vereinheitlichung der Authentifizierung

## Review Round – 14:20

- Datum: 17.09.2026, Beginn 14:20 Uhr, Abschluss 14:37 Uhr (Europe/Berlin).
- Branch: `master`.
- Base-Ref gemäß Skill: `master`, SHA `39d9354528a6d7d19978b88f3e692c3ec38ab086`.
- Merge-Base und geprüfter HEAD: `39d9354528a6d7d19978b88f3e692c3ec38ab086`.
- Der vorgeschriebene Vergleich `merge-base..HEAD` ist leer. Da der Review-Auftrag unmittelbar auf den bereits auf master abgeschlossenen Auth-Umbau folgt, wird ergänzend dessen Commit gegen seinen Vorgänger `5af94b3d56df4b7d4b83ece6d750183b18df1301` geprüft. Es wird kein Feature-Branch-Vergleich behauptet.
- Umfang: Backend-OIDC/Sitzungen/Refresh/CSRF, Frontend-Weiterleitungen, H5P-Anbindung, Keycloak/Proxy, API-Vertrag, Migrationen, neue und geänderte Tests sowie Auth-Referenz.
- Kontext: Es gibt keine externen Anmeldeanbieter. Der Broker-Test ist ausdrücklich nicht Bestandteil der Abnahme. Kein Push und kein Produktionsdeployment.
- Vorgehen: Diff-basierte Prüfung, gezielte Reproduktionen mit ausschließlich synthetischen Daten, anschließend priorisierte Befunde. Produktivcode wird in dieser Review-Runde nicht geändert.

Diese Runde ändert ausschließlich Review-Dokumentation; sie führt keinen neuen nutzerseitigen Ablauf ein. Deshalb ist kein eigenes `verify-feature`-Gate erforderlich. Die gezielten Feature-Gates gehören nach den Reparaturen erneut zur Abnahme.

## Reproduktionsnachweise

1. **Refresh mit veraltetem Snapshot:** Mit der echten lokalen `UnifiedSessionRepository` eine synthetische, erneuerungsbedürftige Sitzung angelegt. A reserviert Version 1. B liest Version 1 mit alten Tokens. A speichert neue Tokens und gibt die Reservierung frei, ohne die Version zu ändern. B setzt den gelesenen Snapshot im echten `SessionService` fort: Seine Reservierung gelingt, und der OIDC-Adapter erhält das alte Refresh-Token. Simuliert der Adapter hierfür `invalid_grant`, wird die bereits erfolgreich erneuerte Sitzung gelöscht. Ausgabe: `stale-token request issued = True`, `already refreshed session deleted = True`. Die synthetische Sitzung wurde anschließend entfernt. Die Ablehnung des alten Tokens wurde kontrolliert simuliert; ein solcher Widerruf durch den laufenden Keycloak wurde damit nicht behauptet. Der zusätzliche Refresh selbst ist unabhängig von einer Token-Rotation reproduziert. Eine zweite Reproduktion verschiebt die kontrollierte Zeit über die alte Ablaufgrenze, während A die Sitzung bereits verlängert hat. B löscht den neuen Datensatz anhand der alten Ablaufzeit und derselben Version: `fresh session deleted = True`. Für diesen Sitzungsverlust ist weder Token-Rotation noch eine simulierte IdP-Ablehnung nötig.
2. **Regulärer Passwort-Reset-Einstieg:** Nach erfolgreichem `make local-ca-status` mit Chromium und aktiver TLS-Prüfung `/forgot-password` geöffnet und das Formular mit leerer optionaler E-Mail abgeschickt. Ergebnis: Dokument `200`, `Referrer-Policy: no-referrer`, Formular-POST `Origin: null`, Antwort `403` mit `Cross-site POST form submissions are forbidden`. Keine Reset-Mail wurde angefordert. Die vorhandene Browser-Spec startet stattdessen direkt unter `/auth/forgot` und umgeht die defekte Seite.
3. **JWKS-Nachladen:** Fünf identische ungültige Bearer-JWTs mit unbekanntem `kid` an die echte Tokenprüfung übergeben; ausschließlich der Schlüsseltransport wurde durch einen zählenden Adapter ersetzt. Ergebnis: sechs Abrufe, einer zum initialen Füllen und fünf erzwungene Abrufe trotz gefülltem Cache. Auch ein unzulässiger Algorithmus wird erst nach dem Schlüsselabruf abgelehnt.
4. **API-Vertrag:** YAML-Auswertung von `/auth/forgot`, GET, Antwort 302 ergibt `Vary: {Location: null, schema: {type: string}}`; ein eigentlicher `Location`-Header fehlt. Der bestehende OpenAPI-Gate prüft primär die Routenoberfläche und erkennt diese Struktur nicht.

## Kurzfazit

Die gemeinsame Backend-Sitzung vereinfacht die Zuständigkeiten deutlich; Sicherheitsgrenzen und Migrationen sind überwiegend nachvollziehbar. Die Freigabe ist dennoch verfrüht: Der normale Passwort-Reset-Einstieg scheitert, und eine reproduzierbare Refresh-Race kann eine bereits erneuerte Sitzung löschen. Zusätzlich fehlen eine Begrenzung erzwungener Schlüsselabrufe und ein korrekter Redirect-Header im API-Vertrag. Gezielte Korrekturen reichen; ein weiterer Architekturumbau ist nicht erforderlich.

## MUST-FIX

- [BUG] Veralteter Snapshot kann eine bereits erneuerte Sitzung löschen (P1) — `backend/identity_access/unified_store.py:97@39d93545`, `backend/identity_access/unified_sessions.py:42@39d93545` — Impact: Ein während des Refreshs gelesener Snapshot behält nach dessen Abschluss dieselbe Version. An der alten Ablaufgrenze kann er die inzwischen verlängerte Sitzung löschen. Außerdem gelingt eine weitere Reservierung mit alten Tokens; deren Ablehnung löscht ebenfalls die gültige Sitzung. — Fix: Sichere zuerst genau diese Reihenfolge durch einen PostgreSQL-Regressionstest ab. Invalidiere alte Snapshots beim Abschluss und prüfe den Erneuerungsbedarf atomar bei der Reservierung; verwende ausschließlich die zur Reservierung gehörenden Tokens. Lies nach einer verlorenen bedingten Ablauf-Löschung erneut, statt vorschnell „abgelaufen“ zu melden.
- [BUG] „Passwort vergessen“ endet vor dem Anmeldedienst mit 403 (P1) — `frontend/src/hooks.server.ts:18@39d93545` — Impact: `no-referrer` erzeugt beim nativen POST von `/forgot-password` `Origin: null`; SvelteKit verweigert das Formular. Die neue Browser-Spec umgeht diesen sichtbaren Einstieg. — Fix: Ergänze einen Browsertest vom Login-Link über dieses Formular. Verwende auf Seiten mit nativen Formularen eine passende Herkunftsrichtlinie oder leite den Reset direkt zu Keycloak; behalte die CSRF-Prüfung bei.

## SHOULD-FIX

- [SEC] Unbekannte Schlüssel umgehen bei jeder Anfrage den JWKS-Cache (P2) — `backend/identity_access/tokens.py:185@39d93545` — Impact: Ohne Anmeldung lässt sich pro erfundenem Token ein zusätzlicher Keycloak-Aufruf erzwingen; parallele Anfragen können den Anmeldedienst und Backend-Threads belasten. Fünf identische ungültige Tokens erzeugten sechs Abrufe. — Fix: Prüfe den Algorithmus vor dem Netzaufruf, koordiniere erzwungene Abrufe je Realm und begrenze ihre Wiederholungsrate. Teste eine Anfragewelle, echte Schlüsselrotation und Nichterreichbarkeit getrennt.
- [API] Weiterleitungsheader unter `Vary` verschachtelt (P2) — `api/openapi.yml:7743@39d93545` — Impact: Der Vertrag deklariert keinen `Location`-Header für den Passwort-Reset, sondern ein ungültiges zusätzliches Header-Feld. Generierte Clients und Dokumentation erhalten einen falschen Vertrag. — Fix: Deklariere `Location` auf Header-Ebene, entferne das überholte `Vary` und ergänze eine strukturelle Vertragsprüfung.

## NICE-TO-HAVE

Keine weiteren belastbaren Befunde. Vermeide zusätzliche Auth-Abstraktionen; die Korrekturen passen in die vorhandenen Zuständigkeiten.

## TESTS

- 35 gezielte Token-, Adapter- und Auth-Vertragstests bestanden.
- 48 vorhandene Sitzungs-/Routentests gegen echte lokale PostgreSQL-Datenhaltung bestanden, einschließlich Prozessparallelität, RLS und Logout. Die oben beschriebene Snapshot-Reihenfolge fehlt darin.
- Browser-Reproduktion des Reset-Fehlers mit vertrauenswürdiger lokaler CA und unveränderter TLS-Prüfung bestätigt.
- Drei zusätzliche isolierte Reproduktionen bestätigen Refresh-Race, wiederholte JWKS-Abrufe und die fehlerhafte Header-Struktur.
- Die fünf früheren Feature-Gates sind im Implementierungsplan dokumentiert; sie wurden in dieser Review-Runde nicht erneut vollständig ausgeführt. Nach den Reparaturen mindestens `auth-registration` und `auth-session-continuity` einschließlich der ergänzten Szenarien erneut abnehmen.
- `make verify` erfolgreich: 2.998 Backend-Tests bestanden, 33 übersprungen; 735 Frontend-Tests sowie H5P-Tests erfolgreich. Import-/Architekturgrenzen, Routen-/DB-Inventar, Supply-Chain, Ruff und Container-Smoke ebenfalls grün. Diese grünen Gates ändern die reproduzierten Befunde nicht.

## Refactor-Budget

Vier begrenzte Korrekturen: Refresh-Versionierung/Reservierung, Reset-Einstieg, JWKS-Abrufbegrenzung und API-Header. Jeweils zuerst den konkret fehlenden Regressionstest ergänzen. Keine neue Datenbanktabelle, kein Auth-Framework und kein erneuter Architekturwechsel nötig.

## Freigabe

**Nicht freigegeben.** Behebe die beiden MUST-FIX-Befunde und prüfe die betroffenen Abläufe erneut. Die vorhandenen grünen Tests belegen diese Randfälle nicht. Produktivcode wurde in dieser Review-Runde nicht verändert; kein Push und kein Deployment.

## Reparaturplan – 2026-09-17 14:40

Ausgangsstand: `master`, HEAD `8348392ad50b3e49fbe90d92d3aafd6e76fd4d04`; sauberer Arbeitsbaum und `git pull --ff-only` ohne neue Änderungen. Der HEAD ergänzt den geprüften Auth-Commit ausschließlich um dieses Review. Alle vier Befunde wurden am aktuellen Code bestätigt; keiner ist veraltet.

User Story: Als Schüler möchte ich beim parallelen Laden angemeldet bleiben und mein Passwort über den sichtbaren Einstieg zuverlässig zurücksetzen können. Ungültige Anfragen sollen den Anmeldedienst nicht unnötig belasten.

Verbindlicher Rahmen: AGENTS.md (Contract-first, Red-Green-Refactor, echte PostgreSQL-Tests, lokal = Produktion), ARCHITECTURE.md und bounded_contexts.md (Zuständigkeit `identity_access`, HTTP-Abbildung im Webadapter). DESIGN.md bleibt für AuthFrame und native Formulare verbindlich; die Reparatur ändert keine Gestaltung, Begriffe oder Komponenten. Es werden weder neue Einstellungen noch Schemaänderungen benötigt. Die vorhandenen Migrationen und restriktiven Tabellenrechte bleiben maßgeblich; ENV-Werte werden nicht veröffentlicht.

1. **P1 – veraltete Sitzungsdaten:** `finish_refresh` lässt die Version unverändert, Reservierungen prüfen den Erneuerungsbedarf nicht erneut und Ablaufentscheidungen ignorieren verlorene Löschversuche. Owner: `identity_access`. Dateien: `unified_store.py`, `unified_sessions.py`, `test_unified_sessions.py`, Auth-Referenz. Zuerst echte DB-Tests: Given B liest während A erneuert, When A fertig wird und B fortsetzt, Then erfolgt kein zweiter Refresh; an der alten Ablaufgrenze bleibt die verlängerte Sitzung verwendbar. Dazu Given frisches Token, When eine Reservierung versucht wird, Then erfolgt kein Netzaufruf. Minimal: Generation bei erfolgreichem Speichern erhöhen, Erneuerungsbedarf atomar prüfen und bei verlorener Ablauf-Löschung erneut lesen. Kein neuer API-Vertrag, keine Migration. Abnahme: vorhandene und neue Parallelitäts-/Logout-Tests sowie `auth-session-continuity`.
2. **P1 – Reset-Formular:** `/forgot-password` liefert weiterhin `no-referrer`. Owner: `identity_access`, Darstellung im SvelteKit-Adapter. Dateien: `hooks.server.ts`, zugehöriger Test, `auth-registration.spec.ts`. Zuerst Hook-Test und echter Rundlauf vom sichtbaren Login-Link über das native Formular zum Reset und zur wiederhergestellten Anmeldung, auch ohne JavaScript. Minimal: `same-origin` für diese native Formularseite; bestehende CSRF-Prüfungen und AuthFrame-Gestaltung erhalten. API-Payload und Schema unverändert. Abnahme: `auth-registration` mit vollständigem `@feature-acceptance`-Rundlauf.
3. **P2 – JWKS-Anfragewellen:** Unbekannte `kid` erzwingen weiterhin pro Anfrage einen Abruf. Owner: `identity_access`. Dateien: `tokens.py`, Token-Tests, Auth-Referenz. Zuerst Tests für falschen Algorithmus ohne Netzaufruf, parallele und sequenzielle unbekannte Schlüssel, echte signierte Schlüsselrotation sowie Ausfall/Wiederanlauf mit kontrollierter Zeit. Minimal: Algorithmus vor dem Abruf prüfen; kurzer gemeinsamer Abrufschutz pro Realm und Backend-Prozess, fünf Sekunden Wiederholungssperre einschließlich Fehlern. Gültig zwischengespeicherte Schlüssel bleiben nutzbar, tatsächliche Abruffehler bleiben Infrastrukturfehler. Kein verteilter Dienst und keine neuen Konfigurationswerte. Abnahme: Token-/Adaptertests sowie die beiden Auth-Feature-Gates.
4. **P2 – OpenAPI-Header:** `Location` ist nach wie vor unter `Vary` verschachtelt. Owner: `identity_access`, API-Vertrag. Dateien: `api/openapi.yml`, `test_openapi_auth_contract.py`. Zuerst fehlschlagende strukturelle Vertragsprüfung für die vorhandene Redirect-Antwort; anschließend ausschließlich den Vertrag korrigieren, bevor anderer API-Code verändert wird. Das Laufzeitverhalten ist bereits eine 302-Weiterleitung. Abnahme: strukturelle Prüfung plus Vergleich mit der tatsächlichen Reset-Route. Keine Migration.

Abschluss: Auth-Referenz und Changelog aktualisieren, Ausführungsnachweise hier anhängen, den lokalen Stack mit Backend/Frontend neu bauen, vor Browserprüfungen `make local-ca-status`, anschließend `make verify-feature FEATURE=auth-registration` und `make verify-feature FEATURE=auth-session-continuity`. Keine Änderungen an Compose/Proxy/Keycloak vorgesehen; `docker-validate` zusätzlich als abschließender Konfigurationsnachweis. Commit lokal auf master, kein Push und kein Produktionsdeployment.

## Umsetzungsstand – 2026-09-17 15:12

Alle vier am Ausgangsstand bestätigten Befunde sind geschlossen. Kein Befund war veraltet; kein MUST-FIX oder SHOULD-FIX aus dieser Runde bleibt offen. Der ursprüngliche Review samt damaliger Freigabeentscheidung bleibt oben als Historie erhalten.

- ✅ **P1 – veraltete Sitzungsdaten:** `UnifiedSessionRepository.finish_refresh` erhöht nun auch beim erfolgreichen Abschluss die Version. Die Reservierung prüft den Erneuerungsbedarf atomar; `SessionService` liest nach einer verlorenen bedingten Ablauf-Löschung erneut. Echte PostgreSQL-Regressionstests erzwingen das Lesen während eines fremden Refreshs und die Fortsetzung nach dessen Abschluss, sowohl vor als auch nach der alten Ablaufgrenze. Die verlängerte Sitzung bleibt erhalten, ein weiterer Token-Aufruf unterbleibt. Tatsächlich abgelaufene Sitzungen werden weiterhin entfernt.
- ✅ **P1 – Reset-Formular:** Der SvelteKit-Hook liefert auf `/forgot-password` wie auf der Abmeldebestätigung `Referrer-Policy: same-origin`. Die Herkunftsprüfung bleibt unverändert. Der authentifizierte Browser-Rundlauf beginnt jetzt beim sichtbaren Link auf der Startseite, durchläuft das native GUSTAV-Formular, Keycloak, Reset-Mail und wiederhergestellte Anmeldung sowie Passwortänderung – ohne JavaScript.
- ✅ **P2 – JWKS-Anfragewellen:** `JWKSCache` teilt Abrufe je Realm und Backend-Prozess. Eine fünfsekündige Wiederholungssperre begrenzt erzwungene und fehlgeschlagene Abrufe; nicht erlaubte Algorithmen werden vor dem Netzwerkzugriff verworfen. Bekannte gültige Schlüssel bleiben während eines Zusatzabrufs ohne Warten verfügbar. Der Token- und HTTP-Adapter übersetzt einen wegen der Sperre vorübergehend nicht prüfbaren neuen Schlüssel in `503`, ohne die Sitzung zu löschen. Tests prüfen parallele und sequenzielle Anfragen, Ausfall/Wiederanlauf, sofortige Cache-Nutzung während eines blockierten Abrufs sowie die erfolgreiche Prüfung eines tatsächlich signierten rotierten Schlüssels nach Ablauf der Sperre.
- ✅ **P2 – OpenAPI-Header:** `/auth/forgot` deklariert `Location` direkt als Header der 302-Antwort. Eine strukturelle Vertragsprüfung sichert die korrigierte Verschachtelung ab.

**Red-Green-Nachweis:** Die ergänzten Tests reproduzierten zunächst die drei Sitzungs-/Reservierungsfehler, den fehlerhaften Reset-Hook und den echten Browser-403, unbegrenzte Schlüsselabrufe beziehungsweise falsche Fehlerklassifizierung sowie den fehlenden `Location`-Header. Nach den minimalen Korrekturen sind alle grün. 80 gezielte Backend-Tests bestanden, einschließlich realer PostgreSQL-Datenhaltung; die acht Hook-Tests und der einzelne echte Registrierungs-/Reset-Rundlauf ebenfalls. Bestehende JWT-Testfixtures wurden um den tatsächlich erwarteten Algorithmus ergänzt.

**Abnahme am abschließenden Codezustand:** Backend und Frontend lokal neu gebaut; vor beiden Browserprüfungen wurde die vertrauenswürdige lokale Caddy-CA bestätigt, TLS blieb aktiv. Beide vollständigen Gates `make verify-feature FEATURE=auth-registration` und `make verify-feature FEATURE=auth-session-continuity` bestanden. Jedes Gate umfasste 3.013 bestandene Backend-Tests (33 übersprungen, vier Warnungen), 736 bestandene Frontend-Tests sowie die weiteren Architektur-, Vertrags-, Repository-, Build- und H5P-Prüfungen. Beide echten `@feature-acceptance`-Rundläufe bestanden; beim zweiten wurden insbesondere Dienst-/Browserneustarts und begrenzte Wiederanmeldung geprüft. Die Bereinigung der jeweils erzeugten Testdaten wurde bestätigt. `make docker-validate` und `git diff --check` waren erfolgreich.

**Architektur und Dokumentation:** Sitzungs- und Schlüsselregeln bleiben im Bounded Context `identity_access`; FastAPI und SvelteKit übernehmen ausschließlich ihre jeweiligen HTTP-Aufgaben. Keine neuen Abstraktionsschichten, Dienste, Einstellungen oder Schemaänderungen; daher keine Migration. Die AuthFrame-Gestaltung und vorhandenen Formularkomponenten bleiben gemäß DESIGN.md erhalten. `docs/references/auth_sessions_and_cookies.md` und `docs/CHANGELOG.md` beschreiben die korrigierten Regeln und Nachweise.

**Restgrenzen und aktualisierte Freigabe:** Die JWKS-Begrenzung gilt je Realm und Backend-Prozess, nicht global über mehrere Prozesse. Ein gerade rotierter, noch unbekannter Schlüssel kann während der fünfsekündigen Sperre vorübergehend `503` auslösen; die Sitzung bleibt dabei erhalten. Die Ergebnisse belegen den freigegebenen lokalen Stack und die genannten Abläufe, keine Produktionsabnahme. Die vier Review-Befunde sind behoben und lokal abgenommen; kein offener MUST-FIX aus dieser Runde. Lokaler Commit auf master, kein Push und kein Produktionsdeployment.
