# Harness Milestones

Status: Draft
Owner: Produktverantwortlicher
Local checks: `.venv/bin/pytest -q backend/tests/test_harness_minimum_contract.py`
CI status: Keine anbietergebundene CI erforderlich; die lokalen Make-Ziele sind maßgeblich
Related plans: `docs/plan/2026-05-02-harness-engineering-refactor-plan.md`
Review cadence: monatlich während des Harness-Refactors

## Zweck
Dieses Dokument fasst die nächsten Refactor-Meilensteine aus dem Harness-Plan zusammen.

## Aktuelle Reihenfolge
1. PR 1 Harness Minimum: umgesetzt im Arbeitsbaum; Orientierung, Skill-Governance, minimale harte Gates und Warnsignale.
2. PR 2 CSRF und Session Baseline: umgesetzt im Arbeitsbaum; `make test-db-security` enthält CSRF-/Session-Baselines.
3. PR 3 Authz und RLS Baseline: umgesetzt im Arbeitsbaum; `make test-db-security` setzt `REQUIRE_DB_TESTS=1` und enthält Authz-/RLS-/RLS-Migrationstests.
4. PR 4 Uploads und LLM-Datengrenzen: umgesetzt im Arbeitsbaum; `make test-upload-llm-boundaries` enthält servicefreie Upload-/Storage-/Signatur-/LLM-/Privacy-Contracts und dokumentiert die Original-Submission-Regel.
5. PR 5 Docker Image-Only Smoke: umgesetzt im Arbeitsbaum; `make test-docker-image-smoke` baut das Web-Image, prüft kritische Imports ohne Volumes und testet `/health`.
6. PR 6 Import Inventory und Blocking Rules: umgesetzt im Arbeitsbaum; `make test-import-boundaries` zählt bestehende Import-Schulden und `make verify` blockiert neue Baseline-Überschreitungen.
7. PR 7 Make Frontend Check Visible: umgesetzt im Arbeitsbaum; `make verify` ruft `make test-frontend-h5p` auf und bindet damit Svelte-Typecheck, Frontend-Vitest und H5P-Tests ein.
8. PR 8 Package-Oriented App Start: umgesetzt im Arbeitsbaum; Docker startet `backend.web.main:app`, Compose mountet Backend nur noch unter `/app/backend`, und `make verify` enthält den Image-only-Smoke.
9. PR 9 Centralize Test Imports: erster Schnitt umgesetzt im Arbeitsbaum; `backend/tests/import_paths.py` zentralisiert pytest-Kompatibilitätspfade und `make test-import-boundaries` blockiert neue verstreute Import-Crutches.
10. PR 10 API Contract Baseline: erster Schnitt umgesetzt im Arbeitsbaum; `make test-api-contract-baseline` vergleicht Runtime-`/api/*`-Routen mit `api/openapi.yml` und klassifiziert Route-Surfaces.
11. PR 11 Make Frontend Verification Hard: durch PR 7 erfüllt; `make verify` enthält `make test-frontend-h5p` als hartes Frontend-/H5P-Gate.
12. PR 12 Architecture Boundary Rules: erster Schnitt umgesetzt im Arbeitsbaum; `make test-architecture-boundaries` blockiert FastAPI-Imports in Use Cases/Services und neues Wachstum direkter Web-DB-/Supabase-Zugriffe.
13. PR 13 Route Surface Map and Refactor Order: erster Schnitt umgesetzt im Arbeitsbaum; `make test-route-map` hält eine generierte Route-für-Route-Inventur mit Risiko, Legacy-Status und Zielschicht synchron.
14. PR 14 Extract Security Guards: erster Schnitt umgesetzt im Arbeitsbaum; `backend/web/security/guards.py` zentralisiert Rollenprüfungen und wird durch einen Guard-Contract abgesichert.
15. PR 15 Shrink `backend/web/main.py` to App Composition: erste Schnitte umgesetzt im Arbeitsbaum; `backend/web/app_composition.py` übernimmt App-Shell, Runtime-Bootstrap, Static-Mount und Core-Router-Registrierung, `backend/web/main_router_wiring.py` übernimmt das konkrete Router-Wiring der verbleibenden FastAPI-Shell, `backend/web/main_middleware_wiring.py` übernimmt die Middleware-Installationsreihenfolge für Auth und Security Headers, `backend/web/main_storage_wiring.py` übernimmt die Storage-Startup-Initialisierung, `backend/web/main_auth_wiring.py` übernimmt das private Auth-Dependency-Wiring, `backend/web/main.py` exportiert eine erste `create_app()`-Shell-Factory, `backend/web/auth_flow.py` übernimmt Auth-Flow-Klassifizierung, `backend/web/auth_claims.py` übernimmt Auth-Claim-Mapping, `backend/web/auth_session.py` übernimmt App-Session-TTL und Session-Cookie-Schreiben, `backend/web/auth_bridge.py` übernimmt `/auth/callback` und `/api/me`, `backend/web/auth_middleware.py` übernimmt Auth-Kontext-Auflösung und Auth-Middleware-Installation, `backend/web/auth_runtime.py` übernimmt Auth-Settings-, OIDC-, Session-Store-, BFF-Session-Store-, CLI-Token-Store-Konstruktion und das explizite `AuthRuntime`-Objekt, `backend/tests/runtime_auth_helpers.py` stellt die erste Runtime-orientierte Testoberfläche für Session-Stores bereit, `backend/web/routes/app.py` nutzt für interne Browser-BFF-Session-Endpunkte bereits `app.state.runtime.bff_session_store`, für `POST /api/app/session-sync` `app.state.runtime.session_store`, für Profil-OIDC und Profil-CLI-Token `app.state.runtime`, für Profil-Bearer-Claims eine direkte routes-eigene Verifier-Dependency sowie für `/auth/logout`, `/auth/forgot`, `/auth/password`, `/auth/login` und `/auth/register` Runtime-Provider, `backend/web/layout_response.py` übernimmt HTMX-aware Layout-Antworten, `backend/web/csrf_tokens.py` übernimmt SSR-CSRF-Token-Erzeugung und -Validierung, `backend/web/internal_api.py` übernimmt SSR-interne API-Client-Policy, `backend/web/ssr_helpers.py` übernimmt kleine SSR-Utilities, `backend/web/submission_history_rendering.py` übernimmt Learning-Submission-/History-Rendering, `backend/web/auth_only_app.py` übernimmt die leichte Auth-only-Test-App, `backend/storage/verification.py` übernimmt lokale Storage-Hash-Verifikation, `backend/web/cli_authoring.py` übernimmt CLI-Authoring-Capabilities, `backend/web/security/headers.py` übernimmt Security-Header-Policy und -Middleware, `backend/web/runtime_config.py` übernimmt kleine Runtime-Konfiguration, `backend/web/legacy_retirement.py` übernimmt Legacy-Retirement-Entscheidungen, `backend/web/routes/basic_pages.py` übernimmt Root-/About-/Health-Routen, und tote Legacy-Learning-Submission-, Teacher-Unit-/Course-, Teaching-Live- sowie Course-Members-SSR-Reste sind aus `backend/web/main.py` entfernt; `backend/web/main.py` liegt aktuell bei 115 Zeilen. Private Alias-Altlasten wie `_is_public_path`, `_build_cli_token_store`, `_auth_middleware_deps` und `load_oidc_config` sind aus dem Entrypoint entfernt; die alten main-level Runtime-Namen bleiben nur als bewusst markierte, zeitlich begrenzte Kompatibilitätsbrücke, bis die Tests auf Runtime-Provider umgestellt sind. Die Alias-Entscheidung ist jetzt explizit: `RUNTIME` ist Zieloberfläche, `SESSION_STORE`, `STATE_STORE`, `OIDC_CFG`, `OIDC`, `SETTINGS`, `BFF_SESSION_STORE` und `CLI_TOKEN_STORE` werden nach weiterem Test-Fixture-Cleanup entfernt statt dauerhaft konserviert. Abgesichert ist der Schnitt durch `backend/tests/test_app_composition_contract.py`, `backend/tests/test_auth_smoke_tool_contract.py`, `backend/tests/test_auth_flow_contract.py`, `backend/tests/test_runtime_auth_helpers_contract.py`, `backend/tests/test_auth_claims_contract.py`, `backend/tests/test_auth_session_contract.py`, `backend/tests/test_session_sync_api.py`, `backend/tests/test_profile_view_api.py`, `backend/tests/test_csrf_tokens_contract.py`, `backend/tests/test_internal_api_client_contract.py`, `backend/tests/test_ssr_helpers_contract.py`, `backend/tests/test_storage_local_hash_contract.py`, `backend/tests/test_cli_authoring_contract.py`, `backend/tests/test_security_headers_policy_contract.py`, `backend/tests/test_runtime_config_contract.py` und `backend/tests/test_legacy_retirement_contract.py`. Die frische Evidenz ist warning-clean: fokussierte Auth-/Profil-Contracts 108 passed, Runtime-Helper-/Health-/Legacy-Fixture-Contracts 13 passed, Members-Pagination-Hänger behoben mit `timeout 30s ...` → 1 passed, weitere Runtime-Session-Fixture-Cleanup-Läufe 10 passed, 4 passed/2 skipped, 4 passed für Upload-Intent-Tests mit explizitem `VisibleLearningRepo`, 1 passed/19 skipped für DB-backed Task-kind-Tests, 9 passed für Navigationstests, 8 passed/1 skipped für Cache-Header-Tests und ein kombinierter Fokuslauf mit 16 passed/2 skipped, Struktur-Gates OK, `make harness-minimum` 215 passed, `make verify` mit Backend 1878 passed/35 skipped, Svelte 0 errors/0 warnings, Frontend 282 passed, H5P 21 pass und `make harness-signals` mit normalem DB-/Docker-Zugriff ohne Warnungen; zusätzlich sind die Runtime-Provider-Contracts für `session-sync`, Profil, Profil-Bearer-Verifier, Auth-Logout, Auth-Forgot, Auth-Password, Auth-Login und Auth-Register grün.
16. PR 16 Legacy HTML/HTMX Exit Wave 1: umgesetzt im Arbeitsbaum; `/learning`, `/courses`, `/units`, `/teaching/live`, `/teaching/live/open`, `/teaching/live/units`, die alten `/courses/{course_id}/*`-Modules-/Members-HTML-Pfade, alle alten `/units*`-HTML-/HTMX-Pfade, die alten `/learning/courses*`-HTML-/HTMX-Pfade, die tiefen Teaching-Live-HTML-/HTMX-Pfade und der alte Teaching-Live-POST-Visibility-Helfer sind nicht mehr als `APIRoute` registriert und werden zentral als retired beantwortet. `make test-route-map` und `make harness-minimum` laufen warning-clean; der letzte vollständige `make verify` war ebenfalls warning-clean.

## Regel
Ein späterer Meilenstein startet erst, wenn sein Sicherheits- oder Contract-Kontext im Harness auffindbar ist.
