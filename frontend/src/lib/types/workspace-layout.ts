/**
 * Selects the horizontal workspace measure for a route.
 *
 * Content components remain responsible for their own readable text and form
 * measures. This type only controls the shared page shell.
 */
export type WorkspaceLayout = "compact" | "standard" | "wide" | "canvas";
