[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
    ".venv",
    "legacy-code-alpha1",
    "frontend/build",
    "h5p-service/vendor",
]

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
]

[tool.ruff.lint.isort]
known-first-party = [
    "backend",
]
