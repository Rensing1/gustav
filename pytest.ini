[pytest]
markers =
    e2e: end-to-end tests requiring running Keycloak + web services
testpaths =
    backend/tests
    backend/tests_e2e
norecursedirs =
    .*
    build
    dist
    node_modules
    venv
    .venv
    0_backup
    legacy*
